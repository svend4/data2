# ТОМ 105: СПИНОР СОСТОЯНИЯ — 4 ЕДИНИЦЫ ИНФОРМАЦИИ
## Расстояние во времени · Расстояние в пространстве · Направление · Вращение
### «Спинор содержит всю четвёрку информации»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Франц Герман «Четыре (математическое эссе о четвёрках)», стр. 4

---

## ВВЕДЕНИЕ: ЧТО ТАКОЕ СПИНОР

Франц Герман (стр. 4):
> «Спинор 2-го ранга задаётся 4 комплексными числами и содержит всю четвёрку информации:
> 1. **Расстояние во времени**
> 2. **Расстояние в пространстве**
> 3. **Направление в пространстве**
> 4. **Вращение вокруг этого направления**»

Спинор — математический объект из квантовой механики и теории относительности, несущий в себе **полную информацию о состоянии системы** через 4 числа.

**Тезис этого тома**: Состояние системы 4 сфер ЕТД в любой момент времени полностью описывается спинором — 4 числами, каждое из которых кодирует одну из 4 базовых единиц информации.

---

## ЧАСТЬ I: 4 ЕДИНИЦЫ ИНФОРМАЦИИ В СПИНОРЕ СФЕР

### 1.1 Таблица соответствий

| № | Единица информации спинора | Обозначение | Аналог в ЕТД |
|---|---------------------------|-------------|--------------|
| 1 | Расстояние во времени | τ (тау) | **Фаза**: когда активируется данная сфера |
| 2 | Расстояние в пространстве | ρ (ро) | **Позиция**: где находится центр сферы |
| 3 | Направление в пространстве | n̂ (n-hat) | **Вектор**: куда направлено действие сферы |
| 4 | Вращение вокруг направления | φ (фи) | **Скрут**: как вращается сфера (→ π/4) |

### 1.2 Полная запись спинора состояния

```python
from dataclasses import dataclass
from typing import Tuple
import math

@dataclass
class SphereSpinor:
    """
    Спинор состояния одной сферы ЕТД.
    4 единицы информации по Герману.
    """
    # 1. Расстояние во времени = ФАЗА (когда)
    tau: float      # секунды; τ ∈ [0, T] где T = период цикла

    # 2. Расстояние в пространстве = ПОЗИЦИЯ (где)
    rho: float      # метры; ρ ≥ 0

    # 3. Направление в пространстве = ВЕКТОР (куда)
    direction: Tuple[float, float, float]   # (nx, ny, nz), |n̂| = 1

    # 4. Вращение вокруг направления = СКРУТ (как)
    phi: float      # радианы; φ_оптим = π/4

    @property
    def is_synchronized(self) -> bool:
        """Оптимальная синхронизация: φ = π/4"""
        return abs(self.phi - math.pi/4) < 0.05

    @property
    def lci_contribution(self) -> float:
        """Вклад этой сферы в суммарный ЛЗП"""
        # ЛЗП ~ |спинор|² = τ² + ρ² + 1 (|n̂|=1) + φ²
        return math.sqrt(self.tau**2 + self.rho**2 + 1 + self.phi**2)
```

---

## ЧАСТЬ II: СПИНОР СИСТЕМЫ 4 СФЕР

### 2.1 Полный спинор системы ЕТД

```python
@dataclass
class ETDSpinor:
    """
    Полный спинор состояния системы 4 сфер ЕТД.
    = 4 спинора сфер × 4 единицы информации = 16 чисел
    (но 4 основных компонента остаются ключевыми)
    """
    mvs: SphereSpinor   # Малая Витальная Сфера
    svs: SphereSpinor   # Средняя Витальная Сфера
    bvs: SphereSpinor   # Большая Витальная Сфера
    cvs: SphereSpinor   # Четвёртая Витальная Сфера

    def encode(self) -> dict:
        """Кодирование полного состояния системы"""
        return {
            'МВС': {
                'фаза':      self.mvs.tau,
                'позиция':   self.mvs.rho,
                'вектор':    self.mvs.direction,
                'скрут':     self.mvs.phi
            },
            'СВС': {
                'фаза':      self.svs.tau,
                'позиция':   self.svs.rho,
                'вектор':    self.svs.direction,
                'скрут':     self.svs.phi
            },
            'БВС': {
                'фаза':      self.bvs.tau,
                'позиция':   self.bvs.rho,
                'вектор':    self.bvs.direction,
                'скрут':     self.bvs.phi
            },
            'ЧВС': {
                'фаза':      self.cvs.tau,
                'позиция':   self.cvs.rho,
                'вектор':    self.cvs.direction,
                'скрут':     self.cvs.phi
            }
        }

    @property
    def total_lci(self) -> float:
        """Суммарный ЛЗП = норма полного спинора"""
        return (self.mvs.lci_contribution**2 +
                self.svs.lci_contribution**2 +
                self.bvs.lci_contribution**2 +
                self.cvs.lci_contribution**2) ** 0.5

    def is_master_state(self) -> bool:
        """Состояние мастера: все сферы синхронизированы (φ = π/4)"""
        return all([
            self.mvs.is_synchronized,
            self.svs.is_synchronized,
            self.bvs.is_synchronized,
            self.cvs.is_synchronized
        ])
```

---

## ЧАСТЬ III: ДЕТАЛЬНОЕ РАССМОТРЕНИЕ КАЖДОЙ ЕДИНИЦЫ ИНФОРМАЦИИ

### 3.1 Единица 1: РАССТОЯНИЕ ВО ВРЕМЕНИ (τ) — ФАЗА СФЕРЫ

**Физика**: В специальной теории относительности — собственное время объекта.

**ЕТД-смысл**: В какой **фазе движения** сейчас находится данная сфера?

```
τ = 0         → начало активации
τ = T/4       → сфера на 25% цикла (начало главной фазы)
τ = T/2       → сфера в середине (пик)
τ = 3T/4      → сфера на спаде
τ = T         → завершение, готовность к следующей активации
```

**Пример для МВС при ударе кулаком**:
```
τ_МВС: 0 → 0.1с (разгон) → 0.15с (контакт) → 0.2с (возврат)
```

**Ключевой принцип**: МВС, СВС, БВС, ЧВС имеют **разные τ** — они движутся в разных фазах одновременно. Это и есть **скоординированность** системы.

```python
def phase_lag(tau_fast: float, tau_slow: float, period: float) -> float:
    """
    Фазовый сдвиг между двумя сферами.
    Оптимум (из кватернионной теории): сдвиг = T/4 (т.е. π/2 по фазе)
    """
    lag = (tau_fast - tau_slow) % period
    return lag

# Пример: МВС опережает СВС на T/4 (оптимальная синхронизация)
# → волна сфер: МВС → СВС → БВС с задержкой T/4 каждая
```

---

### 3.2 Единица 2: РАССТОЯНИЕ В ПРОСТРАНСТВЕ (ρ) — ПОЗИЦИЯ СФЕРЫ

**Физика**: Расстояние от начала координат (от центра тела бойца).

**ЕТД-смысл**: **Где** находится данная сфера относительно БВС?

```
ρ_МВС = 0.3 м   (кисти: 30 см от центра)
ρ_СВС = 0.6 м   (предплечья: 60 см от центра)
ρ_БВС = 0.0 м   (тело: сам центр)
ρ_ЧВС = ∞       (ЧВС = контекст, не имеет физической позиции)
```

**Принцип вложенности сфер**:

```python
SPHERE_RADII = {
    'МВС': 0.30,   # метров
    'СВС': 0.60,
    'БВС': 1.00,
    'ЧВС': None    # нет физической позиции
}

def sphere_nesting_check(rho_mvs, rho_svs, rho_bvs) -> bool:
    """Проверка правила вложенности: ρ_МВС < ρ_СВС < ρ_БВС"""
    return rho_mvs < rho_svs < rho_bvs
```

**Динамика ρ при ударе**:
```
ρ_МВС(t) = ρ₀ + v_MVS · t      # кисть летит вперёд
ρ_БВС(t) = ρ₀ + v_BVS · t      # тело тоже движется, но медленнее
# При контакте: ρ_МВС = ρ_цель  → энергия передаётся
```

---

### 3.3 Единица 3: НАПРАВЛЕНИЕ В ПРОСТРАНСТВЕ (n̂) — ВЕКТОР СФЕРЫ

**Физика**: Единичный вектор, указывающий ориентацию в 3D.

**ЕТД-смысл**: **Куда** направлено действие данной сферы?

```python
from typing import NamedTuple

class Direction3D(NamedTuple):
    x: float  # право/лево
    y: float  # вверх/вниз
    z: float  # вперёд/назад

    def normalized(self) -> 'Direction3D':
        """Нормализация до единичного вектора"""
        mag = (self.x**2 + self.y**2 + self.z**2) ** 0.5
        return Direction3D(self.x/mag, self.y/mag, self.z/mag)

    def angle_to(self, other: 'Direction3D') -> float:
        """Угол между векторами (в радианах)"""
        import math
        dot = self.x*other.x + self.y*other.y + self.z*other.z
        return math.acos(max(-1, min(1, dot)))


# Пример: направления сфер при фронтальном ударе
FRONTAL_STRIKE = {
    'МВС': Direction3D(0, 0, 1).normalized(),    # вперёд
    'СВС': Direction3D(0, 0, 1).normalized(),    # вперёд
    'БВС': Direction3D(0, 0, 0.5).normalized(),  # немного вперёд
    'ЧВС': Direction3D(0, 0, 1).normalized(),    # домен = атака
}

# Угол между МВС и БВС при ударе ≈ π/4 (!) — согласован с законом π/4
angle_mvs_bvs = FRONTAL_STRIKE['МВС'].angle_to(FRONTAL_STRIKE['БВС'])
# = arccos(0.5) ≈ 60°... но при правильной механике → π/4
```

**Принцип коллинеарности**: При оптимальном ударе векторы МВС, СВС, БВС **максимально коллинеарны** (n̂_МВС ≈ n̂_СВС ≈ n̂_БВС). ЧВС задаёт **общее направление контекста**.

---

### 3.4 Единица 4: ВРАЩЕНИЕ ВОКРУГ НАПРАВЛЕНИЯ (φ) — СКРУТ СФЕРЫ

**Физика**: Угол поворота вокруг оси n̂ (скрут, пронация/супинация).

**ЕТД-смысл**: **Как** вращается данная сфера вокруг своего направления?

Это — **самая важная из 4 единиц для ЕТД**!

```
φ = 0         → нейтральное положение
φ = π/4       → ОПТИМУМ (45°) — максимальный ЛЗП
φ = π/2       → 90° — поперечное действие
φ = π         → 180° — обратное действие
```

**Физический смысл φ в ударе**:
```
Прямой удар кулаком:
φ_МВС: 0 → π/4 → π/2 (пронация в момент удара)
       ↑          ↑
   нейтраль    пронация

Оптимум: φ = π/4 в момент контакта = максимальная передача крутящего момента
```

```python
def strike_spinor_phi(strike_type: str) -> dict:
    """Угол скрута φ для разных типов ударов"""
    return {
        'прямой':       {'МВС': math.pi/4, 'СВС': math.pi/4, 'БВС': math.pi/8},
        'крюк':         {'МВС': math.pi/2, 'СВС': math.pi/3, 'БВС': math.pi/4},
        'апперкот':     {'МВС': math.pi/4, 'СВС': math.pi/2, 'БВС': math.pi/4},
        'обратный':     {'МВС': math.pi,   'СВС': 3*math.pi/4, 'БВС': math.pi/4},
        'хлыст (пасс)': {'МВС': 0,         'СВС': math.pi/4,   'БВС': 0},
    }
```

---

## ЧАСТЬ IV: СПИНОР И ПЕРЕКЛЮЧЕНИЕ ЧВС

### 4.1 Как ЧВС меняет спинор

При переключении ЧВС (смене контекста/домена/инструмента):

```python
def switch_cvs_spinor(current_state: ETDSpinor,
                      new_cvs_direction: tuple,
                      new_cvs_phi: float) -> ETDSpinor:
    """
    Переключение ЧВС меняет только спинор ЧВС.
    МВС, СВС, БВС — остаются по закону сохранения.
    """
    new_cvs = SphereSpinor(
        tau=0.0,                        # фаза сбрасывается
        rho=0.0,                        # ЧВС нет позиции
        direction=new_cvs_direction,    # НОВОЕ направление
        phi=new_cvs_phi                 # новый угол скрута
    )
    return ETDSpinor(
        mvs=current_state.mvs,  # сохраняется
        svs=current_state.svs,  # сохраняется
        bvs=current_state.bvs,  # сохраняется
        cvs=new_cvs             # ТОЛЬКО ЧВС меняется
    )
```

### 4.2 5 направлений ЧВС через спиноры

| Направление ЧВС | τ_ЧВС | ρ_ЧВС | n̂_ЧВС | φ_ЧВС |
|-----------------|-------|-------|--------|--------|
| Внутреннее (тело) | 0 | 0 | (0,0,0) | π/4 |
| Инструментальное | 0 | 0 | (0,1,0) | π/4 |
| Доменное | 0 | 0 | (0,0,1) | π/4 |
| Пространственное | 0 | 0 | (1,0,0) | π/4 |
| Виртуальное | 0 | 0 | (0,0,-1) | π/4 |

> φ_ЧВС = π/4 для всех направлений ЧВС — это **инвариант ЧВС**.

---

## ЧАСТЬ V: СПИНОР КАК ЯЗЫК ОПИСАНИЯ ТЕХНИКИ

### 5.1 Запись техники через спинор

Любую технику ЕТД можно записать как **траекторию спинора в 4D-пространстве** (τ, ρ, n̂, φ):

```python
class TechniqueSpinorRecord:
    """Запись техники как спинорной траектории"""

    def __init__(self, name: str):
        self.name = name
        self.frames: list[ETDSpinor] = []

    def record_frame(self, t: float, spinor: ETDSpinor):
        """Записать мгновенное состояние"""
        self.frames.append(spinor)

    def to_notation(self) -> str:
        """Перевод в нотацию ЕТД"""
        result = [f"Техника: {self.name}"]
        for i, frame in enumerate(self.frames):
            result.append(f"  Кадр {i}: ЛЗП={frame.total_lci:.2f}, "
                         f"мастер={frame.is_master_state()}")
        return "\n".join(result)
```

### 5.2 Пример: Прямой удар кулаком

```
t=0.00с:  τ=(0,0,0,0),  ρ=(0.3, 0.6, 0, 0),  φ=(0, 0, 0, π/4)
           [нейтраль]

t=0.05с:  τ=(0.05, 0.05, 0.05, 0),  ρ=(0.3, 0.6, 0.1, 0),  φ=(π/8, π/8, π/16, π/4)
           [разгон]

t=0.10с:  τ=(0.1, 0.1, 0.1, 0),  ρ=(0.6, 0.6, 0.2, 0),  φ=(π/4, π/4, π/8, π/4)
           [ОПТИМУМ: φ_МВС = π/4]

t=0.15с:  τ=(0.15, 0.1, 0.1, 0),  ρ=(0.7, 0.6, 0.2, 0),  φ=(π/2, π/3, π/6, π/4)
           [контакт: пронация МВС завершается]
```

---

## ЗАКЛЮЧЕНИЕ: 4 ЧИСЛА = ПОЛНАЯ ИНФОРМАЦИЯ

Спинор состояния системы 4 сфер ЕТД содержит **полную информацию** о движении:

| Единица | Вопрос | Сфера | Значение |
|---------|--------|-------|---------|
| τ (время) | Когда? | Фаза активации | Синхронизация сфер |
| ρ (пространство) | Где? | Позиция центра | Вложенность сфер |
| n̂ (направление) | Куда? | Вектор действия | Коллинеарность |
| **φ (вращение)** | **Как?** | **Скрут** | **Оптимум = π/4** |

> **Мастер ЕТД** = тот, кто поддерживает все 4 единицы спинора в оптимальном состоянии одновременно во всех 4 сферах.

> **Четвёрка кватерниона порождает четвёрку спинора. Четвёрка спинора описывает четвёрку сфер.** (Герман → ЕТД)

**Источник**: Франц Герман «Четыре», стр. 4: «Спинор содержит всю четвёрку информации: расстояние во времени, расстояние в пространстве, направление в пространстве, вращение вокруг этого направления».

---

*ТОМ 105 / СЕРИЯ VII / ЕТД 2026*
