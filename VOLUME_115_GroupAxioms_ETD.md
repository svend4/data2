# ТОМ 115: 4 АКСИОМЫ ТЕОРИИ ГРУПП — АКСИОМАТИКА ЕТД
## Замкнутость · Ассоциативность · Единица · Обратный элемент
### «Теория групп основана на 4 аксиомах — ЕТД тоже»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Франц Герман «Четыре (математическое эссе о четвёрках)», стр. 4

---

## ВВЕДЕНИЕ

Франц Герман (стр. 4):
> «Теория групп основана на **4 аксиомах**: замкнутость, ассоциативность, единичный элемент, обратный элемент»

Группа — одна из фундаментальнейших структур математики. Она описывает симметрии, преобразования, кристаллы, физические законы. Всё это — из 4 аксиом.

**Тезис тома**: Система 4 сфер ЕТД с операцией «переключения ЧВС» образует **математическую группу** — удовлетворяет всем 4 аксиомам.

---

## ЧАСТЬ I: 4 АКСИОМЫ ГРУППЫ → 4 АКСИОМЫ ЕТД

### Аксиома G1: ЗАМКНУТОСТЬ

**Математика**: Если `a ∈ G` и `b ∈ G`, то `a * b ∈ G`.
Результат операции над элементами группы остаётся в той же группе.

**ЕТД-аксиома Z**: Любая комбинация ЧВС-переключений порождает новый допустимый ЧВС-контекст.

```python
class CVSGroup:
    """
    Группа ЧВС-переключений ЕТД.
    Элементы: все допустимые ЧВС-состояния.
    Операция: последовательное переключение.
    """
    VALID_STATES = {
        'Внутреннее', 'Инструментальное', 'Доменное',
        'Пространственное', 'Виртуальное',
        'Нейтральное'  # единичный элемент
    }

    def compose(self, cvs_1: str, cvs_2: str) -> str:
        """
        Аксиома G1 — Замкнутость:
        Любые два переключения → снова допустимое состояние.
        """
        # Таблица Кэли для группы ЧВС
        result = self._cayley_table.get((cvs_1, cvs_2), cvs_2)
        assert result in self.VALID_STATES, "Нарушение замкнутости!"
        return result

    def verify_closure(self) -> bool:
        """Проверить замкнутость для всех пар"""
        for s1 in self.VALID_STATES:
            for s2 in self.VALID_STATES:
                try:
                    self.compose(s1, s2)
                except AssertionError:
                    return False
        return True
```

**Физический смысл G1**: Нет «несуществующего» состояния — любая ситуация в схватке имеет допустимый ЧВС. Система никогда не «ломается».

---

### Аксиома G2: АССОЦИАТИВНОСТЬ

**Математика**: `(a * b) * c = a * (b * c)`
Порядок выполнения группировки не важен — важен только порядок элементов.

**ЕТД-аксиома А**: Последовательность переключений ЧВС ассоциативна.

```python
def verify_associativity(cvs_group: CVSGroup,
                          a: str, b: str, c: str) -> bool:
    """
    Аксиома G2 — Ассоциативность:
    (A→B)→C = A→(B→C)
    Неважно, как сгруппировать переключения — результат тот же.
    """
    left  = cvs_group.compose(cvs_group.compose(a, b), c)
    right = cvs_group.compose(a, cvs_group.compose(b, c))
    return left == right

# Пример:
# (Внутреннее → Доменное) → Пространственное
# = Внутреннее → (Доменное → Пространственное)
# Оба дают один и тот же финальный ЧВС
```

**Физический смысл G2**: Можно «думать» о серии переключений в любом порядке — (3 шага = 1+2) или (3 шага = 2+1 сначала) — результат одинаков. Это = ментальная свобода при планировании.

---

### Аксиома G3: ЕДИНИЧНЫЙ ЭЛЕМЕНТ (нейтраль)

**Математика**: Существует `e` такой, что `e * a = a * e = a`.
Единичный элемент не изменяет другие элементы.

**ЕТД-аксиома E**: Существует нейтральное ЧВС-состояние, которое не меняет систему.

```python
class NeutralCVS:
    """
    Нейтральное ЧВС = единичный элемент группы.
    Переключение в нейтраль не изменяет систему.
    Аналог: умножение на 1, сложение с 0.
    """
    name = "Нейтральное"
    phi = 0.0        # нет скрута
    lci_change = 0   # ЛЗП не меняется

    @staticmethod
    def is_neutral_state(sphere_state: dict) -> bool:
        """Проверка: находится ли система в нейтральном ЧВС?"""
        return (sphere_state.get('phi_cvs', 0) == 0 and
                sphere_state.get('cvs_direction') is None)

    @staticmethod
    def compose_with_any(any_cvs: str) -> str:
        """Нейтральный × любой = любой (аксиома единицы)"""
        return any_cvs
```

**Физический смысл G3**: Боец в «нейтральном» состоянии (никакого ЧВС не активировано) — готов к любому переключению. Нейтраль = исходная позиция, стойка без намерения.

---

### Аксиома G4: ОБРАТНЫЙ ЭЛЕМЕНТ

**Математика**: Для каждого `a ∈ G` существует `a⁻¹` такой, что `a * a⁻¹ = e`.

**ЕТД-аксиома O**: Для каждого ЧВС-переключения существует обратное переключение.

```python
class CVSInverse:
    """
    Обратный ЧВС — «отмена» переключения.
    Аналог: умножение на 1/a, сложение с -a.
    """

    INVERSE_MAP = {
        'Внутреннее':       'Виртуальное',    # внутрь ↔ вовне
        'Виртуальное':      'Внутреннее',
        'Инструментальное': 'Пространственное', # инструмент ↔ пространство
        'Пространственное': 'Инструментальное',
        'Доменное':         'Доменное',        # домен = своя обратность (инволюция)
        'Нейтральное':      'Нейтральное',     # нейтраль = самообратная
    }

    @staticmethod
    def inverse(cvs: str) -> str:
        """Найти обратный ЧВС"""
        return CVSInverse.INVERSE_MAP.get(cvs, 'Нейтральное')

    @staticmethod
    def verify_inverse(cvs_group: CVSGroup, cvs: str) -> bool:
        """Проверка: cvs * inverse(cvs) = нейтраль"""
        inv = CVSInverse.inverse(cvs)
        result = cvs_group.compose(cvs, inv)
        return result == 'Нейтральное'
```

**Физический смысл G4**: Каждое действие имеет «обратное» — если включил Внутреннее ЧВС, можно отменить переключением к Виртуальному. Никакое переключение не является «необратимым».

---

## ЧАСТЬ II: ТАБЛИЦА КЭЛИ ГРУППЫ ЧВС

Таблица умножения группы ЧВС (упрощённая):

| ∘ | Нейтр. | Внутр. | Инстр. | Домен | Простр. | Вирт. |
|---|--------|--------|--------|-------|---------|-------|
| **Нейтр.** | Нейтр. | Внутр. | Инстр. | Домен | Простр. | Вирт. |
| **Внутр.** | Внутр. | Нейтр. | Домен | Инстр. | Вирт. | Простр. |
| **Инстр.** | Инстр. | Домен | Нейтр. | Вирт. | Внутр. | Простр. |
| **Домен** | Домен | Инстр. | Вирт. | Нейтр. | Простр. | Внутр. |
| **Простр.** | Простр. | Вирт. | Внутр. | Простр. | Нейтр. | Домен |
| **Вирт.** | Вирт. | Простр. | Простр. | Внутр. | Домен | Нейтр. |

---

## ЧАСТЬ III: ПОДГРУППЫ ЧВС = СПЕЦИАЛИЗАЦИИ

Теория групп: подгруппа = замкнутое подмножество с теми же аксиомами.

```python
CVS_SUBGROUPS = {
    'Ближний бой':      {'Внутреннее', 'Инструментальное', 'Нейтральное'},
    'Дистанционный':    {'Пространственное', 'Доменное', 'Нейтральное'},
    'Мысленный':        {'Виртуальное', 'Нейтральное'},
    'Полный ЕТД':       {'Все 5 + Нейтральное'}   # полная группа
}
```

Боец-специалист работает в подгруппе. Мастер ЕТД = работает в полной группе.

---

## ЧАСТЬ IV: АКСИОМАТИКА ЕТД — ЕДИНЫЙ СПИСОК

Объединяя 4 аксиомы теории групп с ранее выведенными аксиомами (Тома 103-107):

| № | Аксиома | Источник | Содержание |
|---|---------|---------|------------|
| G1 | Замкнутость | Теория групп | Любая ситуация имеет допустимый ЧВС |
| G2 | Ассоциативность | Теория групп | Порядок группировки переключений не важен |
| G3 | Нейтраль | Теория групп | Существует нейтральное ЧВС (стойка без намерения) |
| G4 | Обратность | Теория групп | Каждое переключение обратимо |
| E | Энергия | Т.104 | Смена ЧВС сохраняет суммарный ЛЗП |
| P | Импульс | Т.104 | Тактический вектор сохраняется при смене ЧВС |
| Q | Движение | Т.104 | Тело движется непрерывно при любой ЧВС |
| L | π/4 | Т.104 | Оптимальный угол синхронизации = π/4 |
| T | Трансляция | Т.107 | ЛЗП не зависит от места |
| R | Вращение | Т.107 | ЛЗП не зависит от ориентации |

---

## ЗАКЛЮЧЕНИЕ

4 аксиомы теории групп (замкнутость, ассоциативность, нейтраль, обратность) → 4 фундаментальные аксиомы группы ЧВС ЕТД:

1. **G1 (Замкнутость)**: Нет ситуации без ЧВС
2. **G2 (Ассоциативность)**: Порядок мышления о переключениях свободен
3. **G3 (Нейтраль)**: Нейтральная стойка = единица группы
4. **G4 (Обратность)**: Каждое переключение можно отменить

> Система ЧВС ЕТД — математическая группа. Это означает: ЧВС — не «набор опций», а **строгая алгебраическая структура** с полной теоретической базой.

**Источник**: Франц Герман «Четыре», стр. 4: «Теория групп основана на 4 аксиомах: замкнутость, ассоциативность, единичный элемент, обратный элемент».

---
*ТОМ 115 / СЕРИЯ VII / ЕТД 2026*
