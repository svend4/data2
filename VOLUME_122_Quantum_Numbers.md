# ТОМ 122: 4 КВАНТОВЫХ ЧИСЛА — КВАНТОВЫЕ ЧИСЛА СФЕР ЕТД
## Главное · Два внутренних · Спиновое → Полная классификация состояний
### «4 квантовых числа однозначно определяют состояние электрона»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Франц Герман «Четыре», стр. 1

---

## ВВЕДЕНИЕ

Франц Герман (стр. 1):
> «4 квантовых числа: **главное, два внутренних, спиновое**»

Принцип Паули: два электрона не могут находиться в одном квантовом состоянии. Четыре квантовых числа (n, l, mₗ, mₛ) **однозначно** и **исчерпывающе** определяют состояние любого электрона в атоме.

**Тезис тома**: 4 квантовых числа = 4 «координаты» состояния системы сфер ЕТД. Любое состояние системы однозначно описывается этой четвёркой.

---

## ЧАСТЬ I: 4 КВАНТОВЫХ ЧИСЛА → 4 КООРДИНАТЫ СФЕРЫ ЕТД

### Таблица соответствий

| Квантовое число | Физический смысл | Возможные значения | ЕТД-аналог | Обозначение |
|----------------|-----------------|-------------------|-----------|------------|
| **n** (главное) | Энергетический уровень, орбита | n = 1, 2, 3, ... | **Уровень ЧВС** (1=Внутр., 2=Инстр., 3=Домен, 4=Простр., 5=Вирт.) | ν |
| **l** (орбит. момент) | Форма орбитали | 0 ≤ l ≤ n-1 | **Тип движения сферы** (0=перенос, 1=поворот, 2=отражение, 3=скользящее) | λ |
| **mₗ** (магн. квантовое) | Ориентация в пространстве | -l ≤ mₗ ≤ l | **Направление вектора сферы** (проекция на ось) | μ |
| **mₛ** (спиновое) | Спин электрона | +1/2 или -1/2 | **Скрут сферы** (+π/4 или -π/4) | σ |

---

## ЧАСТЬ II: ДЕТАЛЬНОЕ ОПИСАНИЕ

### 2.1 n (главное) → уровень ЧВС (ν)

**Квантовая механика**: n определяет энергию и размер орбиты. Чем больше n — тем дальше электрон от ядра.

**ЕТД**: ν = уровень ЧВС-направления. Чем больше ν — тем «дальше» ЧВС от физического тела:

```python
CVS_LEVELS = {
    1: 'Внутреннее',     # ближайший к телу
    2: 'Инструментальное',
    3: 'Доменное',
    4: 'Пространственное',
    5: 'Виртуальное',    # наиболее удалённое
}

def cvs_energy(nu: int) -> float:
    """
    Энергия ЧВС на уровне ν.
    Аналог: E_n = -13.6/n² эВ для водородоподобного атома.
    ЕТД: E_ЧВС(ν) = E₀ / ν² — ЧВС-«энергия» убывает с ростом ν.
    """
    E0 = 1.0   # нормированная единица ЛЗП
    return E0 / (nu ** 2)
```

---

### 2.2 l (орбитальный момент) → тип движения (λ)

**Квантовая механика**: l = 0 (s-орбиталь, сферическая), l = 1 (p, гантели), l = 2 (d, сложные), l = 3 (f, ещё сложнее).

**ЕТД**: λ = тип движения, которое выполняет сфера:

```python
MOVEMENT_TYPES = {
    0: 'Перенос (s-тип)',       # сферически симметричное движение
    1: 'Поворот (p-тип)',       # 3 направления (x, y, z)
    2: 'Отражение (d-тип)',     # 5 конфигураций
    3: 'Скользящее (f-тип)',    # 7 конфигураций
}

def allowed_lambda(nu: int) -> range:
    """λ ∈ [0, ν-1] — как l ∈ [0, n-1]"""
    return range(nu)
```

---

### 2.3 mₗ (магнитное) → ориентация вектора (μ)

**Квантовая механика**: mₗ = проекция орбитального момента на ось Z (ось квантования).

**ЕТД**: μ = проекция вектора движения сферы на ось опорного движения (БВС):

```python
def allowed_mu(lambda_val: int) -> range:
    """μ ∈ [-λ, +λ] — как mₗ ∈ [-l, +l]"""
    return range(-lambda_val, lambda_val + 1)

# λ=0: μ=0 (одно направление — перенос по одной оси)
# λ=1: μ=-1,0,+1 (три направления поворота — x, y, z)
# λ=2: μ=-2,-1,0,+1,+2 (пять ориентаций отражения)
```

---

### 2.4 mₛ (спиновое) → скрут φ (σ)

**Квантовая механика**: mₛ = +1/2 (спин «вверх») или -1/2 (спин «вниз»). Только два значения.

**ЕТД**: σ = скрут сферы. Только два оптимальных значения:

```python
import math

SPIN_ETD = {
    +0.5: +math.pi/4,   # пронация (скрут +π/4)
    -0.5: -math.pi/4,   # супинация (скрут -π/4)
}

def etd_spin(sigma: float) -> float:
    """Квантовый скрут сферы: ±π/4"""
    return SPIN_ETD[sigma]
```

**Принцип Паули для ЕТД**: Две сферы не могут находиться в одном состоянии (ν, λ, μ, σ). Если МВС занимает состояние (1, 0, 0, +π/4) — СВС должна быть в другом состоянии.

---

## ЧАСТЬ III: ПРИНЦИП ЗАПОЛНЕНИЯ ОРБИТАЛЕЙ → ПОРЯДОК АКТИВАЦИИ СФЕР

### 3.1 Правило Хунда для ЕТД

**Физика**: Электроны заполняют орбитали так, чтобы суммарный спин был максимальным (правило Хунда).

**ЕТД**: Сферы активируются так, чтобы суммарный ЛЗП был максимальным.

```python
def hund_etd(available_cvs_levels: list) -> list:
    """
    Правило Хунда для ЕТД:
    Активировать сферы так, чтобы суммарный ЛЗП был максимальным.
    Сначала заполняем уровни с максимальным ν (наибольшей энергией).
    """
    # Сортируем по убыванию ЛЗП-потенциала
    return sorted(available_cvs_levels,
                  key=lambda lvl: cvs_energy(lvl),
                  reverse=False)  # от низших к высшим

# Аналог: орбитали заполняются от низших энергий к высшим (принцип минимума)
# ЕТД: сначала базовые ЧВС (Внутреннее, ν=1), затем более сложные
```

### 3.2 Таблица заполнения «орбиталей» ЕТД

| Уровень ν | ЧВС-направление | λ-диапазон | Макс. μ-значений | Макс. сфер на уровне | 2(2ν+1) |
|----------|-----------------|------------|-----------------|---------------------|---------|
| 1 | Внутреннее | 0 | 1 | 2 (±σ) | 2 |
| 2 | Инструментальное | 0,1 | 1+3=4 | 8 | 8 |
| 3 | Доменное | 0,1,2 | 1+3+5=9 | 18 | 18 |
| 4 | Пространственное | 0,1,2,3 | 1+3+5+7=16 | 32 | 32 |
| 5 | Виртуальное | 0,1,2,3,4 | 1+3+5+7+9=25 | 50 | 50 |

---

## ЧАСТЬ IV: СПЕКТРАЛЬНЫЕ ПЕРЕХОДЫ ЧВС

**Квантовые переходы** электрона между уровнями → испускание/поглощение фотона.

**ЧВС-переходы** между уровнями → изменение ЛЗП (испускание/поглощение «кванта действия» ħ_ЕТД = π/4).

```python
def cvs_transition_energy(nu_1: int, nu_2: int) -> float:
    """
    Энергия перехода между уровнями ЧВС.
    Аналог формулы Бора: ΔE = E₀(1/n₁² - 1/n₂²)
    """
    E0 = 1.0
    return E0 * (1/nu_1**2 - 1/nu_2**2)

def allowed_transition(nu_1: int, lambda_1: int,
                        nu_2: int, lambda_2: int) -> bool:
    """
    Правило отбора для ЧВС-переходов.
    Аналог Δl = ±1 (правило отбора ЭМ-переходов).
    """
    delta_nu = abs(nu_2 - nu_1)
    delta_lambda = abs(lambda_2 - lambda_1)
    return delta_lambda == 1  # изменение типа движения на ±1
```

---

## ЗАКЛЮЧЕНИЕ

4 квантовых числа (n, l, mₗ, mₛ) → 4 «координаты» состояния сферы ЕТД (ν, λ, μ, σ):

1. **ν (главное)** → Уровень ЧВС (1=Внутреннее → 5=Виртуальное)
2. **λ (орбитальный)** → Тип движения (0=перенос, 1=поворот, 2=отражение, 3=скользящее)
3. **μ (магнитное)** → Ориентация вектора сферы (-λ ... +λ)
4. **σ (спиновое)** → Скрут ±π/4 (два значения)

> Принцип Паули ЕТД: Две сферы не могут находиться в одном (ν, λ, μ, σ)-состоянии.
> Правило Хунда ЕТД: Сферы активируются так, чтобы суммарный ЛЗП был максимальным.

**Источник**: Франц Герман «Четыре», стр. 1: «4 квантовых числа: главное, два внутренних, спиновое».

---
*ТОМ 122 / СЕРИЯ VII / ЕТД 2026*
