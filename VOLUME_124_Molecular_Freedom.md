# ТОМ 124: 4 СТЕПЕНИ СВОБОДЫ МОЛЕКУЛЫ — СТЕПЕНИ СВОБОДЫ СИСТЕМЫ СФЕР
## Трансляционные · Вращательные · Колебательные · Электронные
### «4 типа степеней свободы молекулы исчерпывают все виды движения»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Франц Герман «Четыре», стр. 2

---

## ВВЕДЕНИЕ

Франц Герман (стр. 2):
> «4 степени свободы движения молекулы: **трансляционные, вращательные, колебательные, электронные**»

В статистической физике каждая степень свободы несёт энергию kT/2 (теорема о равнораспределении). 4 типа — полная классификация всех видов молекулярного движения.

**Тезис тома**: 4 степени свободы молекулы = 4 вида движения системы сфер ЕТД. Каждая сфера несёт одну степень свободы.

---

## ЧАСТЬ I: 4 СТЕПЕНИ СВОБОДЫ → 4 ВИДА ДВИЖЕНИЯ ЕТД

| Тип | Физика | Формула энергии | Сфера ЕТД | Движение |
|-----|--------|----------------|----------|---------|
| **Трансляционные** | Движение центра масс в 3D | E = p²/2M | **БВС** | Движение тела в пространстве |
| **Вращательные** | Вращение вокруг осей | E = L²/2I | **СВС** | Ротация предплечий/кистей |
| **Колебательные** | Вибрация атомов в решётке | E = kx²/2 + p²/2m | **МВС** | Вибрация контактного уровня |
| **Электронные** | Переходы электронов | E = ħω | **ЧВС** | Переключения контекста |

---

## ЧАСТЬ II: ДЕТАЛЬНОЕ ОПИСАНИЕ

### Трансляционные → БВС (3 степени свободы)

**Физика**: Молекула движется в 3D-пространстве. 3 трансляционные степени свободы: x, y, z.

**ЕТД**: БВС движется в 3D-пространстве схватки:

```python
class TranslationalDOF:
    """Трансляционные степени свободы — БВС"""
    n_dof = 3  # x, y, z

    def energy(self, mass: float, velocity: tuple) -> float:
        """E_тр = M·v²/2"""
        vx, vy, vz = velocity
        return 0.5 * mass * (vx**2 + vy**2 + vz**2)

    def thermal_energy(self, T: float, k: float = 1.0) -> float:
        """По теореме равнораспределения: E = 3·kT/2"""
        return 1.5 * k * T  # 3 степени × kT/2
```

---

### Вращательные → СВС (2-3 степени свободы)

**Физика**: Вращение вокруг осей инерции. Для линейной молекулы — 2 оси, для нелинейной — 3.

**ЕТД**: СВС вращается вокруг трёх осей (плечо — нелинейная «молекула»):

```python
class RotationalDOF:
    """Вращательные степени свободы — СВС"""
    n_dof = 3  # три оси вращения

    def energy(self, I_x: float, I_y: float, I_z: float,
               omega_x: float, omega_y: float, omega_z: float) -> float:
        """E_вр = Σ(I·ω²/2)"""
        return 0.5 * (I_x*omega_x**2 + I_y*omega_y**2 + I_z*omega_z**2)

    def optimal_omega(self, L_target: float, I: float) -> float:
        """Оптимальная угловая скорость при заданном моменте"""
        return L_target / I
        # При π/4-синхронизации: ω_opt = L_max/I
```

---

### Колебательные → МВС (2 степени: кинет. + потенц.)

**Физика**: Колебания атомов в молекуле. Каждое колебание несёт 2 степени свободы (кинетическую и потенциальную), поэтому вклад в энергию = kT (а не kT/2).

**ЕТД**: МВС «колеблется» у точки контакта — вибрирующий контакт:

```python
import math

class VibrationalDOF:
    """Колебательные степени свободы — МВС"""
    n_dof = 2  # кинетическая + потенциальная (удвоено)

    def energy(self, k_spring: float, x: float,
               mass: float, v: float) -> float:
        """E_кол = kx²/2 + mv²/2 (сумма обоих)"""
        return 0.5 * k_spring * x**2 + 0.5 * mass * v**2

    def resonance_frequency(self, k_spring: float, mass: float) -> float:
        """ω₀ = √(k/m) — резонансная частота МВС"""
        return math.sqrt(k_spring / mass)

    # При ω_МВС = ω₀ → резонанс → максимальная амплитуда → максимальный ЛЗП
```

---

### Электронные → ЧВС (квантованные, дискретные)

**Физика**: Электронные степени свободы — переходы между энергетическими уровнями. Квантованы — ΔE = ħω. При комнатной температуре «заморожены» (kT << ħω_электр).

**ЕТД**: ЧВС-переключения — дискретные, квантованные (5 уровней):

```python
class ElectronicDOF:
    """Электронные степени свободы — ЧВС"""
    n_dof = None  # квантованы — нет классической степени свободы

    CVS_LEVELS = {
        1: 'Внутреннее',
        2: 'Инструментальное',
        3: 'Доменное',
        4: 'Пространственное',
        5: 'Виртуальное',
    }

    def transition_energy(self, level_1: int, level_2: int,
                           h_bar: float = math.pi/4) -> float:
        """
        ΔE = ħ_ЕТД × (1/ν₁² - 1/ν₂²)
        Аналог формулы Бора для электронных переходов.
        ħ_ЕТД = π/4 (квант действия ЕТД, из Тома 116).
        """
        return h_bar * (1/level_1**2 - 1/level_2**2)

    def is_frozen(self, lci: float) -> bool:
        """
        При низком ЛЗП (новичок) ЧВС 'заморожена' —
        аналог заморозки электронных степ. свободы при низкой температуре.
        """
        return lci < 0.3
```

**Важный вывод**: У новичка ЧВС «заморожена» (как электронные степени свободы при низкой T). По мере роста мастерства (рост «температуры» ЛЗП) — ЧВС «размораживается» и становится доступной.

---

## ЧАСТЬ III: ТЕОРЕМА О РАВНОРАСПРЕДЕЛЕНИИ ЕТД

**Физика**: При тепловом равновесии каждая степень свободы несёт энергию kT/2.

**ЕТД**: При оптимальной синхронизации (π/4) каждая сфера несёт одинаковый вклад в ЛЗП:

```python
def equipartition_etd(total_lci: float, n_active_spheres: int) -> float:
    """
    Теорема о равнораспределении ЕТД:
    При синхронизации π/4 каждая сфера несёт ЛЗП_total / n.
    При n=4: каждая сфера = ЛЗП/4.
    """
    return total_lci / n_active_spheres

# При n=4 (все сферы активны): каждая несёт ЛЗП/4
# При n=3 (без ЧВС): только 3 сферы, каждая несёт ЛЗП/3 (перегрузка!)
# Наличие ЧВС СНИЖАЕТ нагрузку на каждую сферу
```

**Следствие**: ЧВС не только добавляет контекст — она **распределяет нагрузку** с трёх физических сфер на четыре, снижая перегрузку каждой из них.

---

## ЧАСТЬ IV: ПОЛНОЕ ЧИСЛО СТЕПЕНЕЙ СВОБОДЫ

| Тип | n_dof | Вклад в E | Сфера | При наличии ЧВС |
|-----|-------|----------|-------|----------------|
| Трансляционные | 3 | 3kT/2 | БВС | Нагрузка / 4 |
| Вращательные | 3 | 3kT/2 | СВС | Нагрузка / 4 |
| Колебательные | 2 | kT (удвоен) | МВС | Нагрузка / 4 |
| Электронные | квантованы | ħω | ЧВС | Активирует сама себя |
| **Итого** | **≥8** | **7kT/2** | **4 сферы** | |

---

## ЗАКЛЮЧЕНИЕ

4 степени свободы молекулы = 4 вида движения системы ЕТД:

1. **Трансляционные** → **БВС**: движение тела в пространстве (3D)
2. **Вращательные** → **СВС**: ротация предплечий (три оси)
3. **Колебательные** → **МВС**: вибрация контакта (кинетич. + потенц.)
4. **Электронные** → **ЧВС**: квантованные дискретные переключения

> Теорема равнораспределения ЕТД: при синхронизации π/4 каждая сфера несёт ЛЗП/4 — равномерное распределение нагрузки. ЧВС снижает нагрузку на физические сферы на 25%.

**Источник**: Франц Герман «Четыре», стр. 2: «4 степени свободы движения молекулы: трансляционные, вращательные, колебательные, электронные».

---
*ТОМ 124 / СЕРИЯ VII / ЕТД 2026*
