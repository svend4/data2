# ТОМ 125: 4 БИНАРНЫХ ОПЕРАЦИИ НА ГРАФАХ + 4 ОПЕРАЦИИ НАД МНОЖЕСТВАМИ
## Объединение · Присоединение · Произведение · Композиция / U · ∩ · \ · Δ
### «Алгебра ЧВС через операции над структурами»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Франц Герман «Четыре», стр. 4

---

## ВВЕДЕНИЕ

Франц Герман (стр. 4):
> «4 бинарные операции на графах: **объединение, присоединение, произведение, композиция**»
> «4 операции над множествами: **объединение (U), пересечение (∩), разность (\), симметрическая разность (Δ)**»

Две четвёрки — для структур (графов) и для содержимого (множеств). Вместе они дают **полную алгебру** для работы с системами ЧВС.

---

## ЧАСТЬ I: 4 ОПЕРАЦИИ НА ГРАФАХ → ОПЕРАЦИИ НАД СИСТЕМАМИ ЧВС

Граф G = (V, E) — вершины + рёбра. Система ЧВС = граф: вершины = ЧВС-состояния, рёбра = переключения.

### 1.1 Объединение (G₁ ∪ G₂)

**Математика**: Все вершины и рёбра обоих графов вместе.

**ЧВС-операция**: Объединение двух ЧВС-систем — например, систем двух бойцов.

```python
def cvs_graph_union(G1: dict, G2: dict) -> dict:
    """
    Объединение ЧВС-систем двух бойцов.
    Результат: все состояния и переходы обоих.
    Применение: совместная работа, тандем.
    """
    united_states = G1['states'] | G2['states']
    united_transitions = G1['transitions'] | G2['transitions']
    return {'states': united_states, 'transitions': united_transitions}
```

---

### 1.2 Присоединение (G₁ + G₂)

**Математика**: Объединение + все рёбра между вершинами G₁ и G₂.

**ЧВС-операция**: Объединение + связи «перекидывания» ЧВС между системами.

```python
def cvs_graph_join(G1: dict, G2: dict) -> dict:
    """
    Присоединение ЧВС-систем + мосты между ними.
    Применение: взаимодействие двух бойцов,
    когда любое состояние G1 может перейти в любое G2.
    """
    joined = cvs_graph_union(G1, G2)
    # Добавляем все мосты: каждый S1 → каждый S2
    cross_transitions = {
        (s1, s2): 'bridge'
        for s1 in G1['states']
        for s2 in G2['states']
    }
    joined['transitions'].update(cross_transitions)
    return joined
```

---

### 1.3 Произведение (G₁ × G₂)

**Математика**: Вершины = пары (v₁, v₂), рёбра — если оба соответствующих ребра в G₁ и G₂.

**ЧВС-операция**: Произведение = «синхронная» система: два ЧВС одновременно.

```python
def cvs_graph_product(G1: dict, G2: dict) -> dict:
    """
    Произведение: одновременная активация двух ЧВС.
    Вершины = пары (CVS_1, CVS_2) → двумерное ЧВС-пространство.
    Применение: боец в двух доменах одновременно
    (напр., Доменное × Пространственное = спорт на конкретной арене).
    """
    product_states = {
        (s1, s2)
        for s1 in G1['states']
        for s2 in G2['states']
    }
    return {'states': product_states, 'type': 'произведение'}
```

---

### 1.4 Композиция (G₁ [G₂])

**Математика**: G₁[G₂] — заменить каждую вершину G₁ копией G₂. Иерархическая вложенность.

**ЧВС-операция**: Каждое ЧВС-состояние содержит полную вложенную ЧВС-систему (из Тома 109!).

```python
def cvs_graph_composition(G1: dict, G2: dict) -> dict:
    """
    Композиция G1[G2]: каждое ЧВС-состояние G1
    заменяется полной копией G2.
    Это = рекурсивная ЧВС из Тома 109!
    Применение: каждый ЧВС-уровень содержит свою внутреннюю ЧВС-систему.
    """
    composite = {}
    for state in G1['states']:
        composite[state] = G2.copy()  # каждое состояние = полная G2
    return composite
```

---

## ЧАСТЬ II: 4 ОПЕРАЦИИ НАД МНОЖЕСТВАМИ → ТАКТИЧЕСКИЕ ОПЕРАЦИИ

Множества ЧВС: C₁ = множество ЧВС-состояний бойца 1, C₂ = бойца 2.

### 2.1 Объединение C₁ ∪ C₂ — Суперпозиция

Все ЧВС-состояния обоих. Максимальный репертуар.

```python
def cvs_union(C1: set, C2: set) -> set:
    """
    C1 ∪ C2: все ЧВС обоих бойцов.
    Применение: тренировка с более опытным партнёром
    расширяет репертуар ЧВС через объединение.
    """
    return C1 | C2
```

---

### 2.2 Пересечение C₁ ∩ C₂ — Общая зона

Только общие ЧВС. Зона взаимопонимания.

```python
def cvs_intersection(C1: set, C2: set) -> set:
    """
    C1 ∩ C2: общие ЧВС-состояния.
    Применение: зона взаимопонимания — те ЧВС, которые
    оба бойца знают. Основа для совместных действий.
    Если пересечение мало → трудно взаимодействовать.
    """
    return C1 & C2
```

---

### 2.3 Разность C₁ \ C₂ — Уникальное преимущество

ЧВС бойца 1, которых нет у бойца 2. Тактическое преимущество.

```python
def cvs_difference(C1: set, C2: set) -> set:
    """
    C1 \\ C2: ЧВС бойца 1, которых нет у бойца 2.
    = Уникальное тактическое преимущество.
    Стратегия: использовать именно эти ЧВС против данного противника.
    """
    unique = C1 - C2
    print(f"Уникальные ЧВС (преимущество): {unique}")
    return unique
```

---

### 2.4 Симметрическая разность C₁ Δ C₂ — Зона различий

Те ЧВС, которые есть только у одного из двух. Область несовместимости и специализации.

```python
def cvs_symmetric_difference(C1: set, C2: set) -> set:
    """
    C1 Δ C2: ЧВС, которые есть только у одного.
    = Зона специализации каждого.
    При большом Δ → бойцы специализированы, трудно адаптироваться.
    При малом Δ → похожие стили (труднее удивить).
    Оптимум: C1 Δ C2 = 2-3 ЧВС (небольшое различие).
    """
    return C1 ^ C2
```

---

## ЧАСТЬ III: АЛГЕБРА ЧВС — СТРУКТУРА БУЛЕВОЙ РЕШЁТКИ

Множество всех ЧВС-конфигураций с операциями ∪, ∩, \ образует **булеву решётку**:

```python
class CVSBooleanLattice:
    """
    Булева решётка ЧВС-конфигураций.
    Операции: ∪ (верхняя грань), ∩ (нижняя грань), \ (дополнение).
    """

    ALL_CVS = {'Внутреннее', 'Инструментальное', 'Доменное',
               'Пространственное', 'Виртуальное'}

    def complement(self, C: set) -> set:
        """Дополнение: ЧВС, которых нет в C"""
        return self.ALL_CVS - C

    def de_morgan(self, C1: set, C2: set) -> bool:
        """
        Законы де Моргана для ЧВС:
        ¬(C1 ∪ C2) = ¬C1 ∩ ¬C2
        ¬(C1 ∩ C2) = ¬C1 ∪ ¬C2
        """
        lhs = self.complement(C1 | C2)
        rhs = self.complement(C1) & self.complement(C2)
        return lhs == rhs   # всегда True

    def covering_number(self) -> int:
        """Минимальное покрытие: теорема 4 красок → 4 ЧВС достаточно"""
        return 4  # по теореме 4 красок (Том 110)
```

---

## ЗАКЛЮЧЕНИЕ

Две четвёрки Германа дают **полную алгебру для работы с ЧВС-системами**:

**4 операции на графах** (операции над структурами):
1. Объединение → суперпозиция ЧВС-систем двух бойцов
2. Присоединение → полное взаимодействие с мостами
3. Произведение → одновременная двойная ЧВС
4. Композиция → рекурсивная вложенная ЧВС (Том 109)

**4 операции над множествами** (операции над содержимым):
1. Объединение (∪) → расширение репертуара ЧВС
2. Пересечение (∩) → зона взаимопонимания
3. Разность (\) → уникальное тактическое преимущество
4. Симм. разность (Δ) → зона специализации

> Вместе: **полная алгебра** — от структуры переключений до анализа тактических преимуществ.

**Источник**: Франц Герман «Четыре», стр. 4: «4 бинарные операции на графах: объединение, присоединение, произведение, композиция» + «4 операции на множествах: U, ∩, \, Δ».

---
*ТОМ 125 / СЕРИЯ VII / ЕТД 2026*
