# ТОМ 126: 4 ТИПА КАСАТЕЛЬНЫХ ОКРУЖНОСТЕЙ — ГЕОМЕТРИЯ КАСАНИЯ СФЕР ЕТД
## Внутренняя · Внешняя · Две промежуточных → Геометрия контакта сфер

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Франц Герман «Четыре», стр. 5

---

## ВВЕДЕНИЕ

Франц Герман (стр. 5):
> «Только **4 типа касательных окружностей** к двум непересекающимся окружностям»

Задача Аполлония: найти окружности, касающиеся двух заданных. К двум непересекающимся окружностям существуют ровно **4 общих касательных**: 2 внешних и 2 внутренних (при разных радиусах).

**Тезис тома**: Геометрия касания сфер ЕТД описывается теми же 4 типами. Любой контакт МВС↔СВС, СВС↔БВС, МВС↔ЧВС классифицируется одним из 4 способов касания.

---

## ЧАСТЬ I: 4 ТИПА КАСАНИЯ → 4 ВИДА КОНТАКТА СФЕР

### 1.1 Физическое описание 4 касательных

К двум окружностям C₁ (радиус r₁) и C₂ (радиус r₂):
- **Внешняя касательная 1** (касание снаружи обеих): прямая, не пересекающая отрезок центров
- **Внешняя касательная 2**: симметричная первой
- **Внутренняя касательная 1** (пересекает отрезок центров)
- **Внутренняя касательная 2**: симметричная третьей

### 1.2 4 Типа контакта сфер ЕТД

| Тип касания | Геометрия | ЕТД-интерпретация | Техника |
|-------------|----------|-----------------|---------|
| **Внешняя (A)** | Обе сферы снаружи | МВС касается БВС снаружи противника | Прямой удар |
| **Внешняя (B)** | Симметрично (A) | МВС с другой стороны | Удар с другой руки |
| **Внутренняя (C)** | Одна «внутри» другой | МВС входит в зону СВС противника | Клинч, захват |
| **Внутренняя (D)** | Симметрично (C) | МВС «обтекает» с другой стороны | Обходной захват |

```python
import math
from dataclasses import dataclass
from typing import Tuple

@dataclass
class Circle:
    """Окружность = сфера в 2D-проекции"""
    center: Tuple[float, float]
    radius: float
    name: str  # 'МВС', 'СВС', 'БВС', 'ЧВС'

def find_tangent_circles_count(C1: Circle, C2: Circle) -> dict:
    """
    Определить тип и количество общих касательных.
    d = расстояние между центрами.
    """
    d = math.dist(C1.center, C2.center)
    r1, r2 = C1.radius, C2.radius

    if d > r1 + r2:
        return {'type': 'внешние', 'external': 2, 'internal': 2, 'total': 4}
    elif d == r1 + r2:
        return {'type': 'внешнее касание', 'external': 2, 'internal': 1, 'total': 3}
    elif abs(r1 - r2) < d < r1 + r2:
        return {'type': 'пересекаются', 'external': 2, 'internal': 0, 'total': 2}
    elif d == abs(r1 - r2):
        return {'type': 'внутреннее касание', 'external': 1, 'internal': 0, 'total': 1}
    else:
        return {'type': 'одна внутри другой', 'external': 0, 'internal': 0, 'total': 0}

# Для системы ЕТД: МВС и БВС разнесены → 4 касательных (максимум)
mvs = Circle((0, 0), 0.3, 'МВС')
bvs = Circle((1.0, 0), 1.0, 'БВС')
result = find_tangent_circles_count(mvs, bvs)
print(result)  # {'type': 'внешние', 'total': 4}
```

---

## ЧАСТЬ II: 4 ТИПА КАСАНИЯ → 4 СТРАТЕГИИ КОНТАКТА

### 2.1 Матрица контактных стратегий

| Пара сфер | Касание A (внешн.) | Касание B (внешн.) | Касание C (внутр.) | Касание D (внутр.) |
|-----------|------------------|--------------------|-------------------|-------------------|
| **МВС ↔ СВС** | Прямой блок снаружи | Обратный блок | Рука внутри блока | Проскальзывание |
| **СВС ↔ БВС** | Предплечье снаружи тела | С другой стороны | Предплечье в клинч | Обходной клинч |
| **МВС ↔ БВС** | Дистанционный удар | Удар с другой руки | Захват с вхождением | Обходной захват |
| **Любая ↔ ЧВС** | Контекст снаружи ситуации | Зеркальный контекст | Контекст внутри ситуации | Вложенный контекст |

---

## ЧАСТЬ III: СПЕЦИАЛЬНЫЕ СЛУЧАИ (3, 2, 1, 0 КАСАТЕЛЬНЫХ)

```python
def etd_contact_topology(sphere_pair: str, distance_ratio: float) -> str:
    """
    Топология контакта в зависимости от дистанции.
    distance_ratio = d / (r1 + r2)
    """
    if distance_ratio > 1.0:
        return "4 касательных: максимальная гибкость контакта (боевая дистанция)"
    elif distance_ratio == 1.0:
        return "3 касательных: внешнее касание (момент первого контакта)"
    elif 0 < distance_ratio < 1.0:
        return "2 касательных: пересечение сфер (средняя дистанция, частичный контакт)"
    elif distance_ratio == 0:
        return "1 касательная: внутреннее касание (клинч)"
    else:
        return "0 касательных: одна сфера внутри другой (полное поглощение — плохо)"

# Оптимальная дистанция: 4 касательных (максимальная тактическая гибкость)
# = расстояние между центрами > сумма радиусов
```

**Тактический вывод**: Оптимальная боевая дистанция = **4 касательных** (d > r₁ + r₂). Это = дистанция, при которой система сфер имеет максимальную тактическую гибкость.

---

## ЧАСТЬ IV: ТЕОРЕМА АПОЛЛОНИЯ ДЛЯ СИСТЕМЫ 3 СФЕР

Задача Аполлония обобщается: найти сферу, касающуюся ТРЁХ заданных. Решений всего **8** (для 3 сфер в общем положении).

**ЕТД-аналог**: К системе МВС+СВС+БВС можно найти **8 различных ЧВС-конфигураций**. Это согласуется с числом 8 = 2³ (бинарные выборы для 3 сфер).

```python
def apollonius_cvs_count(n_physical_spheres: int) -> int:
    """
    Число конфигураций ЧВС для n физических сфер.
    По обобщённой задаче Аполлония: 2^n решений.
    Для n=3 (МВС+СВС+БВС): 2³ = 8 конфигураций.
    Для n=4 (все сферы): 2⁴ = 16 конфигураций.
    """
    return 2 ** n_physical_spheres

print(apollonius_cvs_count(3))   # 8
print(apollonius_cvs_count(4))   # 16  → полное ЧВС-пространство
```

---

## ЗАКЛЮЧЕНИЕ

4 типа касательных окружностей (A, B — внешние; C, D — внутренние) = **4 стратегии контакта сфер ЕТД**:

1. **Внешняя A** → Прямой удар/блок снаружи
2. **Внешняя B** → Симметричный (другой рукой)
3. **Внутренняя C** → Захват/вхождение в зону
4. **Внутренняя D** → Обходной захват/скольжение

> Оптимальная дистанция (4 касательных) = тактический максимум гибкости. По задаче Аполлония: к 3 физическим сферам — 8 ЧВС-конфигураций.

**Источник**: Франц Герман «Четыре», стр. 5: «Только 4 типа касательных окружностей к двум непересекающимся окружностям».

---
*ТОМ 126 / СЕРИЯ VII / ЕТД 2026*
