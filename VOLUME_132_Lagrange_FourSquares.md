# ТОМ 132: ТЕОРЕМА ЛАГРАНЖА О 4 КВАДРАТАХ — ЛЗП КАК СУММА 4 КВАДРАТОВ
## Любое натуральное число = сумма 4 квадратов → ЛЗП = МВС² + СВС² + БВС² + ЧВС²
### «Теорема Лагранжа 1770: n = a² + b² + c² + d²»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Аддитивная теория чисел

---

## ВВЕДЕНИЕ

**Теорема Лагранжа о четырёх квадратах (1770)**:

> Каждое натуральное число может быть представлено как сумма **четырёх** целых квадратов:
> `n = a² + b² + c² + d²`

Три квадрата не всегда достаточно (например, 7 нельзя представить как сумму трёх квадратов). Четыре — всегда достаточно.

**Тезис тома**: ЛЗП = МВС² + СВС² + БВС² + ЧВС² — это теорема Лагранжа! Любой ЛЗП раскладывается в сумму квадратов вкладов 4 сфер. Именно 4 — не 3.

---

## ЧАСТЬ I: ТЕОРЕМА ЛАГРАНЖА → ФОРМУЛА ЛЗП

### 1.1 Прямое соответствие

**Теорема Лагранжа**: n = a² + b² + c² + d²

**Формула ЛЗП-4** (кватернионная норма, Том 103):
```
ЛЗП² = БВС² + СВС² + МВС² + ЧВС²
```

Это **буквально** теорема Лагранжа: квадрат нормы кватерниона = сумма 4 квадратов компонент.

```python
import math
from typing import Tuple, List

def lagrange_lci(bvs: float, svs: float, mvs: float, cvs: float) -> float:
    """
    ЛЗП по теореме Лагранжа — как квадратный корень суммы 4 квадратов.
    ЛЗП² = БВС² + СВС² + МВС² + ЧВС²
    = кватернионная норма (Том 103)!
    """
    return math.sqrt(bvs**2 + svs**2 + mvs**2 + cvs**2)

def lagrange_decomposition(target_lci_squared: int) -> List[Tuple[int,int,int,int]]:
    """
    Разложение целевого ЛЗП² (целочисленного) в сумму 4 квадратов.
    По теореме Лагранжа — всегда существует!
    Возвращает первое найденное разложение.
    """
    n = target_lci_squared
    limit = int(math.isqrt(n))
    for a in range(limit, -1, -1):
        for b in range(int(math.isqrt(n - a**2)), -1, -1):
            for c in range(int(math.isqrt(n - a**2 - b**2)), -1, -1):
                rem = n - a**2 - b**2 - c**2
                if rem >= 0 and int(math.isqrt(rem))**2 == rem:
                    d = int(math.isqrt(rem))
                    return [(a, b, c, d)]
    return []  # никогда не произойдёт — теорема гарантирует существование!

# Пример: ЛЗП² = 7 → нельзя разложить в 3 квадрата, но можно в 4!
decomp = lagrange_decomposition(7)
print(f"7 = {decomp}")  # [(2, 1, 1, 1)] → 4+1+1+1=7 ✓

# ЛЗП² = 4 → БВС=2, СВС=0, МВС=0, ЧВС=0 (или другие)
print(lagrange_decomposition(4))   # [(2,0,0,0)]
```

### 1.2 Число Гурвица = 4

В теории квадратичных форм — единственные нормированные алгебры над ℝ имеют размерности 1, 2, 4, 8 (теорема Гурвица). Размерность 4 → кватернионы.

```python
HURWITZ_DIMENSIONS = [1, 2, 4, 8]
# 1 → ℝ (вещественные)
# 2 → ℂ (комплексные)
# 4 → ℍ (кватернионы) = СИСТЕМА 4 СФЕР!
# 8 → 𝕆 (октонионы)

def hurwitz_check(n: int) -> str:
    if n in HURWITZ_DIMENSIONS:
        return f"{n} — нормированная алгебра Гурвица"
    return f"{n} — не является нормированной алгеброй"

print(hurwitz_check(4))  # "4 — нормированная алгебра Гурвица"
# Следствие: кватернион с 4 компонентами — единственная 4-мерная нормированная алгебра!
```

---

## ЧАСТЬ II: 7 — «ЗАПРЕТНОЕ» ЧИСЛО ДЛЯ 3 КВАДРАТОВ

### 2.1 Числа, не представимые суммой 3 квадратов

Числа вида `4^a(8b+7)` нельзя представить суммой трёх квадратов. Первые такие числа: 7, 15, 23, 28...

**ЕТД-интерпретация**: Системе из 3 сфер (без ЧВС) **недоступен** целый класс ситуаций (аналог «запретных» чисел). Только система из 4 сфер — универсальна.

```python
def can_be_three_squares(n: int) -> bool:
    """Проверка: можно ли n представить как сумму 3 квадратов?"""
    import math
    # Числа вида 4^a(8b+7) — нельзя
    while n % 4 == 0:
        n //= 4
    return n % 8 != 7

# Тест
for n in [1, 2, 3, 4, 5, 6, 7, 14, 15]:
    print(f"{n}: {'✓ 3 квадрата' if can_be_three_squares(n) else '✗ нужно 4!'}")
# 7: ✗ нужно 4! — три сферы не справятся с этой ситуацией
# 15: ✗ нужно 4! — аналогично
```

### 2.2 «Запретные» ситуации для системы из 3 сфер

```
Система без ЧВС (3 сферы: МВС+СВС+БВС):
- Покрывает ~ 87.5% всех целых ЛЗП
- Не покрывает числа вида 4^a(8b+7)

Система с ЧВС (4 сферы):
- Покрывает 100% всех ЛЗП (теорема Лагранжа!)
- Нет «запретных» ситуаций
```

---

## ЗАКЛЮЧЕНИЕ

**Теорема Лагранжа** → **Полнота системы 4 сфер**:

- ЛЗП² = МВС² + СВС² + БВС² + ЧВС² — всегда разложим (теорема Лагранжа)
- 3 сферы недостаточно: класс «запретных чисел» 4^a(8b+7) недостижим
- 4 сферы = **достаточное и минимальное** условие полноты покрытия
- Норма кватерниона = сумма 4 квадратов = прямой аналог теоремы Лагранжа

> Теорема Лагранжа: n = a² + b² + c² + d² ↔ ЛЗП = |кватернион| = √(МВС²+СВС²+БВС²+ЧВС²). Четыре квадрата — математически необходимо и достаточно.

---
*ТОМ 132 / СЕРИЯ VII / ЕТД 2026*
