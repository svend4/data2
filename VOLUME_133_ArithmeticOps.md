# ТОМ 133: 4 АРИФМЕТИЧЕСКИХ ДЕЙСТВИЯ — БАЗИС ВЫЧИСЛЕНИЙ ЕТД
## Сложение · Вычитание · Умножение · Деление → 4 операции над ЛЗП
### «4 действия арифметики — фундамент всей математики»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Базовая арифметика / теория колец

---

## ВВЕДЕНИЕ

В математике 4 основных арифметических действия:
1. **Сложение** (+)
2. **Вычитание** (−)
3. **Умножение** (×)
4. **Деление** (÷)

Всё многообразие математики строится на этих четырёх операциях. Они образуют **поле** (кольцо с делением).

**Тезис тома**: Каждое из 4 арифметических действий соответствует одной базовой операции над сферами ЕТД. Вместе они образуют полный базис «арифметики движений».

---

## ЧАСТЬ I: 4 ОПЕРАЦИИ → 4 ДЕЙСТВИЯ НАД СФЕРАМИ

### 1.1 Таблица соответствий

| Операция | Знак | Сфера | Действие ЕТД | ЛЗП-формула |
|----------|------|-------|-------------|-------------|
| **Сложение** | + | БВС | Объединение потенциалов | ЛЗП = ЛЗП₁ + ЛЗП₂ |
| **Вычитание** | − | МВС | Поглощение (нейтрализация) | ЛЗП = ЛЗП_свой − ЛЗП_чужой |
| **Умножение** | × | СВС | Синергия (усиление) | ЛЗП = ЛЗП_тела × k_синергии |
| **Деление** | ÷ | ЧВС | Распределение по контексту | ЛЗП_узла = ЛЗП_total / N_ЧВС |

```python
import math

class ETDArithmetic:
    """
    4 арифметических действия как 4 базовые операции над ЛЗП системы ЕТД.
    """

    @staticmethod
    def addition_bvs(lci_1: float, lci_2: float) -> float:
        """
        СЛОЖЕНИЕ (БВС): Объединение потенциалов.
        Два тела (или два движения) суммируют ЛЗП.
        Пример: удар + шаг = ЛЗП_удара + ЛЗП_шага.
        """
        return lci_1 + lci_2

    @staticmethod
    def subtraction_mvs(lci_self: float, lci_opponent: float) -> float:
        """
        ВЫЧИТАНИЕ (МВС): Нейтрализация потенциала противника.
        Блок = отнять ЛЗП атаки.
        Пример: ЛЗП_защиты = ЛЗП_мастера − ЛЗП_атаки.
        """
        return lci_self - lci_opponent

    @staticmethod
    def multiplication_svs(lci_body: float, synergy_factor: float) -> float:
        """
        УМНОЖЕНИЕ (СВС): Синергетическое усиление.
        Предплечье умножает импульс тела.
        Пример: ЛЗП_удара = ЛЗП_тела × k (к > 1 при правильной технике).
        Оптимум: k = π/4 × 4/π = 1 (нейтральный), максимум k=e≈2.718.
        """
        return lci_body * synergy_factor

    @staticmethod
    def division_cvs(lci_total: float, n_contexts: int) -> float:
        """
        ДЕЛЕНИЕ (ЧВС): Распределение потенциала по контекстам.
        ЧВС делит общий ЛЗП между несколькими одновременными задачами.
        Пример: ЛЗП_на_одну_задачу = ЛЗП_total / 4 (при 4 ЧВС).
        """
        if n_contexts == 0:
            return float('inf')
        return lci_total / n_contexts

    def field_axioms_check(self, a: float, b: float, c: float) -> dict:
        """
        Проверить, что операции над ЛЗП образуют поле (как числа).
        Коммутативность, ассоциативность, дистрибутивность.
        """
        return {
            'коммутативность_+': abs(self.addition_bvs(a, b) - self.addition_bvs(b, a)) < 1e-9,
            'ассоциативность_+': abs(
                self.addition_bvs(self.addition_bvs(a, b), c) -
                self.addition_bvs(a, self.addition_bvs(b, c))
            ) < 1e-9,
            'дистрибутивность': abs(
                self.multiplication_svs(self.addition_bvs(a, b), c) -
                self.addition_bvs(self.multiplication_svs(a, c),
                                   self.multiplication_svs(b, c))
            ) < 1e-9,
            'нейтральный_+': abs(self.addition_bvs(a, 0) - a) < 1e-9,
            'нейтральный_×': abs(self.multiplication_svs(a, 1.0) - a) < 1e-9,
        }

etd_arith = ETDArithmetic()
# ЛЗП_total = ((ЛЗП₁ + ЛЗП₂) − ЛЗП_противника) × k / N_ЧВС
lci1, lci2 = 2.0, 1.5
lci_opp = 1.0
k = math.pi / 4 * 1.5   # k = 1.5 × π/4 ≈ 1.178
N = 4

lci_combined = etd_arith.addition_bvs(lci1, lci2)
lci_net = etd_arith.subtraction_mvs(lci_combined, lci_opp)
lci_amplified = etd_arith.multiplication_svs(lci_net, k)
lci_per_context = etd_arith.division_cvs(lci_amplified, N)
print(f"ЛЗП на контекст: {lci_per_context:.3f}")
```

### 1.2 Порядок операций = Порядок вовлечения сфер

**Правило приоритета арифметических действий** (умножение перед сложением) ↔ **Правило вовлечения сфер ЕТД**:

```
Приоритет арифметики: ( ) → × ÷ → + −
Приоритет сфер ЕТД:  ЧВС → СВС → МВС → БВС

ЧВС первая (скобки — контекст задаёт рамку)
СВС×ЧВС (умножение — синергия предплечья)
МВС−, МВС+ (сложение/вычитание — финальный удар)
БВС+ (сложение — тело суммирует всё)
```

---

## ЧАСТЬ II: КОЛЬЦО И ПОЛЕ ОПЕРАЦИЙ ЕТД

### 2.1 Алгебраическая структура

4 операции образуют **поле** (field) — алгебраическая структура с полным набором свойств:

```python
class ETDField:
    """
    Поле (Field) операций над ЛЗП.
    Сложение + вычитание = аддитивная группа.
    Умножение + деление = мультипликативная группа.
    Вместе → поле (Field).
    """

    OPERATIONS = {
        '+': ('аддитивная', 'БВС', 'объединение'),
        '-': ('аддитивная', 'МВС', 'нейтрализация'),
        '×': ('мультипликативная', 'СВС', 'синергия'),
        '÷': ('мультипликативная', 'ЧВС', 'распределение'),
    }

    def apply(self, op: str, a: float, b: float) -> float:
        if op == '+': return a + b
        if op == '-': return a - b
        if op == '×': return a * b
        if op == '÷': return a / b if b != 0 else float('inf')
        raise ValueError(f"Неизвестная операция: {op}")

    def etd_expression(self, lci_bvs, lci_svs, lci_mvs, lci_cvs) -> float:
        """
        Полное выражение ЛЗП через 4 операции:
        ЛЗП = (БВС + СВС × МВС) - ЧВС_нейтрализация ÷ N
        Аналог алгебраического выражения с 4 операциями.
        """
        inner = self.apply('+', lci_bvs, self.apply('×', lci_svs, lci_mvs))
        result = self.apply('-', inner, self.apply('÷', lci_cvs, 4))
        return result
```

---

## ЗАКЛЮЧЕНИЕ

**4 арифметических действия** → **4 операции системы ЕТД**:

1. **Сложение (+)** → БВС: объединение потенциалов тела
2. **Вычитание (−)** → МВС: нейтрализация атаки противника
3. **Умножение (×)** → СВС: синергетическое усиление
4. **Деление (÷)** → ЧВС: распределение по контекстам

> 4 операции образуют поле (Field) — полную алгебраическую структуру. Приоритет операций = порядок вовлечения сфер: ЧВС (скобки) → СВС×ЧВС → МВС+− → БВС+.

---
*ТОМ 133 / СЕРИЯ VII / ЕТД 2026*
