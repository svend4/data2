# ТОМ 134: 4-МЕРНОЕ ПРОСТРАНСТВО-ВРЕМЯ МИНКОВСКОГО → ПРОСТРАНСТВО-ВРЕМЯ ЕТД
## (t, x, y, z) → (ЧВС, МВС, СВС, БВС): Релятивистская геометрия движения
### «Пространство Минковского: 3 пространственных + 1 временная координата»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Специальная теория относительности / геометрия Минковского

---

## ВВЕДЕНИЕ

**Пространство Минковского** (1908): 4-мерное пространство-время специальной теории относительности.

Метрика: `ds² = c²dt² − dx² − dy² − dz²`

Четыре координаты: `(ct, x, y, z)` — одна временная и три пространственных. Структура **3+1**: именно такое разбиение — не 2+2, не 4+0 — является физической реальностью.

**Тезис тома**: Пространство ЕТД — тоже 3+1. Три «пространственных» сферы (МВС, СВС, БВС) + одна «временна́я» (ЧВС = контекст, определяющий «когда»). Метрика ЕТД = псевдориманова метрика Минковского.

---

## ЧАСТЬ I: СООТВЕТСТВИЕ 4-МЕРНОГО ПРОСТРАНСТВА И СИСТЕМЫ ЕТД

### 1.1 Координаты Минковского → Сферы ЕТД

| Координата Минковского | Физический смысл | Сфера ЕТД | ЕТД-смысл |
|----------------------|-----------------|-----------|----------|
| **ct** (время) | Временна́я ось | **ЧВС** | Контекст — «когда» и «в каком ЧВС» |
| **x** (горизонталь) | Пространственная X | **МВС** | Горизонтальное движение кисти |
| **y** (вертикаль) | Пространственная Y | **СВС** | Вертикальное движение предплечья |
| **z** (глубина) | Пространственная Z | **БВС** | Движение тела вперёд/назад |

**Структура 3+1**:
- В физике: 3 пространства (x,y,z) + 1 время (t)
- В ЕТД: 3 физических сферы (МВС+СВС+БВС) + 1 контекст (ЧВС)

### 1.2 Метрика ЕТД (Минковского)

```python
import math

class MinkowskiETD:
    """
    Псевдориманова метрика пространства ЕТД.
    Аналог метрики Минковского: ds² = c²dt² − dx² − dy² − dz²
    ds²_ЕТД = c_ЕТД²·dЧВС² − dМВС² − dСВС² − dБВС²
    """

    c_ETD = 1.0   # нормированная скорость переключения ЧВС (в натуральных единицах)

    @staticmethod
    def interval_squared(d_cvs: float, d_mvs: float,
                          d_svs: float, d_bvs: float) -> float:
        """
        Интервал Минковского в пространстве ЕТД:
        ds² = c²·dЧВС² − dМВС² − dСВС² − dБВС²

        ds² > 0: времениподобный (ЧВС-переключение доминирует)
        ds² = 0: светоподобный (ЧВС-переключение = скорости физических сфер)
        ds² < 0: пространственноподобный (физические сферы доминируют)
        """
        c = MinkowskiETD.c_ETD
        return c**2 * d_cvs**2 - d_mvs**2 - d_svs**2 - d_bvs**2

    @staticmethod
    def classify_interval(ds_squared: float) -> str:
        """Классификация интервала ЕТД."""
        if ds_squared > 0:
            return 'Времениподобный: ЧВС управляет ситуацией'
        elif ds_squared == 0:
            return 'Световой конус: ЧВС и сферы синхронизированы (π/4-режим!)'
        else:
            return 'Пространственноподобный: физические сферы опережают ЧВС'

    def etd_proper_time(self, d_cvs: float, d_mvs: float,
                         d_svs: float, d_bvs: float) -> float:
        """
        «Собственное время» ЕТД — инвариант события.
        dτ² = dЧВС² − (dМВС² + dСВС² + dБВС²)/c²
        """
        ds2 = self.interval_squared(d_cvs, d_mvs, d_svs, d_bvs)
        if ds2 >= 0:
            return math.sqrt(ds2)
        return 0.0   # световой конус или пространственноподобный → τ = 0

# Пример: синхронизированное движение π/4
# dЧВС = π/4, dМВС = π/4, dСВС = π/4, dБВС = π/4
d = math.pi / 4
mink = MinkowskiETD()
ds2 = mink.interval_squared(d, d, d, d)
print(f"ds² при π/4-синхронизации: {ds2:.4f}")
# = (π/4)² − 3·(π/4)² = −2·(π/4)² < 0
# → пространственноподобный (движение — реальное, не просто ЧВС-переключение)
```

### 1.3 Световой конус = ЧВС-конус

В релятивистской физике **световой конус** определяет каузально-связанные события. В ЕТД:

```python
def etd_light_cone(t_cvs: float, c_etd: float = 1.0) -> float:
    """
    ЧВС-конус: максимальный «радиус» физических сфер,
    достижимый за время ЧВС-переключения t_cvs.
    r_max = c_ЕТД × t_ЧВС
    """
    return c_etd * t_cvs

# При t_ЧВС = π/4 секунды:
radius = etd_light_cone(math.pi / 4)
print(f"Максимальный радиус движения за π/4 с: {radius:.3f} м")
# = 1 × π/4 ≈ 0.785 м (в натуральных единицах)
```

---

## ЧАСТЬ II: ЛОРЕНЦ-ИНВАРИАНТЫ ЕТД

### 2.1 4-Вектор ЛЗП

В физике 4-вектор `(E/c, p_x, p_y, p_z)` — 4-импульс, инвариантный при преобразованиях Лоренца.

**ЕТД 4-вектор**:

```python
import numpy as np

class ETD4Vector:
    """
    4-вектор ЛЗП в пространстве Минковского ЕТД.
    (ЛЗП_ЧВС / c, ЛЗП_МВС, ЛЗП_СВС, ЛЗП_БВС)
    """

    def __init__(self, lci_cvs: float, lci_mvs: float,
                  lci_svs: float, lci_bvs: float):
        self.components = np.array([lci_cvs, lci_mvs, lci_svs, lci_bvs])

    def minkowski_norm_squared(self) -> float:
        """
        Инвариантная норма Минковского:
        ||p||² = ЛЗП_ЧВС² − ЛЗП_МВС² − ЛЗП_СВС² − ЛЗП_БВС²
        = инвариант (не зависит от системы отсчёта)
        """
        c, x, y, z = self.components
        return c**2 - x**2 - y**2 - z**2

    @property
    def lci_invariant(self) -> float:
        """Инвариантный ЛЗП = «масса покоя» в пространстве ЕТД."""
        norm2 = self.minkowski_norm_squared()
        return math.sqrt(abs(norm2)) if norm2 >= 0 else 0.0

# π/4-состояние: всё синхронизировано
v = ETD4Vector(lci_cvs=math.pi/4, lci_mvs=math.pi/4,
               lci_svs=math.pi/4, lci_bvs=math.pi/4)
print(f"Инвариантный ЛЗП: {v.lci_invariant:.4f}")
```

### 2.2 Диаграмма Минковского ЕТД

```
ЧВС (время)
    │                 световой конус
    │               /│
    │             /  │
    │           /    │  будущее
    │         /      │  (следующая ЧВС)
────┼───────/────────┼──────────────── МВС (x)
    │     /          │
    │   /            │  прошлое
    │ /              │  (предыдущая ЧВС)
    │/               │

Внутри светового конуса = ЧВС-достижимые события
Вне светового конуса = технически невозможное переключение
```

---

## ЗАКЛЮЧЕНИЕ

**Пространство Минковского** → **Пространство-время ЕТД**:

- Структура 3+1: (МВС, СВС, БВС) = пространство + ЧВС = время
- Метрика: ds²_ЕТД = c²·dЧВС² − dМВС² − dСВС² − dБВС²
- Световой конус ЕТД: зона достижимых событий при данной скорости ЧВС
- 4-вектор ЛЗП: инвариантный при «преобразованиях Лоренца» (смене ЧВС)

> ЧВС = «временна́я» координата. ЧВС переключает «систему отсчёта» — точно как время в СТО. МВС+СВС+БВС = «пространство» движения. Инвариант: ЛЗП² = ЧВС²−МВС²−СВС²−БВС².

---
*ТОМ 134 / СЕРИЯ VII / ЕТД 2026*
