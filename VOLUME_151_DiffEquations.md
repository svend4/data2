# ТОМ 151: 4 ТИПА ДИФФЕРЕНЦИАЛЬНЫХ УРАВНЕНИЙ → УРАВНЕНИЯ ДВИЖЕНИЯ ЕТД
## ОДУ · ЧДУ · Системы · Интегро-дифференциальные → 4 модели динамики сфер
### «4 класса дифференциальных уравнений, описывающих природу»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Математический анализ / теория дифференциальных уравнений

---

## ВВЕДЕНИЕ

Дифференциальные уравнения — главный язык физики. 4 типа:

1. **ОДУ** (обыкновенное) — одна независимая переменная: dy/dt = f(t, y)
2. **ЧДУ** (уравнение в частных производных) — несколько переменных: ∂u/∂t = ∂²u/∂x²
3. **Система ДУ** — несколько уравнений, несколько функций
4. **Интегро-дифференциальное** — содержит и производную, и интеграл

**Тезис тома**: 4 типа ДУ = 4 модели динамики системы 4 сфер. Каждая сфера описывается своим типом уравнения.

---

## ЧАСТЬ I: 4 ТИПА ДУ → 4 МОДЕЛИ СФЕР

```python
import math
from typing import Callable, List, Tuple

class DifferentialEquationsETD:
    """
    4 типа дифференциальных уравнений как 4 модели динамики сфер ЕТД.
    """

    # ОДУ (МВС): одна переменная — угол кисти φ(t)
    @staticmethod
    def ode_mvs(phi_0: float, omega_0: float,
                damping: float, dt: float = 0.01,
                steps: int = 100) -> List[float]:
        """
        ОДУ для МВС: φ'' + 2ζω₀φ' + ω₀²φ = F(t)
        Затухающий осциллятор (кисть после удара).
        ζ = damping (коэффициент затухания).
        ω₀ = собственная частота = 4/π (естественная частота ЕТД, Том 149).
        """
        phi = phi_0
        phi_dot = 0.0
        trajectory = [phi]

        for _ in range(steps):
            F = math.pi / 4   # внешняя «сила» = π/4 (квант ЕТД)
            phi_ddot = F - 2*damping*omega_0*phi_dot - omega_0**2*phi
            phi_dot += phi_ddot * dt
            phi += phi_dot * dt
            trajectory.append(phi)

        return trajectory

    # ЧДУ (БВС): два аргумента — время t и позиция x
    @staticmethod
    def pde_bvs_wave(c: float, L: float, T: float,
                      nx: int = 20, nt: int = 100) -> List[List[float]]:
        """
        Волновое ЧДУ для БВС: ∂²u/∂t² = c²·∂²u/∂x²
        «Волна» распространяется по телу при движении.
        c = скорость передачи импульса вдоль тела (c_ЕТД = 1.0).
        """
        dx = L / nx
        dt = T / nt
        # Условие устойчивости: c·dt/dx ≤ 1
        cfl = c * dt / dx

        u = [[0.0]*(nx+1) for _ in range(nt+1)]
        # Начальное условие: импульс в центре тела
        for i in range(nx+1):
            x = i * dx
            u[0][i] = math.exp(-(x - L/2)**2 / (L/10)**2)

        for n in range(1, nt):
            for i in range(1, nx):
                u[n+1][i] = (2*u[n][i] - u[n-1][i] +
                              cfl**2 * (u[n][i+1] - 2*u[n][i] + u[n][i-1]))
        return u

    # Система ДУ (СВС): 4 уравнения, 4 функции
    @staticmethod
    def system_ode_svs(state_0: List[float], dt: float = 0.01,
                        steps: int = 200) -> List[List[float]]:
        """
        Система ДУ для 4 сфер:
        dМВС/dt = f₁(МВС, СВС, БВС, ЧВС)
        dСВС/dt = f₂(МВС, СВС, БВС, ЧВС)
        dБВС/dt = f₃(МВС, СВС, БВС, ЧВС)
        dЧВС/dt = f₄(МВС, СВС, БВС, ЧВС)

        Уравнение Лотки–Вольтерра для сфер:
        Каждая сфера «питается» следующей и «кормит» предыдущую.
        """
        mvs, svs, bvs, cvs = state_0
        trajectory = [[mvs, svs, bvs, cvs]]
        h = math.pi / 4   # константа взаимодействия = π/4

        for _ in range(steps):
            # Уравнение взаимодействия сфер:
            d_mvs = mvs * (1 - svs * h)
            d_svs = svs * (mvs * h - 1)
            d_bvs = bvs * (1 - cvs * h)
            d_cvs = cvs * (bvs * h - 1)

            mvs += d_mvs * dt
            svs += d_svs * dt
            bvs += d_bvs * dt
            cvs += d_cvs * dt
            trajectory.append([mvs, svs, bvs, cvs])

        return trajectory

    # Интегро-дифференциальное (ЧВС): история влияет на настоящее
    @staticmethod
    def integro_diff_cvs(lci_history: List[float],
                           kernel_decay: float = 0.1) -> float:
        """
        Интегро-дифференциальное уравнение для ЧВС:
        dЧВС/dt = f(ЧВС, t) + ∫₀ᵗ K(t−s)·ЛЗП(s) ds

        Интегральный член = «память» ЧВС.
        K(t−s) = e^{−λ(t−s)} — экспоненциальное ядро (память затухает).
        ЧВС «помнит» историю и учитывает её в текущем решении.
        """
        # Свёртка ЛЗП-истории с экспоненциальным ядром
        n = len(lci_history)
        memory_integral = 0.0
        for i, lci in enumerate(lci_history):
            t_minus_s = (n - 1 - i) * 0.01   # временной сдвиг
            kernel = math.exp(-kernel_decay * t_minus_s)
            memory_integral += kernel * lci * 0.01   # Δs = 0.01

        return memory_integral

# Демонстрация: затухание МВС
traj_mvs = DifferentialEquationsETD.ode_mvs(
    phi_0=math.pi/2, omega_0=4/math.pi, damping=0.3
)
print(f"МВС-траектория: начало={traj_mvs[0]:.3f}, конец={traj_mvs[-1]:.3f}")
```

### 1.2 Классификация ДУ → Классификация техник ЕТД

| Тип ДУ | Сфера | Характеристика | Пример техники |
|--------|-------|---------------|----------------|
| **ОДУ** | МВС | 1 переменная (время) | Удар — φ(t) = затухающий осциллятор |
| **ЧДУ** | БВС | Пространство + время | Волна по телу — u(x,t) |
| **Система** | СВС | 4 уравнения вместе | Взаимодействие сфер в комбинации |
| **Интегро-дифф.** | ЧВС | Память + производная | ЧВС помнит всю историю |

---

## ЗАКЛЮЧЕНИЕ

**4 типа ДУ** → **4 модели динамики сфер ЕТД**:

1. **ОДУ** (МВС) → Затухающий осциллятор кисти: φ'' + 2ζω₀φ' + ω₀²φ = π/4
2. **ЧДУ** (БВС) → Волновое уравнение тела: ∂²u/∂t² = c²·∂²u/∂x²
3. **Система** (СВС) → 4 уравнения взаимодействия сфер (Лотка–Вольтерра)
4. **Интегро-дифф.** (ЧВС) → ЧВС с «памятью»: dЧВС/dt = f + ∫K(t−s)·ЛЗП(s)ds

> ЧВС-уравнение с памятью = интегро-дифференциальное: ЧВС не только реагирует на текущее состояние, но «помнит» всю историю ЛЗП. Это математически формализует «опыт» мастера.

---
*ТОМ 151 / СЕРИЯ VII / ЕТД 2026*
