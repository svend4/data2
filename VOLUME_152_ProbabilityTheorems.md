# ТОМ 152: 4 ВЕЛИКИХ ТЕОРЕМЫ ТЕОРИИ ВЕРОЯТНОСТЕЙ → ВЕРОЯТНОСТЬ ЧВС-СОБЫТИЙ
## ЗБЧ · ЦПТ · Неравенство Маркова · Неравенство Чебышёва
### «4 фундаментальных результата теории вероятностей»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория вероятностей / математическая статистика

---

## ВВЕДЕНИЕ

4 фундаментальных теоремы теории вероятностей:

1. **Закон больших чисел (ЗБЧ)**: Среднее выборки сходится к матожиданию
2. **Центральная предельная теорема (ЦПТ)**: Сумма независимых величин → нормальное распределение
3. **Неравенство Маркова**: P(X ≥ a) ≤ E[X] / a
4. **Неравенство Чебышёва**: P(|X−μ| ≥ kσ) ≤ 1/k²

**Тезис тома**: Эти 4 теоремы определяют вероятностную природу ЧВС-переключений. ЧВС — случайная величина, подчиняющаяся законам вероятности.

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ПРИНЦИПА СТОХАСТИЧЕСКОЙ ЧВС

```python
import math
import random
from typing import List

class ProbabilityETD:
    """
    4 великих теоремы теории вероятностей применительно к ЧВС-системе.
    """

    # ЗАКОН БОЛЬШИХ ЧИСЕЛ (ЗБЧ)
    @staticmethod
    def law_of_large_numbers(n_trials: int,
                              true_cvs_prob: float = math.pi/4 / (2*math.pi)) -> dict:
        """
        ЗБЧ: При большом числе ЧВС-переключений
        относительная частота стремится к истинной вероятности.

        P(ЧВС = 'Инструментальное') → 1/5 = 0.2 (5 ЧВС равновероятны).
        P(ЧВС-угол = π/4) → π/4 / (2π) = 1/8 ≈ 0.125.

        ЕТД-смысл: Чем больше спаррингов, тем точнее статистика ЧВС.
        """
        cvs_states = ['Внутреннее', 'Инструментальное', 'Доменное',
                      'Пространственное', 'Виртуальное']
        counts = {s: 0 for s in cvs_states}

        for _ in range(n_trials):
            # Случайное ЧВС-переключение
            state = random.choice(cvs_states)
            counts[state] += 1

        frequencies = {s: c/n_trials for s, c in counts.items()}
        true_prob = 1/5   # равновероятные 5 ЧВС

        max_deviation = max(abs(f - true_prob) for f in frequencies.values())

        return {
            'теорема': 'Закон больших чисел',
            'n': n_trials,
            'частоты': frequencies,
            'истинная_P': true_prob,
            'макс_отклонение': max_deviation,
            'сошёлся': max_deviation < 0.05 if n_trials >= 1000 else None,
            'смысл': 'При n→∞ частота ЧВС → 1/5 (равновероятность)',
        }

    # ЦЕНТРАЛЬНАЯ ПРЕДЕЛЬНАЯ ТЕОРЕМА (ЦПТ)
    @staticmethod
    def central_limit_theorem(n_spheres: int = 4,
                               n_samples: int = 1000) -> dict:
        """
        ЦПТ: Сумма n независимых ЛЗП-вкладов → нормальное распределение.

        Сумма ЛЗП_МВС + ЛЗП_СВС + ЛЗП_БВС + ЛЗП_ЧВС:
        При n=4 сферах → приближённо нормальна N(4·μ, 4·σ²).

        ЕТД-смысл: Суммарный ЛЗП имеет «колокол» вокруг среднего.
        Среднее = 4 × (π/4) = π ≈ 3.14!
        """
        mu = math.pi / 4   # матожидание каждой сферы = π/4
        sigma = math.pi / 8  # стандартное отклонение = π/8

        # Симуляция: суммировать вклады 4 сфер
        totals = []
        for _ in range(n_samples):
            total = sum(random.gauss(mu, sigma) for _ in range(n_spheres))
            totals.append(total)

        mean_total = sum(totals) / n_samples
        var_total = sum((t - mean_total)**2 for t in totals) / n_samples
        std_total = math.sqrt(var_total)

        return {
            'теорема': 'ЦПТ',
            'n_сфер': n_spheres,
            'теор_среднее': n_spheres * mu,      # = 4·π/4 = π ≈ 3.14
            'эмп_среднее': mean_total,
            'теор_σ': math.sqrt(n_spheres) * sigma,  # = 2·π/8 = π/4
            'эмп_σ': std_total,
            'смысл': f'ЛЗП_total ~ N(π, (π/4)²); центр = π ≈ {math.pi:.4f}',
        }

    # НЕРАВЕНСТВО МАРКОВА
    @staticmethod
    def markov_inequality(E_lci: float, threshold: float) -> float:
        """
        Неравенство Маркова: P(ЛЗП ≥ a) ≤ E[ЛЗП] / a.
        Для неотрицательных случайных величин.

        ЕТД: Вероятность того, что ЛЗП превысит порог a,
        не превышает среднего ЛЗП / a.

        При E[ЛЗП] = π, a = 2π:
        P(ЛЗП ≥ 2π) ≤ π / (2π) = 1/2.
        """
        if threshold <= 0:
            return 1.0
        return min(1.0, E_lci / threshold)

    # НЕРАВЕНСТВО ЧЕБЫШЁВА
    @staticmethod
    def chebyshev_inequality(sigma: float, k: float) -> float:
        """
        Неравенство Чебышёва: P(|ЛЗП − μ| ≥ k·σ) ≤ 1/k².

        ЕТД: При k=2: P(|ЛЗП − π| ≥ 2·σ) ≤ 1/4 = 25%.
        75% времени ЛЗП находится в диапазоне [π − 2σ, π + 2σ].

        При σ = π/4:
        [π − π/2, π + π/2] = [π/2, 3π/2] — зона 75% вероятности.
        """
        if k <= 0:
            return 1.0
        return min(1.0, 1.0 / k**2)

proba = ProbabilityETD()

# ЦПТ: среднее суммарного ЛЗП = π
clt_result = proba.central_limit_theorem(n_spheres=4, n_samples=500)
print(f"ЦПТ: теор. среднее = {clt_result['теор_среднее']:.4f} (= π = {math.pi:.4f})")

# Чебышёв: 75% ЛЗП в зоне [π/2, 3π/2]
p_outside = proba.chebyshev_inequality(sigma=math.pi/4, k=2)
print(f"Чебышёв k=2: вне зоны ≤ {p_outside:.1%}")
```

### 1.2 Распределение ЧВС-переключений

```python
def cvs_switching_distribution(n_switches: int = 1000) -> dict:
    """
    Распределение числа ЧВС-переключений до «успеха» (π/4-синхронизации).
    Следует геометрическому распределению с p = 1/(2π/π/4) = 1/8.
    """
    p_success = math.pi/4 / (2*math.pi)   # ≈ 0.125 (вероятность попасть в π/4)
    E_switches = 1 / p_success              # матожидание = 8 переключений!
    Var_switches = (1 - p_success) / p_success**2  # дисперсия

    return {
        'p_π/4': p_success,
        'E[n_переключений]': E_switches,   # ≈ 8 до π/4-синхронизации
        'σ[n_переключений]': math.sqrt(Var_switches),
        'смысл': 'В среднем нужно 8 ЧВС-переключений для достижения π/4-синхронизации',
    }

print(cvs_switching_distribution())
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории вероятностей** → **4 закона стохастической ЧВС**:

1. **ЗБЧ** → При n→∞ спаррингов частота каждой ЧВС → 1/5 (равновероятность)
2. **ЦПТ** → Суммарный ЛЗП_4сфер ~ N(π, (π/4)²): **центр нормального распределения = π!**
3. **Марков** → P(ЛЗП ≥ 2π) ≤ 1/2: редкие выбросы ЛЗП ограничены
4. **Чебышёв** → 75% времени ЛЗП ∈ [π/2, 3π/2]: зона устойчивости

> ЦПТ: среднее суммарного ЛЗП четырёх сфер = 4 × π/4 = **π**. Число π возникает как матожидание — не произвольно, а из центральной предельной теоремы!

---
*ТОМ 152 / СЕРИЯ VII / ЕТД 2026*
