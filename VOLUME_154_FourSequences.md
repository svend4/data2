# ТОМ 154: 4 ТИПА ЧИСЛОВЫХ ПОСЛЕДОВАТЕЛЬНОСТЕЙ → РОСТ МАСТЕРСТВА ЕТД
## Арифметическая · Геометрическая · Гармоническая · Фибоначчи
### «4 классических типа числовых последовательностей»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория чисел / анализ последовательностей

---

## ВВЕДЕНИЕ

4 фундаментальных типа числовых последовательностей:

1. **Арифметическая**: aₙ = a₀ + n·d (линейный рост)
2. **Геометрическая**: aₙ = a₀ · rⁿ (экспоненциальный рост)
3. **Гармоническая**: aₙ = 1/n (убывающая, сумма расходится)
4. **Фибоначчи**: Fₙ = Fₙ₋₁ + Fₙ₋₂ (каждый = сумма двух предыдущих)

**Тезис тома**: 4 типа последовательностей = 4 модели роста мастерства ЕТД. Каждая описывает разный режим обучения.

---

## ЧАСТЬ I: 4 ПОСЛЕДОВАТЕЛЬНОСТИ → 4 РЕЖИМА ОБУЧЕНИЯ

```python
import math
from typing import List

class MasterySequences:
    """
    4 типа числовых последовательностей как 4 режима роста мастерства ЕТД.
    """

    # АРИФМЕТИЧЕСКАЯ: ЛЗП(n) = π/4 + n·d
    @staticmethod
    def arithmetic_mastery(n_steps: int,
                            lci_0: float = math.pi/4,
                            d: float = math.pi/4) -> List[float]:
        """
        Арифметическая последовательность:
        ЛЗП(n) = π/4 + n·(π/4) = (n+1)·π/4.

        Модель линейного обучения:
        Каждая тренировка добавляет ровно π/4 к ЛЗП.
        «Равномерный» темп без ускорения.

        При n=3: ЛЗП = 4·π/4 = π ≈ 3.14 (4 кванта = π!)
        """
        return [lci_0 + i * d for i in range(n_steps)]

    # ГЕОМЕТРИЧЕСКАЯ: ЛЗП(n) = (π/4) · rⁿ
    @staticmethod
    def geometric_mastery(n_steps: int,
                           lci_0: float = math.pi/4,
                           r: float = math.e**(math.pi/4)) -> List[float]:
        """
        Геометрическая последовательность:
        ЛЗП(n) = (π/4) · rⁿ.

        Модель ускоренного обучения:
        Каждая тренировка умножает ЛЗП на r.
        r = e^{π/4} ≈ 2.19 (основание = e^{ħ_ЕТД}).

        Экспоненциальный рост: мастерство «нарастает» быстрее.
        """
        return [lci_0 * r**i for i in range(n_steps)]

    # ГАРМОНИЧЕСКАЯ: ЛЗП(n) = Σ 1/k → ln(n) + γ
    @staticmethod
    def harmonic_mastery(n_steps: int) -> List[float]:
        """
        Гармоническая последовательность:
        ЛЗП(n) = 1 + 1/2 + 1/3 + ... + 1/n = H_n.

        Модель «насыщения» обучения:
        Каждая новая техника добавляет всё меньше.
        H_n ≈ ln(n) + γ (γ = 0.5772, Том 146).

        При n=4: H₄ = 1 + 1/2 + 1/3 + 1/4 = 25/12 ≈ 2.083.
        """
        return [sum(1/k for k in range(1, i+1)) for i in range(1, n_steps+1)]

    # ФИБОНАЧЧИ: Fₙ = Fₙ₋₁ + Fₙ₋₂ → φ
    @staticmethod
    def fibonacci_mastery(n_steps: int,
                           lci_0: float = math.pi/4,
                           lci_1: float = math.pi/2) -> List[float]:
        """
        Последовательность Фибоначчи:
        ЛЗП(n) = ЛЗП(n−1) + ЛЗП(n−2).

        Модель «системного» обучения:
        Новое мастерство = сумма двух предыдущих уровней.
        Отношение ЛЗП(n+1)/ЛЗП(n) → φ = 1.618 (золотое сечение!).

        Начало: π/4, π/2, 3π/4, 5π/4, 8π/4 = 2π, ...
        «Спираль Фибоначчи мастерства».
        """
        seq = [lci_0, lci_1]
        for i in range(2, n_steps):
            seq.append(seq[-1] + seq[-2])
        return seq[:n_steps]

# Сравнение 4 моделей за 8 шагов:
seqs = MasterySequences()
n = 8

arith = seqs.arithmetic_mastery(n)
geom  = seqs.geometric_mastery(n)
harm  = seqs.harmonic_mastery(n)
fib   = seqs.fibonacci_mastery(n)

print(f"Арифм.  (n=7): ЛЗП = {arith[-1]:.3f}")   # = 8·π/4 = 2π ≈ 6.28
print(f"Геометр.(n=7): ЛЗП = {geom[-1]:.3f}")    # = (π/4)·(e^{π/4})^7 >> 1
print(f"Гармон. (n=7): ЛЗП = {harm[-1]:.3f}")    # ≈ ln(8)+γ ≈ 2.656
print(f"Фибон.  (n=7): ЛЗП = {fib[-1]:.3f}")     # Фибоначчи
```

### 1.2 Четыре режима обучения

| Последовательность | Рост | Сфера | Тип ученика | ЛЗП при n=4 |
|-------------------|------|-------|------------|-------------|
| **Арифметическая** | Линейный (+d) | МВС | «Стабильный»: равный прогресс | 5·π/4 ≈ 3.93 |
| **Геометрическая** | Экспоненциальный (×r) | СВС | «Вундеркинд»: ускорение | >> 10 |
| **Гармоническая** | Логарифмический (ln n) | БВС | «Насыщающийся»: замедление | H₄ ≈ 2.08 |
| **Фибоначчи** | Золотосечный (φ) | ЧВС | «Системный»: каждый урок = сумма предыдущих | ... |

### 1.3 Свойство Фибоначчи: π/4 + π/2 = 3π/4

```python
# Первые три шага последовательности ЛЗП-Фибоначчи:
# π/4, π/2, 3π/4, ...
# Заметим: π/4 + π/2 = 3π/4 ✓ — свойство Фибоначчи!
# Следующий: π/2 + 3π/4 = 5π/4
# Затем: 3π/4 + 5π/4 = 8π/4 = 2π (полный оборот!)

fib_lci = [math.pi/4, math.pi/2, 3*math.pi/4, 5*math.pi/4, 2*math.pi]
for i, v in enumerate(fib_lci):
    print(f"F_{i}: {v:.4f} ({v/math.pi:.4f}·π)")
# F_4 = 2π = полный круг мастерства (возврат через 4 шага?)
```

---

## ЗАКЛЮЧЕНИЕ

**4 типа последовательностей** → **4 модели роста мастерства**:

1. **Арифметическая** → Линейный прогресс: ЛЗП(n) = (n+1)·π/4; при n=3 → π
2. **Геометрическая** → Экспоненциальный рост: ЛЗП(n) = (π/4)·(e^{π/4})^n
3. **Гармоническая** → Насыщение: ЛЗП(n) ≈ ln(n) + γ (Том 146); каждая техника ценнее
4. **Фибоначчи** → Золотая спираль: каждый уровень = сумма двух предыдущих → φ

> Арифметическая последовательность: через 3 шага (n=3) от π/4 → ЛЗП = 4·π/4 = **π**. Фибоначчи: π/4 → π/2 → 3π/4 → 5π/4 → **2π** (полный оборот за 4 фибоначчиевых шага!).

---
*ТОМ 154 / СЕРИЯ VII / ЕТД 2026*
