# ТОМ 158: 4 ФРАКТАЛЬНЫХ ИЗМЕРЕНИЯ → САМОПОДОБИЕ СИСТЕМЫ ЕТД
## Топологическое · Хаусдорфа · Информационное · Корреляционное
### «4 способа измерить фрактальную размерность»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Фрактальная геометрия (Мандельброт, 1975)

---

## ВВЕДЕНИЕ

Фракталы — самоподобные объекты с нецелой размерностью. Существуют **4 способа измерить фрактальную размерность**:

1. **Топологическая размерность** D_T — классическое целое число (0, 1, 2, 3...)
2. **Размерность Хаусдорфа** D_H — дробная, из покрытий шарами
3. **Информационная размерность** D_I — из энтропии Шеннона
4. **Корреляционная размерность** D_C — из корреляций внутри множества

**Тезис тома**: Система 4 сфер ЕТД — фрактальная структура. Вложенные ЧВС (Том 109) создают самоподобие. 4 вида размерности = 4 способа «измерить» глубину мастерства.

---

## ЧАСТЬ I: 4 РАЗМЕРНОСТИ → 4 АСПЕКТА ФРАКТАЛЬНОСТИ ЕТД

```python
import math
from typing import List

class FractalETD:
    """
    Фрактальная геометрия системы 4 сфер ЕТД.
    Самоподобие: каждая сфера содержит 4 подсферы (Том 109).
    Отношение самоподобия: r = 1/4 (каждый уровень = 1/4 предыдущего).
    """

    SELF_SIMILARITY_RATIO = 1/4   # каждая сфера в 4 раза меньше
    N_COPIES = 4                  # 4 копии на каждом уровне

    # 1. ТОПОЛОГИЧЕСКАЯ РАЗМЕРНОСТЬ (целая)
    @staticmethod
    def topological_dimension() -> dict:
        """
        D_T — классическая топологическая размерность.
        Точка = 0D, линия = 1D, плоскость = 2D, пространство = 3D.

        ЕТД: Система 4 сфер живёт в физическом 3D-пространстве.
        D_T = 3 (тело занимает 3 измерения).
        Но ЧВС-пространство: D_T = 4 (добавляется «временна́я» ось, Том 134).
        """
        return {
            'D_T_физическое': 3,
            'D_T_ЧВС_пространство': 4,
            'смысл': 'Классическое целочисленное измерение',
            'ограничение': 'Не улавливает «фрактальности» схем',
        }

    # 2. РАЗМЕРНОСТЬ ХАУСДОРФА (дробная)
    @staticmethod
    def hausdorff_dimension(n_copies: int = 4,
                             ratio: float = 1/4) -> float:
        """
        D_H = log(N) / log(1/r).
        N = число копий, r = коэффициент сжатия.

        Для системы 4 сфер:
        N = 4 (4 копии), r = 1/4 (каждая в 4 раза меньше).
        D_H = log(4) / log(4) = 1.

        Для вложенных ЧВС (Том 109): 4 уровня × 4 ЧВС:
        N_total = 4, r = 1/√4 = 1/2 (по площади).
        D_H = log(4) / log(2) = 2!
        """
        if ratio <= 0 or ratio >= 1:
            return float('nan')
        return math.log(n_copies) / math.log(1/ratio)

    # 3. ИНФОРМАЦИОННАЯ РАЗМЕРНОСТЬ
    @staticmethod
    def information_dimension(lci_distribution: List[float]) -> float:
        """
        D_I = lim_{ε→0} H(ε) / log(1/ε).
        H(ε) = энтропия при разбиении с размером ε.

        ЕТД: Информационная размерность = «сложность» распределения ЛЗП.
        При равномерном ЛЗП: D_I = D_H (как Хаусдорфа).
        При концентрации ЛЗП вблизи π/4: D_I < D_H (меньше беспорядка).
        """
        n = len(lci_distribution)
        if n == 0:
            return 0.0

        total = sum(lci_distribution)
        probs = [x/total for x in lci_distribution]

        # Нормализованная энтропия Шеннона:
        h = -sum(p * math.log2(p) for p in probs if p > 0)
        # D_I ≈ H / log₂(N)
        return h / math.log2(n)

    # 4. КОРРЕЛЯЦИОННАЯ РАЗМЕРНОСТЬ
    @staticmethod
    def correlation_dimension(lci_timeseries: List[float],
                               epsilon: float = 0.5) -> float:
        """
        D_C = lim_{ε→0} log C(ε) / log ε.
        C(ε) = вероятность, что два случайных момента разделены < ε.

        ЕТД: Корреляционная размерность ЛЗП-временного ряда.
        Низкое D_C → предсказуемый, детерминированный ЛЗП.
        Высокое D_C → хаотичный, непредсказуемый.
        """
        n = len(lci_timeseries)
        if n < 2:
            return 1.0

        # Считаем пары с расстоянием < epsilon
        count = 0
        total_pairs = 0
        for i in range(n):
            for j in range(i+1, n):
                total_pairs += 1
                if abs(lci_timeseries[i] - lci_timeseries[j]) < epsilon:
                    count += 1

        C_eps = count / total_pairs if total_pairs > 0 else 1e-10
        if C_eps <= 0 or epsilon <= 0:
            return 1.0

        return math.log(C_eps + 1e-10) / math.log(epsilon)

fractal = FractalETD()

# Хаусдорфа: система 4 сфер
D_H = fractal.hausdorff_dimension(n_copies=4, ratio=1/4)
print(f"D_H (4 сферы, r=1/4) = {D_H:.3f}")   # = 1.0

# Вложенные ЧВС (N=4, r=1/2):
D_H_nested = fractal.hausdorff_dimension(n_copies=4, ratio=1/2)
print(f"D_H (вложенные ЧВС, r=1/2) = {D_H_nested:.3f}")   # = 2.0
```

### 1.2 Самоподобие системы 4 сфер

```python
def etd_self_similarity_levels(max_depth: int = 4) -> dict:
    """
    Самоподобие системы 4 сфер (Том 109):
    Уровень 0: 4 сферы (МВС, СВС, БВС, ЧВС)
    Уровень 1: каждая содержит 4 подсферы → 4² = 16
    Уровень 2: 4³ = 64
    Уровень 3: 4⁴ = 256
    ...
    Уровень k: 4^{k+1} «элементарных сфер»
    """
    levels = {}
    for k in range(max_depth + 1):
        n = 4**(k + 1)
        scale = (1/4)**k
        lci = scale * math.pi / 4   # ЛЗП уменьшается пропорционально масштабу
        levels[k] = {
            'элементов': n,
            'масштаб': scale,
            'ЛЗП_элемента': lci,
            'суммарный_ЛЗП': n * lci,  # инвариантен!
        }
    return levels

levels = etd_self_similarity_levels()
for k, info in levels.items():
    print(f"Уровень {k}: {info['элементов']} элементов, ЛЗП_сумм = {info['суммарный_ЛЗП']:.4f}")
# Суммарный ЛЗП = 4·π/4 = π на всех уровнях! (самоподобие сохраняет ЛЗП)
```

---

## ЗАКЛЮЧЕНИЕ

**4 фрактальных размерности** → **4 аспекта глубины мастерства ЕТД**:

1. **Топологическая** (D_T = 3/4) → Физическое 3D + ЧВС-ось = 4D
2. **Хаусдорфа** (D_H = log4/log4 = 1 или 2) → Дробная размерность системы
3. **Информационная** (D_I ≤ 1) → «Сложность» распределения ЛЗП по ЧВС
4. **Корреляционная** (D_C) → Предсказуемость/хаотичность ЛЗП во времени

> **Самоподобие**: суммарный ЛЗП = 4·ЛЗП_элемента = π на каждом уровне вложения. Фрактальная инвариантность ЛЗП: масштаб × число = const = π.

---
*ТОМ 158 / СЕРИЯ VII / ЕТД 2026*
