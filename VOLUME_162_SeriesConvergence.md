# ТОМ 162: 4 ТИПА СХОДИМОСТИ РЯДОВ → УРОВНИ МАСТЕРСТВА ЕТД
## Абсолютная · Условная · Равномерная · Поточечная
### «Когда бесконечная сумма имеет смысл?»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Математический анализ (Коши, Вейерштрасс, XIX в.)

---

## ВВЕДЕНИЕ

Сходимость рядов — центральный вопрос анализа. 4 типа сходимости:

1. **Абсолютная** — Σ|aₙ| < ∞ (самая сильная)
2. **Условная** — Σaₙ сходится, но Σ|aₙ| = ∞ (знаки помогают)
3. **Равномерная** — сходимость одновременна для всего домена
4. **Поточечная** — сходимость в каждой точке отдельно

**Тезис тома**: Накопление мастерства ЕТД — это ряд. Каждая тренировка добавляет Δ(ЛЗП). 4 типа сходимости = 4 стиля накопления мастерства. Мастер класса «абсолютная» — непобедим при любом порядке событий.

---

## ЧАСТЬ I: 4 ТИПА СХОДИМОСТИ → 4 РЕЖИМА РОСТА ЛЗП

```python
import math
from typing import List, Callable, Tuple

class ConvergenceETD:
    """
    Сходимость рядов применительно к накоплению ЛЗП в ЕТД.
    Ряд: ЛЗП_total = Σ δЛЗП_n (сумма приростов из тренировок).
    """

    # 1. АБСОЛЮТНАЯ СХОДИМОСТЬ
    @staticmethod
    def absolute_convergence(deltas: List[float]) -> dict:
        """
        Абсолютная сходимость: Σ|δЛЗП_n| < ∞.

        Физический смысл: общий «объём» вложений конечен,
        вне зависимости от знака (хорошие/плохие тренировки).

        ЕТД: Мастер с абсолютной сходимостью устойчив:
        даже «обратные» тренировки не ломают систему.
        Σ|δЛЗП_n| = полный вложенный объём (физическая работа).
        """
        partial_sums = []
        abs_partial_sums = []
        s = 0.0
        abs_s = 0.0

        for d in deltas:
            s += d
            abs_s += abs(d)
            partial_sums.append(s)
            abs_partial_sums.append(abs_s)

        converges_abs = abs_s < float('inf')

        return {
            'тип': 'Абсолютная',
            'Σδ_ЛЗП': s,
            'Σ|δ_ЛЗП|': abs_s,
            'абс_сходится': converges_abs,
            'смысл': 'Устойчивый мастер: порядок тренировок не важен',
            'ЕТД_статус': 'Мастер' if abs_s < 10 * math.pi else 'Тренируется',
        }

    # 2. УСЛОВНАЯ СХОДИМОСТЬ
    @staticmethod
    def conditional_convergence(n_terms: int = 20) -> dict:
        """
        Условная сходимость: Σaₙ < ∞, но Σ|aₙ| = ∞.
        Пример: Σ(−1)ⁿ/n = ln2 (ряд Лейбница).

        ЕТД-смысл: Чередование хороших/плохих тренировок даёт рост.
        Без чередования (|aₙ|) → бесконечная «усталость».
        Мастер с условной сходимостью нуждается в правильном РИТМЕ.

        Теорема Римана: перестановка условно сходящегося ряда
        даёт ЛЮБУЮ сумму → «неправильный» порядок ломает систему!
        """
        # Ряд: δЛЗП_n = (-1)^n × (π/4) / n
        terms = [(-1)**n * (math.pi / 4) / n for n in range(1, n_terms + 1)]

        total = sum(terms)
        abs_total = sum(abs(t) for t in terms)
        theoretical = math.log(2)   # истинная сумма Σ(−1)^n/n = −ln2

        return {
            'тип': 'Условная',
            'Σδ_ЛЗП': total * (math.pi / 4),    # масштаб к ЕТД
            'Σ|δ_ЛЗП|': abs_total,
            'предел_ряда': math.pi / 4 * math.log(2),
            'теорема_Римана': 'Перестановка → другой результат!',
            'смысл': 'Ритм тренировок критичен: порядок важен',
        }

    # 3. РАВНОМЕРНАЯ СХОДИМОСТЬ
    @staticmethod
    def uniform_convergence_etd(n_terms: int = 50) -> dict:
        """
        Равномерная сходимость: sup_x |Sₙ(x) − S(x)| → 0.

        ЕТД-смысл: Мастер «равномерно» хорош во ВСЕХ ситуациях.
        ЧВС-функция f_n(контекст) → f*(контекст) равномерно.
        При n → ∞: во всех контекстах одновременно → мастерство!
        """
        # Ряд Фурье для ЧВС-синтеза (сумма синусов → f(x))
        # f_N(x) = Σ sin(kx)/k (равномерно сходится к π/4·(π-x)/π)

        x_points = [k * math.pi / 20 for k in range(1, 20)]
        errors = []

        for x in x_points:
            approx = sum(math.sin(k * x) / k for k in range(1, n_terms + 1))
            true_val = (math.pi - x) / 2 if 0 < x < math.pi else 0
            errors.append(abs(approx - true_val))

        sup_error = max(errors) if errors else 0.0

        return {
            'тип': 'Равномерная',
            'sup_ошибка': sup_error,
            'n_членов': n_terms,
            'смысл': 'Мастер одинаково эффективен в любом контексте',
            'ЧВС_статус': 'Мастер равномерен' if sup_error < 0.1 else 'Неравномерен',
        }

    # 4. ПОТОЧЕЧНАЯ СХОДИМОСТЬ
    @staticmethod
    def pointwise_convergence_etd() -> dict:
        """
        Поточечная сходимость: ∀x: fₙ(x) → f(x).

        ЕТД-смысл: В каждой конкретной ситуации мастер освоил.
        НО: время освоения может разниться по ситуациям.
        В «трудных» точках (сложный контекст) сходимость медленнее.

        Равномерная ⊂ Абсолютная (для рядов функций).
        Поточечная ⊃ Равномерная (слабее).
        """
        # Пример: f_n(x) = x^n (сходится поточечно, не равномерно на [0,1])
        x_values = [0.0, 0.25, 0.5, 0.75, 0.9, 0.99, 1.0]
        n_terms = 100

        results = {}
        for x in x_values:
            limit = 0.0 if x < 1.0 else 1.0
            approx = x ** n_terms
            results[x] = {
                'f_n(x)': approx,
                'предел': limit,
                'ошибка': abs(approx - limit),
            }

        return {
            'тип': 'Поточечная',
            'примечание': 'В x=1 (критический контекст) сходимость отсутствует!',
            'ЕТД_смысл': 'Мастер хорош в большинстве ситуаций, но есть «слепые пятна»',
            'сравнение': 'Равномерная > Поточечная по строгости',
        }

conv = ConvergenceETD()

# Абсолютная: 20 тренировок с чередованием хорошо/плохо
deltas = [((-1)**n + 1) * math.pi / 8 for n in range(20)]
abs_result = conv.absolute_convergence(deltas)
print(f"Абсолютная: Σ|δЛЗП| = {abs_result['Σ|δ_ЛЗП|']:.4f}")

# Условная:
cond = conv.conditional_convergence()
print(f"Условная: предел = {cond['предел_ряда']:.4f}")

# Равномерная:
unif = conv.uniform_convergence_etd()
print(f"Равномерная: sup-ошибка = {unif['sup_ошибка']:.6f}")
```

---

## ЧАСТЬ II: ПРИЗНАКИ СХОДИМОСТИ → ТЕСТЫ МАСТЕРСТВА

```python
def convergence_tests_etd(lci_series: List[float]) -> dict:
    """
    4 признака сходимости → 4 теста мастерства:

    1. Признак Даламбера: lim |a_{n+1}/aₙ| < 1 → сходится
       ЕТД: Δ(ЛЗП_{n+1}) / Δ(ЛЗП_n) < 1 → рост замедляется (мастер!)

    2. Признак Коши: limsup |aₙ|^{1/n} < 1 → сходится
       ЕТД: корень степени n из Δ(ЛЗП_n) → 0 (устойчивый рост)

    3. Интегральный признак Коши-Маклорена
       ЕТД: ∫f(x)dx ≈ Σf(n) (непрерывная тренировка ≈ дискретная)

    4. Признак Лейбница (для знакочередующихся)
       ЕТД: чередующиеся «хорошие»/«плохие» сессии сходятся,
       если |aₙ| монотонно → 0.
    """
    n = len(lci_series)
    if n < 2:
        return {'ошибка': 'Нужно минимум 2 члена'}

    # Признак Даламбера
    ratios = [abs(lci_series[i+1] / lci_series[i])
              if lci_series[i] != 0 else float('inf')
              for i in range(n - 1)]
    d_alembert = sum(ratios) / len(ratios) if ratios else 1.0

    # Признак Коши
    cauchy = (abs(lci_series[-1])) ** (1.0 / n) if lci_series[-1] >= 0 else 1.0

    return {
        'Даламбер': d_alembert,
        'Коши': cauchy,
        'Даламбер_вывод': 'Сходится' if d_alembert < 1 else 'Расходится',
        'Коши_вывод': 'Сходится' if cauchy < 1 else 'Расходится',
        'ЕТД_итог': 'Рост мастерства стабилен' if d_alembert < 1 else 'Нужна коррекция',
    }

# Геометрическое уменьшение (абсолютная сходимость):
geo_series = [(math.pi / 4) * (0.7 ** n) for n in range(20)]
test = convergence_tests_etd(geo_series)
print(f"Признак Даламбера: {test['Даламбер']:.4f} → {test['Даламбер_вывод']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 типа сходимости рядов** → **4 режима накопления мастерства ЕТД**:

1. **Абсолютная** → Мастер устойчив к перестановке тренировок: Σ|δЛЗП| < ∞
2. **Условная** → Ритм критичен: правильное чередование даёт ln2·(π/4) → «Теорема Римана»
3. **Равномерная** → Мастер одинаково хорош во всех ЧВС-контекстах одновременно
4. **Поточечная** → Освоение контекст за контекстом (но есть «слепые пятна»)

> Иерархия строгости: Абсолютная → Равномерная → Поточечная. Мастер высшего класса обладает абсолютной сходимостью: его ЛЗП растёт независимо от порядка тренировок. Признак Даламбера: ratio = 0.7 → сходится (рост мастерства экспоненциально убывает к «предельному ЛЗП»).

---
*ТОМ 162 / СЕРИЯ VII / ЕТД 2026*
