# ТОМ 163: 4 ТЕОРЕМЫ КОМБИНАТОРИКИ → СЧЁТ ТЕХНИК И СТРАТЕГИЙ ЕТД
## Включений-исключений · Рамсея · Двойного счёта · Теорема Штейнера
### «Комбинаторика: 4 мощных принципа счёта»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Комбинаторика (Рамсей 1930, Штейнер 1853)

---

## ВВЕДЕНИЕ

Комбинаторика изучает конечные структуры. 4 ключевых результата:

1. **Принцип включений-исключений (PIE)** — счёт объединений через пересечения
2. **Теорема Рамсея** — в любой достаточно большой структуре есть порядок
3. **Принцип двойного счёта** — одна величина двумя способами
4. **Системы Штейнера** — оптимальное покрытие множества блоками

**Тезис тома**: Система 4 сфер ЕТД комбинаторна по природе. 4 теоремы считают техники, стратегии и комбинации — дают точные формулы для «размера» арсенала мастера.

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 АСПЕКТА КОМБИНАТОРИКИ ЕТД

```python
import math
from typing import List, Set, Dict, Tuple
from itertools import combinations, permutations

class CombinatoricsETD:
    """
    Комбинаторика системы 4 сфер ЕТД.
    Объект: 4 сферы × 4-5 направлений × N техник = множество стратегий.
    """

    SPHERES = ['МВС', 'СВС', 'БВС', 'ЧВС']
    N_SPHERES = 4
    CVS_STATES = 5   # ЧВС: 5 направлений

    # 1. ПРИНЦИП ВКЛЮЧЕНИЙ-ИСКЛЮЧЕНИЙ (PIE)
    @staticmethod
    def inclusion_exclusion(sphere_sizes: List[int],
                             pairwise_intersections: List[int],
                             triple_intersections: List[int],
                             quadruple_intersection: int) -> int:
        """
        |A₁ ∪ A₂ ∪ A₃ ∪ A₄| = Σ|Aᵢ| − Σ|Aᵢ∩Aⱼ| + Σ|Aᵢ∩Aⱼ∩Aₖ| − |A₁∩A₂∩A₃∩A₄|

        ЕТД-смысл: Количество УНИКАЛЬНЫХ техник в объединении сфер.
        Техники пересечений (общие для двух сфер) считаются один раз.
        Универсальные техники (в всех 4 сферах) = ядро системы.
        """
        total = sum(sphere_sizes)
        total -= sum(pairwise_intersections)
        total += sum(triple_intersections)
        total -= quadruple_intersection
        return total

    # 2. ТЕОРЕМА РАМСЕЯ
    @staticmethod
    def ramsey_etd(n_sparrings: int) -> dict:
        """
        Теорема Рамсея R(3,3) = 6: среди 6 человек обязательно
        найдутся 3, которые ЛИБО все знакомы, ЛИБО все незнакомы.

        ЕТД-интерпретация:
        6 спаррингов → обязательно 3 с одинаковым паттерном
        (либо 3 «атакующих», либо 3 «защитных» сессии).

        R(4,4) = 18: среди 18 спаррингов — 4 с одним паттерном.
        Мастер ЕТД: замечает паттерн из 4 повторений (R(4,4)).

        Обобщённый Рамсей для ЕТД:
        R(4, 4) = 18: 18 спаррингов → 4 одинаковых паттерна ЧВС.
        """
        # Классические числа Рамсея:
        ramsey_numbers = {
            (3, 3): 6,
            (3, 4): 9,
            (4, 4): 18,
            (3, 5): 14,
        }

        result = {}
        for (r, s), R in ramsey_numbers.items():
            etd_meaning = (
                f"Из {R} спаррингов → обязательно {r} «атак» или {s} «защит»"
                if r == 3 else
                f"Из {R} сессий → {r} одинаковых ЧВС-паттернов"
            )
            result[(r, s)] = {'R': R, 'ЕТД_смысл': etd_meaning}

        # ЕТД-специфика: R(4) = 4 сферы
        etd_r4 = {
            'формула': 'R(4,4) = 18',
            'ЕТД': '18 схваток → 4 с одинаковым ЧВС-паттерном (неизбежно!)',
            'вывод': 'Мастер замечает паттерн после ≤18 схваток',
        }

        return {'числа_Рамсея': result, 'ЕТД_R4': etd_r4}

    # 3. ПРИНЦИП ДВОЙНОГО СЧЁТА
    @staticmethod
    def double_counting_etd() -> dict:
        """
        Двойной счёт: одну и ту же сумму считаем двумя способами.

        Классика: handshaking lemma — Σdeg(v) = 2|E|.
        Каждое ребро дважды (считается обоими концами).

        ЕТД-версия: Σ_{сферы} (техник в сфере) = 2 × (числo_пар_взаимодействий)
        ... если каждая техника задействует ровно 2 сферы.

        Более точно: Матрица инцидентности ЕТД:
        — 4 сферы (строки) × 16 техник (столбцы)
        — Каждая техника принадлежит 1-2 сферам
        — Сумма = 2 × (техники, задействующие 2 сферы) + (чисто-одиночные)

        Результат: |М пересечений| = (Σ степеней) / 2.
        """
        n_spheres = 4
        n_techniques_per_sphere = 4
        n_shared_techniques = 4   # по 1 общей технике в каждой паре соседних сфер

        # Сумма степеней:
        total_degrees = n_spheres * n_techniques_per_sphere + n_shared_techniques * 2
        n_edges_equiv = total_degrees // 2

        return {
            'принцип': 'Двойной счёт',
            'Σ_техник': total_degrees,
            'взаимодействий': n_edges_equiv,
            'формула': 'Σdeg = 2|E| → |E| = (4×4 + 4×2)/2 = 12',
            'ЕТД_смысл': 'Каждое взаимодействие считается с обеих сфер',
            'вывод': f'{n_edges_equiv} пар «сфера→техника» взаимодействуют',
        }

    # 4. СИСТЕМА ШТЕЙНЕРА
    @staticmethod
    def steiner_system_etd() -> dict:
        """
        Система Штейнера S(t, k, n): семейство k-элементных блоков из n-элементного
        множества, каждый t-элементный подмножество покрыт РОВНО одним блоком.

        S(2, 4, 13): из 13 элементов, блоки по 4, каждая пара в 1 блоке.
        Число блоков: b = C(13,2)/C(4,2) = 78/6 = 13.

        ЕТД-применение: S(2, 4, n_техник)
        — n_техник = 13 (или другое подходящее)
        — Блоки по 4 (4 сферы покрывают каждую «ситуацию»)
        — Каждая пара ситуаций покрыта ровно одним «квартетом» сфер.

        Смысл: Система 4 сфер оптимально покрывает все пары ситуаций.
        """
        # S(2, 4, 13): параметры Штейнера
        t, k, n = 2, 4, 13

        # Число блоков = C(n,t) / C(k,t)
        def comb(a, b):
            return math.factorial(a) // (math.factorial(b) * math.factorial(a - b))

        b = comb(n, t) // comb(k, t)

        # Степень вершин = C(n-1, t-1) / C(k-1, t-1)
        r = comb(n - 1, t - 1) // comb(k - 1, t - 1)

        return {
            'система': f'S({t},{k},{n})',
            'блоков': b,
            'степень': r,
            'ЕТД_смысл': f'{b} «квартетов» техник покрывают все пары из {n} ситуаций',
            'оптимальность': 'Каждая пара ситуаций встречается в 1 «квартете» (не больше, не меньше)',
            'вывод': 'Система 4 сфер = оптимальная система Штейнера!',
        }

comb_etd = CombinatoricsETD()

# PIE: 4 сферы по 4 техники, 1 общая между каждой парой соседних
sizes = [4, 4, 4, 4]
pairs = [1, 1, 1, 1, 1, 1]  # 6 пар
triples = [0, 0, 0, 0]
quad = 0
unique = comb_etd.inclusion_exclusion(sizes, pairs, triples, quad)
print(f"Уникальных техник (PIE): {unique}")   # 16 - 6 = 10

# Рамсей:
ramsey = comb_etd.ramsey_etd(20)
print(f"R(4,4) = {ramsey['ЕТД_R4']['формула']}")

# Штейнер:
steiner = comb_etd.steiner_system_etd()
print(f"Система Штейнера: {steiner['блоков']} квартетов")
```

---

## ЧАСТЬ II: ЧИСЛО СТРАТЕГИЙ МАСТЕРА

```python
def count_etd_strategies() -> dict:
    """
    Сколько стратегий у мастера ЕТД?

    1. Выбор ЧВС-направления: C(5,1) = 5
    2. Выбор пары сфер для связки: C(4,2) = 6
    3. Выбор техник в каждой сфере: 4 × 4 = 16
    4. Порядок применения (перестановки): 4! = 24

    Полный арсенал: 5 × 6 × 16 × 24 = 11520 стратегий!

    Но: R(4,4) = 18 → паттерны повторяются.
    Эффективных неповторяющихся: ≈ 11520 / 18 ≈ 640.
    """
    cvs_choices = 5         # C(5,1)
    sphere_pairs = math.comb(4, 2)   # C(4,2) = 6
    technique_choices = 4 * 4        # 4 техники × 4 техники
    order_perms = math.factorial(4)  # 4! = 24

    total = cvs_choices * sphere_pairs * technique_choices * order_perms
    effective = total // 18   # R(4,4)

    return {
        'ЧВС_выборов': cvs_choices,
        'пар_сфер': sphere_pairs,
        'техник': technique_choices,
        'порядков': order_perms,
        'всего_стратегий': total,
        'эффективных': effective,
        'вывод': f'Мастер владеет {total} стратегиями, {effective} уникальных паттернов',
    }

strategies = count_etd_strategies()
print(f"Стратегий ЕТД: {strategies['всего_стратегий']} ({strategies['эффективных']} уникальных)")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы комбинаторики** → **4 принципа счёта ЕТД**:

1. **Включения-исключения** → Уникальных техник: |⋃ сфер| = 16 − 6(общих) = 10 уникальных
2. **Рамсея** → R(4,4) = 18: в любых 18 спаррингах — 4 с одинаковым паттерном
3. **Двойной счёт** → 12 пар «сфера↔техника»: каждое взаимодействие симметрично
4. **Штейнера S(2,4,13)** → 13 «квартетов»: система 4 сфер оптимально покрывает ситуации

> **Полный арсенал мастера**: 5 × 6 × 16 × 24 = **11520 стратегий**. Теорема Рамсея гарантирует: всего за 18 схваток противник выявит паттерн → мастер должен варьировать!

---
*ТОМ 163 / СЕРИЯ VII / ЕТД 2026*
