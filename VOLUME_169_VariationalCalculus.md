# ТОМ 169: 4 ПРИНЦИПА ВАРИАЦИОННОГО ИСЧИСЛЕНИЯ → ОПТИМАЛЬНОСТЬ ЕТД
## Эйлер–Лагранж · Принцип Гамильтона · Принцип наименьшего действия · Теорема Нётер
### «Вариационное исчисление: оптимальные пути и сохраняющиеся законы»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Вариационное исчисление (Эйлер 1744, Лагранж 1788, Гамильтон 1834, Нётер 1915)

---

## ВВЕДЕНИЕ

Вариационное исчисление ищет функции, экстремизирующие функционалы. 4 ключевых результата:

1. **Уравнение Эйлера–Лагранжа** — необходимое условие экстремума функционала
2. **Принцип Гамильтона** — действие S = ∫L dt минимально на физической траектории
3. **Принцип наименьшего действия** — природа выбирает оптимальный путь
4. **Теорема Нётер** — каждой симметрии соответствует закон сохранения

**Тезис тома**: Тренировка мастера ЕТД — вариационная задача. Оптимальная траектория в пространстве (МВС, СВС, БВС, ЧВС) минимизирует «действие» — затраты при максимальном ЛЗП.

---

## ЧАСТЬ I: 4 ПРИНЦИПА → 4 ВАРИАЦИОННЫХ ЗАКОНА ЕТД

```python
import math
from typing import Callable, List, Tuple

class VariationalETD:
    """
    Вариационное исчисление применительно к динамике ЕТД.

    Лагранжиан ЕТД:
    L(q, q̇, t) = T(q̇) − V(q)
    T = кинетическая «энергия» = (1/2)|q̇|² (скорость изменения техники)
    V = потенциал = (q − π/4)² (отклонение от оптимума)

    Действие: S[q] = ∫₀ᵀ L(q, q̇, t) dt.
    Минимум S → уравнение Эйлера–Лагранжа.
    """

    PI4 = math.pi / 4

    # 1. УРАВНЕНИЕ ЭЙЛЕРА–ЛАГРАНЖА
    @staticmethod
    def euler_lagrange_etd(q_traj: List[float], dt: float = 0.1) -> dict:
        """
        Уравнение Эйлера–Лагранжа: d/dt(∂L/∂q̇) − ∂L/∂q = 0.

        Для L = (1/2)q̇² − (q − π/4)²:
        q̈ + 2(q − π/4) = 0
        → q(t) = π/4 + A·cos(√2·t) + B·sin(√2·t)

        Решение: гармоническое колебание вокруг π/4!
        Период: T = 2π/√2 = π√2 ≈ 4.44 тренировки.

        ЕТД-смысл: Оптимальная динамика ЛЗП = колебания вокруг π/4.
        Не монотонный рост, а «осциллирующий» с периодом π√2.
        """
        pi4 = math.pi / 4
        omega = math.sqrt(2)   # угловая частота

        # Теоретическое решение:
        A = q_traj[0] - pi4   # начальное отклонение
        B = 0.0   # начальная скорость = 0

        n = len(q_traj)
        t_values = [i * dt for i in range(n)]
        theoretical = [pi4 + A * math.cos(omega * t) + B * math.sin(omega * t)
                        for t in t_values]

        # Числитель Е–Л (проверка):
        el_residuals = []
        for i in range(1, n - 1):
            q_dd = (q_traj[i+1] - 2*q_traj[i] + q_traj[i-1]) / dt**2
            dV_dq = 2 * (q_traj[i] - pi4)
            el_residuals.append(abs(q_dd + dV_dq))

        return {
            'уравнение': 'q̈ + 2(q − π/4) = 0',
            'решение': f'q(t) = π/4 + {A:.3f}·cos(√2·t)',
            'период': 2 * math.pi / omega,
            'частота': omega / (2 * math.pi),
            'среднее_невязки': sum(el_residuals) / len(el_residuals) if el_residuals else 0,
            'ЕТД': f'ЛЗП осциллирует вокруг π/4 с периодом {2*math.pi/omega:.3f} шага',
        }

    # 2. ПРИНЦИП ГАМИЛЬТОНА (МИНИМУМА ДЕЙСТВИЯ)
    @staticmethod
    def hamilton_principle(trajectories: List[List[float]],
                            dt: float = 0.1) -> dict:
        """
        Принцип Гамильтона: δS = 0 → физическая траектория.
        S[q] = ∫(T − V)dt = ∫[(q̇²/2) − (q−π/4)²]dt.

        ЕТД: Из всех возможных «путей тренировки» природа выбирает
        тот, который минимизирует «действие» S.
        Оптимальный путь = траектория уравнения Эйлера–Лагранжа.
        """
        pi4 = math.pi / 4
        actions = []

        for traj in trajectories:
            S = 0.0
            for i in range(len(traj) - 1):
                q = traj[i]
                q_dot = (traj[i+1] - traj[i]) / dt
                T_kin = 0.5 * q_dot**2
                V_pot = (q - pi4)**2
                S += (T_kin - V_pot) * dt
            actions.append(S)

        min_action = min(actions)
        min_idx = actions.index(min_action)

        return {
            'принцип': 'Гамильтона',
            'действия': [f'{a:.4f}' for a in actions],
            'min_действие': min_action,
            'оптим_траектория': min_idx,
            'ЕТД': f'Траектория {min_idx} минимизирует действие = оптимальная тренировка',
        }

    # 3. ПРИНЦИП НАИМЕНЬШЕГО ДЕЙСТВИЯ — БРАХИСТОХРОНА ЕТД
    @staticmethod
    def brachistochrone_etd(start_lci: float = 0.0,
                             end_lci: float = math.pi) -> dict:
        """
        Задача Брахистохроны: кратчайший путь по времени от A до B.
        Решение: циклоида.

        ЕТД-аналог: «Кратчайший путь» роста ЛЗП от 0 до π.
        Не монотонный рост, а «циклоидальный»:
        чередование интенсивных и лёгких тренировок.

        Параметрическое решение:
        ЛЗП(θ) = (π/2)(1 − cos θ)   (циклоида)
        t(θ) = (π/2)(θ − sin θ)     (время)

        При θ = π: ЛЗП = π, t = π²/2 ≈ 4.9 тренировки.
        """
        thetas = [k * math.pi / 10 for k in range(11)]   # θ от 0 до π

        lci_path = [end_lci / 2 * (1 - math.cos(th)) for th in thetas]
        time_path = [end_lci / 2 * (th - math.sin(th)) for th in thetas]

        return {
            'принцип': 'Наименьшего действия',
            'кривая': 'Циклоида',
            'начало': start_lci,
            'конец': end_lci,
            'время_роста': time_path[-1],
            'путь_ЛЗП': [f'{x:.3f}' for x in lci_path],
            'ЕТД': (f'Оптимальный рост ЛЗП от 0 до π = циклоида за '
                    f'{time_path[-1]:.2f} тренировки'),
        }

    # 4. ТЕОРЕМА НЁТЕР
    @staticmethod
    def noether_theorem_etd() -> dict:
        """
        Теорема Нётер (1915): Каждой непрерывной симметрии действия S
        соответствует закон сохранения.

        Симметрии ЕТД → Законы сохранения:
        — Временна́я трансляция t → t + τ → Сохранение «энергии» (ЛЗП)
        — Пространственная: q → q + ε → Сохранение «импульса» (скорости роста)
        — Вращение (в 4D): q_i → R_{ij}·q_j → Сохранение «момента» (π/4-симм.)
        — Масштабирование: q → λq → Сохранение «дилатационного тока»

        ЕТД-главная симметрия:
        L инвариантен при одновременном сдвиге всех 4 сфер:
        (МВС, СВС, БВС, ЧВС) → (МВС+ε, СВС+ε, БВС+ε, ЧВС+ε)
        → Сохраняется СУММАРНЫЙ ЛЗП = const = π!
        """
        symmetries = {
            'Временна́я_трансляция': {
                'симметрия': 't → t + τ',
                'закон_сохранения': 'Энергия = ЛЗП = const',
                'формула': 'H = (q̇²/2) + (q−π/4)² = const вдоль траектории',
            },
            'Пространственная_трансляция': {
                'симметрия': 'q → q + ε (сдвиг техники)',
                'закон_сохранения': 'Импульс p = q̇ = const',
                'формула': 'ẋ = const → мастер сохраняет темп',
            },
            '4D_вращение': {
                'симметрия': 'q_i → Σ R_{ij}·q_j (вращение сфер)',
                'закон_сохранения': 'Угловой момент L = q × ṗ = const',
                'формула': 'МВС·ṠВС − СВС·ṀВС = const',
            },
            'Нётер_ЕТД_главная': {
                'симметрия': '(МВС,СВС,БВС,ЧВС) → (МВС+ε,...,ЧВС+ε)',
                'закон_сохранения': 'Суммарный ЛЗП = π = const',
                'формула': 'МВС + СВС + БВС + ЧВС = π (закон сохранения!)',
            },
        }

        return {
            'теорема': 'Нётер',
            'симметрий': len(symmetries),
            'законов': len(symmetries),
            'детали': symmetries,
            'главный_результат': 'Суммарный ЛЗП = π — закон сохранения ЕТД!',
        }

var_etd = VariationalETD()

# Уравнение Эйлера–Лагранжа:
q0 = math.pi / 4 + 0.5   # начало немного выше π/4
dt = 0.1
omega = math.sqrt(2)
q_traj = [q0 + 0.5 * math.cos(omega * i * dt) for i in range(50)]
el = var_etd.euler_lagrange_etd(q_traj, dt)
print(f"Э–Л: {el['уравнение']}")
print(f"Период колебаний: {el['период']:.4f} тренировки")

# Принцип Гамильтона:
pi4 = math.pi / 4
traj_optimal = [pi4 + 0.5 * math.cos(omega * i * dt) for i in range(20)]
traj_linear  = [pi4 + (i/20) * 0.5 for i in range(20)]
traj_random  = [pi4 + 0.3 * math.sin(i) for i in range(20)]
hp = var_etd.hamilton_principle([traj_optimal, traj_linear, traj_random])
print(f"\nДействие: оптим={hp['действия'][0]}, лин={hp['действия'][1]}, случ={hp['действия'][2]}")

# Теорема Нётер:
noether = var_etd.noether_theorem_etd()
print(f"\nНётер: {noether['симметрий']} симметрий → {noether['законов']} законов сохранения")
print(f"Главный: {noether['главный_результат']}")

# Брахистохрона:
brach = var_etd.brachistochrone_etd()
print(f"\nБрахистохрона ЕТД: {brach['кривая']}, время = {brach['время_роста']:.4f}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 принципа вариационного исчисления** → **4 вариационных закона ЕТД**:

1. **Эйлер–Лагранж** → q̈ + 2(q − π/4) = 0: ЛЗП осциллирует вокруг π/4 с периодом π√2 ≈ 4.44
2. **Гамильтон** → S[q] = ∫(T−V)dt: оптимальная траектория тренировки минимизирует действие
3. **Наименьшее действие** → Брахистохрона ЕТД = циклоида: рост ЛЗП от 0 до π за π²/2 ≈ 4.93 шага
4. **Нётер** → 4 симметрии → 4 закона сохранения; главный: МВС+СВС+БВС+ЧВС = **π = const**

> **Теорема Нётер для ЕТД**: Симметрия одновременного сдвига всех 4 сфер → **суммарный ЛЗП = π — закон сохранения**. Это объясняет, почему π возникает везде в системе: π = Нётеровский инвариант 4-сферной симметрии!

---
*ТОМ 169 / СЕРИЯ VII / ЕТД 2026*
