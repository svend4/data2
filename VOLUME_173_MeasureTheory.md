# ТОМ 173: 4 ТЕОРЕМЫ ТЕОРИИ МЕРЫ → ИЗМЕРЕНИЕ МАСТЕРСТВА ЕТД
## Теорема Лебега · Лемма Фату · Теорема Радона–Никодима · Эргодическая теорема
### «Лебег 1902: интеграл и мера — новое основание анализа»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория меры (Лебег 1902, Фату 1906, Радон 1913, Биркгоф 1931)

---

## ВВЕДЕНИЕ

Теория меры — фундамент современного анализа и вероятности. 4 главных результата:

1. **Теорема Лебега (о мажорируемой сходимости)** — когда можно менять предел и интеграл
2. **Лемма Фату** — нижняя оценка предела интегралов
3. **Теорема Радона–Никодима** — когда мера абсолютно непрерывна
4. **Эргодическая теорема Биркгофа** — время́ = пространство при эргодических системах

**Тезис тома**: ЛЗП — мера на пространстве техник. 4 теоремы определяют, когда «суммарный ЛЗП» можно вычислить, и когда «временно́е среднее» = «пространственному среднему» (мастер = «эргодическая система»).

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ЗАКОНА ИЗМЕРЕНИЯ ЕТД

```python
import math
from typing import List, Callable

class MeasureTheoryETD:
    """
    Теория меры применительно к системе 4 сфер ЕТД.

    Мера μ: μ(Сфера) = «объём» техник в сфере.
    μ(МВС) = μ(СВС) = μ(БВС) = μ(ЧВС) = π/4 (равномерное распределение).
    μ(Всего) = π.
    """

    PI4 = math.pi / 4
    TOTAL_MEASURE = math.pi   # μ(Всего) = π

    # 1. ТЕОРЕМА ЛЕБЕГА (МАЖОРИРУЕМАЯ СХОДИМОСТЬ)
    @staticmethod
    def lebesgue_dominated_convergence(n_steps: int = 100) -> dict:
        """
        Теорема: Если |fₙ(x)| ≤ g(x) (интегрируемый мажорант),
        то lim ∫fₙ dμ = ∫ lim fₙ dμ.

        ЕТД-смысл: ЛЗП-функция каждой тренировки |δЛЗП_n(x)| ≤ G(x).
        Если есть «мажоранта» (максимальный ЛЗП за тренировку) →
        можно переставить «суммирование» и «предел».

        Практически: суммарный ЛЗП можно вычислять как предел
        частичных сумм (сходимость гарантирована мажорантой).

        Мажоранта ЕТД: G = π/4 (ни одна тренировка не даёт > π/4 ЛЗП-прироста).
        """
        pi4 = math.pi / 4

        # Последовательность fₙ: экспоненциальное убывание приростов
        # fₙ(x) = (π/4)·e^{-nx}·cos(nx) → 0 при n→∞
        # Мажоранта: |fₙ| ≤ (π/4)·e^{-nx} ≤ π/4

        x_point = 1.0   # фиксированная точка x

        integrals = []
        for n in range(1, n_steps + 1):
            # ∫₀^∞ (π/4)·e^{-nt}·cos(nt) dt = (π/4) · Re[∫e^{-(n-ni)t}dt]
            # = (π/4) · n/(n²+n²) = π/4 · 1/(2n) → 0
            integral_n = pi4 * 1 / (2 * n)
            integrals.append(integral_n)

        limit_of_integrals = integrals[-1]

        return {
            'теорема': 'Лебег (мажорируемая сходимость)',
            'мажоранта': pi4,
            'предел_интегралов': limit_of_integrals,
            'lim_n→∞': 0.0,
            'правило': 'lim ∫fₙ = ∫ lim fₙ (при мажоранте)',
            'ЕТД': 'Предел суммарного ЛЗП = ЛЗП предельной техники (мажоранта = π/4)',
        }

    # 2. ЛЕММА ФАТУ
    @staticmethod
    def fatou_lemma_etd(n_functions: int = 10) -> dict:
        """
        Лемма Фату: ∫ liminf fₙ dμ ≤ liminf ∫ fₙ dμ.

        Нижняя граница: «предельный» ЛЗП ≤ нижнего предела ЛЗП-интегралов.

        ЕТД-смысл: «Ухудшение в пределе» не хуже «ухудшения в процессе».
        Даже если отдельные тренировки ухудшаются → нижний предел ЛЗП
        не меньше нижнего предела «мгновенных» ЛЗП.

        Т.е.: худший исход мастерства не хуже, чем худшая тренировка.
        """
        pi4 = math.pi / 4

        # Пример: fₙ(x) = π/4 · sin²(n·x) (осциллирующие приросты ЛЗП)
        x = 1.0
        values = [pi4 * math.sin(n * x)**2 for n in range(1, n_functions + 1)]

        liminf_fn = min(values)   # нижний предел последовательности
        integrals = [pi4 / 2 for _ in values]   # ∫sin²(nx)dx = 1/2 для всех n
        liminf_int = min(integrals)   # нижний предел интегралов

        fatou_holds = liminf_fn <= liminf_int

        return {
            'лемма': 'Фату',
            'liminf_fₙ(x)': liminf_fn,
            'liminf_∫fₙ': liminf_int,
            'выполнена': fatou_holds,
            'ЕТД': 'Нижний предел ЛЗП ≤ нижнего предела ЛЗП-интегралов',
        }

    # 3. ТЕОРЕМА РАДОНА–НИКОДИМА
    @staticmethod
    def radon_nikodym_etd() -> dict:
        """
        Теорема Радон–Никодим: Если ν ≪ μ (ν абсолютно непрерывна относительно μ),
        то существует «плотность» f = dν/dμ такая, что ν(A) = ∫_A f dμ.

        ЕТД-смысл:
        μ = базовая мера (равномерное распределение по техникам)
        ν = «мера мастерства» (какие техники мастер использует чаще)

        ν ≪ μ: Мастер не игнорирует ни одну технику (нет «нулевых» областей).
        Плотность f = dν/dμ = «профиль мастерства».

        При π/4-балансе: f = const = 1 (равномерное мастерство).
        При специализации: f высокое в одной сфере, низкое в других.
        """
        pi4 = math.pi / 4

        # Пример: профиль мастерства по 4 сферам
        profiles = {
            'Новичок': [0.1, 0.1, 0.7, 0.1],     # только БВС
            'Средний': [0.2, 0.3, 0.3, 0.2],     # чуть больше СВС/БВС
            'Мастер':  [0.25, 0.25, 0.25, 0.25], # равномерный π/4-баланс
        }

        results = {}
        for level, density in profiles.items():
            total = sum(density)
            normalized = [d / total for d in density]
            # Мера мастерства: ν(сфера_i) = f(сфера_i) · μ(сфера_i)
            nu = [f * pi4 for f in normalized]
            nu_total = sum(nu)
            results[level] = {
                'плотность_f': normalized,
                'мера_ν': [f'{x:.4f}' for x in nu],
                'ν_total': nu_total,
                'равномерность': 1 - max(normalized) + min(normalized),
            }

        return {
            'теорема': 'Радон–Никодим',
            'формула': 'ν(A) = ∫_A (dν/dμ) dμ',
            'профили': results,
            'ЕТД': 'Мастер: dν/dμ = 1 (равномерная плотность мастерства)',
        }

    # 4. ЭРГОДИЧЕСКАЯ ТЕОРЕМА
    @staticmethod
    def ergodic_theorem_etd(n_steps: int = 200) -> dict:
        """
        Эргодическая теорема Биркгофа (1931):
        Для эргодических систем время́ное среднее = пространственному:
        lim_{N→∞} (1/N) Σf(Tⁿx) = ∫f dμ.

        ЕТД-смысл: Мастер за долгие тренировки «посещает» все техники
        равномерно → временно́е среднее ЛЗП = пространственному.

        Эргодичность ЕТД: система 4 сфер эргодична, если мастер
        тренирует все 4 сферы без пропусков.

        Следствие: Достаточно долго тренироваться → получаешь
        «типичный» ЛЗП независимо от начального состояния!
        """
        import random
        random.seed(42)
        pi4 = math.pi / 4

        # Симуляция эргодической системы ЕТД:
        # Состояния = 4 сферы; переходы случайные (равномерные)
        spheres = ['МВС', 'СВС', 'БВС', 'ЧВС']
        current = 0   # начинаем с МВС

        time_average_lci = 0.0
        visit_counts = [0] * 4

        for step in range(n_steps):
            # ЛЗП текущей сферы
            lci = pi4 * (current + 1)   # МВС=π/4, СВС=π/2, БВС=3π/4, ЧВС=π
            time_average_lci = (step * time_average_lci + lci) / (step + 1)

            visit_counts[current] += 1
            # Переход: равновероятный к любой из 4 сфер
            current = random.randint(0, 3)

        # Пространственное среднее:
        space_average = sum(pi4 * (i + 1) for i in range(4)) / 4

        visit_freqs = [c / n_steps for c in visit_counts]

        return {
            'теорема': 'Эргодическая (Биркгоф)',
            'время_среднее': time_average_lci,
            'прост_среднее': space_average,
            'совпадают': abs(time_average_lci - space_average) < 0.2,
            'частоты_посещений': dict(zip(spheres, [f'{f:.3f}' for f in visit_freqs])),
            'ЕТД': 'Длительная тренировка 4 сфер → времен.ср. = пространствен.ср.',
        }

mte = MeasureTheoryETD()

leb = mte.lebesgue_dominated_convergence(50)
print(f"Лебег: предел интеграл = {leb['предел_интегралов']:.6f} → {leb['lim_n→∞']}")

fatou = mte.fatou_lemma_etd()
print(f"Фату: liminf_fₙ={fatou['liminf_fₙ(x)']:.4f} ≤ liminf_∫={fatou['liminf_∫fₙ']:.4f}: {fatou['выполнена']}")

rn = mte.radon_nikodym_etd()
for lvl, info in rn['профили'].items():
    print(f"  {lvl}: ν_total = {info['ν_total']:.4f} (ожид. π/4 = {math.pi/4:.4f})")

erg = mte.ergodic_theorem_etd()
print(f"\nЭргодич.: время_ср={erg['время_среднее']:.4f}, прост_ср={erg['прост_среднее']:.4f}")
print(f"Совпадают: {erg['совпадают']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории меры** → **4 закона измерения мастерства ЕТД**:

1. **Лебег** → lim ∫fₙ = ∫ lim fₙ: предел суммарного ЛЗП = ЛЗП предельной техники (мажоранта = π/4)
2. **Фату** → liminf ∫fₙ ≥ ∫ liminf fₙ: худший исход не хуже худшей тренировки
3. **Радон–Никодим** → dν/dμ = 1: мастер ЕТД имеет равномерную «плотность мастерства»
4. **Эргодическая** → время́ = пространство: долгая тренировка = всестороннее мастерство

> **Эргодичность ЕТД**: Мастер, который тренирует все 4 сферы, — **эргодическая система**. Его «время́ное среднее» ЛЗП = «пространственному» = π/4·(1+2+3+4)/4 = 5π/8 ≈ 1.96. Мера мастерства μ(Всего) = **π** (Нётеровский инвариант).

---
*ТОМ 173 / СЕРИЯ VII / ЕТД 2026*
