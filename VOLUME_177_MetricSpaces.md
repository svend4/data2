# ТОМ 177: 4 ТИПА МЕТРИЧЕСКИХ ПРОСТРАНСТВ → ГЕОМЕТРИЯ АРСЕНАЛА ЕТД
## Полное · Компактное · Сепарабельное · Связное
### «Метрические пространства: 4 ключевых свойства структуры»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория метрических пространств (Фреше 1906, Хаусдорф 1914)

---

## ВВЕДЕНИЕ

Метрические пространства — абстракция «расстояния». 4 ключевых свойства:

1. **Полное** — все фундаментальные последовательности сходятся (нет «пробелов»)
2. **Компактное** — из любой последовательности можно выбрать сходящуюся подпоследовательность
3. **Сепарабельное** — существует счётное плотное подмножество (можно «аппроксимировать»)
4. **Связное** — нельзя разделить на два непустых открытых множества

**Тезис тома**: Пространство техник ЕТД = метрическое с метрикой d(A,B) = |ЛЗП_A − ЛЗП_B|. 4 свойства описывают «геометрию» арсенала и гарантируют возможность обучения.

---

## ЧАСТЬ I: 4 СВОЙСТВА → 4 СТРУКТУРНЫХ ГАРАНТИИ ЕТД

```python
import math
from typing import List, Tuple, Callable, Optional

class MetricSpaceETD:
    """
    Метрическое пространство (X, d) системы ЕТД.

    X = множество состояний ЕТД (ЛЗП-значения ∈ [0, π]).
    d(x, y) = |x − y| (стандартная метрика).

    Обобщение: X = ℝ⁴ (4-мерные ЛЗП-векторы).
    d(A, B) = |A − B| = √Σ(Aᵢ − Bᵢ)² (евклидова метрика).
    """

    PI4 = math.pi / 4

    # 1. ПОЛНОЕ ПРОСТРАНСТВО (COMPLETE)
    @staticmethod
    def completeness_etd(n_terms: int = 100) -> dict:
        """
        Полное метрическое пространство (X, d):
        Каждая фундаментальная (Коши) последовательность сходится в X.

        Последовательность Коши: ∀ε>0 ∃N: n,m>N → d(xₙ, xₘ) < ε.

        ℝ — полное (теорема Кантора).
        Пространство ЛЗП-значений [0, π] ⊂ ℝ — полное!

        ЕТД: Любая «фундаментальная» последовательность тренировок
        (приращения → 0) имеет предел — «предельный ЛЗП».
        Нет «дыр» в пространстве мастерства: к чему стремятся тренировки,
        того достигают (в ℝ).

        Теорема Банаха о неподвижной точке:
        Если d(f(x), f(y)) ≤ k·d(x,y) (k < 1), то существует единственная
        неподвижная точка x* = f(x*).

        ЕТД: f = «тренировка»; x* = π/4-аттрактор!
        Банах гарантирует: существует и единственна точка π/4-равновесия.
        """
        pi4 = math.pi / 4

        # Последовательность Коши: xₙ = π/4 + 1/n
        cauchy_seq = [pi4 + 1/n for n in range(1, n_terms + 1)]

        # Проверка критерия Коши (d(xₙ, xₘ) → 0):
        pairs_checked = 0
        max_diff = 0.0
        for n in range(0, min(10, n_terms)):
            for m in range(n + 1, min(11, n_terms)):
                diff = abs(cauchy_seq[n] - cauchy_seq[m])
                max_diff = max(max_diff, diff)
                pairs_checked += 1

        limit = cauchy_seq[-1]   # → π/4 при n→∞

        # Банах:
        k = 0.7   # сжатие (из теоремы Ляпунова Т.166)
        fixed_point_exists = (k < 1)

        return {
            'свойство': 'Полное',
            'последовательность': f'xₙ = π/4 + 1/n → π/4',
            'предел': limit,
            'cauchy_max_d': max_diff,
            'банах_k': k,
            'неподв_точка_сущ': fixed_point_exists,
            'ЕТД': 'Банах: π/4-аттрактор существует и единственен (k=0.7<1)',
        }

    # 2. КОМПАКТНОЕ ПРОСТРАНСТВО (COMPACT)
    @staticmethod
    def compactness_etd() -> dict:
        """
        Компактное пространство: из любой последовательности
        можно выбрать сходящуюся подпоследовательность.

        Теорема Хeine–Borel: [a,b] ⊂ ℝ компактно ⟺ замкнуто и ограничено.

        ЛЗП ∈ [0, π]: замкнуто и ограничено → КОМПАКТНО!

        ЕТД-следствия компактности:
        — ЛЗП-функция достигает максимума на компактном [0, π]
        — Любая тренировочная последовательность имеет «лучшую» подпоследовательность
        — Нет «убегания к бесконечности»: ЛЗП ограничен
        — Равномерная непрерывность (Теорема Кантора для компакта)

        Связь с Томом 162: компактность → все последовательности имеют
        Коши-подпоследовательность → сходимость (все 4 типа!) гарантирована.
        """
        lci_min, lci_max = 0.0, math.pi

        # Теорема Хейне–Бореля:
        is_closed = True    # [0, π] содержит свои граничные точки
        is_bounded = True   # |ЛЗП| ≤ π < ∞
        is_compact = is_closed and is_bounded

        # Максимум функции (достигается на компакте):
        # f(ЛЗП) = ЛЗП·sin(ЛЗП) на [0, π]:
        f = lambda x: x * math.sin(x)
        x_vals = [i * math.pi / 100 for i in range(101)]
        f_vals = [f(x) for x in x_vals]
        max_f = max(f_vals)
        x_max = x_vals[f_vals.index(max_f)]

        return {
            'свойство': 'Компактное',
            'область': '[0, π]',
            'замкнуто': is_closed,
            'ограничено': is_bounded,
            'компактно': is_compact,
            'максимум_функции': max_f,
            'достигается_в': x_max,
            'ЕТД': 'ЛЗП ∈ [0,π]: компактно → max ЛЗП достигается (не «убегает»)',
        }

    # 3. СЕПАРАБЕЛЬНОЕ ПРОСТРАНСТВО (SEPARABLE)
    @staticmethod
    def separability_etd() -> dict:
        """
        Сепарабельное пространство: существует счётное плотное подмножество D.
        D = {d₁, d₂, ...}: для любого x ∈ X и ε > 0 существует dₙ ∈ D с d(x, dₙ) < ε.

        ℝ сепарабельно: D = ℚ (рациональные) — счётны и плотны.

        ЕТД: D = {k·π/4 | k = 0, 1, 2, 3, 4} = 5 «ключевых» ЛЗП-уровней.
        Плотны ли они в [0, π]? Нет! Только ε-приближение не < π/4.

        Более точная сетка: D_n = {k·π/(4n) | k = 0, ..., 4n}.
        При n→∞: D_∞ = ℚ∩[0,π] — счётно, плотно → [0,π] сепарабельно.

        ЕТД: При конечном n, «технический словарь» из 4n+1 уровней
        аппроксимирует любой реальный ЛЗП с точностью π/(4n).
        n=4: 17 уровней, точность π/16 ≈ 0.196.
        n=8: 33 уровня, точность π/32 ≈ 0.098.
        """
        pi4 = math.pi / 4

        separable_sets = {}
        for n in [1, 2, 4, 8]:
            D_n = [k * math.pi / (4 * n) for k in range(4 * n + 1)]
            precision = math.pi / (4 * n)
            separable_sets[n] = {
                'точек': len(D_n),
                'точность': precision,
                'пример': [f'{x:.3f}' for x in D_n[:5]],
            }

        return {
            'свойство': 'Сепарабельное',
            'плотное_D': 'ℚ ∩ [0, π]',
            'ЕТД_сетки': separable_sets,
            'ЕТД': 'Конечный словарь D_n аппроксимирует любой ЛЗП с точностью π/(4n)',
        }

    # 4. СВЯЗНОЕ ПРОСТРАНСТВО (CONNECTED)
    @staticmethod
    def connectivity_etd() -> dict:
        """
        Связное пространство: нельзя представить X = U ∪ V,
        где U, V открыты, непусты и U ∩ V = ∅.

        [0, π] связно (как отрезок).
        ℝⁿ связно.

        Теорема о промежуточном значении (следствие связности):
        f: [a,b] → ℝ непрерывна, f(a) < 0 < f(b) → ∃c: f(c) = 0.

        ЕТД: Непрерывный рост ЛЗП от 0 до π обязательно проходит через π/4!
        Теорема о промеж. значении гарантирует: каждый уровень достигается.
        Нельзя «перепрыгнуть» с 0 до π, минуя π/4.
        """
        pi4 = math.pi / 4

        # Теорема о промежуточном значении:
        # f(ЛЗП) = ЛЗП − π/4: f(0) = -π/4 < 0, f(π) = 3π/4 > 0
        # → ∃c: f(c) = 0, т.е. ЛЗП = π/4 ОБЯЗАТЕЛЬНО достигается!

        f_0 = 0 - pi4          # = −π/4 < 0
        f_pi = math.pi - pi4   # = 3π/4 > 0

        ivt_guarantees_pi4 = (f_0 < 0 < f_pi)

        # Путевая связность: любые два ЛЗП-состояния соединены непрерывным путём
        path_connected = True   # ℝ⁴ путево-связно

        return {
            'свойство': 'Связное',
            'f(0)': f_0,
            'f(π)': f_pi,
            'ТПЗ_гарант_π/4': ivt_guarantees_pi4,
            'путево_связно': path_connected,
            'ЕТД': 'Непрерывный рост ЛЗП ОБЯЗАТЕЛЬНО проходит через π/4 (ТПЗ!)',
        }

mse = MetricSpaceETD()

comp = mse.completeness_etd(50)
print(f"Полное: предел = {comp['предел']:.4f}, банах-k={comp['банах_k']}")

compact = mse.compactness_etd()
print(f"Компактное: [0,π] компактно={compact['компактно']}, max_f≈{compact['максимум_функции']:.4f}")

sep = mse.separability_etd()
for n, info in sep['ЕТД_сетки'].items():
    print(f"  Сепарабельное n={n}: {info['точек']} точек, ε={info['точность']:.4f}")

conn = mse.connectivity_etd()
print(f"Связное: ТПЗ гарантирует π/4 = {conn['ТПЗ_гарант_π/4']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 типа метрических пространств** → **4 структурных гарантии арсенала ЕТД**:

1. **Полное** → Банах: π/4-аттрактор существует и единственен; «дыр» в мастерстве нет
2. **Компактное** → ЛЗП ∈ [0,π]: max ЛЗП достигается; не «убегает к бесконечности»
3. **Сепарабельное** → D_n (конечный словарь) аппроксимирует любой ЛЗП с точностью π/(4n)
4. **Связное** → ТПЗ: рост ЛЗП от 0 до π ОБЯЗАТЕЛЬНО проходит через π/4 (нельзя «перепрыгнуть»)

> **Метрическая гарантия ЕТД**: Пространство мастерства [0,π]⁴ полно, компактно, сепарабельно и связно — **совершенное метрическое пространство** (Польский тип). Теорема Банаха + Теорема о промежуточном значении: π/4 — гарантированная «точка прохождения» и «точка покоя» любого мастера.

---
*ТОМ 177 / СЕРИЯ VII / ЕТД 2026*
