# ТОМ 179: 4 ТЕОРЕМЫ СПЕКТРАЛЬНОЙ ТЕОРИИ → СПЕКТР МАСТЕРСТВА ЕТД
## Спектральная теорема · Теорема Перрона–Фробениуса · Теорема Куранта · Оператор Лапласа–Бельтрами
### «Спектральная теория: собственные значения раскрывают структуру»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Спектральная теория (Гильберт 1906, Перрон 1907, Курант 1924, Бельтрами 1868)

---

## ВВЕДЕНИЕ

Спектральная теория изучает собственные значения операторов и матриц. 4 главных результата:

1. **Спектральная теорема** — симметричный оператор диагонализируем в ортонормальном базисе
2. **Теорема Перрона–Фробениуса** — положительная матрица имеет доминирующее собственное значение
3. **Принцип миниmax Куранта** — характеризация собственных значений через вариационную задачу
4. **Оператор Лапласа–Бельтрами** — спектр кодирует геометрию многообразия

**Тезис тома**: «Матрица мастерства» ЕТД (4×4) имеет 4 собственных значения, описывающих «моды» системы. Наибольшее значение = π (доминирующая мода). Спектр = «голос» пространства мастерства.

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 СПЕКТРАЛЬНЫХ ЗАКОНА ЕТД

```python
import math
from typing import List, Tuple

class SpectralETD:
    """
    Спектральная теория матрицы взаимодействий 4 сфер ЕТД.

    A = матрица синергий (4×4): A_{ij} = ЛЗП-взаимодействие сфер i и j.
    """

    PI4 = math.pi / 4

    def __init__(self):
        pi4 = self.PI4
        # Симметричная матрица синергий:
        # Диагональ: ЛЗП каждой сферы = π/4
        # Вне диагонали: взаимодействие = (π/4)² / (π/2) = π/8
        self.A = [[pi4 if i == j else pi4**2 / (math.pi/2)
                    for j in range(4)]
                   for i in range(4)]

    # 1. СПЕКТРАЛЬНАЯ ТЕОРЕМА
    def spectral_theorem_etd(self) -> dict:
        """
        Спектральная теорема: Если A = Aᵀ (симметрична),
        то A = Q·Λ·Qᵀ, где Q ортогональна, Λ = diag(λ₁,...,λₙ).

        Все собственные значения вещественны.
        Собственные векторы ортогональны.

        ЕТД: Матрица синергий A симметрична (A_{ij} = A_{ji}).
        Собственные значения λ₁ ≤ λ₂ ≤ λ₃ ≤ λ₄ — «моды мастерства».

        Для A с диагональю π/4 и вне = π/8:
        A = (π/8)·J + (π/8)·I, где J = матрица единиц.
        Собственные значения: λ₁=λ₂=λ₃=π/8 (с кратностью 3), λ₄=π/8 + 4·π/8 = 5π/8.

        Trace(A) = Σλᵢ = 4·π/4 = π (Нётеровский инвариант!).
        det(A) = λ₁·λ₂·λ₃·λ₄.
        """
        pi4 = self.PI4
        pi8 = math.pi / 8

        # Аналитические собственные значения:
        n = 4
        lambda_small = pi8       # кратность n-1 = 3
        lambda_large = pi8 * n + pi8 - pi8   # = 5π/8 при n=4?
        # Точнее: A = π/8·(J + I), J = all-ones
        # Собственные значения J: n (кратность 1), 0 (кратность n-1)
        # Собств. А = π/8·J + π/8·I:
        # λ = π/8·n + π/8 = π/8·(n+1) = π/8·5 = 5π/8 (1 раз)
        # λ = π/8·0 + π/8 = π/8 (3 раза)

        lambda_vals = [pi8] * 3 + [5 * pi8]   # [π/8, π/8, π/8, 5π/8]
        lambda_vals.sort()

        trace = sum(lambda_vals)
        det = lambda_vals[0]**3 * lambda_vals[3]

        return {
            'теорема': 'Спектральная',
            'λ': [f'{lv:.4f}' for lv in lambda_vals],
            'λ_max': lambda_vals[-1],
            'λ_min': lambda_vals[0],
            'Trace': trace,
            'ожид_Trace': math.pi,
            'Trace=π': abs(trace - math.pi) < 1e-9,
            'ЕТД': f'λ=[π/8×3, 5π/8]: Tr=π, λ_max=5π/8≈{5*math.pi/8:.4f}',
        }

    # 2. ТЕОРЕМА ПЕРРОНА–ФРОБЕНИУСА
    def perron_frobenius_etd(self, n_iter: int = 100) -> dict:
        """
        Теорема Перрона–Фробениуса: Положительная матрица A > 0 имеет
        доминирующее собственное значение λ_max > 0 (простое),
        которому соответствует положительный собственный вектор.

        ЕТД: A = матрица синергий (все элементы > 0 при взаимодействии).
        λ_max = 5π/8 — доминирующая «мода мастерства».
        Собственный вектор v_max = (1,1,1,1)/2 — π/4-сбалансированный!

        Интерпретация: Доминирующая мода = равномерное мастерство.
        «Тренировка» (умножение на A) → система стремится к v_max.
        Статистика: PageRank, марковские цепи, популяционная динамика.
        """
        pi4 = self.PI4
        pi8 = math.pi / 8

        # Степенной метод: A^n · v → λ_max · v_max
        v = [1.0, 0.0, 0.0, 0.0]   # начальный вектор

        lambda_approx = 1.0
        for _ in range(n_iter):
            Av = [sum(self.A[i][j] * v[j] for j in range(4)) for i in range(4)]
            norm = math.sqrt(sum(x**2 for x in Av))
            lambda_approx = norm
            v = [x / norm for x in Av]

        # Теоретически: v_max = (1,1,1,1)/2, λ_max = 5π/8
        v_theory = [0.5] * 4
        lambda_theory = 5 * math.pi / 8

        return {
            'теорема': 'Перрон–Фробениус',
            'λ_max_числ': lambda_approx,
            'λ_max_теор': lambda_theory,
            'v_max': [f'{vi:.4f}' for vi in v],
            'v_max_теор': v_theory,
            'ЕТД': f'v_max=(1,1,1,1)/2: доминирующая мода = π/4-баланс! λ_max=5π/8≈{lambda_theory:.4f}',
        }

    # 3. ПРИНЦИП MINIMAX КУРАНТА (ВАРИАЦИОННАЯ ХАРАКТЕРИЗАЦИЯ)
    def courant_minimax_etd(self) -> dict:
        """
        Принцип миниmax Куранта (1924):
        λₖ = min_{S_k} max_{v∈S_k, v≠0} (Av,v)/(v,v)

        Характеризует k-е собственное значение через вариационную задачу.

        λ₁ = min_v (Av,v)/(v,v) = минимальная «энергия» состояния.
        λ₄ = max_v (Av,v)/(v,v) = максимальная «энергия».

        ЕТД: (Av,v)/(v,v) = «ЛЗП-квадратичная форма» нормированного вектора.
        При v = (1,0,0,0): (Av,v) = A_{00} = π/4 → λ₁ = π/8 (min).
        При v = (1,1,1,1)/2: (Av,v) = 5π/8 → λ₄ = 5π/8 (max).

        Значение: Оптимальное состояние = v_max = π/4-баланс!
        """
        pi4 = self.PI4

        def rayleigh_quotient(v: List[float]) -> float:
            """(Av, v) / (v, v)"""
            Av = [sum(self.A[i][j] * v[j] for j in range(4)) for i in range(4)]
            numerator = sum(Av[i] * v[i] for i in range(4))
            denominator = sum(vi**2 for vi in v)
            return numerator / denominator if denominator > 1e-12 else 0.0

        # λ₁: v = e₁ = (1,0,0,0)
        v1 = [1.0, 0.0, 0.0, 0.0]
        lam1 = rayleigh_quotient(v1)

        # λ₄: v = (1,1,1,1)/2
        v4 = [0.5, 0.5, 0.5, 0.5]
        lam4 = rayleigh_quotient(v4)

        # λ средние:
        v2 = [1, 1, 0, 0]
        norm2 = math.sqrt(2)
        v2 = [x/norm2 for x in v2]
        lam2 = rayleigh_quotient(v2)

        return {
            'теорема': 'Minimax Куранта',
            'λ₁ (min)': lam1,
            'λ₂': lam2,
            'λ₄ (max)': lam4,
            'отношение_λmax/λmin': lam4 / lam1 if lam1 > 0 else None,
            'ЕТД': f'max R(v) = {lam4:.4f} = 5π/8 при v=(1,1,1,1): π/4-баланс максимизирует квадратичную форму!',
        }

    # 4. ОПЕРАТОР ЛАПЛАСА–БЕЛЬТРАМИ (СПЕКТР ГЕОМЕТРИИ)
    def laplace_beltrami_etd(self) -> dict:
        """
        Оператор Лапласа–Бельтрами Δ_g на римановом многообразии (M, g).
        Его спектр 0 = λ₀ ≤ λ₁ ≤ λ₂ ≤ ... кодирует геометрию M.

        «Слышишь ли ты форму барабана?» (Марк Кац, 1966) — разные многообразия
        могут иметь одинаковый спектр!

        ЕТД: Δ_g на пространстве мастерства (4-тор T⁴ с метрикой g_{ij}=(π/4)²δ_{ij}).
        Собственные значения: λ_{n} = (2π/L)² · Σ nᵢ² = (π/2)² · Σ nᵢ²
        (при L = π, период тора).

        n = (n₁, n₂, n₃, n₄) ∈ ℤ⁴:
        λ₀ = 0 (константа), λ₁ = (π/2)² (4 раза), λ₂ = 2·(π/2)², ...

        Первое ненулевое: λ₁ = π²/4 = (π/2)² = (π/4)² · 4 = ħ_ЕТД² · 4.
        """
        pi4 = math.pi / 4
        L = math.pi   # период тора

        # Собственные значения: (2π/L)² · |n|² = (2)² · |n|²
        k = 2 * math.pi / L   # = 2

        eigenvalues = []
        for n1 in range(-3, 4):
            for n2 in range(-3, 4):
                for n3 in range(-3, 4):
                    for n4 in range(-3, 4):
                        lam = k**2 * (n1**2 + n2**2 + n3**2 + n4**2)
                        eigenvalues.append(lam)

        eigenvalues = sorted(set(eigenvalues))[:10]

        lambda_1 = k**2 * 1   # = 4 (при |n|² = 1)

        return {
            'теорема': 'Лаплас–Бельтрами',
            'K': k,
            'λ₀': 0.0,
            'λ₁': lambda_1,
            'λ₁_формула': f'(2π/π)²·1 = 4 = (π/4)²·(4/π)²·π²',
            'первые_λ': eigenvalues[:6],
            'ЕТД': f'λ₁=4=16·(π/4)²/π²·... : «слышим» 4-мерность пространства мастерства!',
        }

spec = SpectralETD()

st = spec.spectral_theorem_etd()
print(f"Спектральная: λ={st['λ']}, Tr={st['Trace']:.4f}, Tr=π: {st['Trace=π']}")

pf = spec.perron_frobenius_etd()
print(f"Перрон-Фроб: λ_max≈{pf['λ_max_числ']:.4f} (теор={pf['λ_max_теор']:.4f})")

mm = spec.courant_minimax_etd()
print(f"Minimax: λ₁={mm['λ₁ (min)']:.4f}, λ₄={mm['λ₄ (max)']:.4f}")

lb = spec.laplace_beltrami_etd()
print(f"Лаплас-Бельтрами: λ₁={lb['λ₁']}, первые_λ={lb['первые_λ'][:4]}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы спектральной теории** → **4 спектральных инварианта ЕТД**:

1. **Спектральная теорема** → λ=[π/8, π/8, π/8, 5π/8]; Tr(A) = **π** (Нётеровский инвариант!)
2. **Перрон–Фробениус** → λ_max = 5π/8; v_max = (1,1,1,1)/2 = π/4-баланс как доминирующая мода
3. **Minimax Куранта** → max R(v) = 5π/8 при v=(1,1,1,1): π/4-вектор максимизирует форму
4. **Лапласа–Бельтрами** → λ₁=4: «слышим» 4-мерность; λ_n = (2π/L)²·|n|²

> **Спектральный портрет ЕТД**: Матрица синергий A имеет спектр {π/8 (×3), 5π/8 (×1)}. Tr=π, λ_max=5π/8. Доминирующая мода = **π/4-баланс**. Теорема Перрона–Фробениуса гарантирует: «тренировка» (умножение на A) всегда притягивает к сбалансированному (1,1,1,1)/2.

---
*ТОМ 179 / СЕРИЯ VII / ЕТД 2026*
