# ТОМ 181: 4 ТЕОРЕМЫ ТЕОРИИ ПРЕДСТАВЛЕНИЙ → СИММЕТРИЯ ЕТД
## Теорема Машке · Лемма Шура · Теорема Питера–Вейля · Теорема Вейля о характерах
### «Теория представлений: группы действуют на векторных пространствах»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория представлений (Машке 1899, Шур 1905, Питер–Вейль 1927, Вейль 1925)

---

## ВВЕДЕНИЕ

Теория представлений изучает, как группы «действуют» на векторных пространствах. 4 главных результата:

1. **Теорема Машке** — всякое конечное представление полупросто (разбивается на неприводимые)
2. **Лемма Шура** — неприводимые представления «не взаимодействуют»
3. **Теорема Питера–Вейля** — L²(G) разлагается в ортогональную прямую сумму
4. **Теорема Вейля о характерах** — характер однозначно определяет представление

**Тезис тома**: Группа симметрий 4 сфер ЕТД — S₄ (порядок 24). Её неприводимые представления описывают «элементарные режимы мастерства». Теорема Машке: любое ЛЗП-представление разбивается на 4 неприводимых блока.

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ЗАКОНА ПРЕДСТАВЛЕНИЙ ЕТД

```python
import math
from typing import List, Dict, Tuple

class RepresentationETD:
    """
    Теория представлений группы S₄ (перестановки 4 сфер).
    |S₄| = 24 = 4! (число перестановок 4 объектов = число Вильсона(5)!).

    Неприводимые представления S₄:
    d₁=1 (тривиальное), d₂=1 (знаковое), d₃=2, d₄=3, d₅=3.
    Проверка: Σdᵢ² = 1²+1²+2²+3²+3² = 1+1+4+9+9 = 24 = |S₄|. ✓
    """

    GROUP_ORDER = 24   # |S₄| = 4!
    IRREPS = [1, 1, 2, 3, 3]   # размерности неприводимых представлений

    # 1. ТЕОРЕМА МАШКЕ
    @staticmethod
    def maschke_theorem_etd() -> dict:
        """
        Теорема Машке: Если |G| не делится на char(F), то любое
        конечное представление G над F полупросто.

        Для G = S₄, F = ℝ (или ℂ): char = 0, не делит |S₄| = 24. ✓
        → Любое ЛЗП-представление S₄ над ℝ полупросто!

        Полупростота: ρ = ρ₁ ⊕ ρ₂ ⊕ ... ⊕ ρₖ (прямая сумма неприводимых).

        ЕТД: Любое «сложное» действие на 4 сферах = сумма элементарных.
        Мастерство разбивается на 5 «элементарных режимов» S₄.
        Нет «запутанных» неразложимых ситуаций!
        """
        group_order = RepresentationETD.GROUP_ORDER
        char_F = 0   # characteristic ℝ = 0

        maschke_applies = (char_F == 0) or (group_order % char_F != 0)

        # Декомпозиция регулярного представления:
        # ρ_reg = ⊕ dᵢ · ρᵢ (d₁ copies of ρ₁, etc.)
        irreps = RepresentationETD.IRREPS
        reg_decomp = {f'ρ_{i+1} (dim={d})': d
                       for i, d in enumerate(irreps)}

        check_sum = sum(d**2 for d in irreps)

        return {
            'теорема': 'Машке',
            'группа': 'S₄',
            '|G|': group_order,
            'char(F)': char_F,
            'полупросто': maschke_applies,
            'регулярная_декомп': reg_decomp,
            'Σdᵢ²': check_sum,
            'Σdᵢ²=|G|': check_sum == group_order,
            'ЕТД': 'Любое ЛЗП-представление S₄ = сумма из 5 элементарных режимов',
        }

    # 2. ЛЕММА ШУРА
    @staticmethod
    def schur_lemma_etd() -> dict:
        """
        Лемма Шура: Если ρ, σ — неприводимые представления G, то:
        — Hom_G(ρ, σ) = 0, если ρ ≇ σ (несхожие → нет интертвайнеров)
        — Hom_G(ρ, ρ) = F·Id (над алгебраически замкнутым полем)

        ЕТД: Неприводимые режимы мастерства «не влияют» друг на друга.
        ρ₁ (тривиальное = π/4-баланс) и ρ₄ (3D = динамика тела) — независимы.
        Интертвайнер между ними = 0: одна техника не «разрушает» другую.

        Следствие: Разные «режимы» S₄ действуют в ортогональных «каналах».
        Мастер использует независимые каналы без помех.
        """
        irreps = RepresentationETD.IRREPS
        n = len(irreps)

        # Матрица Hom_G(ρᵢ, ρⱼ):
        hom_matrix = [[1 if i == j else 0 for j in range(n)]
                       for i in range(n)]

        return {
            'лемма': 'Шура',
            'Hom_matrix': hom_matrix,
            'независимые_пары': sum(1 for i in range(n) for j in range(n) if i != j),
            'зависимые': n,
            'ЕТД': '5 режимов S₄ действуют в 5 независимых каналах (Шур)',
        }

    # 3. ТЕОРЕМА ПИТЕРА–ВЕЙЛЯ
    @staticmethod
    def peter_weyl_etd() -> dict:
        """
        Теорема Питера–Вейля: Для компактной группы G:
        L²(G) = ⊕_ρ (ρ ⊗ ρ̄) (прямая сумма по неприводимым ρ).

        Матричные элементы ρᵢⱼ(g) образуют ортонормальный базис L²(G).

        ЕТД: G = S₄; L²(S₄) = пространство «квадратично-интегрируемых»
        функций на группе симметрий 4 сфер.

        Разложение: L²(S₄) = ρ₁⊗ρ₁̄ ⊕ ρ₂⊗ρ₂̄ ⊕ ρ₃⊗ρ₃̄ ⊕ ρ₄⊗ρ₄̄ ⊕ ρ₅⊗ρ₅̄.
        Размерности: 1 + 1 + 4 + 9 + 9 = 24 = |S₄|. ✓

        Физический смысл: Любое «поле мастерства» на группе S₄ разлагается
        в ортогональные «частоты» — моды перестановок сфер.
        """
        irreps = RepresentationETD.IRREPS

        # Размерности ρᵢ ⊗ ρᵢ̄ = dᵢ²
        tensor_dims = [d**2 for d in irreps]
        total = sum(tensor_dims)

        peter_weyl_decomp = {
            f'ρ_{i+1}⊗ρ_{i+1}̄ (dim {irreps[i]}²={tensor_dims[i]})': tensor_dims[i]
            for i in range(len(irreps))
        }

        return {
            'теорема': 'Питера–Вейля',
            'L²(S₄)_dim': total,
            '|S₄|': RepresentationETD.GROUP_ORDER,
            'совпадает': total == RepresentationETD.GROUP_ORDER,
            'декомпозиция': peter_weyl_decomp,
            'ЕТД': 'Любое ЛЗП-поле на S₄ = 5 ортогональных мод (П–В)',
        }

    # 4. ТЕОРЕМА ВЕЙЛЯ О ХАРАКТЕРАХ
    @staticmethod
    def weyl_character_theorem_etd() -> dict:
        """
        Теорема о характерах: Два представления изоморфны ⟺ у них совпадают характеры.
        χ_ρ(g) = Tr(ρ(g)) — функция класса (характер).

        Ортогональность характеров: ⟨χᵢ, χⱼ⟩ = δᵢⱼ.
        Характеры образуют ортонормальный базис в пространстве функций классов.

        ЕТД: Характер ρ_тривиального = 1 (для всех g ∈ S₄).
        χ_баланс(g) = 1 при любой перестановке сфер → π/4-баланс инвариантен!

        Характер ρ₁: χ₁(e)=1, χ₁(любой цикл)=1.
        Характер ρ₄ (3D): χ₄(e)=3, χ₄(транспозиция)=1, χ₄((12)(34))=-1.
        """
        # Таблица характеров S₄ (5 классов сопряжённых):
        # классы: e, (12), (123), (1234), (12)(34)
        # размеры классов: 1, 6, 8, 6, 3
        class_sizes = [1, 6, 8, 6, 3]
        class_names = ['e', '(ij)', '(ijk)', '(ijkl)', '(ij)(kl)']

        character_table = {
            'ρ₁ (тривиальн.)': [1,  1,  1,  1,  1],
            'ρ₂ (знаковое)':   [1, -1,  1, -1,  1],
            'ρ₃ (2D)':         [2,  0, -1,  0,  2],
            'ρ₄ (3D,stand.)':  [3,  1,  0, -1, -1],
            'ρ₅ (3D,sign.)':   [3, -1,  0,  1, -1],
        }

        # Ортогональность: ⟨χᵢ,χⱼ⟩ = (1/|G|) Σ |Cₖ|·χᵢ(Cₖ)·χⱼ(Cₖ)
        def inner_product(chi1, chi2):
            G = RepresentationETD.GROUP_ORDER
            return sum(class_sizes[k] * chi1[k] * chi2[k]
                        for k in range(5)) / G

        chars = list(character_table.values())
        ortho_matrix = [[round(inner_product(chars[i], chars[j]), 6)
                          for j in range(5)]
                         for i in range(5)]

        is_orthonormal = all(
            abs(ortho_matrix[i][i] - 1.0) < 1e-5 and
            all(abs(ortho_matrix[i][j]) < 1e-5 for j in range(5) if j != i)
            for i in range(5)
        )

        return {
            'теорема': 'Вейля о характерах',
            'таблица': character_table,
            'классы': class_names,
            'ортонормальность': is_orthonormal,
            'ЕТД': 'χ₁(g)=1 для всех g: π/4-баланс инвариантен при любой перестановке сфер!',
        }

rep = RepresentationETD()

maschke = rep.maschke_theorem_etd()
print(f"Машке: Σdᵢ²={maschke['Σdᵢ²']} = |S₄|={maschke['|G|']}: {maschke['Σdᵢ²=|G|']}")

pw = rep.peter_weyl_etd()
print(f"Питер–Вейль: dim L²(S₄)={pw['L²(S₄)_dim']}, совпад.={pw['совпадает']}")

weyl = rep.weyl_character_theorem_etd()
print(f"Вейль: ортонормальность таблицы = {weyl['ортонормальность']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории представлений** → **4 закона симметрии ЕТД**:

1. **Машке** → S₄ полупроста: любое ЛЗП-представление = сумма 5 элементарных режимов
2. **Шур** → 5 режимов S₄ независимы: Hom_G(ρᵢ,ρⱼ)=δᵢⱼ·ℝ
3. **Питер–Вейль** → L²(S₄) = ⊕ dᵢ²: Σ1+1+4+9+9 = **24 = |S₄| = 4!**
4. **Вейль** → χ₁(g)=1: π/4-баланс **инвариантен при любой перестановке** 4 сфер!

> **Вывод**: π/4-баланс — инвариант группы S₄ (тривиальный характер = 1). Любая перестановка сфер не изменяет равновесного состояния. Число 24 = 4! = |S₄| = число Вильсона(5) связывает теорию представлений с теорией чисел.

---
*ТОМ 181 / СЕРИЯ VII / ЕТД 2026*
