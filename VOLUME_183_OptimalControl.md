# ТОМ 183: 4 ПРИНЦИПА ОПТИМАЛЬНОГО УПРАВЛЕНИЯ → СТРАТЕГИЯ ЕТД
## Принцип Понтрягина · Уравнение Беллмана · LQR-регулятор · Управление с учётом ограничений
### «Понтрягин 1956: максимальный принцип и оптимальные траектории»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория оптимального управления (Понтрягин 1956, Беллман 1957)

---

## ВВЕДЕНИЕ

Теория оптимального управления ищет лучшую стратегию управления системой. 4 ключевых принципа:

1. **Принцип максимума Понтрягина** — необходимое условие оптимальности через гамильтониан
2. **Уравнение Беллмана** — динамическое программирование (оптимальность подзадач)
3. **LQR-регулятор** — линейно-квадратичное управление (аналитическое решение)
4. **Управление с ограничениями** — задача с ограничениями на управление u ∈ U

**Тезис тома**: Тренировка мастера ЕТД — задача оптимального управления. Состояние x = (МВС, СВС, БВС, ЧВС). Управление u = «интенсивность тренировки» каждой сферы. Цель: максимизировать ЛЗП за минимальное время.

---

## ЧАСТЬ I: 4 ПРИНЦИПА → 4 ЗАКОНА ОПТИМАЛЬНОЙ ТРЕНИРОВКИ ЕТД

```python
import math
from typing import List, Tuple, Callable

class OptimalControlETD:
    """
    Оптимальное управление системой 4 сфер ЕТД.

    Состояние: x = (x₁, x₂, x₃, x₄) = (МВС, СВС, БВС, ЧВС) ∈ ℝ⁴.
    Управление: u = (u₁, u₂, u₃, u₄) = «интенсивности тренировок».
    Динамика: ẋᵢ = −αᵢ(xᵢ − π/4) + uᵢ.
    Критерий: J = ∫₀ᵀ [Σ(xᵢ−π/4)² + r·Σuᵢ²] dt → min.
    """

    PI4 = math.pi / 4
    ALPHA = 0.3   # естественная скорость затухания

    # 1. ПРИНЦИП МАКСИМУМА ПОНТРЯГИНА
    @staticmethod
    def pontryagin_etd() -> dict:
        """
        Принцип максимума: Оптимальное управление u*(t) максимизирует гамильтониан:
        H(x, u, p, t) = L(x,u) + p·f(x,u)
        (p = «сопряжённая» переменная = «цена» состояния).

        Условие: ∂H/∂u = 0 (или u* = argmax H при u ∈ U).

        ЕТД гамильтониан:
        H = −Σ(xᵢ−π/4)² − r·Σuᵢ² + Σpᵢ·(−α(xᵢ−π/4) + uᵢ)
        ∂H/∂uᵢ = −2r·uᵢ + pᵢ = 0
        → u*ᵢ = pᵢ / (2r) (оптимальное управление!)

        Сопряжённая система: ṗᵢ = −∂H/∂xᵢ = 2(xᵢ−π/4) + α·pᵢ.
        """
        pi4 = math.pi / 4
        alpha = OptimalControlETD.ALPHA
        r = 0.1   # вес управления (штраф за интенсивность)

        # Стационарное решение (при T→∞):
        # xᵢ* = π/4, pᵢ* = 0, uᵢ* = 0 (равновесие без управления)
        # НО: если начинаем вдали от π/4, оптимальное u ≠ 0.

        # Оптимальная интенсивность (bang-bang при r=0, плавная при r>0):
        x0 = 0.0   # начальное состояние (новичок)
        p0 = 2 * (x0 - pi4) / alpha   # начальное сопряжённое (аппрокс.)

        u_optimal = p0 / (2 * r)
        u_clipped = min(max(u_optimal, 0), pi4)   # u ∈ [0, π/4]

        return {
            'принцип': 'Понтрягин',
            'гамильтониан': 'H = −Σ(x−π/4)² − r·Σu² + Σp·(−α(x−π/4)+u)',
            'условие': '∂H/∂u = 0 → u* = p/(2r)',
            'r': r,
            'u*_начало': u_clipped,
            'ЕТД': f'Оптимальная начальная интенсивность тренировки: u*={u_clipped:.4f}',
        }

    # 2. УРАВНЕНИЕ БЕЛЛМАНА
    @staticmethod
    def bellman_equation_etd(T: int = 10, n: int = 20) -> dict:
        """
        Уравнение Беллмана (динамическое программирование):
        V(x, t) = min_u [L(x,u)·dt + V(x + f(x,u)·dt, t+dt)]

        Принцип оптимальности: «Хвост оптимальной траектории оптимален».

        ЕТД: V(x) = «ценность функции» = min будущих потерь.
        V*(x) = минимальные затраты на доведение x до π/4.

        Уравнение Гамильтона–Якоби–Беллмана (ГЯБ):
        −∂V/∂t = min_u [L(x,u) + (∂V/∂x)·f(x,u)]

        Для LQR: V*(x) = (x − π/4)ᵀ · P · (x − π/4)
        где P — матрица Риккати (решение уравнения Риккати).
        """
        pi4 = math.pi / 4
        alpha = OptimalControlETD.ALPHA
        Q = 1.0   # вес состояния
        R = 0.1   # вес управления

        # Решение уравнения Риккати для 1D:
        # α·P + P·α − P²/R + Q = 0
        # P = R·α + R·√(α² + Q/R)
        P = R * alpha + R * math.sqrt(alpha**2 + Q / R)
        K_gain = P / R   # оптимальный коэффициент усиления

        # Оптимальное управление: u* = −K·(x − π/4)
        x_test = 0.5   # тестовое состояние
        u_star = -K_gain * (x_test - pi4)

        return {
            'принцип': 'Беллман',
            'уравнение': 'V*(x) = (x−π/4)ᵀ·P·(x−π/4)',
            'P_Риккати': P,
            'K_gain': K_gain,
            'u*(x=0.5)': u_star,
            'ЕТД': f'Оптимальный регулятор K={K_gain:.3f}: u*=−{K_gain:.3f}·(x−π/4)',
        }

    # 3. LQR-РЕГУЛЯТОР
    @staticmethod
    def lqr_etd(n_steps: int = 100) -> dict:
        """
        Линейно-квадратичный регулятор (LQR):
        ẋ = Ax + Bu, J = ∫(xᵀQx + uᵀRu)dt → min.
        u* = −Kx, K = R⁻¹·Bᵀ·P (P = решение уравнения Риккати).

        ЕТД: A = −α·I₄ (матрица системы), B = I₄ (прямое управление).
        Q = I₄ (все сферы равноважны), R = r·I₄.

        Решение: K = √((α² + 1/r) − α)·I₄ = k_opt·I₄.
        Для r=0.1, α=0.3: k_opt ≈ 2.9.

        Траектория: x(t) = π/4 + (x₀−π/4)·e^{−(α+k_opt)·t}.
        Время сходимости: τ = 1/(α+k_opt).
        """
        pi4 = math.pi / 4
        alpha = OptimalControlETD.ALPHA
        r = 0.1
        Q_weight = 1.0

        k_opt = math.sqrt(alpha**2 + Q_weight / r) - alpha
        conv_rate = alpha + k_opt
        tau = 1 / conv_rate

        # Симуляция:
        x0 = 0.0   # новичок
        trajectory = [x0]
        dt = tau * 4 / n_steps

        x = x0
        for _ in range(n_steps):
            u = -k_opt * (x - pi4)
            x_dot = -alpha * (x - pi4) + u
            x += x_dot * dt
            trajectory.append(x)

        final_x = trajectory[-1]

        return {
            'регулятор': 'LQR',
            'k_opt': k_opt,
            'τ_сходим': tau,
            'x_final': final_x,
            'x*': pi4,
            'ошибка': abs(final_x - pi4),
            'ЕТД': f'LQR: τ={tau:.3f}, k={k_opt:.3f}: сходится к π/4={pi4:.4f}',
        }

    # 4. УПРАВЛЕНИЕ С ОГРАНИЧЕНИЯМИ
    @staticmethod
    def constrained_control_etd() -> dict:
        """
        u ∈ U = {u : 0 ≤ uᵢ ≤ U_max} (ограничения на управление).

        Задача: max ЛЗП при ограниченных ресурсах тренировки.
        Σuᵢ ≤ U_total (суммарный ресурс фиксирован).

        Bang-bang управление: uᵢ* = 0 или uᵢ* = U_max.
        Для наших ограничений: оптимально распределить U_total равномерно!

        Σuᵢ = U_total, max ЛЗП = f(u₁, u₂, u₃, u₄).

        По неравенству Коши–Шварца:
        ЛЗП = Σuᵢ·вес ≤ √(Σuᵢ²)·√(Σвес²) — максимально при uᵢ = const.
        → u*ᵢ = U_total / 4 для всех i (равномерное распределение = π/4-баланс!).
        """
        pi4 = math.pi / 4
        U_total = math.pi   # суммарный ресурс = π (Нётеровский инвариант!)

        u_uniform = U_total / 4   # равномерное распределение = π/4!

        # Проверка: u_uniform = π/4
        is_pi4 = abs(u_uniform - pi4) < 1e-10

        # Альтернатива: неравномерное (хуже по Коши-Шварца):
        u_nonuniform = [U_total * 0.5, U_total * 0.3,
                         U_total * 0.15, U_total * 0.05]
        # ЛЗП при равномерном:
        lci_uniform = math.sqrt(sum(u_uniform**2 for _ in range(4)))
        # ЛЗП при неравномерном (взвешенный):
        lci_nonuniform = math.sqrt(sum(u**2 for u in u_nonuniform))

        return {
            'принцип': 'Управление с ограничениями',
            'U_total': U_total,
            'u*_равном': u_uniform,
            'u*=π/4': is_pi4,
            'ЛЗП_равном': lci_uniform,
            'ЛЗП_неравном': lci_nonuniform,
            'вывод': 'Равномерное распределение ресурса = π/4-баланс ОПТИМАЛЬНО!',
            'ЕТД': f'U_total=π; u*=π/4 для каждой сферы (оптимум Коши–Шварца)',
        }

oc = OptimalControlETD()

pont = oc.pontryagin_etd()
print(f"Понтрягин: u*(начало)={pont['u*_начало']:.4f}")

bell = oc.bellman_equation_etd()
print(f"Беллман: K={bell['K_gain']:.3f}, P={bell['P_Риккати']:.3f}")

lqr = oc.lqr_etd()
print(f"LQR: k={lqr['k_opt']:.3f}, τ={lqr['τ_сходим']:.3f}, x_final={lqr['x_final']:.4f}")

constr = oc.constrained_control_etd()
print(f"Ограничения: u*={constr['u*_равном']:.4f} = π/4: {constr['u*=π/4']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 принципа оптимального управления** → **4 закона оптимальной тренировки ЕТД**:

1. **Понтрягин** → u* = p/(2r): оптимальная интенсивность определяется «ценой» состояния
2. **Беллман** → K=2.9: оптимальный регулятор u* = −2.9·(x−π/4); Риккати даёт P
3. **LQR** → τ = 1/(α+k_opt) ≈ 0.3: время сходимости к π/4; единственное аналитическое решение
4. **Ограничения** → u*ᵢ = U_total/4 = **π/4**: при суммарном ресурсе π оптимальная стратегия = π/4 на каждую сферу!

> **Оптимальное управление ЕТД**: Если суммарный ресурс тренировки фиксирован как π (Нётеровский инвариант), то оптимальная стратегия по Коши–Шварца = **равномерно π/4 на каждую сферу**. Это доказывает π/4-баланс как оптимум из 4-х независимых принципов.

---
*ТОМ 183 / СЕРИЯ VII / ЕТД 2026*
