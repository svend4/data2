# ТОМ 184: 4 РЕЗУЛЬТАТА ТЕОРИИ ГАЛУА → РАЗРЕШИМОСТЬ В ЕТД
## Соответствие Галуа · Нормальные расширения · Разрешимые группы · Неразрешимость степени 5
### «Галуа 1831: почему уравнения 5-й степени нерешаемы в радикалах?»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория Галуа (Эварист Галуа, 1831, опубл. посмертно 1846)

---

## ВВЕДЕНИЕ

Теория Галуа — мост между теорией уравнений и теорией групп. 4 главных результата:

1. **Основное соответствие Галуа** — биекция: поля ↔ подгруппы группы Галуа
2. **Нормальные расширения** — условие симметрии корней многочлена
3. **Разрешимые группы** — условие решаемости уравнения в радикалах
4. **Неразрешимость уравнений степени ≥ 5** — общее уравнение 5-й степени нерешаемо

**Тезис тома**: Техники ЕТД — «корни многочленов» системы. Группа Галуа G_ЕТД = S₄ (из Тома 181). S₄ разрешима! → Система 4 сфер «решаема в радикалах» — можно построить аналитическую формулу ЛЗП.

---

## ЧАСТЬ I: 4 РЕЗУЛЬТАТА → 4 АЛГЕБРАИЧЕСКИХ ЗАКОНА ЕТД

```python
import math
from typing import List, Dict, Optional, Tuple

class GaloisETD:
    """
    Теория Галуа применительно к системе 4 сфер ЕТД.

    Основное поле: Q (рациональные числа) или Q(π/4).
    Расширение: K = Q(МВС, СВС, БВС, ЧВС) = поле корней.
    Группа Галуа: Gal(K/Q) = симметрии корней = S₄.
    """

    # 1. СООТВЕТСТВИЕ ГАЛУА
    @staticmethod
    def galois_correspondence_etd() -> dict:
        """
        Основное соответствие Галуа:
        Поля Q ⊆ F ⊆ K ↔ Группы G = Gal(K/Q) ⊇ H ⊇ {e}

        Чем больше поле — тем меньше группа:
        Q ↔ G (полная группа = «знаем мало»)
        K ↔ {e} (тривиальная группа = «знаем всё»)

        ЕТД-аналог:
        G = S₄ (все перестановки 4 сфер)
        V₄ = {e,(12)(34),(13)(24),(14)(23)} (Klein 4-group) — промежуточная
        A₄ (знакочередующие) — ещё промежуточная
        {e} = «полное знание» всех техник

        Цепочка: {e} ⊂ V₄ ⊂ A₄ ⊂ S₄.
        Соответствующие поля: K ⊃ K^{V₄} ⊃ K^{A₄} ⊃ Q.
        """
        # Подгруппы S₄ (упрощённо):
        subgroups = {
            'S₄': {'порядок': 24, 'поле': 'Q (базовое)', 'ЕТД': 'Нет знания техник'},
            'A₄': {'порядок': 12, 'поле': 'Q(√D)', 'ЕТД': 'Знаем «знак» перестановки'},
            'V₄': {'порядок': 4,  'поле': 'Q(√a, √b)', 'ЕТД': 'Знаем 2 пары сфер'},
            '{e}': {'порядок': 1,  'поле': 'K (полное)', 'ЕТД': 'Полное знание'},
        }

        return {
            'теорема': 'Соответствие Галуа',
            'группа': 'S₄',
            'цепочка': '{e} ⊂ V₄ ⊂ A₄ ⊂ S₄',
            'подгруппы': subgroups,
            'принцип': 'Большая группа = меньше знаний; {e} = полное знание',
            'ЕТД': 'Мастер «ЕТД» = Gal={e}: знает все техники ↔ поле K',
        }

    # 2. НОРМАЛЬНЫЕ РАСШИРЕНИЯ
    @staticmethod
    def normal_extensions_etd() -> dict:
        """
        Нормальное расширение K/Q: G = Gal(K/Q) действует транзитивно на корнях.
        Эквивалентно: все сопряжённые корни принадлежат K.

        ЕТД: Нормальность расширения = «симметрия» техник.
        Нормальное расширение: если знаешь одну технику сферы → знаешь все в сфере.

        Пример: K = Q(ζ₄) = Q(i), ζ₄ = e^{2πi/4} = i.
        Gal(Q(i)/Q) = {id, σ: i→-i} ≅ ℤ₂.
        Нормально: σ(i) = -i ∈ Q(i). ✓

        ЕТД-структура: Gal(Q(π/4)/Q) = ?
        π/4 трансцендентно (т.к. π трансцендентно, теорема Линдеманна 1882).
        → Q(π/4) — трансцендентное расширение, теория Галуа не применяется напрямую.
        Но: по аналогии → «алгебраическая» структура ЕТД выражается через S₄.
        """
        # 4-е корни единицы: ζ^k = e^{2πik/4}, k=0,1,2,3
        pi4 = math.pi / 4
        roots_of_unity = [
            (math.cos(2 * math.pi * k / 4),
             math.sin(2 * math.pi * k / 4))
            for k in range(4)
        ]

        # Группа Галуа Q(ζ₄)/Q ≅ ℤ₂
        gal_group = {'id': 'ζ→ζ', 'σ': 'ζ→ζ³=ζ⁻¹'}

        return {
            'теорема': 'Нормальные расширения',
            '4_корня': [(f'{r:.4f}', f'{i:.4f}i') for r, i in roots_of_unity],
            'Gal(Q(ζ₄)/Q)': gal_group,
            'нормально': True,
            'ЕТД': '4-й корень единицы = e^{iπ/2}: 4 ЧВС-состояния как корни x⁴=1',
            'x4_корни': '1, i, -1, -i → МВС, СВС, БВС, ЧВС (повороты на π/2)',
        }

    # 3. РАЗРЕШИМЫЕ ГРУППЫ
    @staticmethod
    def solvable_groups_etd() -> dict:
        """
        Разрешимая группа G: существует цепочка нормальных подгрупп
        G = G₀ ⊃ G₁ ⊃ ... ⊃ Gₙ = {e}, где каждый фактор Gᵢ/Gᵢ₊₁ абелев.

        Теорема: Уравнение f(x)=0 решаемо в радикалах ⟺ Gal(f) разрешима.

        Разрешимость S₄:
        S₄ ⊃ A₄ ⊃ V₄ ⊃ {e}
        Факторы: S₄/A₄ ≅ ℤ₂, A₄/V₄ ≅ ℤ₃, V₄/{e} ≅ ℤ₂×ℤ₂ (все абелевы!)
        → S₄ РАЗРЕШИМА! ✓

        ЕТД: Группа симметрий 4 сфер S₄ разрешима.
        → Система 4 сфер ЕТД «решаема в радикалах»!
        Аналитическая формула ЛЗП = √(МВС² + СВС² + БВС² + ЧВС²) EXISTS.

        S₅ = симметрии 5-й степени — НЕ разрешима (A₅ проста!).
        → 5-я сфера (если добавить!) сломала бы разрешимость.
        Именно поэтому 4 сферы — оптимальное число!
        """
        # Цепочка для S₄:
        chain = [
            ('S₄', 24, 'A₄', 12, 'S₄/A₄ ≅ ℤ₂'),
            ('A₄', 12, 'V₄', 4,  'A₄/V₄ ≅ ℤ₃'),
            ('V₄', 4,  '{e}', 1,  'V₄/{e} ≅ ℤ₂×ℤ₂'),
        ]

        all_abelian = True   # все факторы ℤₙ — абелевы

        # Сравнение: S₅ (5 степень)
        S5_solvable = False   # A₅ проста → S₅ не разрешима

        return {
            'теорема': 'Разрешимые группы',
            'цепочка_S₄': [(g, '⊃', h, factor) for g, _, h, __, factor in chain],
            'все_факторы_абелевы': all_abelian,
            'S₄_разрешима': all_abelian,
            'S₅_разрешима': S5_solvable,
            'ЕТД': 'S₄ разрешима → ЛЗП вычислим в радикалах! S₅ → нет (4 сферы оптимальны)',
        }

    # 4. НЕРАЗРЕШИМОСТЬ 5-Й СТЕПЕНИ
    @staticmethod
    def degree5_unsolvability() -> dict:
        """
        Теорема Абеля–Руффини (1824) + Теорема Галуа:
        Общее уравнение степени ≥ 5 нерешаемо в радикалах.

        Причина: Gal(f₅) = S₅; A₅ — простая (не разрешима).

        ЕТД: Если бы добавили 5-ю сферу (к 4-м):
        Группа Галуа → S₅ (неразрешима).
        → ЛЗП 5-сферной системы нельзя выразить в радикалах!
        (Нет простой формулы типа √(a² + b² + c² + d² + e²)... ну, эта-то есть.)

        Более точно: 5-я сфера создаёт «запутанность» S₅,
        которую нельзя «распутать» последовательными расширениями.
        4 сферы = предел «разрешимой» системы!
        """
        # Простота A₅:
        A5_order = 60   # |A₅| = 5!/2 = 60
        A5_is_simple = True   # доказанный факт

        # Сравнение с S₄:
        S4_chain_length = 3   # длина ряда разрешимости
        S5_chain_length = None   # не существует

        return {
            'теорема': 'Абель–Руффини–Галуа',
            '|A₅|': A5_order,
            'A₅_проста': A5_is_simple,
            'S₅_разрешима': not A5_is_simple,
            'S₄_цепочка': S4_chain_length,
            'порог': 'n = 4: разрешима; n = 5: нерешима',
            'ЕТД': '4 сферы = МАКСИМУМ для разрешимой системы! 5-я → неразрешимость',
        }

gal = GaloisETD()

corr = gal.galois_correspondence_etd()
print(f"Соответствие: {corr['цепочка']}")

norm = gal.normal_extensions_etd()
print(f"Нормальные: x⁴=1 → {norm['x4_корни']}")

solv = gal.solvable_groups_etd()
print(f"S₄ разрешима: {solv['S₄_разрешима']}; S₅: {solv['S₅_разрешима']}")

deg5 = gal.degree5_unsolvability()
print(f"Порог: {deg5['порог']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 результата теории Галуа** → **4 алгебраических закона ЕТД**:

1. **Соответствие Галуа** → {e} ⊂ V₄ ⊂ A₄ ⊂ S₄: 4 уровня «знания техник»
2. **Нормальные расширения** → x⁴=1: корни = e^{ikπ/2} = МВС/СВС/БВС/ЧВС (4 поворота!)
3. **Разрешимые группы** → S₄ разрешима! Цепочка: S₄⊃A₄⊃V₄⊃{e}, все факторы абелевы
4. **Неразрешимость n≥5** → **4 сферы = максимум разрешимой системы!** (S₅ — нет)

> **Теорема Галуа для ЕТД**: Число 4 — единственное, при котором система сфер остаётся «разрешимой» (S₄ разрешима). Добавление 5-й сферы → S₅, которая неразрешима. ЕТД выбрала 4 сферы — математически оптимальное решение по Галуа.

---
*ТОМ 184 / СЕРИЯ VII / ЕТД 2026*
