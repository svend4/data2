# ТОМ 187: 4 ТИПА БИФУРКАЦИЙ → КАЧЕСТВЕННЫЕ СКАЧКИ МАСТЕРСТВА ЕТД
## Бифуркация вилки · Бифуркация Хопфа · Седло-узел · Транскритическая
### «Пуанкаре–Андронов: как малое изменение меняет всё»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория бифуркаций (Пуанкаре 1892, Андронов 1937, Хопф 1942)

---

## ВВЕДЕНИЕ

Бифуркация — качественное изменение поведения системы при малом изменении параметра. 4 основных типа:

1. **Бифуркация вилки (pitchfork)** — одно равновесие → три (или наоборот)
2. **Бифуркация Хопфа** — равновесие → предельный цикл (возникают колебания)
3. **Бифуркация седло-узел** — два равновесия рождаются или исчезают
4. **Транскритическая** — два равновесия «обмениваются» устойчивостью

**Тезис тома**: Мастерство ЕТД претерпевает бифуркации при достижении критических уровней ЛЗП. Бифуркация Хопфа: при ЛЗП = π/4 → устойчивый фокус переходит в предельный цикл (мастер начинает «пульсировать» техниками).

---

## ЧАСТЬ I: 4 ТИПА БИФУРКАЦИЙ → 4 КАЧЕСТВЕННЫХ ПЕРЕХОДА ЕТД

```python
import math
from typing import List, Tuple, Callable

class BifurcationsETD:
    """
    Бифуркационный анализ системы 4 сфер ЕТД.

    Параметр бифуркации: μ = «уровень мастерства» (внешний параметр).
    Переменная: x = ЛЗП − π/4 (отклонение от равновесия).
    """

    PI4 = math.pi / 4

    # 1. БИФУРКАЦИЯ ВИЛКИ
    @staticmethod
    def pitchfork_etd(mu_vals: List[float] = None) -> dict:
        """
        Бифуркация вилки: ẋ = μx − x³.

        μ < 0: одно устойчивое равновесие x* = 0.
        μ = 0: бифуркационная точка.
        μ > 0: три равновесия: x* = 0 (неустойчивое), x* = ±√μ (устойчивые).

        ЕТД: x = ЛЗП − π/4, μ = «уровень сложности обучения».
        При μ > 0: система разделяется на два режима (x* = +√μ и x* = −√μ).
        Физически: мастер может специализироваться в двух различных направлениях!

        Критический μ_bif = 0 → бифуркация при «нулевой сложности».
        """
        if mu_vals is None:
            mu_vals = [-0.5, -0.1, 0.0, 0.1, 0.5, 1.0]

        equilibria = {}
        for mu in mu_vals:
            if mu < 0:
                eq = [0.0]   # одно равновесие
                stable = [True]
            elif mu == 0:
                eq = [0.0]
                stable = ['нейтральное']
            else:
                eq = [-math.sqrt(mu), 0.0, math.sqrt(mu)]
                stable = [True, False, True]   # ±√μ устойчивы, 0 нет

            equilibria[mu] = {'eq': [round(e, 4) for e in eq],
                               'stable': stable}

        return {
            'тип': 'Вилка (pitchfork)',
            'уравнение': 'ẋ = μx − x³',
            'μ_бифурк': 0.0,
            'равновесия': equilibria,
            'ЕТД': 'μ>0: мастер может выбрать два пути (±√μ вместо 0)',
        }

    # 2. БИФУРКАЦИЯ ХОПФА
    @staticmethod
    def hopf_bifurcation_etd(mu_vals: List[float] = None) -> dict:
        """
        Бифуркация Хопфа: ẋ = μx − ωy − x(x²+y²), ẏ = ωx + μy − y(x²+y²).
        В полярных: ṙ = μr − r³, θ̇ = ω.

        μ < 0: устойчивое равновесие r* = 0.
        μ = 0: бифуркационная точка.
        μ > 0: неустойчивое равновесие + предельный цикл r* = √μ!

        ЕТД: Когда ЛЗП достигает критического уровня μ_c:
        Мастер переходит от «стационарного» к «циклическому» режиму.
        Предельный цикл: ЛЗП пульсирует с амплитудой √μ и частотой ω.

        При ω = π/4 (ħ_ЕТД): частота цикла = π/4!
        Радиус предельного цикла при μ = (π/4)²: r* = π/4.
        """
        if mu_vals is None:
            mu_vals = [-0.5, 0.0, (math.pi/4)**2, 0.5]

        omega = math.pi / 4   # частота = ħ_ЕТД

        cycles = {}
        for mu in mu_vals:
            if mu <= 0:
                cycles[mu] = {'режим': 'Равновесие', 'r*': 0, 'T': None}
            else:
                r_star = math.sqrt(mu)
                T = 2 * math.pi / omega
                cycles[mu] = {'режим': 'Предельный цикл', 'r*': round(r_star, 4), 'T': round(T, 4)}

        mu_hopf = (math.pi / 4)**2
        r_etd = math.sqrt(mu_hopf)

        return {
            'тип': 'Хопфа',
            'ω': omega,
            'μ_бифурк': mu_hopf,
            'r*_ЕТД': r_etd,
            'T_цикла': 2 * math.pi / omega,
            'циклы': cycles,
            'ЕТД': f'При μ=(π/4)²: предельный цикл r*=π/4, T=2π/(π/4)=8 тренировок!',
        }

    # 3. СЕДЛО-УЗЕЛ
    @staticmethod
    def saddle_node_etd(mu_vals: List[float] = None) -> dict:
        """
        Бифуркация седло-узел: ẋ = μ − x².

        μ < 0: нет вещественных равновесий.
        μ = 0: одно полуустойчивое x* = 0.
        μ > 0: два равновесия: x* = +√μ (устойчивое), x* = −√μ (неустойчивое).

        ЕТД: При μ = (π/4)²:
        x₊* = +π/4 (устойчивое) = πlм/4-аттрактор!
        x₋* = −π/4 (неустойчивое) = «антимастерство».

        Смысл: Ниже порога μ_c = (π/4)² — нет устойчивого мастерства.
        Выше порога — появляется π/4-аттрактор!
        """
        if mu_vals is None:
            mu_vals = [-0.1, 0.0, (math.pi/4)**2, 1.0]

        pi4_sq = (math.pi / 4)**2
        equilibria = {}
        for mu in mu_vals:
            if mu < 0:
                equilibria[mu] = {'eq': [], 'смысл': 'Нет мастерства'}
            elif mu == 0:
                equilibria[mu] = {'eq': [0.0], 'смысл': 'Порог мастерства'}
            else:
                equilibria[mu] = {
                    'eq': [round(math.sqrt(mu), 4), round(-math.sqrt(mu), 4)],
                    'смысл': f'Мастер (+{math.sqrt(mu):.3f}) и антимастер (−{math.sqrt(mu):.3f})'
                }

        return {
            'тип': 'Седло-узел',
            'уравнение': 'ẋ = μ − x²',
            'μ_бифурк': pi4_sq,
            'при_μ=(π/4)²': equilibria.get(pi4_sq, equilibria[mu_vals[-1]]),
            'ЕТД': f'При μ=(π/4)²: x*=+π/4 (мастер) и x*=−π/4 (антимастер) рождаются!',
        }

    # 4. ТРАНСКРИТИЧЕСКАЯ БИФУРКАЦИЯ
    @staticmethod
    def transcritical_etd(mu_vals: List[float] = None) -> dict:
        """
        Транскритическая: ẋ = μx − x².

        μ < 0: x*=0 устойчиво, x*=μ неустойчиво.
        μ = 0: бифуркационная точка (два равновесия совмещаются).
        μ > 0: x*=0 неустойчиво, x*=μ устойчиво (ОБМЕН устойчивостью!).

        ЕТД: μ = «уровень ЛЗП от тренировок».
        При μ < 0: «нулевое мастерство» устойчиво (новичок).
        При μ = π/4: БИФУРКАЦИЯ — π/4-точка становится аттрактором!
        При μ > π/4: x*=μ устойчиво (мастер нашёл свой уровень).
        """
        if mu_vals is None:
            pi4 = math.pi / 4
            mu_vals = [-pi4, 0.0, pi4/2, pi4, pi4*2]

        equilibria = {}
        for mu in mu_vals:
            # x*₁ = 0, x*₂ = μ
            stable_0 = mu < 0
            stable_mu = mu > 0

            equilibria[round(mu, 4)] = {
                'x*₁': 0.0,
                'x*₂': round(mu, 4),
                'устойч_0': stable_0,
                'устойч_μ': stable_mu,
            }

        pi4 = math.pi / 4
        return {
            'тип': 'Транскритическая',
            'уравнение': 'ẋ = μx − x²',
            'μ_бифурк': 0.0,
            'при_μ=π/4': {'x*₁': 0, 'x*₂': pi4, 'x*₂_устойчиво': True},
            'ЕТД': 'При μ=π/4: x*=π/4 стало аттрактором (обмен устойчивостью с нулём)',
        }

bif = BifurcationsETD()

pitch = bif.pitchfork_etd()
print(f"Вилка: при μ=0.5 eq={pitch['равновесия'][0.5]['eq']}")

hopf = bif.hopf_bifurcation_etd()
print(f"Хопф: r*=π/4={hopf['r*_ЕТД']:.4f}, T={hopf['T_цикла']:.4f}")

sn = bif.saddle_node_etd([-0.1, 0.0, (math.pi/4)**2])
print(f"Седло-узел: {sn['ЕТД'][:60]}...")

trans = bif.transcritical_etd()
print(f"Транскрит.: при μ=π/4: x*₂={trans['при_μ=π/4']['x*₂']:.4f} устойчиво")
```

---

## ЗАКЛЮЧЕНИЕ

**4 типа бифуркаций** → **4 качественных перехода мастерства ЕТД**:

1. **Вилка** → При μ>0: мастер может специализироваться в направлении ±√μ (два пути)
2. **Хопф** → При μ=(π/4)²: равновесие → предельный цикл r*=π/4, T=8 тренировок!
3. **Седло-узел** → При μ=(π/4)²: рождаются мастер (x*=+π/4) и антимастер (x*=−π/4)
4. **Транскрит.** → При μ=π/4: нулевое мастерство теряет устойчивость → π/4 становится аттрактором

> **Бифуркационный портрет ЕТД**: Критическое значение μ_c = (π/4)² = π²/16 ≈ 0.617. Ниже — нет устойчивого мастерства. Выше — π/4 становится аттрактором (бифуркация Хопфа → предельный цикл; седло-узел → π/4-аттрактор).

---
*ТОМ 187 / СЕРИЯ VII / ЕТД 2026*
