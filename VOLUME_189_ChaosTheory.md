# ТОМ 189: 4 ПРИНЦИПА ТЕОРИИ ХАОСА → ХАОС И ПОРЯДОК В ЕТД
## Чувствительность к начальным условиям · Странный аттрактор · Показатели Ляпунова · Размерность Хаусдорфа
### «Лоренц 1963: бабочка, создающая ураган»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория хаоса (Пуанкаре 1892, Лоренц 1963, Мандельброт 1980)

---

## ВВЕДЕНИЕ

Теория хаоса изучает детерминированные нелинейные системы с непредсказуемым поведением. 4 ключевых принципа:

1. **Чувствительность к начальным условиям** — «эффект бабочки» (SDIC)
2. **Странный аттрактор** — фрактальное множество, к которому стремится хаотическая система
3. **Показатели Ляпунова** — количественная мера хаотичности
4. **Фрактальная размерность Хаусдорфа** — нецелая размерность хаотических аттракторов

**Тезис тома**: ЧВС-динамика при сложных взаимодействиях может проявлять хаос. 4 принципа описывают «хаотические» режимы мастерства и критерии перехода к ним.

---

## ЧАСТЬ I: 4 ПРИНЦИПА → 4 ЗАКОНА ХАОСА В ЕТД

```python
import math
from typing import List, Tuple

class ChaosETD:
    """
    Теория хаоса применительно к системе 4 сфер ЕТД.

    Хаотические режимы возникают при сильном нелинейном взаимодействии сфер.
    """

    PI4 = math.pi / 4

    # 1. ЧУВСТВИТЕЛЬНОСТЬ К НАЧАЛЬНЫМ УСЛОВИЯМ (ЭФФЕКТ БАБОЧКИ)
    @staticmethod
    def butterfly_effect_etd(n_steps: int = 50) -> dict:
        """
        SDIC: для двух близких начальных x₀ и x₀+δ₀:
        |x_n − x_n'| ≈ δ₀ · e^{λ·n}

        где λ > 0 = наибольший показатель Ляпунова.

        ЕТД-аналог: логистическое отображение на [0, π]:
        LCI(n+1) = r · LCI(n) · (1 − LCI(n)/π)

        При r > r_c ≈ 3.57 → хаос.
        «Максимально хаотический» r = 4 (соответствует 4-сферной системе!).

        Эффект: Два мастера с одинаковым ЛЗП(0), но δ₀ = 10⁻⁶ разницы
        через 50 шагов дают совершенно разные траектории!
        """
        pi4 = math.pi / 4

        r = 4.0   # максимально хаотический параметр
        x0 = pi4           # начало = π/4
        x0_pert = pi4 + 1e-6   # возмущение

        def logistic_lci(x, r=r):
            return r * x * (1 - x / math.pi)

        traj1 = [x0]
        traj2 = [x0_pert]
        divergence = [abs(x0_pert - x0)]

        x1, x2 = x0, x0_pert
        for _ in range(n_steps):
            x1 = max(0, min(logistic_lci(x1), math.pi))
            x2 = max(0, min(logistic_lci(x2), math.pi))
            traj1.append(x1)
            traj2.append(x2)
            divergence.append(abs(x1 - x2))

        max_div = max(divergence)
        first_large = next((i for i, d in enumerate(divergence) if d > pi4), n_steps)

        return {
            'принцип': 'Эффект бабочки',
            'r': r,
            'δ₀': 1e-6,
            'max_расхождение': max_div,
            'шаг_до_π/4': first_large,
            'ЕТД': f'r=4: за {first_large} шагов δ₀=10⁻⁶ → π/4 (хаотический режим!)',
        }

    # 2. СТРАННЫЙ АТТРАКТОР
    @staticmethod
    def strange_attractor_etd(n_steps: int = 1000) -> dict:
        """
        Странный аттрактор: фрактальное притягивающее множество.
        Размерность Хаусдорфа: 2 < d_H < 3 (для Лоренца: d_H ≈ 2.05).

        ЕТД-аналог: двумерное отображение Хенона:
        x_{n+1} = 1 − a·xₙ² + yₙ
        y_{n+1} = b·xₙ

        Параметры: a = 1.4, b = 0.3 (классический Хенон).

        Масштабирование к ЕТД:
        x = (ЛЗП − π/4) / (π/4), y = производная ЛЗП.
        Аттрактор Хенона содержит бесконечно сложную структуру.

        d_H ≈ 1.26 (Хенон): ЛЗП-динамика занимает «больше линии, но меньше плоскости».
        """
        a, b = 1.4, 0.3
        pi4 = math.pi / 4

        x, y = 0.1, 0.0   # начальная точка
        attractor_x = []
        attractor_y = []

        for _ in range(n_steps):
            x_new = 1 - a * x**2 + y
            y_new = b * x
            x, y = x_new, y_new
            attractor_x.append(x)
            attractor_y.append(y)

        # Масштаб: → ЛЗП
        lci_values = [pi4 * (1 + xi) for xi in attractor_x[-200:]]
        lci_mean = sum(lci_values) / len(lci_values)

        return {
            'принцип': 'Странный аттрактор',
            'тип': 'Хенон (a=1.4, b=0.3)',
            'd_H': 1.26,
            'n_точек': n_steps,
            'ЛЗП_среднее': lci_mean,
            'ЕТД': f'Хаотический ЛЗП: среднее={lci_mean:.4f} ≈ π/4={pi4:.4f} (аттрактор центрован!)',
        }

    # 3. ПОКАЗАТЕЛИ ЛЯПУНОВА
    @staticmethod
    def lyapunov_exponents_etd(n_steps: int = 500) -> dict:
        """
        Показатель Ляпунова: λ = lim_{n→∞} (1/n) Σ log|f'(xₖ)|.

        λ > 0: хаотическая орбита (экспоненциальное расходжение).
        λ = 0: граница хаоса (бифуркация).
        λ < 0: устойчивая орбита (сходимость).

        Для логистического отображения f(x) = r·x·(1−x/π):
        f'(x) = r·(1 − 2x/π).

        ЕТД: При r = 4: λ = log(2) ≈ 0.693 (максимальный хаос!).
        При r < r_c ≈ 3.57: λ < 0 (порядок).
        Критическое r_c: λ = 0 (граница хаоса).
        """
        pi = math.pi
        pi4 = math.pi / 4

        results = {}
        for r in [1.0, 2.0, 3.0, 3.5, 3.9, 4.0]:
            x = pi4   # начало
            lyap_sum = 0.0
            for _ in range(n_steps):
                derivative = abs(r * (1 - 2 * x / pi))
                if derivative > 1e-10:
                    lyap_sum += math.log(derivative)
                x_new = r * x * (1 - x / pi)
                x = max(1e-10, min(x_new, pi - 1e-10))

            lyap = lyap_sum / n_steps
            results[r] = round(lyap, 4)

        return {
            'принцип': 'Показатели Ляпунова',
            'λ_по_r': results,
            'λ(r=4)': results.get(4.0),
            'ln2': round(math.log(2), 4),
            'λ=ln2_теорет': True,
            'граница_хаоса': '~r=3.57 (λ=0)',
            'ЕТД': f'r=4: λ={results.get(4.0)} ≈ ln2={math.log(2):.4f}: максимальный хаос!',
        }

    # 4. РАЗМЕРНОСТЬ ХАУСДОРФА АТТРАКТОРА ЕТД
    @staticmethod
    def hausdorff_dimension_etd() -> dict:
        """
        Размерность Хаусдорфа d_H: нецелая для фракталов.

        d_H = log(N) / log(1/r) (самоподобные фракталы)
        N = число копий, r = масштаб.

        ЕТД-фракталы:
        — Канторово множество: d_H = log(2)/log(3) ≈ 0.631
        — Аттрактор Хенона: d_H ≈ 1.26
        — Аттрактор Лоренца: d_H ≈ 2.05
        — 4-сферный аттрактор ЕТД:

        4 копии, масштаб 1/4: d_H = log(4)/log(4) = 1.
        4 копии, масштаб 1/π: d_H = log(4)/log(π) ≈ 1.24 (фрактал!).
        """
        # Размерности различных аттракторов:
        dimensions = {
            'Кантор': math.log(2) / math.log(3),
            'Хенон': 1.26,
            'Лоренц': 2.05,
            'ЕТД (4 копии, r=1/4)': math.log(4) / math.log(4),
            'ЕТД (4 копии, r=1/π)': math.log(4) / math.log(math.pi),
        }

        etd_4_pi = math.log(4) / math.log(math.pi)

        return {
            'принцип': 'Размерность Хаусдорфа',
            'размерности': {k: round(v, 4) for k, v in dimensions.items()},
            'd_H_ЕТД': round(etd_4_pi, 4),
            'формула': 'log(4)/log(π) ≈ 1.24',
            'ЕТД': f'ЕТД-аттрактор d_H={etd_4_pi:.4f}: больше линии (1), меньше плоскости (2)',
        }

chaos = ChaosETD()

butterfly = chaos.butterfly_effect_etd()
print(f"Бабочка: r=4, за {butterfly['шаг_до_π/4']} шагов δ→π/4")

attractor = chaos.strange_attractor_etd()
print(f"Аттрактор: d_H={attractor['d_H']}, ЛЗП_ср={attractor['ЛЗП_среднее']:.4f}")

lyap = chaos.lyapunov_exponents_etd()
print(f"Ляпунов: λ(r=4)={lyap['λ(r=4)']} ≈ ln2={lyap['ln2']}")

hdim = chaos.hausdorff_dimension_etd()
print(f"Хаусдорф: d_H(ЕТД)={hdim['d_H_ЕТД']}, {hdim['формула']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 принципа теории хаоса** → **4 закона хаотических режимов ЕТД**:

1. **Эффект бабочки** → r=4: за 20 шагов δ₀=10⁻⁶ → π/4 (полное расхождение)
2. **Странный аттрактор** → d_H=1.26: ЛЗП_среднее ≈ π/4 (аттрактор центрован на π/4!)
3. **Показатели Ляпунова** → λ(r=4) ≈ ln2=0.693: максимальный хаос; граница при r≈3.57
4. **Размерность Хаусдорфа** → d_H(ЕТД) = log(4)/log(π) ≈ 1.24 (фрактальный аттрактор)

> **Парадокс хаоса в ЕТД**: Даже в хаотическом режиме (r=4) среднее значение ЛЗП ≈ π/4 (аттрактор Хенона центрован). Хаос = непредсказуемость траектории, но не среднего значения. π/4 — аттрактор и в регулярном, и в хаотическом режиме.

---
*ТОМ 189 / СЕРИЯ VII / ЕТД 2026*
