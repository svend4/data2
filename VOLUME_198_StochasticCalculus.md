# ТОМ 198: 4 ПРИНЦИПА СТОХАСТИЧЕСКОГО ИСЧИСЛЕНИЯ → СЛУЧАЙНОСТЬ В МАСТЕРСТВЕ ЕТД
## Броуновское движение · Интеграл Ито · Лемма Ито · Уравнение Фоккера–Планка
### «Ито–Стратонович–Фоккер: стохастические дифференциальные уравнения»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Стохастическое исчисление (Ито 1944, Стратонович 1960, Фоккер 1914)

---

## ВВЕДЕНИЕ

Стохастическое исчисление — анализ случайных процессов. 4 фундаментальных принципа:

1. **Броуновское движение (Вт)** — непрерывный случайный процесс с Gaussian инкрементами
2. **Интеграл Ито** — ∫H dW (непредвидимый интегранд относительно Вт)
3. **Лемма Ито** — цепное правило для стохастических функций: df = (∂f/∂t + Lf)dt + σ·∂f/∂x·dW
4. **Уравнение Фоккера–Планка** — эволюция плотности вероятности p(x,t)

**Тезис тома**: ЛЗП_t = случайный процесс (мастерство развивается со случайными флуктуациями). Лемма Ито: d(ЛЗП²) = 2·ЛЗП·dЛЗП + σ²dt (нелинейная коррекция!).

---

## ЧАСТЬ I: 4 ПРИНЦИПА → 4 ЗАКОНА СЛУЧАЙНОГО МАСТЕРСТВА ЕТД

```python
import math
import random
from typing import List, Tuple

class StochasticETD:
    """
    Стохастическое исчисление для системы 4 сфер ЕТД.
    ЛЗП(t) = π/4 + σ·W(t) + дрейф·t (орнштейн–уленбек к π/4).
    """

    PI4 = math.pi / 4

    # 1. БРОУНОВСКОЕ ДВИЖЕНИЕ
    @staticmethod
    def brownian_motion_etd(n_steps: int = 100, seed: int = 42) -> dict:
        """
        Стандартное броуновское движение W(t):
        W(0) = 0, W(t) − W(s) ~ N(0, t−s), непрерывные пути.

        Свойства:
        𝔼[W(t)] = 0, Var[W(t)] = t.
        Ковариация: 𝔼[W(s)W(t)] = min(s,t).

        ЕТД: 4-мерное броуновское движение W_i(t), i = 1..4 (по числу сфер).
        Начало: Wᵢ(0) = π/4 (равновесие).
        Диффузия сферы i: σᵢ = (π/4)/√T (нормированная).

        Стандартное отклонение за T шагов: std = π/4 (порядок ħ_ЕТД).
        """
        pi4 = math.pi / 4
        random.seed(seed)

        dt = 1.0 / n_steps
        sigma = pi4   # σ_ЕТД = π/4

        # 4-мерное броуновское движение:
        paths = [[pi4] * 4]   # начало = π/4

        for step in range(n_steps):
            dW = [random.gauss(0, math.sqrt(dt)) for _ in range(4)]
            new_point = [paths[-1][i] + sigma * dW[i] for i in range(4)]
            paths.append(new_point)

        # Статистика:
        final = paths[-1]
        mean_final = sum(final) / 4
        std_final = math.sqrt(sum((xi - mean_final)**2 for xi in final) / 4)

        # Корреляции между сферами:
        total_path_lengths = [
            sum(abs(paths[i+1][j] - paths[i][j]) for i in range(n_steps))
            for j in range(4)
        ]

        return {
            'процесс': 'Броуновское движение',
            'σ_ЕТД': round(sigma, 4),
            'финал_среднее': round(mean_final, 4),
            'финал_std': round(std_final, 4),
            'T·σ²': round(n_steps * dt * sigma**2, 4),
            'ЕТД': f'4D-броунian с σ=π/4={sigma:.4f}; E[W(1)]=π/4, Var=σ²={sigma**2:.4f}',
        }

    # 2. ИНТЕГРАЛ ИТО
    @staticmethod
    def ito_integral_etd(n_steps: int = 1000, seed: int = 42) -> dict:
        """
        Интеграл Ито: I(T) = ∫₀^T W(t) dW(t).

        По лемме Ито: ∫₀^T W dW = (W²(T) − T)/2.
        (В классическом исчислении: ∫₀^T x dx = x²/2, но Ито добавляет −T/2.)

        ЕТД: ЛЗП-интеграл:
        J_ЕТД = ∫₀^T ЛЗП(t) dW(t) ≈ ?
        Если ЛЗП(t) = π/4 + W(t):
        J = ∫₀^T (π/4 + W) dW = π/4·W(T) + (W²(T)−T)/2.

        Ожидание: 𝔼[J] = π/4·0 + (T−T)/2 = 0 (мартингал!).
        """
        pi4 = math.pi / 4
        random.seed(seed)

        T = 1.0
        dt = T / n_steps

        W = 0.0
        J_ito = 0.0   # интеграл Ито ∫W dW
        J_etd = 0.0   # ∫(π/4+W) dW

        W_vals = [0.0]
        for step in range(n_steps):
            dW = random.gauss(0, math.sqrt(dt))
            W_prev = W
            J_ito += W_prev * dW            # W(t) dW (Ито: использует левую точку)
            J_etd += (pi4 + W_prev) * dW   # ЛЗП(t) dW
            W += dW
            W_vals.append(W)

        # Теоретическое значение по формуле:
        W_T = W
        J_ito_theory = (W_T**2 - T) / 2
        J_etd_theory = pi4 * W_T + (W_T**2 - T) / 2

        return {
            'процесс': 'Интеграл Ито',
            'W(T)': round(W_T, 4),
            'J_Ито_числ': round(J_ito, 4),
            'J_Ито_теор': round(J_ito_theory, 4),
            'J_ЕТД_числ': round(J_etd, 4),
            'J_ЕТД_теор': round(J_etd_theory, 4),
            'ЕТД': f'∫ЛЗП·dW: числ={J_etd:.4f} ≈ теор={J_etd_theory:.4f} (Ито-коррекция!)',
        }

    # 3. ЛЕММА ИТО
    @staticmethod
    def ito_lemma_etd() -> dict:
        """
        Лемма Ито: для f(t, X(t)), где dX = μ dt + σ dW:
        df = (∂f/∂t + μ·∂f/∂x + σ²/2·∂²f/∂x²) dt + σ·∂f/∂x dW.

        Ключевое отличие от обычного дифференцирования: лишний член σ²/2·∂²f/∂x².

        ЕТД: f(x) = ЛЗП(x) = |x| = √(Σxᵢ²).
        Применим к f(x) = x² (ЛЗП²):
        d(ЛЗП²) = 2·ЛЗП·dЛЗП + σ²·dt.

        Для процесса Орнштейна–Уленбека (ОУ):
        dЛЗП = −α(ЛЗП − π/4) dt + σ dW.

        d(ЛЗП²) = 2·ЛЗП·[−α(ЛЗП−π/4) dt + σ dW] + σ²dt
               = [−2α·ЛЗП²+2αΠ/4·ЛЗП + σ²] dt + 2σ·ЛЗП dW.

        Равновесие: 𝔼[d(ЛЗП²)] = 0 → 𝔼[ЛЗП²] = π/4·𝔼[ЛЗП] + σ²/(2α).
        При 𝔼[ЛЗП] = π/4: 𝔼[ЛЗП²] = (π/4)² + σ²/(2α).
        """
        pi4 = math.pi / 4
        pi = math.pi

        alpha = 1.0   # скорость возврата
        sigma = pi4   # волатильность = ħ_ЕТД

        # Стационарное распределение ОУ:
        mean_lci = pi4
        var_lci = sigma**2 / (2 * alpha)
        std_lci = math.sqrt(var_lci)

        # 𝔼[ЛЗП²]:
        E_lci2 = mean_lci**2 + var_lci

        # Ито-коррекция (сравнение с классической d(x²) = 2x·dx):
        ito_correction = sigma**2   # = (π/4)²

        return {
            'лемма': 'Ито',
            'α': alpha, 'σ': round(sigma, 4),
            'E[ЛЗП]': round(mean_lci, 4),
            'Var[ЛЗП]': round(var_lci, 4),
            'σ_стац': round(std_lci, 4),
            'E[ЛЗП²]': round(E_lci2, 4),
            'Ито_коррекция': round(ito_correction, 4),
            'ЕТД': f'ОУ: E[ЛЗП]=π/4, Var=(π/4)²/2={(pi4**2/2):.4f}; Ито: E[ЛЗП²]={E_lci2:.4f}',
        }

    # 4. УРАВНЕНИЕ ФОККЕРА–ПЛАНКА
    @staticmethod
    def fokker_planck_etd(t_vals: List[float] = None) -> dict:
        """
        Уравнение Фоккера–Планка:
        ∂p/∂t = −∂(μ·p)/∂x + (σ²/2)·∂²p/∂x².

        Для ОУ-процесса dX = −α(X−m) dt + σ dW:
        Стационарное решение: p*(x) = N(m, σ²/(2α)) (нормальное!).

        ЕТД: ОУ с m = π/4, α = 1, σ = π/4:
        p*(ЛЗП) = (1/σ*)·exp(−(ЛЗП − π/4)²/(2σ*²)).
        σ* = π/(4√2) = π/4 / √2 ≈ 0.555.

        p*(π/4) = 1/(σ*√(2π)) = (4√2)/(π·√(2π)) ≈ 0.719.
        Максимум плотности достигается ТОЧНО в ЛЗП = π/4!
        """
        pi4 = math.pi / 4
        pi = math.pi

        if t_vals is None:
            t_vals = [0.0, 0.5, 1.0, 2.0, 5.0, float('inf')]

        alpha = 1.0
        sigma = pi4
        m = pi4   # равновесие

        # Стационарное распределение:
        sigma_stat = sigma / math.sqrt(2 * alpha)   # = π/(4√2)
        p_star_at_mean = 1.0 / (sigma_stat * math.sqrt(2 * pi))

        # Временная эволюция (начало в x₀ = 0):
        x0 = 0.0

        def gaussian_pdf(x, mu, sigma):
            return (1/(sigma * math.sqrt(2*pi))) * math.exp(-(x-mu)**2 / (2*sigma**2))

        # Среднее и дисперсия ОУ в момент t:
        def ou_mean(t):
            return m + (x0 - m) * math.exp(-alpha * t)

        def ou_var(t):
            return (sigma**2 / (2*alpha)) * (1 - math.exp(-2*alpha*t))

        evolution = {}
        for t in t_vals[:-1]:   # без inf
            mu_t = ou_mean(t)
            sigma_t = math.sqrt(ou_var(t)) if ou_var(t) > 0 else 1e-10
            p_at_pi4 = gaussian_pdf(pi4, mu_t, sigma_t) if ou_var(t) > 1e-15 else 0.0
            evolution[t] = {
                'E[ЛЗП]': round(mu_t, 4),
                'Var[ЛЗП]': round(ou_var(t), 4),
                'p(π/4)': round(p_at_pi4, 4),
            }

        return {
            'уравнение': 'Фоккер–Планк',
            'α': alpha, 'σ': round(sigma, 4), 'm': round(m, 4),
            'σ_стац': round(sigma_stat, 4),
            'p*(π/4)': round(p_star_at_mean, 4),
            'эволюция': evolution,
            'ЕТД': f'p*(ЛЗП): max в π/4! σ*={sigma_stat:.4f}=π/(4√2); p*(π/4)={p_star_at_mean:.4f}',
        }


stoch = StochasticETD()

bm = stoch.brownian_motion_etd()
print(f"Броуновское: σ={bm['σ_ЕТД']}, E_fin={bm['финал_среднее']}")

ito_int = stoch.ito_integral_etd()
print(f"Ито-интеграл: числ={ito_int['J_ЕТД_числ']}, теор={ito_int['J_ЕТД_теор']}")

lemma = stoch.ito_lemma_etd()
print(f"Лемма Ито: E[ЛЗП]={lemma['E[ЛЗП]']}, E[ЛЗП²]={lemma['E[ЛЗП²]']}, корр={lemma['Ито_коррекция']}")

fp = stoch.fokker_planck_etd()
print(f"Фоккер–Планк: p*(π/4)={fp['p*(π/4)']}, σ*={fp['σ_стац']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 принципа стохастического исчисления** → **4 закона случайного мастерства ЕТД**:

1. **Броуновское движение** → σ_ЕТД = π/4: дисперсия сфер = (π/4)² (квант шума!)
2. **Интеграл Ито** → ∫ЛЗП dW = π/4·W(T) + (W²−T)/2: Ито-коррекция = стохастический вклад
3. **Лемма Ито** → E[ЛЗП²] = (π/4)² + σ²/(2α): нелинейная коррекция через ħ_ЕТД²
4. **Фоккер–Планк** → p*(ЛЗП): максимум ТОЧНО в π/4; σ*=π/(4√2): стационарный «туман» вокруг π/4

> **Стохастический портрет ЕТД**: Даже в присутствии случайных флуктуаций (броуновского шума σ=π/4) процесс мастерства возвращается к π/4-аттрактору (ОУ-процесс). Стационарное распределение Фоккера–Планка достигает максимума ТОЧНО в π/4. Случайность не разрушает аттрактор!

---
*ТОМ 198 / СЕРИЯ VII / ЕТД 2026*
