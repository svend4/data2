# ТОМ 204: 4 ТЕОРЕМЫ ГОМОЛОГИЧЕСКОЙ АЛГЕБРЫ → ТОЧНОСТЬ В МАСТЕРСТВЕ ЕТД
## Теорема Хорсхо · Лемма о змее · Пять-лемма · Производные функторы
### «Эйленберг–Маклейн–Карtan: гомологические методы»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Гомологическая алгебра (Эйленберг–Маклейн 1945, Картан–Эйленберг 1956)

---

## ВВЕДЕНИЕ

Гомологическая алгебра изучает точные последовательности и производные функторы. 4 ключевых результата:

1. **Лемма о змее** — связь ядер и коядер в коммутативной диаграмме
2. **Пять-лемма** — изоморфизм из четырёх соседних изоморфизмов
3. **Теорема Хорсхо** — разрешения и Tor/Ext функторы
4. **Производные функторы** — Tor и Ext как меры «неточности»

**Тезис тома**: Точность = π/4-баланс. «Точная последовательность ЕТД»: 0 → МВС → МВС⊕СВС → СВС → 0. Гомология = «дефект точности» = отклонение от π/4.

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ПРИНЦИПА ТОЧНОСТИ ЕТД

```python
import math
from typing import List, Dict, Optional

class HomologicalAlgebraETD:
    """
    Гомологическая алгебра для системы 4 сфер ЕТД.
    Цепной комплекс: C₄ → C₃ → C₂ → C₁ → C₀ → 0.
    Cₙ = ℤ^{C(4,n)} (группы из Тома 199).
    """

    PI4 = math.pi / 4

    # 1. ЛЕММА О ЗМЕЕ
    @staticmethod
    def snake_lemma_etd() -> dict:
        """
        Лемма о змее: В коммутативной диаграмме с точными строками:
        A' → A → A'' → 0
        ↓α  ↓β   ↓γ
        0 → B' → B → B''

        Существует «связывающий гомоморфизм» δ: ker(γ) → coker(α),
        и длинная точная последовательность:
        ker(α) → ker(β) → ker(γ) →^δ coker(α) → coker(β) → coker(γ).

        ЕТД: Диаграмма «передачи мастерства»:
        МВС → МВС⊕СВС → СВС → 0
         ↓α      ↓β        ↓γ
        0 → БВС → БВС⊕ЧВС → ЧВС

        ker(γ) = {x ∈ СВС : γ(x)=0} = дефект передачи СВС→ЧВС.
        δ(ker γ) = связывающий «змей» между сферами.

        При π/4-балансе: ker(β)=0 (точная!), δ=0 (нет дефекта).
        """
        pi4 = math.pi / 4

        # Моделирование: α, β, γ = умножение на λ:
        lam_alpha = pi4   # МВС-дефект
        lam_beta = pi4    # общий дефект
        lam_gamma = pi4   # СВС-дефект

        # Ядра при отображении x → λ·x:
        # ker(f_λ) = {0} если λ≠0
        ker_alpha = 0 if lam_alpha != 0 else float('inf')
        ker_beta = 0 if lam_beta != 0 else float('inf')
        ker_gamma = 0 if lam_gamma != 0 else float('inf')

        # Коядра: coker(f_λ) = ℤ/(λℤ) ≅ 0 (для λ∈ℝ ненулевого)
        coker_alpha = 0
        coker_beta = 0
        coker_gamma = 0

        # При π/4-балансе: все ядра и коядра = 0 → точная последовательность!
        is_exact = (ker_beta == 0 and coker_beta == 0)

        # Связывающий гомоморфизм δ:
        delta = 0.0   # нет дефекта при π/4-балансе

        return {
            'теорема': 'Лемма о змее',
            'ядра': {'α': ker_alpha, 'β': ker_beta, 'γ': ker_gamma},
            'коядра': {'α': coker_alpha, 'β': coker_beta, 'γ': coker_gamma},
            'δ': delta,
            'точная': is_exact,
            'ЕТД': f'При π/4-балансе: все ядра/коядра=0, δ=0: точная последовательность без дефекта!',
        }

    # 2. ПЯТЬ-ЛЕММА
    @staticmethod
    def five_lemma_etd() -> dict:
        """
        Пять-лемма: В коммутативной диаграмме с точными строками:
        A₁ → A₂ → A₃ → A₄ → A₅
        ↓f₁  ↓f₂  ↓f₃  ↓f₄  ↓f₅

        B₁ → B₂ → B₃ → B₄ → B₅

        Если f₁, f₂, f₄, f₅ — изоморфизмы, то f₃ — изоморфизм.

        ЕТД: 5 объектов = 4 сферы + ноль:
        0 → МВС → МВС⊕СВС⊕БВС⊕ЧВС → МВС⊕СВС⊕БВС → 0 [... → 0]
        Пять-лемма: если 4 крайних «морфизма» = изоморфизм,
        то центральный (полная синергия) = изоморфизм.

        Следствие: при π/4-балансе все 4 сферы взаимно-изоморфны
        (равноправны!) → центральный оператор ЕТД = изоморфизм!
        """
        pi4 = math.pi / 4

        # 5 объектов:
        objects_A = [0, pi4, 4*pi4, 3*pi4, 0]   # = [0, π/4, π, 3π/4, 0]
        objects_B = [0, pi4, 4*pi4, 3*pi4, 0]

        # Вертикальные морфизмы (изоморфизмы при балансе):
        f_morphisms = [True, True, True, True, True]   # f₁..f₅ = изоморфизмы

        # Применение пяти-леммы:
        outer_isos = f_morphisms[0] and f_morphisms[1] and f_morphisms[3] and f_morphisms[4]
        f3_is_iso = outer_isos   # следствие пяти-леммы

        return {
            'теорема': 'Пять-лемма',
            'объекты_A': objects_A,
            'объекты_B': objects_B,
            'внешние_изо': outer_isos,
            'f₃_изоморфизм': f3_is_iso,
            'ЕТД': f'Пять-лемма: 4 крайних изоморфны → центральный (π/4-баланс) = изоморфизм! Все сферы равноправны.',
        }

    # 3. Tor И Ext (ПРОИЗВОДНЫЕ ФУНКТОРЫ)
    @staticmethod
    def tor_ext_etd() -> dict:
        """
        Производные функторы измеряют «неточность»:
        Tor_n(A, B) = n-я гомология комплекса P⊗B, где P → A → 0 проективная резольвента.
        Ext^n(A, B) = n-я когомология комплекса Hom(P, B).

        Tor₀(A,B) = A⊗B, Tor₁(A,B) = Tor(A,B) (торсионный дефект).
        Ext⁰(A,B) = Hom(A,B), Ext¹(A,B) = скручивание.

        ЕТД: A = ℤ⁴ (4-сферная структура), B = ℤ:
        A⊗B = ℤ⁴ ⊗ ℤ = ℤ⁴.
        Tor₁(ℤ⁴, ℤ) = 0 (ℤ⁴ — свободная, нет торсиона!).
        Ext¹(ℤ⁴, ℤ) = 0 (тоже!).

        → 4-сферная система ЕТД СВОБОДНА (нет «скручивания»)!
        Ранг A = 4 = числу сфер.
        """
        pi4 = math.pi / 4

        # Для свободных абелевых групп:
        A_rank = 4   # ℤ⁴
        B_rank = 1   # ℤ

        # Тензорное произведение:
        A_tensor_B_rank = A_rank * B_rank   # = 4

        # Tor₁ для свободных групп:
        Tor1 = 0   # нет торсиона

        # Ext¹ для свободных групп:
        Ext1 = 0   # нет скручивания

        # Homологическое измерение:
        hom_dim = 0   # ℤ⁴ — проективный модуль над ℤ

        # Эйлерова характеристика (знакочередующаяся сумма рангов):
        # χ = rank(A) - Tor₁ + ... = 4 - 0 = 4
        euler_hom = A_tensor_B_rank - Tor1

        return {
            'теорема': 'Tor/Ext (производные функторы)',
            'A': f'ℤ⁴ (ранг {A_rank})',
            'B': f'ℤ (ранг {B_rank})',
            'A⊗B': f'ℤ⁴ (ранг {A_tensor_B_rank})',
            'Tor₁': Tor1,
            'Ext¹': Ext1,
            'hom_dim': hom_dim,
            'χ_гомол': euler_hom,
            'ЕТД': f'ℤ⁴: Tor₁=Ext¹=0 (СВОБОДНАЯ!); χ_гом={euler_hom}=числу сфер. Нет скручивания!',
        }

    # 4. ТЕОРЕМА ХОРСХО (ПРОЕКТИВНЫЕ РЕЗОЛЬВЕНТЫ)
    @staticmethod
    def horseshoe_theorem_etd() -> dict:
        """
        Теорема подковы Хорсхо (Horseshoe Lemma):
        Если 0 → A' → A → A'' → 0 точная, P' и P'' — проективные резольвенты A' и A'',
        то P' ⊕ P'' — проективная резольвента A.

        Конструкция: диаграмма «подковы»:
           P'_n ————→ P''_n
            ↑              ↑
           P'_{n-1} → P''_{n-1}

        ЕТД: A' = «МВС+СВС» (2 сферы), A'' = «БВС+ЧВС» (2 сферы).
        P' = ℤ² (свободная резольвента), P'' = ℤ².
        Резольвента ЕТД: P = P' ⊕ P'' = ℤ² ⊕ ℤ² = ℤ⁴.

        Минимальная разрешающая длина = 0 (ℤ⁴ — уже проективный!).
        → «Идеальная» система: не нужны производные коррекции.
        """
        pi4 = math.pi / 4

        # Проективные резольвенты:
        P_prime = 2     # ℤ² для МВС+СВС
        P_dprime = 2    # ℤ² для БВС+ЧВС
        P_total = P_prime + P_dprime   # = 4 = ℤ⁴

        # Длина резольвенты:
        # ℤ⁴ проективен → pd(ℤ⁴) = 0
        proj_dim = 0

        # Подкова: P' ⊕ P'' → A
        horseshoe = {
            'P\'': f'ℤ^{P_prime}',
            'P\'\'': f'ℤ^{P_dprime}',
            'P_total': f'ℤ^{P_total}',
            'proj_dim': proj_dim,
        }

        # Глобальное гомологическое измерение ℤ: 1 (gl.dim = 1).
        global_dim_Z = 1

        return {
            'теорема': 'Хорсхо (подкова)',
            'подкова': horseshoe,
            'P_ЕТД': f'ℤ^{P_total}',
            'pd(ℤ⁴)': proj_dim,
            'gl.dim(ℤ)': global_dim_Z,
            'ЕТД': f'ℤ⁴ = P\'⊕P\'\' (ℤ²⊕ℤ²): подкова ЕТД = прямая сумма 2 пар сфер! pd=0 (идеально)',
        }


ha = HomologicalAlgebraETD()

snake = ha.snake_lemma_etd()
print(f"Змея: точная={snake['точная']}, δ={snake['δ']}")

five = ha.five_lemma_etd()
print(f"Пять-лемма: f₃_изо={five['f₃_изоморфизм']}")

tor_ext = ha.tor_ext_etd()
print(f"Tor/Ext: Tor₁={tor_ext['Tor₁']}, Ext¹={tor_ext['Ext¹']}, χ={tor_ext['χ_гомол']}")

horse = ha.horseshoe_theorem_etd()
print(f"Хорсхо: P={horse['P_ЕТД']}, pd={horse['pd(ℤ⁴)']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы гомологической алгебры** → **4 принципа точности ЕТД**:

1. **Лемма о змее** → При π/4-балансе: все ядра/коядра=0, δ=0: точная последовательность без дефекта!
2. **Пять-лемма** → 4 крайних изоморфны → центральный (общий ЕТД-оператор) = изоморфизм
3. **Tor₁=Ext¹=0** → ℤ⁴ СВОБОДНА (нет скручивания); χ_гом=4=числу сфер
4. **Хорсхо** → ℤ⁴=ℤ²⊕ℤ² (2 пары сфер!); pd=0: идеальная «нулевой длины» резольвента

> **Гомологический портрет ЕТД**: Система 4 сфер является свободным ℤ-модулем ℤ⁴ с нулевыми Tor и Ext. При π/4-балансе все последовательности точны (нет дефекта). Хорсхо: ℤ⁴ = ℤ²⊕ℤ² — разбивается на 2 пары сфер (МВС+СВС) ⊕ (БВС+ЧВС).

---
*ТОМ 204 / СЕРИЯ VII / ЕТД 2026*
