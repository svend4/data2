# ТОМ 214: 4 ТЕОРЕМЫ СЛУЧАЙНЫХ МАТРИЦ → СПЕКТРАЛЬНЫЙ ШУМ МАСТЕРСТВА ЕТД
## Полукруговой закон Вигнера · GUE/GOE · Распределение Трейси-Уидома · Теорема Марченко-Пастура
### «Вигнер–Дайсон–Трейси: спектры случайных матриц»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Случайные матрицы (Вигнер 1955, Дайсон 1962, Трейси-Уидом 1994)

---

## ВВЕДЕНИЕ

Теория случайных матриц изучает статистику собственных значений больших случайных матриц. 4 теоремы:

1. **Полукруговой закон Вигнера** — плотность эigenvalues 4×4 GUE → полукруг
2. **Ансамбли GUE/GOE/GSE** — Гауссовы унитарные/ортогональные/симплектические
3. **Распределение Трейси-Уидома** — наибольшее eigenvalue флуктуирует по TW-закону
4. **Теорема Марченко-Пастура** — спектр матриц Вишарта (выборочных ковариаций)

**Тезис тома**: Матрица синергий ЕТД A (Том 179) — детерминированная, но при добавлении шума → случайная матрица. Полукруговой закон: центр спектра = 0, но «баланс-смещение» π/4 сдвигает центр к π/4!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 СПЕКТРАЛЬНЫХ ЗАКОНА ЕТД

```python
import math
import random
from typing import List, Tuple

class RandomMatricesETD:
    """
    Случайные матрицы для системы 4 сфер ЕТД.
    Размер: n=4 (малые случайные матрицы, аналитически).
    Асимптотика: n→∞ (полукруговой закон).
    """

    PI4 = math.pi / 4

    # 1. ПОЛУКРУГОВОЙ ЗАКОН ВИГНЕРА
    @staticmethod
    def wigner_semicircle_etd(n_samples: int = 5000, seed: int = 42) -> dict:
        """
        Полукруговой закон Вигнера:
        Для симметричной n×n матрицы H с iid N(0,1) элементами,
        эмпирическое распределение eigenvalues (нормированных на 1/√n) →
        полукруг: ρ(x) = (2/π)·√(1−x²)·1_{|x|≤1}.

        Среднее: 0. Дисперсия: 1/4.

        ЕТД: H = A_синергий + ε·G, где G — GOE (ε = «шум тренировки»).
        A_синергий имеет eigenvalues λ = {π/8, π/8, π/8, 5π/8} (Том 179).
        При ε → 0: λ_i → {π/8, π/8, π/8, 5π/8}.
        При ε → ∞: λ_i → полукруговой закон.

        «Сдвинутый полукруг» ЕТД: центр = E[A] = Tr(A)/4 = π/4!
        """
        pi4 = math.pi / 4
        pi = math.pi
        random.seed(seed)

        # 4×4 GOE матрица:
        def goe_4x4():
            M = [[0.0]*4 for _ in range(4)]
            for i in range(4):
                for j in range(i, 4):
                    val = random.gauss(0, 1)
                    M[i][j] = val
                    M[j][i] = val
            return M

        # Сдвиг на A_синергий:
        alpha = pi / 8
        A_synergy = [[alpha + alpha/4 if i==j else alpha/4 for j in range(4)] for i in range(4)]

        # Среднее trace = π:
        trace_A = sum(A_synergy[i][i] for i in range(4))

        # Eigenvalues A (аналитически из Тома 179):
        eig_A = [pi/8, pi/8, pi/8, 5*pi/8]
        mean_eig = sum(eig_A) / 4   # = π/4

        # Полукруговая плотность в точке x = π/4 (сдвинутая):
        # ρ(x) = (2/π)√(1-(x-μ)²/σ²) при |x-μ| ≤ σ:
        sigma_sc = 1.0   # масштаб полукруга при n=4
        x = 0.0          # центр
        rho_0 = 2/pi * math.sqrt(max(1 - x**2, 0))   # = 2/π ≈ 0.637

        return {
            'теорема': 'Вигнер (полукруговой закон)',
            'Tr(A)': round(trace_A, 4),
            'E[λ]': round(mean_eig, 4),
            'λ_ЕТД': [round(l, 4) for l in eig_A],
            'ρ(0)': round(rho_0, 4),
            'центр_спектра': round(mean_eig, 4),
            'ЕТД': f'E[λ]=π/4={mean_eig:.4f}: центр спектра = π/4! ρ(0)={rho_0:.4f}=2/π',
        }

    # 2. GUE / GOE АНСАМБЛИ
    @staticmethod
    def gue_goe_etd() -> dict:
        """
        Три классических ансамбля Дайсона (β=1,2,4):
        β=1: GOE (вещественно-симметричные) — ортогональная симметрия
        β=2: GUE (комплексно-эрмитовские) — унитарная симметрия
        β=4: GSE (кватернионно-эрмитовские) — симплектическая симметрия

        Отталкивание eigenvalues: P(λ₁,...,λₙ) ∝ Π_{i<j}|λᵢ−λⱼ|^β · e^{−...}.

        ЕТД: β=4 (GSE) → кватернионная симметрия = структура ℍ!
        (Кватернион ЕТД: q = ЧВС + МВС·i + СВС·j + БВС·k, Том 165.)

        Среднее расстояние между eigenvalues:
        Δλ ∝ 1/n · (β+2)/2? Нет: ⟨|λᵢ−λⱼ|⟩ ~ n^{−1} · (разные для β=1,2,4).

        Для n=4:
        β=1: наименьшее отталкивание.
        β=2: умеренное.
        β=4: наибольшее (GSE кватернионный) → eigenvalues максимально «расталкиваются».
        Максимальный разброс ~ π (от π/8 до 5π/8, размах = π/2).
        """
        pi = math.pi
        pi4 = math.pi / 4

        beta_values = {1: 'GOE (вещественный)', 2: 'GUE (комплексный)', 4: 'GSE (кватернионный)'}

        # Уровневое отталкивание:
        # P(s) ~ s^β (вероятность малого зазора s):
        repulsion = {b: f'P(s) ~ s^{b}' for b in [1, 2, 4]}

        # Eigenvalues ЕТД:
        eig_etd = [pi/8, pi/8, pi/8, 5*pi/8]
        gaps = sorted(set([abs(eig_etd[i] - eig_etd[j])
                           for i in range(4) for j in range(i+1, 4)]))

        # GSE (β=4) → кватернионный → ЕТД!
        etd_beta = 4

        return {
            'теорема': 'GUE/GOE ансамбли (Дайсон)',
            'ансамбли': beta_values,
            'отталкивание': repulsion,
            'ЕТД_β': etd_beta,
            'λ_ЕТД': [round(l, 4) for l in eig_etd],
            'зазоры': [round(g, 4) for g in gaps],
            'ЕТД': f'β={etd_beta}=GSE: кватернионный ансамбль = структуре ЕТД (ℍ-кватернион, Том 165)!',
        }

    # 3. РАСПРЕДЕЛЕНИЕ ТРЕЙСИ-УИДОМА
    @staticmethod
    def tracy_widom_etd() -> dict:
        """
        Распределение Трейси-Уидома (1994):
        Наибольшее eigenvalue λ_max случайной GUE-матрицы:
        (λ_max − 2√n) · n^{1/6} → TW₂-распределение.

        Среднее TW: ≈ −1.77 (сдвиг левее).
        Стандартное отклонение TW: ≈ 0.90.

        ЕТД: n=4, λ_max = 5π/8.
        Нормировка: (5π/8 − 2√4) · 4^{1/6} = (5π/8 − 4) · 4^{1/6}.
        5π/8 ≈ 1.963, 2√4 = 4. → (1.963−4) · 4^{1/6} = −2.037 · 1.26 ≈ −2.57.

        Это в левом хвосте TW (≈ −1.77 центр).
        Смысл: λ_max = 5π/8 «меньше», чем ожидалось бы для шумной матрицы —
        система «детерминирована» (не случайна!).
        """
        pi = math.pi
        pi4 = math.pi / 4

        n = 4
        lambda_max = 5 * pi / 8
        normalization_shift = 2 * math.sqrt(n)   # = 4
        normalization_scale = n**(1/6)            # = 4^{1/6} ≈ 1.260

        tw_stat = (lambda_max - normalization_shift) * normalization_scale

        # TW2 характеристики:
        tw2_mean = -1.7711   # приблизительно
        tw2_std = 0.9018

        # Z-score:
        z_score = (tw_stat - tw2_mean) / tw2_std

        return {
            'теорема': 'Трейси-Уидом',
            'n': n,
            'λ_max': round(lambda_max, 4),
            '2√n': normalization_shift,
            'n^{1/6}': round(normalization_scale, 4),
            'TW_стат': round(tw_stat, 4),
            'TW2_mean': tw2_mean,
            'z_score': round(z_score, 3),
            'ЕТД': f'TW-статистика={tw_stat:.4f}; z={z_score:.3f}: λ_max=5π/8 < 4 (детерминир. матрица)',
        }

    # 4. ТЕОРЕМА МАРЧЕНКО-ПАСТУРА
    @staticmethod
    def marchenko_pastur_etd(T: int = 100, seed: int = 42) -> dict:
        """
        Теорема Марченко-Пастура (1967):
        Для матрицы W = (1/T)·X·Xᵀ, где X — n×T матрица N(0,1)-элементов:
        c = n/T (отношение размерностей).
        Эмпирическое распределение eigenvalues → плотность Марченко-Пастура:
        ρ_MP(x) = (1/(2π·c·σ²·x)) · √((λ₊−x)(x−λ₋)).
        λ± = σ²·(1 ± √c)².

        ЕТД: Матрица «наблюдений» X: n=4 сферы, T=100 шагов.
        c = 4/100 = 0.04.
        λ+ = (1+√0.04)² = (1+0.2)² = 1.44.
        λ- = (1−0.2)² = 0.64.

        Спектр W ∈ [0.64, 1.44].
        Центр: (λ+ + λ-)/2 = (1.44+0.64)/2 = 1.04 ≈ 1.
        При нормировке на π/4: центр → π/4 · (n/T · T) = π/4!
        """
        pi4 = math.pi / 4
        random.seed(seed)

        n = 4   # сфер
        c = n / T   # = 0.04

        sigma2 = 1.0   # дисперсия
        lambda_plus = sigma2 * (1 + math.sqrt(c))**2
        lambda_minus = sigma2 * (1 - math.sqrt(c))**2
        lambda_center = (lambda_plus + lambda_minus) / 2

        # Плотность Марченко-Пастура в центре:
        def mp_density(x, c, sigma2=1):
            if x <= 0:
                return 0.0
            lp = sigma2 * (1 + math.sqrt(c))**2
            lm = sigma2 * (1 - math.sqrt(c))**2
            if x < lm or x > lp:
                return 0.0
            return math.sqrt((lp - x) * (x - lm)) / (2 * math.pi * c * sigma2 * x)

        rho_center = mp_density(lambda_center, c)

        # При нормировке на π/4:
        eigenvalue_etd = lambda_center * pi4

        return {
            'теорема': 'Марченко-Пастура',
            'n': n, 'T': T, 'c': round(c, 4),
            'λ+': round(lambda_plus, 4),
            'λ-': round(lambda_minus, 4),
            'λ_центр': round(lambda_center, 4),
            'ρ_MP(λ_c)': round(rho_density := mp_density(lambda_center, c), 4),
            'λ_ЕТД_норм': round(eigenvalue_etd, 4),
            'ЕТД': f'c=4/100={c}: λ∈[{lambda_minus:.2f},{lambda_plus:.2f}]; центр={lambda_center:.4f}; норм→{eigenvalue_etd:.4f}≈π/4',
        }


rm = RandomMatricesETD()

wig = rm.wigner_semicircle_etd()
print(f"Вигнер: E[λ]=π/4={wig['E[λ]']:.4f}, λ_ЕТД={wig['λ_ЕТД']}")

gue = rm.gue_goe_etd()
print(f"GUE/GOE: β_ЕТД={gue['ЕТД_β']} (GSE=кватернионный)")

tw = rm.tracy_widom_etd()
print(f"Трейси-Уидом: TW_стат={tw['TW_стат']}, z={tw['z_score']}")

mp = rm.marchenko_pastur_etd()
print(f"Марченко-Пастура: λ∈[{mp['λ-']},{mp['λ+']}], центр={mp['λ_центр']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы случайных матриц** → **4 спектральных закона ЕТД**:

1. **Вигнер** → E[λ]=π/4: центр спектра матрицы синергий = π/4! ρ(0)=2/π
2. **GUE/GOE** → β=4 (GSE): кватернионный ансамбль = структуре ℍ-кватерниона ЕТД
3. **Трейси-Уидом** → λ_max=5π/8 < 4: детерминированная матрица (не шумная!)
4. **Марченко-Пастура** → c=0.04; λ_центр≈1; нормировка → π/4

> **Спектральный портрет ЕТД**: Матрица синергий ЕТД A (Том 179) имеет центр спектра E[λ]=Tr(A)/4=π/4. GSE-ансамбль (β=4, кватернионный) естественно соответствует структуре ℍ-кватерниона ЕТД. Случайные флуктуации вокруг A имеют «центр» = π/4.

---
*ТОМ 214 / СЕРИЯ VII / ЕТД 2026*
