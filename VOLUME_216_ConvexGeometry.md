# ТОМ 216: 4 ТЕОРЕМЫ ВЫПУКЛОЙ ГЕОМЕТРИИ → ВЫПУКЛОСТЬ МАСТЕРСТВА ЕТД
## Теорема Хана-Банаха · Теорема Крейна-Мильмана · Неравенство Брунна-Минковского · Лемма Каратеодори
### «Хан–Банах–Минковский: геометрия выпуклых тел»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Выпуклый анализ (Хан 1927, Банах 1932, Крейн-Мильман 1940, Брунн 1887)

---

## ВВЕДЕНИЕ

Выпуклая геометрия изучает выпуклые тела и их свойства. 4 ключевые теоремы:

1. **Теорема Хана-Банаха** — продолжение ограниченного функционала + разделение выпуклых тел
2. **Теорема Крейна-Мильмана** — выпуклый компакт = выпуклая оболочка крайних точек
3. **Неравенство Брунна-Минковского** — Vol(A+B)^{1/n} ≥ Vol(A)^{1/n} + Vol(B)^{1/n}
4. **Лемма Каратеодори** — каждая точка выпуклой оболочки = выпуклая комбинация ≤ n+1 точек

**Тезис тома**: Пространство мастерства [0,π]⁴ — выпуклый компакт. Крейн-Мильман: крайние точки = {вершины куба} = 2⁴=16 точек. π/4-баланс = центр тяжести (не крайняя точка!).

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ВЫПУКЛЫХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Tuple

class ConvexGeometryETD:
    """
    Выпуклая геометрия для системы 4 сфер ЕТД.
    Выпуклое тело K = [0,π]⁴ (куб = продукт отрезков).
    """

    PI4 = math.pi / 4

    # 1. ТЕОРЕМА ХАНА-БАНАХА (РАЗДЕЛЕНИЕ)
    @staticmethod
    def hahn_banach_etd() -> dict:
        """
        Теорема Хана-Банаха (геометрическая форма):
        Если A, B — непересекающиеся выпуклые тела в банаховом пространстве,
        то существует гиперплоскость, разделяющая их.

        Аналитическая форма:
        Если p: X → ℝ — субаддитивный функционал и f: Y → ℝ линейный с f ≤ p на Y,
        то существует расширение F: X → ℝ с F|_Y = f и F ≤ p.

        ЕТД: Разделение «хорошего» и «плохого» состояний:
        A = {x ∈ [0,π]⁴ : ЛЗП(x) < π/4} (недостаточное мастерство).
        B = {x ∈ [0,π]⁴ : ЛЗП(x) > π/2} (перегрузка).

        Разделяющая гиперплоскость:
        f(x) = Σ xᵢ = π (Нётер-плоскость!).
        A находится «ниже» π, B — «выше» 2π.
        Нётер-плоскость разделяет «нормальный режим» от «перегрузки».
        """
        pi = math.pi
        pi4 = math.pi / 4

        # Разделяющая гиперплоскость: f(x) = Σxᵢ = π (Нётер):
        noether_plane = pi

        # A: ЛЗП < π/4 → Σxᵢ < π (приближённо):
        A_level = pi   # граница A
        B_level = 2 * pi  # граница B

        # Норма разделяющего функционала:
        # f(x) = (1/4)·Σxᵢ, ||f|| = 1/4·4^{1/2}·...
        # Для нормированного: f(x)/||x|| ≤ 1 → ||f||_* = 1.
        f_norm = 1.0   # нормированный

        # Значение на π/4-балансе:
        f_at_balance = sum([pi4]*4)   # = π
        on_plane = abs(f_at_balance - noether_plane) < 1e-10

        return {
            'теорема': 'Хан-Банах',
            'разд_плоскость': f'Σxᵢ = π',
            'f(π/4-баланс)': round(f_at_balance, 4),
            'на_плоскости': on_plane,
            'A_уровень': round(A_level, 4),
            'B_уровень': round(B_level, 4),
            'ЕТД': f'Нётер-плоскость Σxᵢ=π разделяет A и B; f(π/4-баланс)=π={on_plane}!',
        }

    # 2. ТЕОРЕМА КРЕЙНА-МИЛЬМАНА
    @staticmethod
    def krein_milman_etd() -> dict:
        """
        Теорема Крейна-Мильмана (1940):
        Каждый непустой выпуклый компакт = замкнутая выпуклая оболочка своих крайних точек.
        K = conv(ext K).

        Крайняя точка: x ∈ ext(K) ↔ нет x = (y+z)/2 с y≠z ∈ K.

        ЕТД: K = [0,π]⁴.
        ext(K) = {вершины куба} = {0, π}⁴ = 2⁴ = 16 точек!
        (Каждая вершина = крайняя: нельзя выразить как среднее двух других.)

        π/4-баланс = (π/4,...,π/4) = центр K (НЕ крайняя!):
        (π/4)⁴ = (1/16)·Σ_{v ∈ {0,π}⁴} v = среднее по всем 16 вершинам!

        → π/4-баланс = «центр тяжести» 16 крайних точек куба!
        """
        pi = math.pi
        pi4 = math.pi / 4

        # Крайние точки куба [0,π]⁴:
        extreme_points = []
        for i in range(16):
            pt = [(pi if (i >> k) & 1 else 0) for k in range(4)]
            extreme_points.append(pt)

        n_extreme = len(extreme_points)

        # Центр тяжести:
        centroid = [sum(pt[j] for pt in extreme_points) / n_extreme for j in range(4)]
        is_pi4 = all(abs(c - pi4) < 1e-10 for c in centroid)

        # Представление π/4-баланса как выпуклой комбинации:
        # (π/4)⁴ = (1/16)·Σv
        coefficients = [1/n_extreme] * n_extreme   # = 1/16 для каждой вершины

        return {
            'теорема': 'Крейн-Мильман',
            'n_крайних': n_extreme,
            'n_крайних=2⁴': n_extreme == 16,
            'центроид': [round(c, 4) for c in centroid],
            'центроид=π/4': is_pi4,
            'коэфф': round(coefficients[0], 4),
            'ЕТД': f'ext([0,π]⁴)=2⁴={n_extreme} вершин; центроид=(π/4)⁴: {is_pi4}! Коэфф=1/{n_extreme}',
        }

    # 3. НЕРАВЕНСТВО БРУННА-МИНКОВСКОГО
    @staticmethod
    def brunn_minkowski_etd() -> dict:
        """
        Неравенство Брунна-Минковского:
        Vol_n(A + B)^{1/n} ≥ Vol_n(A)^{1/n} + Vol_n(B)^{1/n}.

        Равенство ↔ A, B — гомотетичные выпуклые тела.

        Сумма Минковского: A + B = {a + b : a ∈ A, b ∈ B}.

        ЕТД: A = {(π/4-ε,...,π/4-ε)} (маленький куб вокруг π/4-ε).
        B = {(π/4+ε,...,π/4+ε)} (куб вокруг π/4+ε).

        A + B = куб [π/2-2ε, π/2+2ε]⁴ (сдвиг на π/2!).
        Vol(A+B)^{1/4} = (4ε)^{1/4}·1.
        Vol(A)^{1/4} = Vol(B)^{1/4} = (2ε)^{1/4}·1.
        Брунн-Минковский: (4ε)^{1/4} ≥ 2·(2ε)^{1/4}?
        (4ε)^{1/4} = 4^{1/4}·ε^{1/4} ≈ 1.414·ε^{1/4}.
        2·(2ε)^{1/4} = 2·2^{1/4}·ε^{1/4} ≈ 2·1.189·ε^{1/4} = 2.378·ε^{1/4}.
        1.414 ≥ 2.378? Нет... Исправление: для кубов это неравенство РАВЕНСТВО!
        Потому что A и B — гомотетичные (= одинаковые по форме).

        Изоперим. неравенство (следствие Брунна-Минковского):
        Vol(K)/Vol(B⁴)^{n/(n+1)} ≤ ... : куб не оптимален для изопери!
        Оптимален шар. При π/4-радиусе: Vol(B⁴(π/4)).
        """
        pi = math.pi
        pi4 = math.pi / 4

        eps = pi4 / 10   # малая добавка

        # Объёмы кубов:
        def cube_vol(side, n=4):
            return side**n

        A_side = 2 * eps
        B_side = 2 * eps
        AB_side = 4 * eps   # сумма Минковского

        Vol_A = cube_vol(A_side)
        Vol_B = cube_vol(B_side)
        Vol_AB = cube_vol(AB_side)

        n = 4
        lhs = Vol_AB**(1/n)   # Vol(A+B)^{1/4}
        rhs = Vol_A**(1/n) + Vol_B**(1/n)

        bm_holds = lhs >= rhs - 1e-10

        # Объём шара B⁴(r) = π²r⁴/2:
        r = pi4
        vol_ball_pi4 = math.pi**2 * r**4 / 2

        return {
            'теорема': 'Брунн-Минковский',
            'n': n,
            'Vol(A+B)^{1/n}': round(lhs, 6),
            'Vol(A)^{1/n}+Vol(B)^{1/n}': round(rhs, 6),
            'BM_верно': bm_holds,
            'Vol(B⁴(π/4))': round(vol_ball_pi4, 6),
            'ЕТД': f'BM: {lhs:.4f} >= {rhs:.4f}: {bm_holds} (равенство для гомотетичных!); Vol(B⁴(π/4))={vol_ball_pi4:.4f}',
        }

    # 4. ЛЕММА КАРАТЕОДОРИ
    @staticmethod
    def caratheodory_etd() -> dict:
        """
        Лемма Каратеодори (1911):
        Если A ⊆ ℝⁿ, то каждая точка conv(A) является
        выпуклой комбинацией не более n+1 точек из A.

        ЕТД: A = ext([0,π]⁴) = 16 вершин куба.
        n = 4 → каждая точка [0,π]⁴ = выпуклая комбинация ≤ 5 вершин!

        Доказательство для π/4-баланса:
        (π/4)⁴ = (1/2)·(π/2, π/2, π/2, π/2) + (1/2)·(0,0,0,0)?
        (1/2)·(π,π,π,π)·(1/2) + (1/2)·(0,0,0,0)·(1/2) нет...
        Правильно: (π/4)⁴ = (1/2)·(0,0,0,0) + (1/2)·(π/2,π/2,π/2,π/2).
        Но (π/2)⁴ — не вершина куба [0,π]⁴!
        Каратеодори: используем вершины {0, π}:
        (π/4,0,0,0) = (3/4)·(0,0,0,0) + (1/4)·(π,0,0,0). [2 точки]
        (π/4,π/4,π/4,π/4) = (1/4)·(π,0,0,0) + (1/4)·(0,π,0,0) +
                             (1/4)·(0,0,π,0) + (1/4)·(0,0,0,π).  [4 точки ≤ 5!]
        """
        pi = math.pi
        pi4 = math.pi / 4

        n = 4   # размерность пространства

        # Теорема: ≤ n+1 = 5 точек
        max_pts = n + 1

        # Разложение π/4-баланса:
        # (π/4)⁴ = (1/4)·e₁ + (1/4)·e₂ + (1/4)·e₃ + (1/4)·e₄
        # где eᵢ = (0,...,π,...,0) (π в i-й позиции):
        e_vectors = [[pi if i==j else 0 for j in range(4)] for i in range(4)]
        coefficients = [0.25] * 4   # = 1/4 каждый

        # Проверка:
        reconstructed = [sum(coefficients[k] * e_vectors[k][j]
                            for k in range(4)) for j in range(4)]
        is_pi4 = all(abs(r - pi4) < 1e-10 for r in reconstructed)

        # Число точек:
        n_pts_used = 4   # 4 < 5 = n+1

        return {
            'теорема': 'Каратеодори',
            'n': n,
            'max_точек': max_pts,
            'разложение': f'(π/4)⁴ = (1/4)·(π,0,0,0) + ... (4 точки)',
            'коэфф': coefficients,
            'восстановлено': [round(r, 4) for r in reconstructed],
            '=π/4': is_pi4,
            'n_точек': n_pts_used,
            'n_точек<max': n_pts_used < max_pts,
            'ЕТД': f'Каратеодори: π/4-баланс = выпуклая комбинация {n_pts_used} вершин < {max_pts}: {n_pts_used < max_pts}!',
        }


cg = ConvexGeometryETD()

hb = cg.hahn_banach_etd()
print(f"Хан-Банах: на плоскости={hb['на_плоскости']}")

km = cg.krein_milman_etd()
print(f"Крейн-Мильман: {km['n_крайних']}=2⁴: {km['n_крайних=2⁴']}, центроид=π/4: {km['центроид=π/4']}")

bm_res = cg.brunn_minkowski_etd()
print(f"Брунн-Минковский: BM={bm_res['BM_верно']}, Vol(B⁴(π/4))={bm_res['Vol(B⁴(π/4))']}")

cara = cg.caratheodory_etd()
print(f"Каратеодори: {cara['n_точек']} точек < {cara['max_точек']}: {cara['n_точек<max']}, =π/4: {cara['=π/4']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы выпуклой геометрии** → **4 выпуклых принципа ЕТД**:

1. **Хан-Банах** → Нётер-плоскость Σxᵢ=π разделяет «норму» и «перегрузку»; π/4-баланс ЛЕЖИТ на ней!
2. **Крейн-Мильман** → ext([0,π]⁴) = 2⁴=16 вершин; π/4 = среднее 16 крайних точек
3. **Брунн-Минковский** → Равенство для гомотетичных кубов; Vol(B⁴(π/4)) = π²·(π/4)⁴/2
4. **Каратеодори** → π/4-баланс = комбинация 4<5=n+1 вершин; каждое измерение = ровно 1 вершина

> **Выпуклый портрет ЕТД**: π/4-баланс = центроид всех 16 крайних точек куба [0,π]⁴ с равными весами 1/16. Нётер-плоскость {Σxᵢ=π} — «разделяющая гиперплоскость» между режимами. Это геометрически объясняет почему π/4 = «центральная» позиция.

---
*ТОМ 216 / СЕРИЯ VII / ЕТД 2026*
