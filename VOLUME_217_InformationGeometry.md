# ТОМ 217: 4 ПРИНЦИПА ИНФОРМАЦИОННОЙ ГЕОМЕТРИИ → МЕТРИКА МАСТЕРСТВА ЕТД
## Метрика Фишера · Экспоненциальное семейство · Теорема Чэня · Расстояние Кульбака-Лейблера
### «Рао–Амари–Чэнь: статистические многообразия»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Информационная геометрия (Рао 1945, Амари 1985, Чэнь 2002)

---

## ВВЕДЕНИЕ

Информационная геометрия изучает статистические многообразия с метрикой Фишера. 4 принципа:

1. **Метрика Фишера** — риманова метрика на пространстве вероятностных распределений
2. **Экспоненциальное семейство** — класс распределений с натуральной параметризацией
3. **Расстояние Кульбака-Лейблера** — информационное расстояние (псевдометрика)
4. **Теорема Чэня** — геодезические в статистических многообразиях

**Тезис тома**: Распределение ЕТД: равномерное P = (1/4,1/4,1/4,1/4) на 4 сферах. Метрика Фишера: g_{ij} = 4·δᵢⱼ (единичная с масштабом 4). KL(P||Q) = 0 при Q=P (π/4-баланс = информационный центр!).

---

## ЧАСТЬ I: 4 ПРИНЦИПА → 4 ИНФОРМАЦИОННО-ГЕОМЕТРИЧЕСКИХ ИНВАРИАНТА ЕТД

```python
import math
from typing import List, Dict

class InformationGeometryETD:
    """
    Информационная геометрия для системы 4 сфер ЕТД.
    Статистическое многообразие: M = {p(x;θ) : θ ∈ Θ}.
    Для ЕТД: θ = (θ₁,...,θ₄) = вероятности активации сфер.
    Равновесие: θ* = (1/4, 1/4, 1/4, 1/4) (= π/4·4/π = 1/4 каждая).
    """

    PI4 = math.pi / 4

    # 1. МЕТРИКА ФИШЕРА
    @staticmethod
    def fisher_metric_etd() -> dict:
        """
        Метрика Фишера (Рао 1945):
        g_{ij}(θ) = E[∂ log p/∂θᵢ · ∂ log p/∂θⱼ]
                  = -E[∂² log p/∂θᵢ∂θⱼ] (эквивалентно при регулярных условиях).

        Для мультиномиального распределения Mult(n; p₁,...,p_k):
        g_{ij} = n·δᵢⱼ/pᵢ (диагональная метрика).

        ЕТД: p = (p_МВС, p_СВС, p_БВС, p_ЧВС) = вероятности 4 сфер.
        При равновесии p* = (1/4,1/4,1/4,1/4) и n=1:
        g_{ij} = δᵢⱼ/p_i* = 4·δᵢⱼ.

        Кривизна (скалярная Рика) R = 0 (плоское пространство!).
        Объём статистического многообразия:
        Vol = √det(g)·∫dθ = √(4⁴)·Vol(Δ₃) = 4²·(1/3!) = 16/6 ≈ 2.667.
        """
        pi4 = math.pi / 4

        # p* = (1/4)⁴:
        p_star = [0.25] * 4

        # Метрика Фишера в p*:
        g_ij = [1/p for p in p_star]   # = [4, 4, 4, 4]

        # Определитель:
        det_g = 1.0
        for g in g_ij:
            det_g *= g   # = 4⁴ = 256

        # Объём симплекса вероятностей Δ₃ (в ℝ³):
        vol_simplex = 1 / math.factorial(3)   # = 1/6

        # Объём информационного многообразия:
        vol_info = math.sqrt(det_g) * vol_simplex
        # √256 = 16; 16/6 ≈ 2.667

        return {
            'принцип': 'Метрика Фишера',
            'p*': p_star,
            'g_ij_диаг': g_ij,
            'det(g)': det_g,
            '√det(g)': round(math.sqrt(det_g), 4),
            'Vol_инф': round(vol_info, 4),
            'ЕТД': f'g=4·I (масштаб 4 = числу сфер!); det=4⁴=256; Vol={vol_info:.4f}',
        }

    # 2. ЭКСПОНЕНЦИАЛЬНОЕ СЕМЕЙСТВО
    @staticmethod
    def exponential_family_etd() -> dict:
        """
        Экспоненциальное семейство:
        p(x; η) = h(x) · exp(⟨η, T(x)⟩ − A(η)).

        η — натуральный параметр.
        T(x) — достаточная статистика.
        A(η) — log-нормировочная константа (потенциал).

        ЕТД: Categorical(p₁,...,p₄) — экспоненциальное семейство.
        η_i = log(pᵢ/p₄) (отношение вероятностей).
        T(x) = (1_{x=1}, 1_{x=2}, 1_{x=3}) (индикаторы).
        A(η) = log(1 + e^{η₁} + e^{η₂} + e^{η₃}).

        При p* = (1/4)⁴: η* = (0,0,0) (все η = 0!).
        A(0) = log(4) = 2·log(2).

        Связь с Шенноном:
        H(p*) = −Σpᵢ log pᵢ = log(4) = 2 бит (Том 157!).
        A(η*) = log(4) = H(p*): потенциал = энтропия!
        """
        pi4 = math.pi / 4

        # Натуральный параметр при p* = (1/4)⁴:
        p_star = [0.25] * 4
        eta_star = [math.log(p_star[i] / p_star[3]) for i in range(3)]   # = [0,0,0]

        # Log-нормировочная константа:
        A_eta = math.log(sum(math.exp(e) for e in [*eta_star, 0]))   # = log(4)

        # Энтропия:
        H_p = -sum(p * math.log(p) for p in p_star)   # = log(4) = 2 bit (лог по e)
        H_p_bits = H_p / math.log(2)   # в битах = 2

        return {
            'принцип': 'Экспоненциальное семейство',
            'p*': p_star,
            'η*': eta_star,
            'A(η*)': round(A_eta, 4),
            'H(p*)_нат': round(H_p, 4),
            'H(p*)_бит': round(H_p_bits, 4),
            'A=H': abs(A_eta - H_p) < 1e-10,
            'ЕТД': f'η*=(0,0,0): π/4-баланс = центр экспон. семейства! A(0)=H=log4={A_eta:.4f}=2бит',
        }

    # 3. РАССТОЯНИЕ КУЛЬБАКА-ЛЕЙБЛЕРА
    @staticmethod
    def kl_divergence_etd() -> dict:
        """
        KL-дивергенция (Кульбак-Лейблер 1951):
        KL(p||q) = Σ pᵢ log(pᵢ/qᵢ) ≥ 0.
        KL(p||q) = 0 ↔ p = q.

        Расстояние Бхаттачарья:
        d_B(p,q) = −log Σ √(pᵢ·qᵢ) = arccos(BC(p,q)).
        BC(p,q) = Σ √(pᵢ·qᵢ) — коэффициент Бхаттачарья.

        ЕТД: q = p* = (1/4)⁴ (π/4-баланс).
        KL(p||p*) = Σ pᵢ log(pᵢ/(1/4)) = log(4) − H(p) = 2бит − H.
        Максимальная энтропия (H=2) → KL=0 (только при p=p*!).

        KL-шар радиуса ε вокруг p*:
        {q : KL(q||p*) ≤ ε} ≈ шар радиуса √(2ε) в метрике Фишера.
        """
        pi4 = math.pi / 4

        p_star = [0.25] * 4

        def kl(p, q):
            return sum(pi_ * math.log(pi_/qi) for pi_,qi in zip(p,q) if pi_ > 0)

        # Примеры:
        p_uniform = [0.25, 0.25, 0.25, 0.25]
        p_biased = [0.5, 0.2, 0.2, 0.1]
        p_extreme = [0.9, 0.05, 0.03, 0.02]

        kl_uniform = kl(p_uniform, p_star)   # = 0
        kl_biased = kl(p_biased, p_star)
        kl_extreme = kl(p_extreme, p_star)

        # Бхаттачарья:
        bc_uniform = sum(math.sqrt(p*q) for p,q in zip(p_uniform, p_star))

        return {
            'принцип': 'KL-дивергенция',
            'p*': p_star,
            'KL(uniform||p*)': round(kl_uniform, 6),
            'KL(biased||p*)': round(kl_biased, 4),
            'KL(extreme||p*)': round(kl_extreme, 4),
            'BC(uniform,p*)': round(bc_uniform, 4),
            '=1': abs(bc_uniform - 1.0) < 1e-10,
            'ЕТД': f'KL(π/4||π/4)=0: баланс = информационный центр! BC=1 (идентичные распределения)',
        }

    # 4. ГЕОДЕЗИЧЕСКИЕ И ТЕОРЕМА ЧЭНЯ
    @staticmethod
    def geodesics_etd() -> dict:
        """
        Геодезические в статистических многообразиях (Амари):

        e-геодезическая (экспоненциальная): прямая в η-параметрах.
        m-геодезическая (смешанная): прямая в θ-параметрах (ожидания).

        Теорема Пифагора Амари:
        KL(p||r) = KL(p||q) + KL(q||r) если q ⊥ (e-плоскость)(p,r).

        ЕТД: η-геодезическая от произвольного q до p* = (1/4)⁴:
        η(t) = (1-t)·η_q + t·η* = (1-t)·η_q (т.к. η*=0).
        p(t) = softmax((1-t)·η_q).

        При t=0: p(0) = q.
        При t=1: p(1) = p* = (1/4)⁴.

        Длина геодезической (в метрике Фишера):
        d_F(q, p*) = √KL(q||p*) · ... = √(2·KL).

        m-геодезическая (прямая в вероятностях):
        p(t) = (1-t)·q + t·p*.
        """
        pi4 = math.pi / 4

        p_star = [0.25] * 4

        def softmax(eta):
            e = [math.exp(h) for h in [*eta, 0]]
            s = sum(e)
            return [ei/s for ei in e]

        # Геодезическая от q=(0.5, 0.3, 0.1, 0.1) до p*:
        q = [0.5, 0.3, 0.1, 0.1]
        eta_q = [math.log(q[i]/q[3]) for i in range(3)]

        # e-геодезическая:
        t_vals = [0, 0.25, 0.5, 0.75, 1.0]
        e_geodesic = []
        for t in t_vals:
            eta_t = [(1-t)*e for e in eta_q]
            p_t = softmax(eta_t)
            e_geodesic.append([round(p, 3) for p in p_t])

        # KL-расстояние:
        kl_q = sum(qi * math.log(qi/p) for qi, p in zip(q, p_star))
        d_fisher = math.sqrt(2 * kl_q)

        return {
            'принцип': 'Геодезические (Амари)',
            'q': q,
            'p*': p_star,
            'e-геодезич': e_geodesic,
            'KL(q||p*)': round(kl_q, 4),
            'd_Fisher': round(d_fisher, 4),
            'финал': e_geodesic[-1],
            '→π/4': all(abs(p - 0.25) < 0.01 for p in e_geodesic[-1]),
            'ЕТД': f'e-геодезич: q→p*; KL={kl_q:.4f}, d_F={d_fisher:.4f}; финал={e_geodesic[-1]}→π/4',
        }


ig = InformationGeometryETD()

fm = ig.fisher_metric_etd()
print(f"Метрика Фишера: g=4·I, det={fm['det(g)']}, Vol={fm['Vol_инф']}")

ef = ig.exponential_family_etd()
print(f"Эксп. семейство: η*={ef['η*']}, A=H: {ef['A=H']}")

kl = ig.kl_divergence_etd()
print(f"KL: KL(баланс)={kl['KL(uniform||p*)']}, BC=1: {kl['=1']}")

geo = ig.geodesics_etd()
print(f"Геодезические: d_F={geo['d_Fisher']}, финал→π/4: {geo['→π/4']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 принципа информационной геометрии** → **4 информационно-геометрических инварианта ЕТД**:

1. **Метрика Фишера** → g=4·I (масштаб = числу сфер!); det=4⁴=256; √det=16
2. **Эксп. семейство** → η*=(0,0,0): π/4-баланс = центр; A(0)=H=log4=2бит (Том 157!)
3. **KL-дивергенция** → KL(p*||p*)=0: баланс = информационный центр; BC=1
4. **Геодезические** → e-геодезич: любое q → p*=(1/4)⁴; d_F=√(2·KL)

> **Информационно-геометрический портрет ЕТД**: Равновесное распределение p*=(1/4)⁴ — информационный центр статистического многообразия 4 сфер. Метрика Фишера g=4·I — масштаб = числу сфер. Натуральный параметр η*=0: π/4-баланс лежит в «нуле» экспоненциального семейства. Все e-геодезические сходятся к p*.

---
*ТОМ 217 / СЕРИЯ VII / ЕТД 2026*
