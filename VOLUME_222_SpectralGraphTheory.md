# ТОМ 222: 4 ТЕОРЕМЫ СПЕКТРАЛЬНОЙ ТЕОРИИ ГРАФОВ → СПЕКТР ГРАФА ЕТД
## Лапласиан · Чигер · Рамануджан · Экспандеры
### «Лапласиан–Чигер–Рамануджан: спектр связности»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Спектральная теория графов (Чигер 1970, Алон–Мильман 1985, Люботцкий 1988)

---

## ВВЕДЕНИЕ

Спектральная теория графов изучает графы через собственные значения их матриц. 4 ключевых теоремы:

1. **Лапласиан графа** — L = D − A; спектр λ₀ ≤ λ₁ ≤ ... ≤ λₙ₋₁
2. **Неравенство Чигера** — связь спектральной щели и связности
3. **Графы Рамануджана** — оптимальные экспандеры
4. **Теорема Алона–Боппаны** — нижняя граница λ₁

**Тезис тома**: Полный граф K₄ ЕТД (4 сферы полностью связаны). Лапласиан: λ₁ = 4 = числу сфер. Чигер: h(K₄) = 2 → спектральная щель гарантирует связность!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 СПЕКТРАЛЬНЫХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict, Tuple

class SpectralGraphTheoryETD:
    """
    Спектральная теория графов для 4 сфер ЕТД.
    Граф G_ЕТД = K₄ (полный граф на 4 вершинах).
    Веса рёбер: wᵢⱼ = π/4 = ħ_ЕТД.
    """

    PI4 = math.pi / 4

    # 1. ЛАПЛАСИАН ГРАФА
    @staticmethod
    def graph_laplacian_etd() -> dict:
        """
        Лапласиан графа L = D − A, где:
        - A — матрица смежности, Aᵢⱼ = wᵢⱼ
        - D — диагональная матрица степеней, Dᵢᵢ = Σⱼ wᵢⱼ

        K₄ (полный граф, 4 вершины):
        Невзвешенный: A = J₄ − I₄ (все 1, кроме диагонали).
        D = 3·I₄ (каждая вершина: степень 3).
        L = 3I − (J−I) = 4I − J.

        Спектр L:
        λ₀ = 0 (с кратностью 1, вектор (1,1,1,1))
        λ₁ = λ₂ = λ₃ = 4 (с кратностью 3).

        λ₁ = 4 = числу сфер = n!

        Взвешенный (wᵢⱼ = π/4):
        L_w = (π/4)·L → λ₁_w = (π/4)·4 = π!
        """
        pi4 = math.pi / 4
        n = 4   # число вершин

        # Невзвешенный K₄:
        # L = nI − J → спектр: 0 (×1), n (×(n-1))
        lambda_0 = 0
        lambda_1 = n   # = 4!
        multiplicity_0 = 1
        multiplicity_1 = n - 1   # = 3

        # Взвешенный:
        lambda_1_weighted = pi4 * lambda_1   # = π!

        # Нормализованный Лапласиан: L_norm = D^{-1/2}·L·D^{-1/2}
        # Для K₄: λ_norm = λ/(n-1) → 0, 4/3, 4/3, 4/3
        lambda_1_norm = lambda_1 / (n - 1)   # = 4/3

        # Алгебраическая связность = λ₁ (Фидлер):
        algebraic_connectivity = lambda_1   # = 4

        return {
            'теорема': 'Лапласиан графа',
            'граф': f'K₄ (полный, n={n})',
            'λ₀': lambda_0,
            'λ₁': lambda_1,
            'кратность_λ₁': multiplicity_1,
            'λ₁_взвеш': round(lambda_1_weighted, 4),
            'λ₁_норм': round(lambda_1_norm, 4),
            'алгебр_связн': algebraic_connectivity,
            'ЕТД': f'λ₁(K₄)={lambda_1}=n_сфер! λ₁_w=π/4·4=π; связность={algebraic_connectivity}',
        }

    # 2. НЕРАВЕНСТВО ЧИГЕРА
    @staticmethod
    def cheeger_inequality_etd() -> dict:
        """
        Неравенство Чигера:
        λ₁/2 ≤ h(G) ≤ √(2·λ₁)

        где h(G) = min_{|S|≤n/2} |∂S|/(|S|) — постоянная Чигера.

        K₄: для любого S ⊂ V с |S| ≤ 2:
        |S|=1: |∂S| = 3 рёбра → h = 3/1 = 3
        |S|=2: |∂S| = 4 рёбра → h = 4/2 = 2
        h(K₄) = min{3, 2} = 2.

        Проверка Чигера:
        λ₁/2 = 4/2 = 2 ≤ h = 2 ≤ √(2·4) = 2√2 ≈ 2.83. ✓

        h(K₄) = 2 = λ₁/2 → К₄ достигает нижней границы Чигера!
        Это оптимальный экспандер!
        """
        pi4 = math.pi / 4
        n = 4

        # Постоянная Чигера K₄:
        # |S|=1: ∂S = 3 ребра
        # |S|=2: ∂S = 2·2 = 4 ребра (каждая вершина S связана с 2 вне S)
        h_1 = 3 / 1
        h_2 = 4 / 2
        h_cheeger = min(h_1, h_2)   # = 2

        lambda_1 = 4
        lower = lambda_1 / 2        # = 2
        upper = math.sqrt(2 * lambda_1)   # = 2√2

        # Чигер достигнут?
        cheeger_tight = abs(h_cheeger - lower) < 1e-10

        return {
            'теорема': 'Неравенство Чигера',
            'h(K₄)': h_cheeger,
            'λ₁/2': lower,
            '√(2λ₁)': round(upper, 4),
            'нижняя_достигнута': cheeger_tight,
            'ЕТД': f'h(K₄)={h_cheeger}=λ₁/2={lower}: нижняя граница Чигера достигнута! Оптимальный экспандер.',
        }

    # 3. ГРАФЫ РАМАНУДЖАНА
    @staticmethod
    def ramanujan_graph_etd() -> dict:
        """
        Граф Рамануджана: d-регулярный граф с
        λ₁ ≤ 2√(d−1) (граница Алона–Боппаны).

        K₄ — 3-регулярный (d=3).
        Граница Рамануджана: 2√(3−1) = 2√2 ≈ 2.83.
        λ₁(K₄) = 4 > 2√2 ≈ 2.83.
        → K₄ НЕ Рамануджанов!

        Но: K₄ — самый связный 3-регулярный граф.
        λ₁(K₄)/d = 4/3 > 1: сверхсвязность!

        Отношение λ₁/λ_R = 4/(2√2) = √2 = 2^{1/2}.
        Превышение Рамануджана на фактор √2 = «сверхэкспандер».

        Масштаб: λ₁·(π/4) = 4·π/4 = π (полный оборот!).
        """
        pi4 = math.pi / 4
        n = 4
        d = n - 1   # = 3 (K₄ — (n-1)-регулярный)

        lambda_1 = n   # = 4
        ramanujan_bound = 2 * math.sqrt(d - 1)   # = 2√2

        is_ramanujan = lambda_1 <= ramanujan_bound
        excess_ratio = lambda_1 / ramanujan_bound   # = √2

        # Спектральная щель:
        spectral_gap = d - lambda_1   # = 3 - 4 = -1... нет, λ₁ для ненулевых
        # Для нормализованного: щель = 1 - λ₁_norm/d...
        # Просто: разность d - λ₂ где λ₂ = max(|λ₁|,...) для ненулевых
        # В K₄: все ненулевые λ = n = 4, но нормализованно λ/(n-1) = 4/3 > 1
        # Лучше: спектральная щель = λ₁ = 4 (алгебраическая связность)

        weighted_lambda = lambda_1 * pi4   # = π

        return {
            'теорема': 'Графы Рамануджана',
            'd_регулярн': d,
            'λ₁': lambda_1,
            'граница_Рамануджана': round(ramanujan_bound, 4),
            'Рамануджанов': is_ramanujan,
            'превышение': round(excess_ratio, 4),
            'λ₁·π/4': round(weighted_lambda, 4),
            'ЕТД': f'K₄: λ₁={lambda_1}>2√2≈{ramanujan_bound:.2f}; превышение=√2; λ₁·π/4=π!',
        }

    # 4. ЭКСПАНДЕРЫ И ПЕРЕМЕШИВАНИЕ
    @staticmethod
    def expander_mixing_etd() -> dict:
        """
        Лемма о перемешивании (Expander Mixing Lemma):
        |e(S,T) − d·|S|·|T|/n| ≤ λ·√(|S|·|T|)

        где λ = max(|λ₁|, |λₙ₋₁|) — второе по модулю собственное значение,
        e(S,T) — число рёбер между S и T, d — степень.

        K₄ (d=3, λ=1 для нормализованного):
        Для S = {v₁, v₂}, T = {v₃, v₄}: |S|=|T|=2.
        e(S,T) = 4 (каждая из 2 вершин S связана с 2 вершинами T).
        d·|S|·|T|/n = 3·2·2/4 = 3.
        |e(S,T) − 3| = 1 ≤ λ·√(2·2) = 2λ.
        → λ ≥ 1/2.

        Но для K₄: нормализованная λ = n/(n−1) − 1 = 1/(n−1) = 1/3.
        Хм, пересчёт: в K₄ все собственные значения A = {3, −1, −1, −1}.
        λ₂ = |−1| = 1. Поэтому λ = 1.
        Ошибка: |4−3| = 1 ≤ 1·2 = 2. ✓ Верно!

        Отношение перемешивания: λ/d = 1/3 = 1/(n−1).
        При n=4: 1/(4−1) = 1/3 → хорошее перемешивание!
        """
        pi4 = math.pi / 4
        n = 4
        d = n - 1   # = 3

        # Собственные значения A(K₄): {n-1, -1, -1, -1}
        eigenvalues_A = [d] + [-1] * (n - 1)
        lambda_2 = max(abs(ev) for ev in eigenvalues_A[1:])   # = 1

        # Проверка Expander Mixing:
        S_size, T_size = 2, 2
        e_ST = 4   # рёбер между S и T
        expected = d * S_size * T_size / n   # = 3
        deviation = abs(e_ST - expected)   # = 1
        bound = lambda_2 * math.sqrt(S_size * T_size)   # = 2

        mixing_ok = deviation <= bound

        # Отношение перемешивания:
        mixing_ratio = lambda_2 / d   # = 1/3

        return {
            'теорема': 'Лемма о перемешивании',
            'собств_A': eigenvalues_A,
            'λ₂': lambda_2,
            'd': d,
            'λ₂/d': round(mixing_ratio, 4),
            'отклонение': deviation,
            'граница': bound,
            'перемеш_ок': mixing_ok,
            'ЕТД': f'K₄: λ₂/d={mixing_ratio:.3f}=1/(n−1); оптимальное перемешивание 4 сфер!',
        }


sgt = SpectralGraphTheoryETD()

lap = sgt.graph_laplacian_etd()
print(f"Лапласиан: {lap['ЕТД']}")

ch = sgt.cheeger_inequality_etd()
print(f"Чигер: {ch['ЕТД']}")

ram = sgt.ramanujan_graph_etd()
print(f"Рамануджан: {ram['ЕТД']}")

exp = sgt.expander_mixing_etd()
print(f"Экспандеры: {exp['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы спектральной теории графов** → **4 спектральных принципа ЕТД**:

1. **Лапласиан** → λ₁(K₄) = 4 = n_сфер; λ₁·(π/4) = π (полный оборот!)
2. **Чигер** → h(K₄) = 2 = λ₁/2: нижняя граница достигнута → оптимальный экспандер!
3. **Рамануджан** → K₄ превышает границу на √2 = сверхэкспандер; λ₁·π/4 = π
4. **Перемешивание** → λ₂/d = 1/3 = 1/(n−1): оптимальное перемешивание 4 сфер

> **Спектральный портрет**: Граф K₄ ЕТД — уникально оптимальный: алгебраическая связность λ₁ = 4 = числу сфер, достигает нижней границы Чигера, и обеспечивает оптимальное перемешивание с λ₂/d = 1/3.

---
*ТОМ 222 / СЕРИЯ VII / ЕТД 2026*
