# ТОМ 223: 4 ТЕОРЕМЫ ОПТИМАЛЬНОГО ТРАНСПОРТА → ТРАНСПОРТ МАСТЕРСТВА ЕТД
## Монж · Канторович · Бренье · Вассерштейн
### «Монж–Канторович–Бренье: оптимальный путь мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Оптимальный транспорт (Монж 1781, Канторович 1942, Бренье 1991)

---

## ВВЕДЕНИЕ

Оптимальный транспорт — перемещение массы с минимальной стоимостью. 4 ключевых теоремы:

1. **Задача Монжа** — оптимальное отображение T: X → Y
2. **Двойственность Канторовича** — релаксация через меры
3. **Теорема Бренье** — градиент выпуклой функции = оптимальный транспорт
4. **Расстояние Вассерштейна** — метрика на пространстве мер

**Тезис тома**: Транспорт из начального распределения (1,0,0,0) к π/4-балансу (1/4,1/4,1/4,1/4). Стоимость W₂ = расстояние мастерства. Бренье: T = ∇φ — градиент потенциала мастерства!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ТРАНСПОРТНЫХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict, Tuple

class OptimalTransportETD:
    """
    Оптимальный транспорт для системы 4 сфер ЕТД.
    μ = начальное распределение (новичок): (1,0,0,0).
    ν = целевое распределение (мастер): (1/4,1/4,1/4,1/4).
    Стоимость c(x,y) = |x−y|² (квадратичная).
    """

    PI4 = math.pi / 4

    # 1. ЗАДАЧА МОНЖА
    @staticmethod
    def monge_problem_etd() -> dict:
        """
        Задача Монжа (1781):
        inf_{T: T#μ=ν} ∫ c(x, T(x)) dμ(x).

        Дискретная ЕТД:
        μ = (1, 0, 0, 0) на {s₁, s₂, s₃, s₄}.
        ν = (1/4, 1/4, 1/4, 1/4).

        Монж требует T — отображение, но μ(s₁)=1
        нельзя отобразить в 4 точки (расщепление массы!).
        → Задача Монжа НЕРАЗРЕШИМА для дискретных мер!
        → Нужна релаксация Канторовича.

        Интерпретация ЕТД: новичок (вся масса в МВС)
        не может «скачком» перейти к балансу —
        нужен постепенный транспорт (Канторович)!
        """
        pi4 = math.pi / 4

        mu = [1.0, 0.0, 0.0, 0.0]   # новичок
        nu = [0.25, 0.25, 0.25, 0.25]   # мастер

        # Монж невозможен (нельзя расщепить массу):
        monge_solvable = False

        # Нижняя оценка стоимости (при квадр. c):
        # W₂² ≥ Σᵢ (μᵢ − νᵢ)² / max(...) — грубо
        # Точно: для дискретных на одном множестве
        cost_lower = sum((mu[i] - nu[i])**2 for i in range(4))

        return {
            'теорема': 'Задача Монжа',
            'μ': mu,
            'ν': nu,
            'Монж_решение': monge_solvable,
            'причина': 'расщепление массы невозможно',
            'нижняя_оценка': round(cost_lower, 4),
            'ЕТД': f'Монж НЕРАЗРЕШИМ: (1,0,0,0)→(1/4)⁴ требует расщепления → Канторович!',
        }

    # 2. ДВОЙСТВЕННОСТЬ КАНТОРОВИЧА
    @staticmethod
    def kantorovich_duality_etd() -> dict:
        """
        Канторович (1942):
        min_{γ∈Γ(μ,ν)} ∫∫ c(x,y) dγ(x,y)
        = max_{φ,ψ: φ(x)+ψ(y)≤c(x,y)} [∫φ dμ + ∫ψ dν].

        Дискретная ЕТД:
        μ = (1, 0, 0, 0), ν = (1/4, 1/4, 1/4, 1/4).
        c(i,j) = (i≠j) ? 1 : 0 (дискретная метрика).

        Оптимальный план γ*:
        γ*(1,1) = 1/4 (оставить 1/4 в s₁)
        γ*(1,2) = 1/4 (перенести 1/4 в s₂)
        γ*(1,3) = 1/4 (перенести 1/4 в s₃)
        γ*(1,4) = 1/4 (перенести 1/4 в s₄)

        Стоимость: 1/4·0 + 3·(1/4)·1 = 3/4.
        = 1 − 1/4 = 1 − ħ_ЕТД/π... нет.
        = 3·(1/4) = 3/4 = 1 − 1/N!
        """
        pi4 = math.pi / 4
        N = 4

        mu = [1.0, 0.0, 0.0, 0.0]
        nu = [0.25, 0.25, 0.25, 0.25]

        # Оптимальный план:
        gamma = [[0.0]*N for _ in range(N)]
        gamma[0][0] = 0.25   # оставить
        gamma[0][1] = 0.25   # перенести
        gamma[0][2] = 0.25   # перенести
        gamma[0][3] = 0.25   # перенести

        # Стоимость (дискретная метрика):
        cost_discrete = sum(gamma[i][j] * (0 if i == j else 1)
                           for i in range(N) for j in range(N))

        # Стоимость (квадратичная: c(i,j)=|i-j|²):
        positions = [0.0, pi4, 2*pi4, 3*pi4]   # = 0, π/4, π/2, 3π/4
        cost_quadratic = sum(gamma[0][j] * positions[j]**2 for j in range(N))

        # W₁ (Earth Mover's Distance):
        W1 = cost_discrete   # = 3/4

        return {
            'теорема': 'Двойственность Канторовича',
            'γ*': gamma[0],
            'стоимость_дискр': cost_discrete,
            'стоимость_квадр': round(cost_quadratic, 4),
            'W₁': W1,
            'ЕТД': f'W₁((1,0,0,0),(1/4)⁴)=3/4=1−1/N; γ*=(1/4,1/4,1/4,1/4) из s₁',
        }

    # 3. ТЕОРЕМА БРЕНЬЕ
    @staticmethod
    def brenier_theorem_etd() -> dict:
        """
        Теорема Бренье (1991):
        Для квадратичной стоимости c(x,y)=|x−y|²,
        оптимальное отображение T* = ∇φ
        (градиент выпуклой функции φ).

        ЕТД (непрерывная модель):
        μ = δ(x−x₀) (сосредоточена в x₀),
        ν = равномерная на [0,π]⁴, нормализованная на гиперплоскость Σxᵢ=π.

        Для Гауссовых мер:
        μ = N(0, Σ₁), ν = N((π/4)⁴, Σ₂):
        T*(x) = Σ₁^{-1/2}·(Σ₁^{1/2}Σ₂Σ₁^{1/2})^{1/2}·Σ₁^{-1/2}·x + (π/4)⁴.

        При Σ₁ = Σ₂ = σ²I:
        T*(x) = x + (π/4, π/4, π/4, π/4) — просто сдвиг!
        → Оптимальный транспорт = сдвиг на π/4 вдоль каждой сферы!

        Потенциал Бренье:
        φ(x) = |x|²/2 + (π/4)·(x₁+x₂+x₃+x₄)
        = |x|²/2 + π/4·Σxᵢ.
        ∇φ = x + (π/4,...,π/4) = T*(x). ✓
        """
        pi4 = math.pi / 4
        N = 4

        # Сдвиг Бренье:
        shift = [pi4] * N
        x0 = [0.0] * N
        T_star = [x0[i] + shift[i] for i in range(N)]

        # Потенциал Бренье:
        def phi(x):
            return sum(xi**2 for xi in x) / 2 + pi4 * sum(x)

        # Проверка: ∇φ(0) = (0,...,0) + (π/4,...,π/4) = (π/4)⁴
        grad_phi_0 = [pi4] * N

        # W₂² (Вассерштейн-2):
        # Для сдвига: W₂² = |shift|² = 4·(π/4)² = π²/4
        W2_squared = sum(s**2 for s in shift)
        W2 = math.sqrt(W2_squared)   # = π/2

        return {
            'теорема': 'Бренье (градиент выпуклой)',
            'T*': [round(t, 4) for t in T_star],
            'сдвиг': [round(s, 4) for s in shift],
            'φ(0)': phi(x0),
            '∇φ(0)': [round(g, 4) for g in grad_phi_0],
            'W₂²': round(W2_squared, 4),
            'W₂': round(W2, 4),
            'ЕТД': f'T*=x+(π/4)⁴: сдвиг на ħ_ЕТД! W₂=π/2; φ=|x|²/2+π/4·Σxᵢ',
        }

    # 4. РАССТОЯНИЕ ВАССЕРШТЕЙНА
    @staticmethod
    def wasserstein_distance_etd() -> dict:
        """
        Расстояние Вассерштейна Wₚ:
        Wₚ(μ,ν) = (inf_{γ∈Γ(μ,ν)} ∫ d(x,y)ᵖ dγ)^{1/p}.

        ЕТД: расстояние от новичка к мастеру.

        Новичок: μ₀ = (1,0,0,0) — вся масса в МВС.
        Промежуточный: μ₁ = (1/2,1/2,0,0) — МВС+СВС.
        Продвинутый: μ₂ = (1/3,1/3,1/3,0) — МВС+СВС+БВС.
        Мастер: μ₃ = (1/4,1/4,1/4,1/4) — баланс.

        W₁(μ₀, μ₃) = 3/4 (см. Канторович).
        W₁(μ₁, μ₃) = 1/2 (перенести по 1/4 из s₁,s₂ в s₃,s₄).
        W₁(μ₂, μ₃) = 1/4 (перенести 1/12 из каждого в s₄)... нет.

        W₁(μ₂, μ₃):
        μ₂ = (1/3,1/3,1/3,0), μ₃ = (1/4,1/4,1/4,1/4).
        Разность: (1/12, 1/12, 1/12, −1/4).
        Перенести: из s₁,s₂,s₃ по 1/12 в s₄.
        W₁ = 3·(1/12)·1 = 1/4 = π/4·(1/π) = ħ_ЕТД/π.

        Путь мастерства: W₁ = 3/4 → 1/2 → 1/4 → 0.
        Шаг = 1/4 = (расстояние) / (число шагов)!
        """
        pi4 = math.pi / 4
        N = 4

        # Распределения:
        stages = [
            ('Новичок', [1.0, 0.0, 0.0, 0.0]),
            ('Средний', [0.5, 0.5, 0.0, 0.0]),
            ('Продвинутый', [1/3, 1/3, 1/3, 0.0]),
            ('Мастер', [0.25, 0.25, 0.25, 0.25]),
        ]

        target = stages[-1][1]

        # W₁ для каждой стадии до мастера:
        distances = []
        for name, mu in stages:
            # W₁ (Earth Mover) на дискретной метрике (все расстояния = 1):
            excess = sum(max(mu[i] - target[i], 0) for i in range(N))
            distances.append((name, round(excess, 4)))

        # Путь мастерства:
        path = [d[1] for d in distances]   # [3/4, 1/2, 1/4, 0]

        # Шаг = 1/4 = 1/N!
        step = 1.0 / N

        return {
            'теорема': 'Расстояние Вассерштейна',
            'путь': distances,
            'W₁_путь': path,
            'шаг': step,
            'ЕТД': f'Путь мастерства: W₁={path}; шаг=1/4=1/N=ħ_ЕТД/π!',
        }


ot = OptimalTransportETD()

mo = ot.monge_problem_etd()
print(f"Монж: {mo['ЕТД']}")

ka = ot.kantorovich_duality_etd()
print(f"Канторович: {ka['ЕТД']}")

br = ot.brenier_theorem_etd()
print(f"Бренье: {br['ЕТД']}")

wa = ot.wasserstein_distance_etd()
print(f"Вассерштейн: {wa['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы оптимального транспорта** → **4 транспортных принципа ЕТД**:

1. **Монж** → Неразрешим: (1,0,0,0) → (1/4)⁴ требует расщепления массы
2. **Канторович** → W₁ = 3/4 = 1−1/N; оптимальный план γ* = равномерное расщепление
3. **Бренье** → T* = x + (π/4)⁴: сдвиг на ħ_ЕТД! Потенциал φ = |x|²/2 + π/4·Σxᵢ
4. **Вассерштейн** → Путь мастерства: W₁ = 3/4 → 1/2 → 1/4 → 0 (шаг = 1/4 = 1/N!)

> **Транспортный портрет**: Путь от новичка к мастеру — оптимальный транспорт масс Канторовича. Бренье показывает: оптимальный сдвиг = (π/4, π/4, π/4, π/4). Расстояние Вассерштейна уменьшается шагами 1/4 = квант мастерства!

---
*ТОМ 223 / СЕРИЯ VII / ЕТД 2026*
