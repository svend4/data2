# ТОМ 231: 4 ТЕОРЕМЫ ТЕОРИИ ПРЕДСТАВЛЕНИЙ → ПРЕДСТАВЛЕНИЯ ДВИЖЕНИЙ ЕТД
## Вейль · Шур · Питер–Вейль · Бранчинг
### «Вейль–Шур–Питер: симметрии мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория представлений (Вейль 1925, Шур 1901, Питер–Вейль 1927)

---

## ВВЕДЕНИЕ

Теория представлений изучает реализацию абстрактных групп как матричных преобразований. 4 ключевых теоремы:

1. **Формула характера Вейля** — точная формула для характеров простых модулей
2. **Лемма Шура** — морфизмы между неприводимыми
3. **Теорема Питера–Вейля** — разложение L²(G) на неприводимые
4. **Правило ветвления** — ограничение представлений на подгруппы

**Тезис тома**: Группа ЕТД G = SO(4) ≅ (SU(2)×SU(2))/ℤ₂. Представления индексируются парами (j₁,j₂). Фундаментальное: (1/2,1/2) — 4-мерное. Характер Вейля при π/4 даёт dim = 4!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ПРИНЦИПА ПРЕДСТАВЛЕНИЙ ЕТД

```python
import math
from typing import List, Dict, Tuple

class RepresentationTheoryETD:
    """
    Теория представлений для группы ЕТД.
    G = SO(4) ≅ (SU(2)×SU(2))/ℤ₂.
    Алгебра Ли: so(4) ≅ su(2) ⊕ su(2).
    Представления: V_{j₁,j₂}, dim = (2j₁+1)(2j₂+1).
    """

    PI4 = math.pi / 4

    # 1. ФОРМУЛА ХАРАКТЕРА ВЕЙЛЯ
    @staticmethod
    def weyl_character_formula_etd() -> dict:
        """
        Формула характера Вейля для SU(2):
        χⱼ(θ) = sin((2j+1)θ) / sin(θ).

        При θ = π/4 = ħ_ЕТД:
        χ₀(π/4) = sin(π/4)/sin(π/4) = 1.
        χ_{1/2}(π/4) = sin(2·π/4)/sin(π/4) = sin(π/2)/sin(π/4)
                     = 1/(√2/2) = √2.
        χ₁(π/4) = sin(3π/4)/sin(π/4) = (√2/2)/(√2/2) = 1.
        χ_{3/2}(π/4) = sin(4·π/4)/sin(π/4) = sin(π)/sin(π/4) = 0!

        Замечательно: χ_{3/2}(π/4) = 0!
        j = 3/2 = (N−1)/2: представление «обнуляется» при ħ_ЕТД!

        Для SO(4) = SU(2)×SU(2):
        χ_{j₁,j₂}(θ₁,θ₂) = χ_{j₁}(θ₁)·χ_{j₂}(θ₂).
        При θ₁ = θ₂ = π/4:
        χ_{1/2,1/2}(π/4,π/4) = √2·√2 = 2.
        dim V_{1/2,1/2} = (2·1/2+1)² = 4 = N_сфер!
        """
        pi4 = math.pi / 4

        # Характер SU(2):
        def chi_su2(j, theta):
            if abs(math.sin(theta)) < 1e-10:
                return 2*j + 1   # dim (предел при θ→0)
            return math.sin((2*j+1)*theta) / math.sin(theta)

        # Значения при θ = π/4:
        characters = {}
        for j2 in range(4):  # j = 0, 1/2, 1, 3/2
            j = j2 / 2
            chi_val = chi_su2(j, pi4)
            characters[f'j={j}'] = round(chi_val, 6)

        # SO(4) = SU(2)×SU(2):
        chi_half_half = chi_su2(0.5, pi4) * chi_su2(0.5, pi4)
        dim_fund = int((2*0.5+1) * (2*0.5+1))   # = 4

        return {
            'теорема': 'Формула характера Вейля',
            'χ_SU(2)': characters,
            'χ_{3/2}(π/4)': characters['j=1.5'],
            'χ_{1/2,1/2}': round(chi_half_half, 4),
            'dim_V_{1/2,1/2}': dim_fund,
            'ЕТД': f'χ_{{3/2}}(π/4)=0 (обнуление!); dim V_{{1/2,1/2}}={dim_fund}=N_сфер!',
        }

    # 2. ЛЕММА ШУРА
    @staticmethod
    def schur_lemma_etd() -> dict:
        """
        Лемма Шура:
        (1) Если V, W неприводимы и f: V → W — морфизм,
            то f = 0 или f — изоморфизм.
        (2) Если V неприводимо над ℂ, то End_G(V) ≅ ℂ.

        Ортогональность Шура:
        ∫_G χ_V(g)·χ̄_W(g) dg = δ_{V,W}.

        ЕТД: Для SO(4), неприводимые V_{j₁,j₂}:
        ⟨χ_{j₁,j₂}, χ_{j₁',j₂'}⟩ = δ_{j₁j₁'}·δ_{j₂j₂'}.

        End_{SO(4)}(V_{1/2,1/2}) ≅ ℂ.
        dim End = 1 (единственный морфизм = скаляр!).

        Число неприводимых V с dim ≤ 4:
        dim = 1: V_{0,0} (тривиальное)
        dim = 2: V_{1/2,0}, V_{0,1/2}
        dim = 3: V_{1,0}, V_{0,1}
        dim = 4: V_{1/2,1/2} (фундаментальное ЕТД!)
        Всего: 6 представлений с dim ≤ 4.
        Но dim=4 только ОДНО: V_{1/2,1/2}!
        """
        pi4 = math.pi / 4
        N = 4

        # Неприводимые с dim ≤ N:
        irreps = []
        for j1_2 in range(4):
            for j2_2 in range(4):
                j1 = j1_2 / 2
                j2 = j2_2 / 2
                dim = int((2*j1+1) * (2*j2+1))
                if dim <= N:
                    irreps.append((j1, j2, dim))

        n_irreps = len(irreps)
        dim4_reps = [(j1,j2) for j1,j2,d in irreps if d == N]

        return {
            'теорема': 'Лемма Шура',
            'End(V_{1/2,1/2})': 'ℂ (dim=1)',
            'неприводимых_dim≤4': n_irreps,
            'dim=4_представлений': dim4_reps,
            'единственное_dim4': len(dim4_reps) == 1,
            'ЕТД': f'V_{{1/2,1/2}} — единственное dim={N} представление SO(4)!',
        }

    # 3. ТЕОРЕМА ПИТЕРА–ВЕЙЛЯ
    @staticmethod
    def peter_weyl_etd() -> dict:
        """
        Теорема Питера–Вейля:
        L²(G) = ⊕̂_π (dim π)·V_π
        (ортогональное разложение L² по неприводимым).

        Для конечной группы |G|:
        |G| = Σ_π (dim π)².

        SO(4): непрерывная группа, dim = 6.
        Разложение L²(SO(4)):
        = ⊕_{j₁,j₂} (2j₁+1)(2j₂+1)·V_{j₁,j₂}.

        Касимир для V_{j₁,j₂}:
        C = j₁(j₁+1) + j₂(j₂+1).
        Для V_{1/2,1/2}: C = 3/4 + 3/4 = 3/2.

        Нормализованный Касимир: C/dim = (3/2)/4 = 3/8.
        Или: C·(π/4) = (3/2)·(π/4) = 3π/8.
        При j₁=j₂=0: C = 0 (тривиальное).
        Разность: ΔC = 3/2 − 0 = 3/2 (щель!).
        ΔC·(π/4) = 3π/8 = Δq·Δp (неопределённость, Том 207!).
        """
        pi4 = math.pi / 4

        # Касимир:
        def casimir(j1, j2):
            return j1*(j1+1) + j2*(j2+1)

        C_fund = casimir(0.5, 0.5)   # = 3/2
        C_triv = casimir(0, 0)        # = 0
        gap = C_fund - C_triv          # = 3/2

        # Размерность группы:
        dim_group = 6   # dim SO(4) = 4·3/2

        # Объём SO(4):
        vol_SO4 = 2 * math.pi**2   # = Vol(S³)·... нет, Vol(SO(4)) = 8π²

        # C·π/4:
        C_weighted = C_fund * pi4   # = 3π/8

        return {
            'теорема': 'Питер–Вейль',
            'C_{1/2,1/2}': C_fund,
            'щель': gap,
            'C·π/4': round(C_weighted, 4),
            'dim_SO(4)': dim_group,
            'ЕТД': f'C(V_{{1/2,1/2}})={C_fund}; щель={gap}; C·π/4={C_weighted:.4f}=3π/8',
        }

    # 4. ПРАВИЛО ВЕТВЛЕНИЯ
    @staticmethod
    def branching_rule_etd() -> dict:
        """
        Правило ветвления: ограничение V_G на подгруппу H ⊂ G.

        SO(4) ⊃ SO(3):
        Res^{SO(4)}_{SO(3)} V_{j₁,j₂} = ⊕_{j=|j₁−j₂|}^{j₁+j₂} V_j.

        Для V_{1/2,1/2}:
        Res = V₀ ⊕ V₁.
        dim: 4 = 1 + 3. ✓

        V₀ = скаляр (ЧВС — «четвёртое» скрытое).
        V₁ = вектор (МВС, СВС, БВС — видимые 3 сферы).

        Разложение 4 = 1 + 3: «невидимая» + «видимые» сферы!

        SO(4) ⊃ SO(2)×SO(2):
        V_{1/2,1/2} = ⊕_{m₁,m₂} ℂ_{m₁,m₂}.
        Веса: (±1/2, ±1/2) — 4 весовых вектора!
        Число весов = 4 = N_сфер.
        """
        pi4 = math.pi / 4
        N = 4

        # Ветвление SO(4) → SO(3):
        j1, j2 = 0.5, 0.5
        branching_SO3 = list(range(abs(int(2*j1-2*j2)), int(2*j1+2*j2)+1, 2))
        branching_SO3 = [j/2 for j in branching_SO3]   # [0, 1]

        dims = [int(2*j+1) for j in branching_SO3]
        total_dim = sum(dims)   # = 1 + 3 = 4 ✓

        # Весовые векторы:
        weights = []
        for m1 in [-0.5, 0.5]:
            for m2 in [-0.5, 0.5]:
                weights.append((m1, m2))
        n_weights = len(weights)   # = 4!

        # Разложение:
        decomposition = {
            'V₀': {'dim': 1, 'ЕТД': 'ЧВС (скрытая)'},
            'V₁': {'dim': 3, 'ЕТД': 'МВС+СВС+БВС (видимые)'},
        }

        return {
            'теорема': 'Правило ветвления',
            'Res_SO(3)': [f'V_{j}' for j in branching_SO3],
            'dims': dims,
            'total': total_dim,
            'весов': n_weights,
            'разложение': f'4 = 1 + 3 (скрытая + видимые)',
            'ЕТД': f'V_{{1/2,1/2}}→V₀⊕V₁: 4=1+3; {n_weights} весов; ЧВС скрытая, МСБ видимые',
        }


rt = RepresentationTheoryETD()

wc = rt.weyl_character_formula_etd()
print(f"Вейль: {wc['ЕТД']}")

sl = rt.schur_lemma_etd()
print(f"Шур: {sl['ЕТД']}")

pw = rt.peter_weyl_etd()
print(f"Питер-Вейль: {pw['ЕТД']}")

br = rt.branching_rule_etd()
print(f"Ветвление: {br['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории представлений** → **4 принципа представлений ЕТД**:

1. **Вейль** → χ_{3/2}(π/4) = 0 (обнуление!); dim V_{1/2,1/2} = 4 = N_сфер
2. **Шур** → V_{1/2,1/2} — единственное 4-мерное неприводимое SO(4)
3. **Питер–Вейль** → Касимир C = 3/2; щель ΔC·(π/4) = 3π/8 (неопределённость!)
4. **Ветвление** → 4 = 1 + 3: ЧВС (скрытая) + МВС+СВС+БВС (видимые)

> **Представленческий портрет**: SO(4) имеет единственное 4-мерное неприводимое V_{1/2,1/2} — это пространство ЕТД. Ветвление на SO(3) даёт 4 = 1 + 3: одна скрытая сфера (ЧВС) + три видимые.

---
*ТОМ 231 / СЕРИЯ VII / ЕТД 2026*
