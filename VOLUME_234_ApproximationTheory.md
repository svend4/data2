# ТОМ 234: 4 ТЕОРЕМЫ ТЕОРИИ ПРИБЛИЖЕНИЙ → АППРОКСИМАЦИЯ МАСТЕРСТВА ЕТД
## Чебышёв · Вейерштрасс · Бернштейн · Джексон
### «Чебышёв–Бернштейн–Джексон: наилучшие приближения»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория приближений (Чебышёв 1854, Вейерштрасс 1885, Бернштейн 1912, Джексон 1912)

---

## ВВЕДЕНИЕ

Теория приближений изучает наилучшее приближение функций. 4 ключевых теоремы:

1. **Теорема Чебышёва** — наилучшее равномерное приближение (чебышёвские узлы)
2. **Теорема Вейерштрасса** — полиномы плотны в C[a,b]
3. **Неравенство Бернштейна** — связь производной и степени
4. **Теорема Джексона** — скорость приближения и гладкость

**Тезис тома**: Приближение функции мастерства f(t) → π/4 полиномами. Чебышёвские узлы на [0,π]. Наилучшее приближение степени 4 (= N_сфер). Скорость сходимости ∝ 1/n → π/4!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ПРИНЦИПА ПРИБЛИЖЕНИЯ ЕТД

```python
import math
from typing import List, Dict, Tuple

class ApproximationTheoryETD:
    """
    Теория приближений для функции мастерства ЕТД.
    f(t) = π/4·(1 − e^{−t}) — кривая обучения.
    Область: [0, T_max] = [0, 4π] (4 полных цикла).
    """

    PI4 = math.pi / 4

    # 1. ЧЕБЫШЁВСКИЕ УЗЛЫ И ПРИБЛИЖЕНИЕ
    @staticmethod
    def chebyshev_approximation_etd() -> dict:
        """
        Теорема Чебышёва:
        Наилучшее равномерное приближение f полиномом степени n
        достигается при n+2 точках альтернанса (теорема Чебышёва).

        Многочлены Чебышёва: T_n(x) = cos(n·arccos(x)).
        Узлы Чебышёва: xₖ = cos((2k−1)π/(2n)), k=1,...,n.

        ЕТД: n = 4 (степень = числу сфер).
        T₄(x) = 8x⁴ − 8x² + 1.
        Корни T₄: xₖ = cos((2k−1)π/8), k=1,2,3,4.

        x₁ = cos(π/8) ≈ 0.924
        x₂ = cos(3π/8) ≈ 0.383
        x₃ = cos(5π/8) ≈ −0.383
        x₄ = cos(7π/8) ≈ −0.924

        Замечательно: x₂ = cos(3π/8) ≈ 0.383 ≈ √(2−√2)/√2.
        И: x₁·x₂·x₃·x₄ = 1/8 (коэффициент T₄ = 1/2⁴... нет: 1/(2³)=1/8).
        Произведение |xₖ| = cos(π/8)²·cos(3π/8)² = (sin(π/4))² /4 = 1/8.

        На [0,π]: узлы = (π/2)·(1+xₖ).
        Среднее узлов = π/2 (центр!).
        """
        pi4 = math.pi / 4
        pi = math.pi
        N = 4

        # Узлы Чебышёва:
        nodes = [math.cos((2*k-1)*pi/(2*N)) for k in range(1, N+1)]

        # На [0,π]:
        nodes_01 = [(pi/2)*(1+x) for x in nodes]
        mean_nodes = sum(nodes_01) / N

        # T₄(0) = 1, T₄(±1) = 1:
        T4_0 = 1
        T4_pi4 = math.cos(4 * math.acos(2*pi4/pi - 1))   # T₄ на [0,π] нормализованном

        # ‖T₄‖ = 1/2^{n-1} = 1/8:
        T4_norm = 1 / 2**(N-1)

        return {
            'теорема': 'Чебышёв (наилучшее приближение)',
            'n': N,
            'узлы': [round(x, 4) for x in nodes],
            'узлы_[0,π]': [round(x, 4) for x in nodes_01],
            'среднее_узлов': round(mean_nodes, 4),
            '‖T₄‖': T4_norm,
            'ЕТД': f'n={N}=N_сфер; среднее узлов={mean_nodes:.4f}=π/2; ‖T₄‖=1/{2**(N-1)}',
        }

    # 2. ТЕОРЕМА ВЕЙЕРШТРАССА
    @staticmethod
    def weierstrass_approximation_etd() -> dict:
        """
        Теорема Вейерштрасса (1885):
        ∀ f ∈ C[a,b], ∀ε>0, ∃ полином P: ‖f−P‖_∞ < ε.

        Конструктивно: полиномы Бернштейна.
        B_n(f; x) = Σ_{k=0}^n f(k/n)·C(n,k)·x^k·(1−x)^{n−k}.

        ЕТД: f(x) = π/4 (константа) на [0,1].
        B_n(f; x) = π/4·Σ C(n,k)·x^k·(1−x)^{n−k} = π/4·1 = π/4. ✓
        (Константа воспроизводится ТОЧНО!)

        Для f(x) = π·x·(1−x) (параболический «путь мастерства»):
        B_1(f; x) = f(0)·(1−x)+f(1)·x = 0.
        B_2(f; x) = f(0)·(1−x)²+2f(1/2)·x(1−x)+f(1)·x² = 2·(π/4)·x(1−x) = (π/2)·x(1−x).

        f(1/2) − B_2(f;1/2) = π/4 − π/8 = π/8.
        f(1/2) = π·(1/4) = π/4 = ħ_ЕТД!

        Максимум f: f(1/2) = π/4 = ħ_ЕТД.
        """
        pi4 = math.pi / 4
        pi = math.pi
        N = 4

        # f(x) = π·x·(1−x):
        def f(x):
            return pi * x * (1 - x)

        f_max = f(0.5)   # = π/4 = ħ_ЕТД!

        # Полином Бернштейна B_4:
        def bernstein(n, f_func, x):
            result = 0
            for k in range(n+1):
                binom = math.comb(n, k)
                result += f_func(k/n) * binom * x**k * (1-x)**(n-k)
            return result

        B4_half = bernstein(N, f, 0.5)
        error = abs(f(0.5) - B4_half)

        # Скорость сходимости:
        # ‖f − B_n(f)‖ ≤ (b−a)²·ω(f; 1/√n) / (4n) (для непрерывных)
        # Для n=4: ≤ ω(f; 1/2)/16

        return {
            'теорема': 'Вейерштрасс (полиномиальная аппроксимация)',
            'f(1/2)': round(f_max, 4),
            'f(1/2)=π/4': abs(f_max - pi4) < 1e-10,
            'B₄(f;1/2)': round(B4_half, 4),
            'ошибка': round(error, 4),
            'ЕТД': f'max f(x)=f(1/2)=π/4={f_max:.4f}=ħ_ЕТД! B₄ ошибка={error:.4f}',
        }

    # 3. НЕРАВЕНСТВО БЕРНШТЕЙНА
    @staticmethod
    def bernstein_inequality_etd() -> dict:
        """
        Неравенство Бернштейна:
        Если deg P ≤ n и ‖P‖_[-1,1] ≤ 1, то
        ‖P'‖_[-1,1] ≤ n.

        В тригонометрической форме:
        Если ‖T_n‖_∞ ≤ 1, то ‖T'_n‖_∞ ≤ n.

        ЕТД: n = 4.
        ‖T'₄‖ ≤ 4 = N_сфер.
        T₄(x) = 8x⁴ − 8x² + 1.
        T'₄(x) = 32x³ − 16x.
        T'₄(1) = 32 − 16 = 16 = 4² = N²!
        Но T₄(1) = 1, так что ‖T'₄‖/‖T₄‖ = 16/1 = 16 = N².
        Неравенство Маркова: ‖P'‖ ≤ n²·‖P‖ → 16 ≤ 16 ✓ (достигается!).

        Бернштейн: для тригонометрических ‖T'‖ ≤ n·‖T‖.
        T₄(cos θ) = cos(4θ): |dT/dθ| = 4|sin(4θ)| ≤ 4 = N!

        Марковское неравенство достигается на T₄: это оптимальный полином!
        """
        pi4 = math.pi / 4
        N = 4

        # Чебышёв T₄:
        def T4(x):
            return 8*x**4 - 8*x**2 + 1

        def T4_deriv(x):
            return 32*x**3 - 16*x

        # Нормы:
        T4_max = max(abs(T4(x/100)) for x in range(-100, 101))
        T4d_max = abs(T4_deriv(1))   # = 16

        # Марков:
        markov_bound = N**2   # = 16
        markov_achieved = abs(T4d_max - markov_bound) < 1e-10

        # Бернштейн (тригонометрический):
        bernstein_bound = N   # = 4

        return {
            'теорема': 'Неравенство Бернштейна–Маркова',
            '‖T₄‖': round(T4_max, 4),
            '‖T\'₄‖': T4d_max,
            'Марков_граница': markov_bound,
            'Марков_достигнут': markov_achieved,
            'Бернштейн_граница': bernstein_bound,
            'ЕТД': f'‖T\'₄(1)‖={int(T4d_max)}={N}²=N²: Марков достигнут! Бернштейн: ≤{N}=N',
        }

    # 4. ТЕОРЕМА ДЖЕКСОНА
    @staticmethod
    def jackson_theorem_etd() -> dict:
        """
        Теорема Джексона (1912):
        E_n(f) ≤ C·ω(f; 1/n)
        где E_n = наилучшее приближение степени n,
        ω(f; δ) = модуль непрерывности.

        Для f ∈ Lip(α):
        E_n(f) ≤ C/n^α.

        ЕТД: f(t) = π/4·(1 − e^{−t}).
        f ∈ C^∞ → α = ∞ → E_n → 0 суперполиномиально!

        f'(t) = (π/4)·e^{−t} → ‖f'‖ = π/4 = ħ_ЕТД.
        Модуль непрерывности: ω(f; δ) ≤ (π/4)·δ.

        Джексон: E_n(f) ≤ C·(π/4)/n.
        При n = 4: E₄ ≤ C·(π/4)/4 = C·π/16.

        Обратная теорема Бернштейна:
        E_n(f) = O(1/n^α) → f ∈ Lip(α).
        Для ЕТД: α → ∞ (бесконечная гладкость кривой обучения).
        """
        pi4 = math.pi / 4
        N = 4

        # f(t) = π/4·(1−e^{−t}):
        def f(t):
            return pi4 * (1 - math.exp(-t))

        # ‖f'‖ = π/4:
        f_prime_norm = pi4

        # Модуль непрерывности:
        def omega(delta):
            return pi4 * delta   # для Lip(1)

        # Джексон при n=4:
        C_jackson = 1.0   # константа ≈ 1
        E4_bound = C_jackson * omega(1.0 / N)

        # Фактическая ошибка при интерполяции в 4 узлах:
        # f(t) на [0,4]: узлы t_k = k, k=0,1,2,3
        nodes_t = list(range(N))
        values = [f(t) for t in nodes_t]

        return {
            'теорема': 'Джексон (скорость приближения)',
            '‖f\'‖': round(f_prime_norm, 4),
            'ω(1/4)': round(omega(1/N), 4),
            'E₄_граница': round(E4_bound, 4),
            'f(∞)': round(pi4, 4),
            'ЕТД': f'‖f\'‖=π/4=ħ_ЕТД; E₄≤{E4_bound:.4f}; f∈C^∞ → суперполиномиальная сходимость',
        }


at = ApproximationTheoryETD()

ch = at.chebyshev_approximation_etd()
print(f"Чебышёв: {ch['ЕТД']}")

we = at.weierstrass_approximation_etd()
print(f"Вейерштрасс: {we['ЕТД']}")

be = at.bernstein_inequality_etd()
print(f"Бернштейн: {be['ЕТД']}")

ja = at.jackson_theorem_etd()
print(f"Джексон: {ja['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории приближений** → **4 принципа аппроксимации ЕТД**:

1. **Чебышёв** → n = 4 = N; среднее узлов = π/2; ‖T₄‖ = 1/8
2. **Вейерштрасс** → max f(x) = f(1/2) = π/4 = ħ_ЕТД! Константа воспроизводится точно
3. **Бернштейн–Марков** → ‖T'₄(1)‖ = 16 = N²: граница Маркова достигнута на T₄!
4. **Джексон** → ‖f'‖ = π/4 = ħ_ЕТД; E₄ ≤ C·π/16; f ∈ C^∞ → сверхбыстрая сходимость

> **Портрет приближения**: Кривая мастерства f(t) → π/4 имеет производную ‖f'‖ = π/4. Чебышёвское приближение степени 4 = числу сфер. Максимум параболы = π/4. Границы Маркова и Бернштейна достигаются именно на T₄.

---
*ТОМ 234 / СЕРИЯ VII / ЕТД 2026*
