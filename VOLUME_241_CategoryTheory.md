# ТОМ 241: 4 ТЕОРЕМЫ ТЕОРИИ КАТЕГОРИЙ → КАТЕГОРНАЯ СТРУКТУРА ЕТД
## Йонеда · Присоединённые · Пределы · Монады
### «Йонеда–Макклейн–Кан: категорные основания мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория категорий (Макклейн 1945, Йонеда 1954, Кан 1958)

---

## ВВЕДЕНИЕ

Теория категорий — «математика математики», язык структур и отношений. 4 ключевых теоремы:

1. **Лемма Йонеды** — представимые функторы полностью определяются
2. **Присоединённые функторы** — универсальные конструкции
3. **Теорема о пределах** — существование пределов и копределов
4. **Монады** — алгебраические теории как эндофункторы

**Тезис тома**: Категория ЕТД = C с 4 объектами (сферами). Hom(C) ≅ матрица переходов. Йонеда: каждая сфера определяется отношениями с остальными. Предел = (π/4)⁴ (уравнитель)!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 КАТЕГОРНЫХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict, Tuple

class CategoryTheoryETD:
    """
    Теория категорий для системы ЕТД.
    Категория C_ЕТД:
    Объекты: {МВС, СВС, БВС, ЧВС} = {1,2,3,4}.
    Морфизмы: переходы между сферами + тождественные.
    """

    PI4 = math.pi / 4

    # 1. ЛЕММА ЙОНЕДЫ
    @staticmethod
    def yoneda_lemma_etd() -> dict:
        """
        Лемма Йонеды:
        Для функтора F: C → Set и объекта A ∈ C:
        Nat(Hom(A,−), F) ≅ F(A).
        «Функтор определяется своими представлениями.»

        ЕТД: C = категория с 4 объектами.
        Представимый функтор Hom(МВС, −):
        Hom(МВС, МВС) = {id} → |·| = 1
        Hom(МВС, СВС) = {f₁₂} → |·| = 1
        Hom(МВС, БВС) = {f₁₃} → |·| = 1
        Hom(МВС, ЧВС) = {f₁₄} → |·| = 1

        Общее: |Hom(A,B)| = 1 для всех A,B.
        → C — «тонкая» категория (preorder)!
        Фактически: все объекты изоморфны.

        Йонеда: Nat(Hom(A,−), Hom(B,−)) ≅ Hom(B,A).
        |Hom(B,A)| = 1 → ровно 1 натуральное преобразование!
        «Все сферы структурно эквивалентны.»

        Размер: |Hom(C)| = N² = 16 морфизмов.
        """
        N = 4
        pi4 = math.pi / 4

        n_objects = N   # = 4
        n_morphisms = N**2   # = 16
        n_iso = N * (N-1)   # = 12 нетождественных

        # Функтор Hom(A,−): C → Set
        hom_sizes = {f'Hom({i},{j})': 1 for i in range(1,N+1) for j in range(1,N+1)}

        # Натуральные преобразования:
        n_nat = 1   # ровно одно между любыми двумя представимыми

        return {
            'теорема': 'Лемма Йонеды',
            'объектов': n_objects,
            'морфизмов': n_morphisms,
            '|Hom(A,B)|': 1,
            'нат_преобр': n_nat,
            'ЕТД': f'{n_objects} объектов; {n_morphisms}=N² морфизмов; все сферы эквивалентны (Йонеда!)',
        }

    # 2. ПРИСОЕДИНЁННЫЕ ФУНКТОРЫ
    @staticmethod
    def adjoint_functors_etd() -> dict:
        """
        Присоединённые функторы F ⊣ G:
        Hom_D(FA, B) ≅ Hom_C(A, GB) (естественно).

        Примеры:
        Свободный ⊣ Забывающий (Free ⊣ Forgetful).
        Σ ⊣ Δ ⊣ Π (суммы ⊣ диагональ ⊣ произведения).

        ЕТД: Функтор «балансировки» B: Vect → Vect:
        B(x₁,...,xₙ) = ((Σxᵢ)/n, ..., (Σxᵢ)/n).
        B = среднее (проекция на диагональ).

        B — левый присоединённый к включению Δ → ℝⁿ:
        B ⊣ Δ? Нет, B — проекция, это самоприсоединённый:
        ⟨Bx, y⟩ = ⟨x, By⟩ (B самосопряжён!).

        B² = B (идемпотент!): B(π/4,...,π/4) = (π/4,...,π/4).
        Образ B = диагональ Δ = {(a,...,a) : a ∈ ℝ}.
        dim Im(B) = 1, dim Ker(B) = N−1 = 3.

        Единица/коединица присоединения:
        η: Id → GB: x ↦ Bx (балансировка).
        ε: FG → Id: тождество на Δ.
        η∘η = η (идемпотент = «балансировка — одноразовый акт»!).
        """
        N = 4
        pi4 = math.pi / 4

        # Проекция B на диагональ:
        x = [0.3, 0.5, 0.9, 0.7]
        mean = sum(x) / N
        Bx = [mean] * N

        # B² = B:
        BBx = [sum(Bx)/N] * N
        idempotent = all(abs(Bx[i]-BBx[i]) < 1e-10 for i in range(N))

        dim_image = 1
        dim_kernel = N - 1   # = 3

        return {
            'теорема': 'Присоединённые функторы',
            'B(x)': [round(b, 4) for b in Bx],
            'идемпотент': idempotent,
            'dim_Im': dim_image,
            'dim_Ker': dim_kernel,
            'ЕТД': f'B=проекция на Δ; B²=B; dim Im={dim_image}; dim Ker={dim_kernel}=N−1={N-1}',
        }

    # 3. ПРЕДЕЛЫ И КОПРЕДЕЛЫ
    @staticmethod
    def limits_colimits_etd() -> dict:
        """
        Предел = универсальный конус:
        lim F = уравнитель всех морфизмов.

        Копредел = универсальный кокон:
        colim F = коуравнитель.

        ЕТД: Диаграмма D = 4 объекта с попарными морфизмами.
        lim D = {x : f_ij(x) = x для всех i,j}
        = неподвижные точки всех переходов = (π/4)⁴!

        Произведение: ∏ᵢ Sᵢ = МВС × СВС × БВС × ЧВС.
        dim ∏ = N = 4 (если каждая Sᵢ ≅ ℝ).
        Уравнитель: Eq(f,g) = {x : f(x)=g(x)}.
        Eq(id, B) = {x : x = Bx} = Δ (диагональ).

        Копроизведение: ∐ Sᵢ = МВС ⊔ СВС ⊔ БВС ⊔ ЧВС.
        |∐| = 4 (дизъюнктное объединение).

        Предел ↔ Копредел:
        lim = (π/4)⁴ ∈ Δ (одна точка).
        colim = ⊔ (4 компоненты).
        Дуальность: lim/colim взаимно отражают баланс/разнообразие.
        """
        N = 4
        pi4 = math.pi / 4

        limit = [pi4] * N   # = (π/4)⁴
        colimit_components = N   # = 4 компоненты

        # Уравнитель:
        eq_dim = 1   # dim Δ = 1

        # Произведение:
        prod_dim = N   # = 4

        return {
            'теорема': 'Пределы и копределы',
            'lim': [round(l, 4) for l in limit],
            'colim_комп': colimit_components,
            'dim_уравн': eq_dim,
            'dim_произв': prod_dim,
            'ЕТД': f'lim=(π/4)⁴ (баланс!); colim=⊔{N} компонент; Eq=Δ (dim={eq_dim})',
        }

    # 4. МОНАДЫ
    @staticmethod
    def monads_etd() -> dict:
        """
        Монада (T, η, μ):
        T: C → C (эндофунктор)
        η: Id → T (единица)
        μ: T² → T (умножение)
        μ∘Tμ = μ∘μT, μ∘Tη = μ∘ηT = id (когерентность).

        ЕТД: T = «оператор обучения»:
        T(x) = (1−α)x + α·(π/4)⁴.

        η: x ↦ T(x) = x + α(x*−x).
        μ: T²(x) ↦ T(x) (итерация → однократное).
        T²(x) = (1−α)((1−α)x+α·x*) + α·x*
              = (1−α)²x + α(2−α)·x*.
        μ: T²(x) → T(x) → отождествление по балансу.

        Алгебры монады T:
        T-алгебра = (A, h: TA → A) с h∘η_A = id, h∘μ_A = h∘Th.
        Свободная T-алгебра на x: T(x).

        Категория Клейсли: морфизмы A → TB.
        Категория Эйленберга–Мура: T-алгебры.

        Число T-алгебр на 4 объектах:
        Каждая алгебра = выбор «аттрактора».
        При T = усреднение: 1 алгебра = (π/4)⁴ (единственная!).
        """
        N = 4
        pi4 = math.pi / 4
        alpha = 0.5

        # T(x):
        x = [0.3, 0.5, 0.9, 0.1]
        Tx = [(1-alpha)*x[i] + alpha*pi4 for i in range(N)]

        # T²(x):
        TTx = [(1-alpha)*Tx[i] + alpha*pi4 for i in range(N)]

        # T^n(x) → x* при n → ∞:
        x_n = x[:]
        for _ in range(50):
            x_n = [(1-alpha)*x_n[i] + alpha*pi4 for i in range(N)]
        fixed = [round(xi, 6) for xi in x_n]

        # Когерентность: μ∘Tη = id:
        coherence = True   # по построению

        return {
            'теорема': 'Монады',
            'T(x)': [round(t, 4) for t in Tx],
            'T²(x)': [round(t, 4) for t in TTx],
            'T^∞(x)': fixed,
            'когерентность': coherence,
            'T-алгебр': 1,
            'ЕТД': f'T(x)→(π/4)⁴; T^∞=x*; 1 T-алгебра (единственный аттрактор!)',
        }


ct = CategoryTheoryETD()

yl = ct.yoneda_lemma_etd()
print(f"Йонеда: {yl['ЕТД']}")

af = ct.adjoint_functors_etd()
print(f"Присоединённые: {af['ЕТД']}")

lc = ct.limits_colimits_etd()
print(f"Пределы: {lc['ЕТД']}")

mn = ct.monads_etd()
print(f"Монады: {mn['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории категорий** → **4 категорных принципа ЕТД**:

1. **Йонеда** → 4 объекта, 16 = N² морфизмов; все сферы эквивалентны
2. **Присоединённые** → B = проекция на Δ; B² = B; dim Ker = N−1 = 3
3. **Пределы** → lim = (π/4)⁴ (баланс!); Eq = Δ (dim = 1)
4. **Монады** → T^∞(x) = (π/4)⁴; 1 T-алгебра (единственный аттрактор!)

> **Категорный портрет**: В категории ЕТД все 4 объекта изоморфны (Йонеда). Предел диаграммы = (π/4)⁴. Монада обучения имеет единственную T-алгебру = аттрактор баланса.

---
*ТОМ 241 / СЕРИЯ VII / ЕТД 2026*
