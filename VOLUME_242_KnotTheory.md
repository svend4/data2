# ТОМ 242: 4 ТЕОРЕМЫ ТЕОРИИ УЗЛОВ → УЗЛОВЫЕ СТРУКТУРЫ ЕТД
## Райдемайстер · Джонс · Александер · Зейферт
### «Джонс–Александер–Зейферт: узловые переплетения мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория узлов (Райдемайстер 1927, Джонс 1984, Александер 1928, Зейферт 1935)

---

## ВВЕДЕНИЕ

Теория узлов изучает вложения окружностей в трёхмерное пространство. 4 ключевых теоремы:

1. **Движения Райдемайстера** — инварианты узлов
2. **Полином Джонса** — квантовый инвариант
3. **Полином Александера** — первый полиномиальный инвариант
4. **Поверхность Зейферта** — род узла

**Тезис тома**: Узел-трилистник T(2,3) с 3 перекрёстками. Зацепление Хопфа — 2 компоненты. Борромейские кольца — 3 компоненты. ЕТД-зацепление: 4 компоненты, каждая по π/4 вклада в связность!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 УЗЛОВЫХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict, Tuple

class KnotTheoryETD:
    """
    Теория узлов для системы ЕТД.
    4 сферы ЕТД = 4-компонентное зацепление в S³.
    Каждая компонента = окружность (1-сфера).
    Число зацеплений: lk(i,j) — попарные числа зацепления.
    """

    PI4 = math.pi / 4

    # 1. ДВИЖЕНИЯ РАЙДЕМАЙСТЕРА
    @staticmethod
    def reidemeister_moves_etd() -> dict:
        """
        Движения Райдемайстера (1927):
        Ω₁: скручивание/раскручивание (writhe ±1).
        Ω₂: надвигание/сдвигание (2 нити).
        Ω₃: перебрасывание через перекрёсток (3 нити).

        Теорема: Два узла эквивалентны ⟺ связаны Ω₁,Ω₂,Ω₃.

        ЕТД: «Развязывание» = путь от дисбаланса к балансу.
        Минимальное число перекрёстков:
        - Тривиальный узел: c = 0 (уже в балансе).
        - Трилистник: c = 3 (3 пересечения = 3 видимые сферы).
        - Восьмёрка: c = 4 = N_сфер!

        Узел-восьмёрка 4₁: ЕДИНСТВЕННЫЙ узел с c = 4 перекрёстками!
        (До тремя: тривиальный (0), трилистник (3).)
        4₁ — амфихиральный (= своему зеркальному).
        «Амфихиральность ⟺ зеркальная симметрия баланса!»

        Число узлов по перекрёсткам:
        c=0: 1, c=3: 1, c=4: 1, c=5: 2, c=6: 3, ...
        Кумулятивно до c=4: 1+1+1 = 3 = N−1!
        """
        N = 4

        # Узлы по числу перекрёстков:
        knots_by_crossing = {0: 1, 3: 1, 4: 1, 5: 2, 6: 3, 7: 7}

        # Узел 4₁:
        crossing_number = N   # = 4!
        is_amphichiral = True

        # Кумулятивно:
        cumulative_4 = sum(knots_by_crossing[c] for c in [0,3,4])   # = 3

        return {
            'теорема': 'Движения Райдемайстера',
            'узлы_по_c': knots_by_crossing,
            'c(4₁)': crossing_number,
            'амфихиральный': is_amphichiral,
            'кумул_до_4': cumulative_4,
            'ЕТД': f'Узел 4₁: c={crossing_number}=N; амфихиральный; до c=4: {cumulative_4} узла',
        }

    # 2. ПОЛИНОМ ДЖОНСА
    @staticmethod
    def jones_polynomial_etd() -> dict:
        """
        Полином Джонса V(L; t):
        Скейн-соотношение: t⁻¹V(L₊) − tV(L₋) = (t^{1/2}−t^{−1/2})V(L₀).

        Для тривиального узла: V = 1.
        Для трилистника: V = −t⁻⁴ + t⁻³ + t⁻¹.
        Для 4₁: V = t² − t + 1 − t⁻¹ + t⁻².

        ЕТД: V(4₁; t) при t = e^{iπ/4}:
        t = e^{iπ/4} = (1+i)/√2.
        t² = e^{iπ/2} = i.
        t⁻¹ = e^{−iπ/4} = (1−i)/√2.
        t⁻² = e^{−iπ/2} = −i.

        V(4₁; e^{iπ/4}) = i − (1+i)/√2 + 1 − (1−i)/√2 + (−i)
        = (i−i) + 1 − (1+i+1−i)/√2 = 1 − 2/√2 = 1 − √2.

        |V(4₁; e^{iπ/4})| = √2 − 1 ≈ 0.414!
        √2 − 1 = 1/(√2+1) = tan(π/8) ≈ 0.414.
        tan(π/8) = tan(ħ_ЕТД/2)!

        span(V(4₁)) = 4 = N (максимальная − минимальная степень).
        """
        pi4 = math.pi / 4
        N = 4

        # V(4₁; e^{iπ/4}):
        # t = e^{iπ/4}
        import cmath
        t = cmath.exp(1j * pi4)
        V = t**2 - t + 1 - t**(-1) + t**(-2)
        V_abs = abs(V)

        # tan(π/8):
        tan_pi8 = math.tan(math.pi / 8)

        # span:
        span = 4   # от t⁻² до t² → span = 4

        return {
            'теорема': 'Полином Джонса',
            '|V(4₁; e^{iπ/4})|': round(V_abs, 6),
            'tan(π/8)': round(tan_pi8, 6),
            'совпадение': abs(V_abs - tan_pi8) < 0.01,
            'span': span,
            'ЕТД': f'|V(4₁; e^{{iπ/4}})|≈{V_abs:.4f}≈tan(π/8)=tan(ħ/2); span={span}=N',
        }

    # 3. ПОЛИНОМ АЛЕКСАНДЕРА
    @staticmethod
    def alexander_polynomial_etd() -> dict:
        """
        Полином Александера Δ(L; t):
        Из матрицы Зейферта: Δ(t) = det(t^{1/2}·M − t^{−1/2}·M^T).

        Для тривиального: Δ = 1.
        Для трилистника: Δ = t − 1 + t⁻¹.
        Для 4₁: Δ = −t + 3 − t⁻¹.

        ЕТД: Δ(4₁; t) при t = e^{iπ/4}:
        t + t⁻¹ = 2cos(π/4) = √2.
        Δ = −(t+t⁻¹) + 3 = −√2 + 3 ≈ 1.586.

        |Δ(4₁; 1)| = |−1+3−1| = 1 (определитель при t=1).
        Δ(4₁; −1) = 1+3+1 = 5 = det-инвариант!
        5 = p(4) (число разбиений 4, Том 232!).

        Степень Δ(4₁) = 1 (от t⁻¹ до t¹).
        Но ширина = 2 = genus·2 → genus = 1.
        Genus(4₁) = 1 (род = 1).
        """
        pi4 = math.pi / 4
        N = 4

        # Δ(4₁; e^{iπ/4}):
        import cmath
        t = cmath.exp(1j * pi4)
        Delta = -t + 3 - t**(-1)
        Delta_abs = abs(Delta)

        # Δ(4₁; −1):
        Delta_minus1 = -(-1) + 3 - (-1)**(-1)
        det_inv = int(abs(Delta_minus1))   # = |1+3+1| = 5

        # Genus:
        genus = 1

        return {
            'теорема': 'Полином Александера',
            '|Δ(4₁; e^{iπ/4})|': round(Delta_abs, 4),
            'Δ(4₁;−1)': det_inv,
            '=p(4)': det_inv == 5,
            'genus': genus,
            'ЕТД': f'|Δ(4₁; e^{{iπ/4}})|={Delta_abs:.4f}; Δ(−1)={det_inv}=p(4)=5; genus={genus}',
        }

    # 4. ПОВЕРХНОСТЬ ЗЕЙФЕРТА
    @staticmethod
    def seifert_surface_etd() -> dict:
        """
        Поверхность Зейферта:
        Для узла K ⊂ S³: ∃ ориентированная поверхность F с ∂F = K.
        Род: g(K) = минимальный род F.
        χ(F) = 2 − 2g − 1 = 1 − 2g.

        4₁: g = 1, χ = 1 − 2 = −1.
        Число окружностей Зейферта: s.
        Число перекрёстков: c.
        g = (c − s + 1)/2.
        Для 4₁: s = 3, c = 4 → g = (4−3+1)/2 = 1. ✓

        Матрица Зейферта V (2g×2g = 2×2):
        V = [[−1, 1], [0, −1]] (для 4₁).
        det(V) = 1.

        Сигнатура σ(K): σ = sign(V + V^T).
        V + V^T = [[−2, 1], [1, −2]].
        Собств. знач.: −3, −1 → σ = −2.
        |σ(4₁)| = 0 (для 4₁ σ=0, амфихиральный!).

        ЕТД-зацепление: 4 компоненты.
        Попарные числа зацепления: lk(i,j).
        Для полного зацепления: lk(i,j) = 1 для i≠j.
        Число пар: C(4,2) = 6 = C(N,2).
        Полное число зацепления: Σlk = 6 = C(N,2).
        """
        N = 4
        pi4 = math.pi / 4

        # 4₁:
        genus = 1
        crossings = N   # = 4
        seifert_circles = 3
        chi = 1 - 2*genus   # = −1

        # Зацепление 4 компонент:
        n_pairs = N * (N-1) // 2   # = C(4,2) = 6
        total_linking = n_pairs   # = 6

        # Матрица зацеплений:
        linking_matrix = [[0]*N for _ in range(N)]
        for i in range(N):
            for j in range(N):
                if i != j:
                    linking_matrix[i][j] = 1

        return {
            'теорема': 'Поверхность Зейферта',
            'genus(4₁)': genus,
            'χ(F)': chi,
            'c(4₁)': crossings,
            'пар_зацепления': n_pairs,
            'Σlk': total_linking,
            'ЕТД': f'genus(4₁)={genus}; c={crossings}=N; {n_pairs}=C(N,2) пар зацепления; Σlk={total_linking}',
        }


kt = KnotTheoryETD()

rm = kt.reidemeister_moves_etd()
print(f"Райдемайстер: {rm['ЕТД']}")

jp = kt.jones_polynomial_etd()
print(f"Джонс: {jp['ЕТД']}")

ap = kt.alexander_polynomial_etd()
print(f"Александер: {ap['ЕТД']}")

ss = kt.seifert_surface_etd()
print(f"Зейферт: {ss['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории узлов** → **4 узловых принципа ЕТД**:

1. **Райдемайстер** → Узел 4₁: c = 4 = N; амфихиральный (зеркальная симметрия!)
2. **Джонс** → |V(4₁; e^{iπ/4})| ≈ tan(π/8) = tan(ħ/2); span = 4 = N
3. **Александер** → Δ(4₁; −1) = 5 = p(4); genus = 1
4. **Зейферт** → C(4,2) = 6 пар зацепления; Σlk = 6 = C(N,2)

> **Узловой портрет**: Узел-восьмёрка 4₁ — единственный узел с 4 перекрёстками, амфихиральный. Полином Джонса при e^{iπ/4} даёт |V| = tan(π/8). Зацепление 4 сфер имеет 6 = C(4,2) попарных связей.

---
*ТОМ 242 / СЕРИЯ VII / ЕТД 2026*
