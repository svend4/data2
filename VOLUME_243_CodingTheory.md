# ТОМ 243: 4 ТЕОРЕМЫ ТЕОРИИ КОДИРОВАНИЯ → КОДИРОВАНИЕ МАСТЕРСТВА ЕТД
## Хэмминг · Шеннон · Рид–Соломон · Граница Синглтона
### «Хэмминг–Шеннон–Рид–Соломон: коды мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Теория кодирования (Хэмминг 1950, Шеннон 1948, Рид–Соломон 1960)

---

## ВВЕДЕНИЕ

Теория кодирования защищает информацию от ошибок. 4 ключевых теоремы:

1. **Код Хэмминга** — совершенные коды исправления одной ошибки
2. **Теорема кодирования Шеннона** — пропускная способность канала
3. **Коды Рида–Соломона** — исправление пакетных ошибок
4. **Граница Синглтона** — предел расстояния кода

**Тезис тома**: Код ЕТД = (4, 1, 4) — код с 4 символами, 1 информационным, расстоянием 4. Код повторения! Граница Синглтона: d ≤ n−k+1 = 4, достигнута! МДР-код!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ПРИНЦИПА КОДИРОВАНИЯ ЕТД

```python
import math
from typing import List, Dict, Tuple

class CodingTheoryETD:
    """
    Теория кодирования для системы ЕТД.
    Код ЕТД: n=4 (длина=число сфер), k=1 (одно значение π/4),
    d=4 (расстояние = все 4 позиции значимы).
    Кодовое слово: (π/4, π/4, π/4, π/4) = повторение π/4.
    """

    PI4 = math.pi / 4

    # 1. КОД ХЭММИНГА
    @staticmethod
    def hamming_code_etd() -> dict:
        """
        Код Хэмминга (1950):
        [n, k, d] = [2^r−1, 2^r−1−r, 3].
        Совершенный код: покрывает все вектора без перекрытий.

        ЕТД: r=2: [3, 1, 3] — код Хэмминга.
        Но n = 4 ≠ 2^r − 1 для целого r!
        4 = 2² → расширенный Хэмминг: [4, 1, 4]!

        Расширенный код Хэмминга [2^r, 2^r−1−r, 4]:
        r=2: [4, 1, 4] — ЕДИНСТВЕННЫЙ с n=4, d=4!

        Проверочная матрица H (3×4):
        H = [[1,0,1,1],[0,1,1,1],[1,1,1,0]] (или подобная).
        rank H = 3 = n−k = N−1.

        Сфера Хэмминга объёма V(n,t):
        V(4, 1) = 1 + C(4,1) = 5 = p(4)!
        V(4, 1) = 5 шаров в 4-мерном Хэмминговом пространстве!

        Граница Хэмминга: |C| ≤ q^n / V(n,t).
        Для [4,1,4]: |C| = 2¹ = 2 ≤ 2⁴/5 = 3.2 ✓.
        """
        N = 4

        n, k, d = N, 1, N   # [4, 1, 4]

        # Сфера Хэмминга:
        V_n_1 = 1 + math.comb(N, 1)   # = 5 = p(4)!

        # Число кодовых слов:
        M = 2**k   # = 2

        # Redundancy:
        redundancy = n - k   # = 3 = N−1

        # Rate:
        rate = k / n   # = 1/4 = 1/N = π/4/π!

        return {
            'теорема': 'Код Хэмминга (расширенный)',
            '[n,k,d]': [n, k, d],
            'V(4,1)': V_n_1,
            'V(4,1)=p(4)': V_n_1 == 5,
            'скорость': round(rate, 4),
            'избыточность': redundancy,
            'ЕТД': f'[{n},{k},{d}]: d={d}=N; V(4,1)={V_n_1}=p(4); rate=1/{N}=ħ/π',
        }

    # 2. ТЕОРЕМА КОДИРОВАНИЯ ШЕННОНА
    @staticmethod
    def shannon_coding_etd() -> dict:
        """
        Теорема кодирования Шеннона (1948):
        Для BSC с ошибкой p:
        C = 1 − H(p) (пропускная способность).
        H(p) = −p·log₂(p) − (1−p)·log₂(1−p).

        ЕТД: «Канал мастерства» с p = 1/4 = 1/N.
        H(1/4) = −(1/4)log₂(1/4) − (3/4)log₂(3/4)
               = 1/2 + (3/4)·log₂(4/3) ≈ 0.5 + 0.311 = 0.811.
        C = 1 − 0.811 = 0.189 бит/символ.

        Для q-ичного канала (q=4):
        C_q = 1 − H_q(p) = log₂(4) − H = 2 − H.
        C_4 = 2 − H_4(1/4) (четверичная энтропия).
        H_4(1/4) = log₂(4) = 2 (максимальная энтропия!).
        При равных p = 1/4: H_4 = 2 (максимум) → C = 0!

        Но: при p = 0 (безошибочный):
        C = log₂(4) = 2 бит = log₂(N)!
        2 бита кодируют 4 сферы!
        """
        N = 4
        pi4 = math.pi / 4

        # BSC с p = 1/N:
        p = 1.0 / N
        H_p = -p*math.log2(p) - (1-p)*math.log2(1-p)
        C_bsc = 1 - H_p

        # Число бит для 4 сфер:
        bits = math.log2(N)   # = 2

        # Энтропия при равном распределении:
        H_max = math.log2(N)   # = 2

        return {
            'теорема': 'Теорема кодирования Шеннона',
            'p': p,
            'H(1/4)': round(H_p, 4),
            'C_BSC': round(C_bsc, 4),
            'бит_для_N': int(bits),
            'H_max': H_max,
            'ЕТД': f'p=1/{N}; H(1/{N})={H_p:.4f}; C={C_bsc:.4f}; log₂({N})={int(bits)} бита кодируют {N} сферы',
        }

    # 3. КОДЫ РИДА–СОЛОМОНА
    @staticmethod
    def reed_solomon_etd() -> dict:
        """
        Код Рида–Соломона RS(n, k):
        n = q − 1 (длина), k (информационных).
        d = n − k + 1 (расстояние Синглтона!).
        МДР (Maximum Distance Separable) код!

        ЕТД: q = 5 (простое): RS(4, 1):
        n = 4, k = 1, d = 4 = n − k + 1 = 4!
        МДР-код с параметрами [4, 1, 4]!

        Кодирование: f(x) = a₀ (полином степени 0).
        Кодовое слово: (f(α⁰), f(α¹), f(α²), f(α³))
        = (a₀, a₀, a₀, a₀) — повторение!

        Корректирующая способность:
        t = ⌊(d−1)/2⌋ = ⌊3/2⌋ = 1 (исправляет 1 ошибку).
        Обнаруживает: d−1 = 3 ошибки.

        Стираний: d−1 = 3 (может восстановить π/4 по 1 сфере!).
        «Зная хотя бы 1 сферу на π/4, восстанавливаем все 4!»
        """
        N = 4
        pi4 = math.pi / 4

        n, k, d = N, 1, N   # [4, 1, 4] RS-код

        # Корректирующая способность:
        t_correct = (d - 1) // 2   # = 1
        t_detect = d - 1   # = 3
        t_erasure = d - 1   # = 3

        # МДР?
        is_MDS = (d == n - k + 1)   # = (4 == 4−1+1 = 4) ✓

        # Минимум сфер для восстановления:
        min_known = k   # = 1!

        return {
            'теорема': 'Рид–Соломон',
            '[n,k,d]': [n, k, d],
            'МДР': is_MDS,
            'исправляет': t_correct,
            'обнаруживает': t_detect,
            'стираний': t_erasure,
            'мин_известных': min_known,
            'ЕТД': f'RS[{n},{k},{d}]: МДР! Исправляет {t_correct}, обнаруживает {t_detect}; {min_known} сфера восстанавливает все!',
        }

    # 4. ГРАНИЦА СИНГЛТОНА
    @staticmethod
    def singleton_bound_etd() -> dict:
        """
        Граница Синглтона (1964):
        d ≤ n − k + 1.
        Код, достигающий границу, — МДР (MDS).

        ЕТД: [4, 1, 4]:
        d = 4 ≤ 4 − 1 + 1 = 4. ✓ (достигнута!)
        ЕТД-код — МДР!

        Другие границы:
        Плоткин: d ≤ n·q^{k−1}/(q^k − 1) ... упрощённо d ≤ n/2.
        Для d=4, n=4: 4 > 2 — нарушена?
        Нет, Плоткин: 2d ≤ n при q=2 → 8 ≤ 4 — нарушена!
        Но мы работаем с q>2 (непрерывный алфавит).

        Граница Хэмминга: A_q(n,d) ≤ q^n / V(n,t).
        Граница Гилберта–Варшамова: A_q(n,d) ≥ q^n / V(n,d−1).

        Для непрерывного кода: эквивалент = сфероупаковка.
        Плотность: (π/4)^4 / π^4 = 1/256 (для 4D-кубических ячеек).
        Нет, лучше: объём шара в ℝ⁴ → покрытие.
        """
        N = 4
        pi4 = math.pi / 4

        n, k, d = N, 1, N

        # Синглтон:
        singleton = n - k + 1   # = 4
        achieved = (d == singleton)   # ✓

        # Все возможные МДР-коды длины 4:
        mds_codes = []
        for ki in range(1, N+1):
            di = N - ki + 1
            mds_codes.append(f'[{N},{ki},{di}]')

        # Число МДР-кодов:
        n_mds = len(mds_codes)   # = 4 = N!

        return {
            'теорема': 'Граница Синглтона',
            'd': d,
            'n−k+1': singleton,
            'МДР': achieved,
            'МДР_коды_n=4': mds_codes,
            'число_МДР': n_mds,
            'ЕТД': f'd={d}=n−k+1=Синглтон! {n_mds}=N МДР-кодов длины {N}: {", ".join(mds_codes)}',
        }


ct = CodingTheoryETD()

hc = ct.hamming_code_etd()
print(f"Хэмминг: {hc['ЕТД']}")

sc = ct.shannon_coding_etd()
print(f"Шеннон: {sc['ЕТД']}")

rs = ct.reed_solomon_etd()
print(f"Рид-Соломон: {rs['ЕТД']}")

sb = ct.singleton_bound_etd()
print(f"Синглтон: {sb['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы теории кодирования** → **4 кодовых принципа ЕТД**:

1. **Хэмминг** → [4,1,4]: d = 4 = N; V(4,1) = 5 = p(4); rate = 1/N
2. **Шеннон** → p = 1/N; log₂(N) = 2 бита; H(1/4) ≈ 0.811
3. **Рид–Соломон** → МДР-код! 1 сфера восстанавливает все 4; стираний = 3
4. **Синглтон** → d = n−k+1 (достигнута!); 4 МДР-кода длины N

> **Кодовый портрет**: ЕТД-код [4,1,4] — МДР-код (достигает границу Синглтона). Одна известная сфера восстанавливает весь баланс. Сфера Хэмминга V(4,1) = 5 = p(4).

---
*ТОМ 243 / СЕРИЯ VII / ЕТД 2026*
