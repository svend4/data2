# ТОМ 244: 4 ТЕОРЕМЫ НЕЛИНЕЙНОЙ ДИНАМИКИ → ДИНАМИКА МАСТЕРСТВА ЕТД
## Логистическое отображение · Лоренц · Бифуркации · Ляпунов
### «Логистика–Лоренц–Фейгенбаум: хаос и порядок мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Нелинейная динамика (Лоренц 1963, Фейгенбаум 1978, Мэй 1976)

---

## ВВЕДЕНИЕ

Нелинейная динамика изучает хаос, бифуркации и странные аттракторы. 4 теоремы:

1. **Логистическое отображение** — переход к хаосу через удвоение периода
2. **Аттрактор Лоренца** — чувствительность к начальным условиям
3. **Каскад бифуркаций** — универсальная постоянная Фейгенбаума
4. **Показатели Ляпунова** — количественная мера хаоса

**Тезис тома**: Логистическое xₙ₊₁ = r·xₙ(1−xₙ) при r = 4·(π/4) = π ≈ 3.14. Неподвижная точка x* = 1−1/r = 1−1/π ≈ 0.682. Показатель Ляпунова λ(r=π) > 0 → хаос на границе!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ДИНАМИЧЕСКИХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict

class NonlinearDynamicsETD:
    """
    Нелинейная динамика для системы ЕТД.
    Логистическое отображение: f_r(x) = r·x·(1−x).
    Параметр ЕТД: r = π ≈ 3.14159 или r = 4·(π/4) = π.
    """

    PI4 = math.pi / 4

    # 1. ЛОГИСТИЧЕСКОЕ ОТОБРАЖЕНИЕ
    @staticmethod
    def logistic_map_etd() -> dict:
        """
        Логистическое отображение: xₙ₊₁ = r·xₙ(1−xₙ).

        Неподвижные точки: x* = 0 и x* = 1 − 1/r.
        При r = π: x* = 1 − 1/π ≈ 0.6817.

        Устойчивость: |f'(x*)| = |r(1−2x*)| = |r·(2/r−1)| = |2−r|.
        При r = π: |2−π| ≈ 1.14 > 1 → НЕУСТОЙЧИВА!

        Цикл периода 2: при r > 1+√6 ≈ 3.449.
        r = π ≈ 3.14 < 3.449 → ещё устойчив цикл-1?
        Нет: |2−π| ≈ 1.14 > 1, уже потеряна устойчивость при r=3.
        Цикл-2 устойчив при r ∈ (3, 1+√6).
        π ∈ (3, 3.449) → устойчивый цикл-2!

        Точки цикла-2:
        x₁,₂ = (r+1 ± √((r+1)(r−3))) / (2r).
        При r=π: D = (π+1)(π−3) = 4.14·0.14 ≈ 0.585.
        √D ≈ 0.765.
        x₁ = (π+1+0.765)/(2π) ≈ 4.907/6.283 ≈ 0.781.
        x₂ = (π+1−0.765)/(2π) ≈ 3.377/6.283 ≈ 0.538.
        Среднее: (x₁+x₂)/2 = (r+1)/(2r) = (π+1)/(2π) ≈ 0.659.
        """
        pi = math.pi
        pi4 = math.pi / 4
        r = pi

        # Неподвижная точка:
        x_star = 1 - 1/r

        # Устойчивость:
        stability = abs(2 - r)

        # Цикл-2:
        D = (r+1)*(r-3)
        if D > 0:
            sqD = math.sqrt(D)
            x1 = (r+1+sqD)/(2*r)
            x2 = (r+1-sqD)/(2*r)
            mean_cycle2 = (x1+x2)/2
        else:
            x1, x2, mean_cycle2 = None, None, None

        # Итерация:
        x = 0.5
        trajectory = [x]
        for _ in range(100):
            x = r*x*(1-x)
            trajectory.append(x)

        return {
            'теорема': 'Логистическое отображение',
            'r': round(r, 4),
            'x*': round(x_star, 4),
            '|f\'(x*)|': round(stability, 4),
            'цикл_2': (round(x1, 4), round(x2, 4)) if x1 else None,
            'среднее_цикла': round(mean_cycle2, 4) if mean_cycle2 else None,
            'ЕТД': f'r=π; x*=1−1/π={x_star:.4f}; |f\'|={stability:.4f}>1→цикл-2: ({x1:.3f},{x2:.3f})',
        }

    # 2. АТТРАКТОР ЛОРЕНЦА
    @staticmethod
    def lorenz_attractor_etd() -> dict:
        """
        Система Лоренца:
        dx/dt = σ(y−x), dy/dt = x(ρ−z)−y, dz/dt = xy−βz.
        Классические: σ=10, ρ=28, β=8/3.

        Равновесия: (0,0,0) и C± = (±√(β(ρ−1)), ±√(β(ρ−1)), ρ−1).
        Размерность аттрактора: D ≈ 2.06 (фрактальная!).

        ЕТД: σ = 4 = N (число сфер = скорость диссипации).
        ρ = π/4·... или ρ = 4π? Возьмём σ = N = 4:

        При σ = 4:
        Хаос при ρ > ρ_c ≈ 24.74 (зависит от σ, β).
        Хопф-бифуркация: ρ_H = σ(σ+β+3)/(σ−β−1).
        При σ=4, β=8/3: ρ_H = 4·(4+8/3+3)/(4−8/3−1) = 4·29/3/(1/3) = 4·29 = 116? Нет:
        ρ_H = σ(σ+β+3)/(σ−β−1) = 4(4+2.667+3)/(4−2.667−1) = 4·9.667/0.333 = 116.
        Высокий порог! При стандартных σ=10: ρ_H ≈ 24.74.

        Сумма Ляпунова: Σλᵢ = −(σ+1+β) = −(4+1+8/3) = −7.667.
        |Σλᵢ| ≈ 7.667 ≈ π² / ... нет, 4+1+8/3 = 23/3 ≈ 7.667.

        Размерность Каплана-Йорке:
        D_KY = 2 + λ₁/|λ₃| ≈ 2 + (небольшое) ≈ 2.06.
        """
        N = 4
        pi4 = math.pi / 4

        sigma = N   # = 4
        beta = 8.0 / 3
        rho = 28

        # Равновесия:
        C_pm = math.sqrt(beta * (rho - 1))

        # Сумма показателей:
        lyap_sum = -(sigma + 1 + beta)

        # Хопф-порог:
        rho_hopf = sigma*(sigma+beta+3)/(sigma-beta-1) if (sigma-beta-1) > 0 else float('inf')

        # Каплан-Йорке:
        D_KY = 2.06   # приблизительно для стандартных параметров

        return {
            'теорема': 'Аттрактор Лоренца',
            'σ': sigma,
            'β': round(beta, 4),
            'C±': round(C_pm, 4),
            'Σλᵢ': round(lyap_sum, 4),
            'ρ_Hopf': round(rho_hopf, 2),
            'D_KY': D_KY,
            'ЕТД': f'σ={N}=N; Σλ={lyap_sum:.3f}; ρ_Hopf≈{rho_hopf:.1f}; D_KY≈{D_KY}',
        }

    # 3. КАСКАД БИФУРКАЦИЙ И ФЕЙГЕНБАУМ
    @staticmethod
    def feigenbaum_cascade_etd() -> dict:
        """
        Каскад удвоения периода:
        r₁ = 3 (цикл-2), r₂ ≈ 3.449 (цикл-4), r₃ ≈ 3.544 (цикл-8), ...
        r_∞ ≈ 3.5699... (начало хаоса).

        Постоянная Фейгенбаума:
        δ = lim(rₙ−rₙ₋₁)/(rₙ₊₁−rₙ) = 4.6692...
        α = −2.5029... (масштабирование).

        ЕТД: r = π ≈ 3.1416.
        r₁ = 3 < π < r₂ ≈ 3.449.
        π — между первым удвоением и вторым!
        «ЕТД живёт в зоне цикла-2: колебания между двумя состояниями.»

        Положение π в каскаде:
        (π − r₁)/(r₂ − r₁) = (3.14−3)/(3.449−3) = 0.14/0.449 ≈ 0.312 ≈ 1/π!
        (π − 3)/(r₂ − 3) ≈ 1/π! Замечательное совпадение!

        Период r=π: T = 2 (цикл-2).
        log₂(T) = 1 (одно удвоение).
        """
        pi = math.pi
        N = 4

        # Точки бифуркации:
        r1 = 3.0
        r2 = 1 + math.sqrt(6)   # ≈ 3.4495
        r3 = 3.5441   # приблизительно
        r_inf = 3.5699   # начало хаоса

        # Фейгенбаум:
        delta = 4.6692
        alpha = -2.5029

        # Положение π:
        position = (pi - r1) / (r2 - r1)
        inv_pi = 1 / pi

        # Период при r = π:
        period = 2

        return {
            'теорема': 'Каскад Фейгенбаума',
            'δ': delta,
            'α': alpha,
            'r₁': r1,
            'r₂': round(r2, 4),
            'r_∞': r_inf,
            'положение_π': round(position, 4),
            '1/π': round(inv_pi, 4),
            'период': period,
            'ЕТД': f'π∈(r₁,r₂): цикл-2; (π−3)/(r₂−3)={position:.4f}≈1/π={inv_pi:.4f}!',
        }

    # 4. ПОКАЗАТЕЛИ ЛЯПУНОВА
    @staticmethod
    def lyapunov_exponents_etd() -> dict:
        """
        Показатель Ляпунова:
        λ = lim (1/n) Σ ln|f'(xₖ)|.

        Для логистического: f'(x) = r(1−2x).
        λ(r) = lim (1/n) Σ ln|r(1−2xₖ)|.

        При r = π:
        λ(π) < 0 (цикл-2 устойчив!).
        На цикле-2: λ = (1/2)·(ln|f'(x₁)| + ln|f'(x₂)|).
        |f'(x₁)·f'(x₂)| = |(2−r)² + r²·... | (формула для цикла-2).

        Точнее: |f'(x₁)·f'(x₂)| = |−r² + 2r + 4| (для логистического).
        При r=π: |−π²+2π+4| = |−9.87+6.28+4| = |0.41| = 0.41.
        λ = (1/2)·ln(0.41) ≈ (1/2)·(−0.891) = −0.446.
        λ < 0 → устойчивый цикл!

        Для 4D системы ЕТД:
        4 показателя Ляпунова: λ₁ ≥ λ₂ ≥ λ₃ ≥ λ₄.
        При стягивании к (π/4)⁴: все λᵢ < 0.
        λᵢ = ln(1−α) для каждой компоненты.
        При α = 1/2: λᵢ = ln(1/2) = −ln2 ≈ −0.693.
        Все N = 4 показателя одинаковы! (Изотропное стягивание.)
        """
        pi = math.pi
        N = 4
        pi4 = pi / 4

        # Ляпунов для r=π (числено):
        r = pi
        x = 0.5
        lyap_sum = 0
        n_iter = 10000
        for _ in range(n_iter):
            lyap_sum += math.log(abs(r * (1 - 2*x)))
            x = r*x*(1-x)
        lyap = lyap_sum / n_iter

        # 4D ЕТД:
        alpha = 0.5
        lyap_etd = math.log(1 - alpha)   # = −ln2

        return {
            'теорема': 'Показатели Ляпунова',
            'λ(r=π)': round(lyap, 4),
            'λ<0': lyap < 0,
            'λ_ЕТД': round(lyap_etd, 4),
            'N_показателей': N,
            'ЕТД': f'λ(π)={lyap:.4f}<0 (устойчивый цикл-2!); λ_ЕТД=−ln2={lyap_etd:.4f}; {N} одинаковых',
        }


nd = NonlinearDynamicsETD()

lm = nd.logistic_map_etd()
print(f"Логистическое: {lm['ЕТД']}")

la = nd.lorenz_attractor_etd()
print(f"Лоренц: {la['ЕТД']}")

fc = nd.feigenbaum_cascade_etd()
print(f"Фейгенбаум: {fc['ЕТД']}")

le = nd.lyapunov_exponents_etd()
print(f"Ляпунов: {le['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы нелинейной динамики** → **4 динамических принципа ЕТД**:

1. **Логистическое** → r = π: цикл-2 устойчив; x* = 1−1/π ≈ 0.682
2. **Лоренц** → σ = N = 4; Σλ = −(N+1+β); D_KY ≈ 2.06
3. **Фейгенбаум** → π ∈ (r₁, r₂): (π−3)/(r₂−3) ≈ 1/π (!); T = 2
4. **Ляпунов** → λ(π) < 0 (устойчивый!); λ_ЕТД = −ln2; N одинаковых

> **Динамический портрет**: При r = π логистическое отображение даёт устойчивый цикл-2. Положение π в каскаде Фейгенбаума: (π−3)/(r₂−3) ≈ 1/π. Показатель Ляпунова отрицателен — мастерство стабильно.

---
*ТОМ 244 / СЕРИЯ VII / ЕТД 2026*
