# ТОМ 245: 4 ТЕОРЕМЫ СЛУЧАЙНЫХ ГРАФОВ → СЕТЕВАЯ СТРУКТУРА ЕТД
## Эрдёш–Реньи · Гигантская компонента · Пороги · Малый мир
### «Эрдёш–Реньи–Боллобаш: случайные сети мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Случайные графы (Эрдёш–Реньи 1959, Боллобаш 1985, Уоттс–Строгац 1998)

---

## ВВЕДЕНИЕ

Случайные графы моделируют сетевые структуры. 4 ключевых теоремы:

1. **Модель Эрдёша–Реньи** — G(n,p) и пороговые явления
2. **Гигантская компонента** — фазовый переход при p = 1/n
3. **Пороговые функции** — резкие переходы свойств
4. **Модель малого мира** — кластеризация и короткие пути

**Тезис тома**: G(4, p) — случайный граф на 4 вершинах. Порог связности p = ln(4)/4 ≈ 0.347. При p = π/4/π = 1/4: подпороговый! При p = π/4 ≈ 0.785: надпороговый, почти полный!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 СЕТЕВЫХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict
from itertools import combinations

class RandomGraphsETD:
    """
    Случайные графы для сети ЕТД.
    G(4, p): 4 вершины = 4 сферы.
    Рёбра: C(4,2) = 6 возможных.
    """

    PI4 = math.pi / 4

    # 1. ЭРДЁШ–РЕНЬИ G(n,p)
    @staticmethod
    def erdos_renyi_etd() -> dict:
        """
        G(n,p): n вершин, каждое ребро с вероятностью p.
        E[рёбер] = C(n,2)·p.
        E[степени] = (n−1)·p.

        ЕТД: n = 4, p = π/4 ≈ 0.785.
        E[рёбер] = 6·(π/4) = 3π/2 ≈ 4.71.
        E[степени] = 3·(π/4) = 3π/4 ≈ 2.356.

        P(полный граф) = p^6 = (π/4)^6 ≈ 0.234.
        P(пустой граф) = (1−p)^6 = (1−π/4)^6 ≈ 0.215^6 ≈ 9.86·10⁻⁵.

        Число графов на 4 вершинах: 2^6 = 64.
        Число помеченных деревьев: 4^2 = 16 (формула Кэли!).
        Число связных графов на 4 вершинах: 38.

        P(связный) = Σ_{связных} p^e·(1−p)^{6−e}.
        При p = π/4: P(связный) ≈ 0.99 (очень высока!).
        """
        N = 4
        pi4 = math.pi / 4
        p = pi4

        n_edges_max = N*(N-1)//2   # = 6
        E_edges = n_edges_max * p
        E_degree = (N-1) * p

        # P(K₄):
        P_complete = p**n_edges_max
        P_empty = (1-p)**n_edges_max

        # Кэли:
        n_trees = N**(N-2)   # = 16

        # Число графов:
        n_total = 2**n_edges_max   # = 64

        return {
            'теорема': 'Эрдёш–Реньи G(n,p)',
            'n': N,
            'p': round(p, 4),
            'E[рёбер]': round(E_edges, 4),
            'E[степени]': round(E_degree, 4),
            'P(K₄)': round(P_complete, 4),
            'P(∅)': round(P_empty, 6),
            'деревьев': n_trees,
            'ЕТД': f'G({N},{p:.3f}): E[рёбер]={E_edges:.2f}≈3π/2; деревьев={n_trees}=N^(N−2); P(K₄)={P_complete:.3f}',
        }

    # 2. ГИГАНТСКАЯ КОМПОНЕНТА
    @staticmethod
    def giant_component_etd() -> dict:
        """
        Фазовый переход в G(n,p):
        При p = c/n:
        c < 1: все компоненты O(log n).
        c = 1: наибольшая O(n^{2/3}).
        c > 1: гигантская компонента O(n).

        ЕТД: n = 4, p = 1/n = 1/4 — критическая точка!
        p = 1/4 = 1/N: фазовый переход!

        При p = π/4 ≈ 0.785: c = np = 4·(π/4) = π ≈ 3.14.
        c = π > 1 → гигантская компонента!
        Размер GC: n·(1−e^{−c·ζ}) где ζ — решение ζ = 1−e^{−cζ}.
        При c = π: ζ ≈ 0.96 → GC ≈ 4·0.96 ≈ 3.84 ≈ 4.
        «Почти все 4 сферы в одной компоненте!»

        Пороги:
        Связность: p = ln(n)/n = ln4/4 ≈ 0.347.
        π/4 ≈ 0.785 > 0.347 → связный!
        1/N = 0.25 < 0.347 → может быть несвязным.

        Точная P(связный при p=1/4):
        P = 1 − 4·(1−1/4)³ + ... (включение-исключение).
        """
        N = 4
        pi4 = math.pi / 4

        # c = np:
        c_critical = 1.0   # p = 1/n
        c_pi4 = N * pi4   # = π ≈ 3.14

        # Порог связности:
        p_conn = math.log(N) / N

        # π/4 vs порог:
        above_threshold = pi4 > p_conn

        # Гигантская компонента при c = π:
        # ζ = 1 − e^{−πζ}:
        zeta = 0.5
        for _ in range(100):
            zeta = 1 - math.exp(-math.pi * zeta)
        gc_size = N * zeta

        return {
            'теорема': 'Гигантская компонента',
            'c(π/4)': round(c_pi4, 4),
            'p_связн': round(p_conn, 4),
            'π/4>порога': above_threshold,
            'ζ': round(zeta, 4),
            'GC_размер': round(gc_size, 4),
            'ЕТД': f'c=π; p_связн=ln4/4={p_conn:.3f}<π/4; GC={gc_size:.2f}≈{N} (все сферы связаны!)',
        }

    # 3. ПОРОГОВЫЕ ФУНКЦИИ
    @staticmethod
    def threshold_functions_etd() -> dict:
        """
        Пороговые функции: свойство Q имеет порог t(n) если
        P(G(n,p) ∈ Q) → {0 при p/t→0, 1 при p/t→∞}.

        Пороги для G(n,p):
        Первое ребро: p = 1/C(n,2) = 1/6.
        Первый треугольник: p = 1/n = 1/4.
        Гамильтонов цикл: p = (ln n + ln ln n)/n ≈ 0.69.
        Связность: p = ln(n)/n = ln4/4 ≈ 0.347.

        ЕТД при p = π/4 ≈ 0.785:
        > порога первого ребра (1/6 ≈ 0.167) ✓
        > порога треугольника (1/4 = 0.25) ✓
        > порога связности (0.347) ✓
        > порога Гамильтонова цикла (≈ 0.69) ✓

        Все пороги пройдены при p = π/4!
        4 порога < π/4 < 1: «π/4 = суперкритический режим!»

        Число пройденных порогов = 4 = N!
        """
        N = 4
        pi4 = math.pi / 4

        thresholds = {
            'первое_ребро': 1.0 / (N*(N-1)/2),
            'треугольник': 1.0 / N,
            'связность': math.log(N) / N,
            'Гамильтонов': (math.log(N) + math.log(math.log(N))) / N,
        }

        passed = {k: pi4 > v for k, v in thresholds.items()}
        n_passed = sum(passed.values())

        return {
            'теорема': 'Пороговые функции',
            'пороги': {k: round(v, 4) for k, v in thresholds.items()},
            'пройдено': passed,
            'число_пройденных': n_passed,
            'ЕТД': f'π/4={pi4:.3f} проходит все {n_passed}={N} порога! Суперкритический режим!',
        }

    # 4. МАЛЫЙ МИР
    @staticmethod
    def small_world_etd() -> dict:
        """
        Модель малого мира (Уоттс–Строгац 1998):
        Кластеризация C высокая + средний путь L малый.

        K₄ (полный граф на 4 вершинах):
        C(K₄) = 1 (каждая тройка — треугольник).
        L(K₄) = 1 (прямое ребро между любыми двумя).
        Диаметр = 1.

        Цикл C₄:
        C(C₄) = 0 (нет треугольников).
        L(C₄) = (1+1+2+2+1+1)/(6·2) = ...
        Средний путь: (4·1+2·2)/(6) = 8/6 = 4/3.
        Диаметр = 2.

        ЕТД при p = π/4:
        E[C] ≈ p = π/4 ≈ 0.785 (кластеризация ≈ p для G(n,p)).
        E[L] ≈ ln(n)/(ln(np)) = ln4/ln(π) ≈ 1.386/1.145 ≈ 1.21.
        «Малый мир: C ≈ π/4, L ≈ 1.2 — высокая кластеризация, короткие пути!»

        Число треугольников: C(4,3)·p³ = 4·(π/4)³ = 4·π³/64 = π³/16 ≈ 1.94.
        E[треугольников] ≈ 2 ≈ N/2!
        """
        N = 4
        pi4 = math.pi / 4

        # Кластеризация:
        C_cluster = pi4

        # Средний путь:
        np_val = N * pi4   # = π
        L_avg = math.log(N) / math.log(np_val) if np_val > 1 else float('inf')

        # Треугольники:
        E_triangles = math.comb(N, 3) * pi4**3

        return {
            'теорема': 'Малый мир (Уоттс–Строгац)',
            'C': round(C_cluster, 4),
            'L': round(L_avg, 4),
            'E[△]': round(E_triangles, 4),
            'ЕТД': f'C≈π/4={C_cluster:.3f}; L≈{L_avg:.3f}; E[△]={E_triangles:.2f}≈N/2; малый мир!',
        }


rg = RandomGraphsETD()

er = rg.erdos_renyi_etd()
print(f"Эрдёш-Реньи: {er['ЕТД']}")

gc = rg.giant_component_etd()
print(f"Гигантская: {gc['ЕТД']}")

tf = rg.threshold_functions_etd()
print(f"Пороги: {tf['ЕТД']}")

sw = rg.small_world_etd()
print(f"Малый мир: {sw['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы случайных графов** → **4 сетевых принципа ЕТД**:

1. **Эрдёш–Реньи** → G(4, π/4): E[рёбер] ≈ 3π/2; деревьев = 16 = N^(N−2)
2. **Гигантская** → c = π > 1: все 4 сферы связаны; GC ≈ N
3. **Пороги** → π/4 проходит все 4 = N порога (суперкритический!)
4. **Малый мир** → C ≈ π/4; L ≈ 1.2; E[треугольников] ≈ 2 = N/2

> **Сетевой портрет**: При p = π/4 граф G(4,p) суперкритический: все 4 порога пройдены, гигантская компонента = все вершины, кластеризация = π/4 (высокая!).

---
*ТОМ 245 / СЕРИЯ VII / ЕТД 2026*
