# ТОМ 247: 4 ТЕОРЕМЫ СТОХАСТИЧЕСКИХ ДУ → СТОХАСТИКА МАСТЕРСТВА ЕТД
## Ито · Стратонович · Диффузии · Формула Фейнмана–Каца
### «Ито–Стратонович–Фейнман–Кац: случайная динамика мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Стохастические ДУ (Ито 1944, Стратонович 1966, Фейнман–Кац 1949)

---

## ВВЕДЕНИЕ

Стохастические ДУ моделируют системы с шумом. 4 ключевых теоремы:

1. **Формула Ито** — цепное правило для стохастических процессов
2. **Интеграл Стратоновича** — симметричное определение
3. **Диффузии и генератор** — уравнение Колмогорова
4. **Формула Фейнмана–Каца** — связь вероятности и УрЧП

**Тезис тома**: dXₜ = −α(Xₜ − π/4)dt + σdWₜ — Орнштейн–Уленбек к π/4. Стационарное: N(π/4, σ²/(2α)). Формула Ито при f(x) = x² даёт «квантовую поправку» σ²t!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 СТОХАСТИЧЕСКИХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict

class StochasticDEETD:
    """
    Стохастические ДУ для динамики ЕТД.
    dXₜ = μ(Xₜ)dt + σ(Xₜ)dWₜ.
    ЕТД: dXₜ = −α(Xₜ − π/4)dt + σdWₜ (OU-процесс).
    """

    PI4 = math.pi / 4

    # 1. ФОРМУЛА ИТО
    @staticmethod
    def ito_formula_etd() -> dict:
        """
        Формула Ито:
        df(Xₜ) = f'(Xₜ)dXₜ + (1/2)f''(Xₜ)σ²dt.

        Дополнительный член (1/2)f''σ² — «квантовая поправка»!

        ЕТД: dXₜ = −α(Xₜ−π/4)dt + σdWₜ, α > 0.
        f(x) = (x − π/4)²:
        f' = 2(x−π/4), f'' = 2.

        df = 2(X−π/4)·(−α(X−π/4)dt + σdW) + (1/2)·2·σ²dt
           = [−2α(X−π/4)² + σ²]dt + 2σ(X−π/4)dW.

        E[d‖X−π/4‖²] = [−2α·E(X−π/4)² + σ²]dt.
        Стационарное: −2α·Var + σ² = 0 → Var = σ²/(2α).

        При σ = √(2α·(π/4)²) = (π/4)√(2α):
        Var = (π/4)² (дисперсия = квадрат масштаба!).
        Стд. откл. = π/4 = ħ_ЕТД!

        4 компоненты ЕТД:
        dXₖ = −α(Xₖ−π/4)dt + σdWₖ, k=1,...,4.
        Var(Σ Xₖ) = 4σ²/(2α) (при независимых Wₖ).
        Var(X̄) = σ²/(2αN) = σ²/(8α).
        «Среднее сходится в √N раз быстрее!»
        """
        pi4 = math.pi / 4
        N = 4

        alpha = 1.0
        sigma = pi4 * math.sqrt(2 * alpha)

        # Стационарная дисперсия:
        var_stat = sigma**2 / (2 * alpha)
        std_stat = math.sqrt(var_stat)

        # Дисперсия среднего:
        var_mean = var_stat / N

        # Время релаксации:
        tau = 1.0 / alpha

        return {
            'теорема': 'Формула Ито',
            'σ': round(sigma, 4),
            'Var_стац': round(var_stat, 4),
            'std_стац': round(std_stat, 4),
            '=π/4': abs(std_stat - pi4) < 1e-10,
            'Var_среднего': round(var_mean, 4),
            'τ': tau,
            'ЕТД': f'σ=(π/4)√(2α); std={std_stat:.4f}=π/4=ħ_ЕТД! Var(X̄)={(pi4**2/N):.4f}',
        }

    # 2. ИНТЕГРАЛ СТРАТОНОВИЧА
    @staticmethod
    def stratonovich_etd() -> dict:
        """
        Стратонович vs Ито:
        ∫f(X)∘dX = ∫f(X)dX + (1/2)∫f'(X)σ²dt.

        Стратонович: цепное правило обычное!
        df(X) = f'(X)∘dX (без дополнительного члена).

        ЕТД: Конвертация Ито ↔ Стратонович.
        dXₜ = μdt + σdWₜ (Ито)
        = (μ − (1/2)σσ')dt + σ∘dWₜ (Стратонович).

        Для OU (σ = const): μ_Стр = μ_Ито (нет поправки!).
        «Для линейного шума Ито = Стратонович.»

        Для нелинейного σ(x) = σ₀·x:
        dX = (μ − σ₀²x/2)dt + σ₀x·dW (Ито)
        = μdt + σ₀x∘dW (Стратонович).
        Поправка: −σ₀²x/2 — «шумовой дрейф».

        Геометрическое БД: dX = μXdt + σ₀XdW.
        X_T = X₀·exp((μ−σ₀²/2)T + σ₀W_T).
        E[X_T] = X₀·exp(μT).
        При μ = 0, T = 1: E[X] = X₀, но Med[X] = X₀·exp(−σ₀²/2) < X₀.
        При σ₀ = √(2·ln(4/π)) ≈ √0.472 ≈ 0.687:
        Med[X] = X₀·exp(−ln(4/π)/2)... = X₀·√(π/4) = X₀·√ħ!
        """
        pi4 = math.pi / 4
        N = 4

        # Для OU: нет поправки
        correction_OU = 0

        # Для геометрического:
        sigma_0 = math.sqrt(2 * math.log(4 / math.pi))
        median_ratio = math.exp(-sigma_0**2 / 2)
        sqrt_h = math.sqrt(pi4)

        return {
            'теорема': 'Интеграл Стратоновича',
            'поправка_OU': correction_OU,
            'σ₀_геом': round(sigma_0, 4),
            'Med/X₀': round(median_ratio, 4),
            '√(π/4)': round(sqrt_h, 4),
            'ЕТД': f'OU: Ито=Стратонович; геом: σ₀={sigma_0:.3f}→Med=X₀·√(π/4)=X₀·√ħ',
        }

    # 3. ДИФФУЗИИ И ГЕНЕРАТОР
    @staticmethod
    def diffusion_generator_etd() -> dict:
        """
        Генератор диффузии:
        Lf = μf' + (1/2)σ²f''.

        Уравнение Колмогорова (назад):
        ∂u/∂t = Lu, u(T,x) = g(x).

        Фоккер–Планк (вперёд):
        ∂p/∂t = −∂(μp)/∂x + (1/2)∂²(σ²p)/∂x².

        ЕТД: OU-процесс dX = −α(X−π/4)dt + σdW.
        L = −α(x−π/4)∂/∂x + (σ²/2)∂²/∂x².

        Стационарное решение Фоккер–Планк:
        p_∞(x) = N(π/4, σ²/(2α)).

        Спектр генератора L:
        Собственные значения: λₙ = −nα, n = 0,1,2,...
        Собственные функции: полиномы Эрмита Hₙ((x−π/4)/σ_∞).

        Спектральная щель: Δλ = λ₁ − λ₀ = −α − 0 = α.
        Время смешивания: t_mix = 1/α.

        Для 4D: L = Σ_{k=1}^4 Lₖ (независимые компоненты).
        Собственные значения: λ_{n₁,...,n₄} = −α·(n₁+n₂+n₃+n₄).
        Основное: λ₀ = 0 (стационарное).
        Первое возбуждённое: λ₁ = −α (4-кратно вырождено!).
        Вырождение первого уровня = N = 4!
        """
        pi4 = math.pi / 4
        N = 4
        alpha = 1.0
        sigma = pi4

        # Стационарная дисперсия:
        var_inf = sigma**2 / (2 * alpha)

        # Спектр:
        eigenvalues = [-n * alpha for n in range(5)]

        # Щель:
        gap = alpha

        # Вырождение λ₁ = −α:
        degeneracy_1 = N   # = 4 (по числу компонент)

        # Время смешивания:
        t_mix = 1.0 / alpha

        return {
            'теорема': 'Диффузии и генератор',
            'λₙ': eigenvalues,
            'щель': gap,
            'вырождение_1': degeneracy_1,
            't_mix': t_mix,
            'ЕТД': f'λₙ=−nα; щель=α; вырождение₁={degeneracy_1}=N; t_mix=1/α',
        }

    # 4. ФОРМУЛА ФЕЙНМАНА–КАЦА
    @staticmethod
    def feynman_kac_etd() -> dict:
        """
        Формула Фейнмана–Каца:
        u(t,x) = E[g(X_T)·exp(−∫ₜᵀ V(Xₛ)ds) | Xₜ=x]
        решает ∂u/∂t + Lu − Vu = 0, u(T,x) = g(x).

        ЕТД: V(x) = β·‖x−(π/4)⁴‖² (штраф за дисбаланс).
        g(x) = 1 (конечное условие).

        u(0, x) = E[exp(−β∫₀ᵀ‖Xₛ−x*‖²ds)].

        При x = x* = (π/4)⁴:
        ‖X−x*‖² стартует из 0, но растёт из-за шума.
        E[‖X−x*‖²] = Nσ²(1−e^{−2αt})/(2α).

        ∫₀ᵀ E[‖X−x*‖²]ds ≈ Nσ²T/(2α) (при T → ∞).

        «Энергия» диффузии: E = ∫ ‖X−x*‖² ~ Nσ²/(2α).
        При σ = (π/4)√(2α): E = N·(π/4)² = 4·(π/4)² = π²/4.
        E = π²/4 = (π/2)² — «квантовый потенциал»!

        u(0, x*) ≈ exp(−β·π²/4·T).
        При βT = 4/π²: u ≈ e⁻¹ ≈ 0.368.
        «π²/4 — характерная энергия диффузии ЕТД.»
        """
        pi4 = math.pi / 4
        pi = math.pi
        N = 4

        alpha = 1.0
        sigma = pi4 * math.sqrt(2 * alpha)

        # Энергия:
        E_diffusion = N * sigma**2 / (2 * alpha)
        pi2_over4 = pi**2 / 4

        return {
            'теорема': 'Фейнман–Кац',
            'E_диффузии': round(E_diffusion, 4),
            'π²/4': round(pi2_over4, 4),
            'совпадение': abs(E_diffusion - pi2_over4) < 1e-10,
            'ЕТД': f'E=Nσ²/(2α)=N(π/4)²={E_diffusion:.4f}=π²/4={pi2_over4:.4f}!',
        }


sde = StochasticDEETD()

it = sde.ito_formula_etd()
print(f"Ито: {it['ЕТД']}")

st = sde.stratonovich_etd()
print(f"Стратонович: {st['ЕТД']}")

dg = sde.diffusion_generator_etd()
print(f"Генератор: {dg['ЕТД']}")

fk = sde.feynman_kac_etd()
print(f"Фейнман-Кац: {fk['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы стохастических ДУ** → **4 стохастических принципа ЕТД**:

1. **Ито** → σ = (π/4)√(2α) → std_стац = π/4 = ħ_ЕТД!
2. **Стратонович** → OU: Ито = Стратонович; Med = X₀·√(π/4) для геометрического
3. **Генератор** → λₙ = −nα; вырождение₁ = 4 = N; щель = α
4. **Фейнман–Кац** → E = N(π/4)² = π²/4 (характерная энергия!)

> **Стохастический портрет**: OU-процесс с аттрактором π/4 имеет стационарную дисперсию (π/4)² при настроенном σ. Вырождение первого возбуждённого уровня = N = 4. Энергия диффузии = π²/4.

---
*ТОМ 247 / СЕРИЯ VII / ЕТД 2026*
