# ТОМ 248: 4 ТЕОРЕМЫ ГОМОЛОГИЧЕСКОЙ АЛГЕБРЫ → ГОМОЛОГИЯ МАСТЕРСТВА ЕТД
## Ext/Tor · Длинные последовательности · Производные функторы · Спектральные последовательности
### «Ext–Tor–Спектральные: гомологические структуры мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Гомологическая алгебра (Картан–Эйленберг 1956, Гротендик 1957, Серр 1955)

---

## ВВЕДЕНИЕ

Гомологическая алгебра — язык «неточностей» и «препятствий». 4 ключевых теоремы:

1. **Функторы Ext и Tor** — расширения и кручение модулей
2. **Длинная точная последовательность** — связь гомологий
3. **Производные функторы** — универсальные δ-функторы
4. **Спектральные последовательности** — итеративное вычисление

**Тезис тома**: Цепной комплекс ЕТД длины 4. Гомологии H_k: H₀ = ℤ, H₁ = 0, H₂ = 0, H₃ = ℤ (для S³). Ext¹(ℤ/4, ℤ) = ℤ/4. Tor₁(ℤ/4, ℤ/4) = ℤ/4!

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ГОМОЛОГИЧЕСКИХ ПРИНЦИПА ЕТД

```python
import math
from typing import List, Dict

class HomologicalAlgebraETD:
    """
    Гомологическая алгебра для системы ЕТД.
    Цепной комплекс: C₃ → C₂ → C₁ → C₀.
    Гомологии: Hₖ = Ker(∂ₖ)/Im(∂ₖ₊₁).
    """

    PI4 = math.pi / 4

    # 1. EXT И TOR
    @staticmethod
    def ext_tor_etd() -> dict:
        """
        Ext^n(A, B): n-е расширения A на B.
        Tor_n(A, B): n-е кручение.

        Для ℤ/N (N=4):
        Ext^0(ℤ/4, ℤ) = Hom(ℤ/4, ℤ) = 0.
        Ext^1(ℤ/4, ℤ) = ℤ/4.
        Ext^n(ℤ/4, ℤ) = 0 для n ≥ 2.

        Tor_0(ℤ/4, ℤ/4) = ℤ/4 ⊗ ℤ/4 = ℤ/4.
        Tor_1(ℤ/4, ℤ/4) = ℤ/4.
        Tor_n = 0 для n ≥ 2 (ℤ/4 имеет проективную размерность 1).

        ЕТД:
        |Ext^1(ℤ/4, ℤ)| = 4 = N!
        |Tor_1(ℤ/4, ℤ/4)| = 4 = N!
        «Число расширений = число кручений = число сфер!»

        Проективная размерность pd(ℤ/4) = 1.
        Инъективная размерность id(ℤ) = 1 (над ℤ: ℤ — PID).
        Глобальная размерность gl.dim(ℤ) = 1.

        Резольвента: 0 → ℤ →×4→ ℤ → ℤ/4 → 0.
        Длина = 1 (одна нетривиальная карта = ×4).
        """
        N = 4

        # Ext:
        ext_0 = 0   # Hom(ℤ/4, ℤ) = 0
        ext_1 = N   # |Ext¹(ℤ/4, ℤ)| = 4
        ext_2 = 0

        # Tor:
        tor_0 = N   # |ℤ/4 ⊗ ℤ/4| = 4
        tor_1 = N   # |Tor₁| = 4
        tor_2 = 0

        # Размерности:
        pd = 1   # проективная
        gl_dim = 1   # глобальная (для ℤ)

        return {
            'теорема': 'Ext и Tor',
            'Ext¹(ℤ/4,ℤ)': f'ℤ/{N}',
            '|Ext¹|': ext_1,
            'Tor₁(ℤ/4,ℤ/4)': f'ℤ/{N}',
            '|Tor₁|': tor_1,
            'pd': pd,
            'ЕТД': f'|Ext¹|=|Tor₁|={N}=N! pd=1; расширений=кручений=N_сфер',
        }

    # 2. ДЛИННАЯ ТОЧНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ
    @staticmethod
    def long_exact_sequence_etd() -> dict:
        """
        Короткая точная: 0 → A → B → C → 0.
        Длинная точная (в гомологиях):
        ... → Hₙ(A) → Hₙ(B) → Hₙ(C) →δ Hₙ₋₁(A) → ...

        ЕТД: 0 → ℤ →×4→ ℤ → ℤ/4 → 0.
        Длинная:
        0 → H₁(ℤ/4) → H₀(ℤ) →×4→ H₀(ℤ) → H₀(ℤ/4) → 0.
        = 0 → 0 → ℤ →×4→ ℤ → ℤ/4 → 0.

        Связывающий гомоморфизм δ: Hₙ(C) → Hₙ₋₁(A).
        δ «измеряет неточность»!

        Для расслоения: S¹ → S³ → S² (Хопф):
        ... → π₂(S²) →δ→ π₁(S¹) → π₁(S³) → π₁(S²) → ...
        = ... → ℤ →≅→ ℤ → 0 → 0 → ...
        δ: π₂(S²) → π₁(S¹) — изоморфизм!

        Число членов в конечном сегменте:
        0 → Hₙ(A) → Hₙ(B) → Hₙ(C) → Hₙ₋₁(A) → ...
        На каждом уровне: 3 члена + 1 связующий = 4 = N!
        """
        N = 4

        # Сегмент длинной последовательности:
        members_per_level = 3   # A, B, C
        connecting = 1   # δ
        total_per_level = members_per_level + connecting   # = 4 = N!

        # Гомологии S³:
        H_S3 = {0: 'ℤ', 1: '0', 2: '0', 3: 'ℤ'}

        # Эйлерова характеристика:
        chi_S3 = 1 - 0 + 0 - 1   # = 0

        return {
            'теорема': 'Длинная точная последовательность',
            'членов_на_уровне': total_per_level,
            '=N': total_per_level == N,
            'H(S³)': H_S3,
            'χ(S³)': chi_S3,
            'ЕТД': f'{total_per_level}=N членов на уровне; H(S³): H₀=H₃=ℤ, H₁=H₂=0; χ={chi_S3}',
        }

    # 3. ПРОИЗВОДНЫЕ ФУНКТОРЫ
    @staticmethod
    def derived_functors_etd() -> dict:
        """
        Производный функтор RⁿF = Hⁿ(F(I•)):
        R⁰F = F (сам функтор).
        RⁿHom(A, −) = Extⁿ(A, −).
        L_nTor = Torₙ(−, −).

        ЕТД: Производная категория D^b(ℤ/4-mod).
        Объекты: комплексы модулей над ℤ/4.
        Морфизмы: до квазиизоморфизма.

        Число простых ℤ/4-модулей:
        ℤ/4 — локальное кольцо с максимальным идеалом (2).
        Единственный простой модуль: ℤ/2.
        Число простых = 1.

        Но ℤ/4 = ℤ/2² — примарное!
        Длина ℤ/4 как ℤ/4-модуля: l(ℤ/4) = 2.
        l = 2 = N/2.

        Глобальная размерность:
        gl.dim(ℤ/4) = ∞ (не наследственное!).
        Периодическая резольвента: 0 → ℤ/4 →×2→ ℤ/4 →×2→ ℤ/4 → ...
        Период = 1 (×2 повторяется).

        Ext^n(ℤ/2, ℤ/2) = ℤ/2 для всех n ≥ 0.
        Бесконечная когомология! (ℤ/4 — «глубокое» кольцо.)
        """
        N = 4

        # Простые модули:
        n_simples = 1   # ℤ/2

        # Длина:
        length = 2   # = N/2

        # Глобальная размерность:
        gl_dim = float('inf')   # бесконечная!

        # Период:
        period = 1   # ×2 повторяется

        # Ext^n(ℤ/2, ℤ/2):
        ext_all = 'ℤ/2'   # для всех n

        return {
            'теорема': 'Производные функторы',
            'простых': n_simples,
            'длина': length,
            'gl_dim': '∞',
            'период': period,
            'Ext^n': ext_all,
            'ЕТД': f'ℤ/{N}: {n_simples} простой модуль; длина={length}=N/2; gl.dim=∞ (глубокое!)',
        }

    # 4. СПЕКТРАЛЬНЫЕ ПОСЛЕДОВАТЕЛЬНОСТИ
    @staticmethod
    def spectral_sequences_etd() -> dict:
        """
        Спектральная последовательность:
        {E_r^{p,q}, d_r}: E_2^{p,q} ⟹ H^{p+q}.

        Серр: для расслоения F → E → B:
        E_2^{p,q} = H^p(B; H^q(F)) ⟹ H^{p+q}(E).

        Хопф: S¹ → S³ → S²:
        E_2^{p,q} = H^p(S²; H^q(S¹)).
        H^p(S²): ℤ при p=0,2; 0 иначе.
        H^q(S¹): ℤ при q=0,1; 0 иначе.

        E_2 (таблица):
            q=0: ℤ 0 ℤ
            q=1: ℤ 0 ℤ
                 p=0 p=1 p=2

        d₂: E_2^{0,1} → E_2^{2,0}: ℤ → ℤ.
        d₂ — изоморфизм (иначе H³(S³) = ℤ⊕ℤ, противоречие).

        E_3 = E_∞:
            q=0: ℤ 0 0
            q=1: 0 0 0
                 p=0 p=1 p=2 (+ H³ = E_∞^{0,1}... нет)

        H⁰(S³) = ℤ, H³(S³) = ℤ (от ℤ на (2,1) не выживает? Нет,
        ℤ на (0,1) убита d₂, и (2,0) тоже → H¹=H²=0, H³ от (0,3)... )

        Число ненулевых E_2-членов = 4 = N!
        """
        N = 4

        # E_2-члены:
        E2_nonzero = {
            '(0,0)': 'ℤ', '(2,0)': 'ℤ',
            '(0,1)': 'ℤ', '(2,1)': 'ℤ',
        }
        n_nonzero = len(E2_nonzero)   # = 4 = N!

        # Дифференциал d₂:
        d2_iso = True   # d₂: E₂^{0,1} → E₂^{2,0} — изоморфизм

        # E_∞:
        E_inf = {'(0,0)': 'ℤ'}   # только H⁰ = ℤ
        # H³ приходит от E_∞^{3,0} или E_∞^{...}: в итоге H³(S³)=ℤ

        # Число страниц: r=2 до стабилизации: 1 шаг → E₃ = E_∞
        n_pages = 2   # стабилизация на E₃

        return {
            'теорема': 'Спектральные последовательности',
            'E₂_ненулевых': n_nonzero,
            '=N': n_nonzero == N,
            'd₂_изо': d2_iso,
            'страниц': n_pages,
            'ЕТД': f'{n_nonzero}=N ненулевых E₂-членов; d₂ — изоморфизм; стабилизация за {n_pages} шага',
        }


ha = HomologicalAlgebraETD()

et = ha.ext_tor_etd()
print(f"Ext/Tor: {et['ЕТД']}")

les = ha.long_exact_sequence_etd()
print(f"Длинная: {les['ЕТД']}")

df = ha.derived_functors_etd()
print(f"Производные: {df['ЕТД']}")

ss = ha.spectral_sequences_etd()
print(f"Спектральные: {ss['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

**4 теоремы гомологической алгебры** → **4 гомологических принципа ЕТД**:

1. **Ext/Tor** → |Ext¹| = |Tor₁| = 4 = N; pd = 1; расширений = кручений = N_сфер
2. **Длинная** → 4 = N членов на уровне; H(S³): H₀ = H₃ = ℤ; χ = 0
3. **Производные** → ℤ/4: 1 простой модуль; длина = 2 = N/2; gl.dim = ∞
4. **Спектральные** → 4 = N ненулевых E₂-членов; d₂ — изоморфизм

> **Гомологический портрет**: |Ext¹(ℤ/4,ℤ)| = 4 = N. Длинная точная последовательность имеет 4 члена на уровне. S³ — пространство ЕТД с H₀ = H₃ = ℤ. Спектральная последовательность Хопфа имеет 4 ненулевых E₂-члена.

---
*ТОМ 248 / СЕРИЯ VII / ЕТД 2026*
