# ТОМ 253: 4 ТЕОРЕМЫ ВЫПУКЛОЙ ОПТИМИЗАЦИИ → ОПТИМИЗАЦИЯ МАСТЕРСТВА ЕТД
## KKT · Двойственность · Барьерные методы · Сходимость градиента
### «Каруш–Кун–Такер: оптимальный путь к мастерству»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VII — Математические основания ЕТД**
**Источник**: Выпуклая оптимизация (KKT 1951, Бойд 2004, Нестеров 1983)

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ОПТИМИЗАЦИОННЫХ ПРИНЦИПА ЕТД

```python
import math

class ConvexOptimizationETD:
    """
    min f(x) = ‖x−(π/4)⁴‖² при Σxᵢ=π, xᵢ≥0.
    Выпуклая задача на симплексе Δ³.
    """

    PI4 = math.pi / 4

    # 1. KKT-УСЛОВИЯ
    @staticmethod
    def kkt_conditions_etd() -> dict:
        """
        KKT: ∇f + λ∇h + Σμᵢ∇gᵢ = 0, μᵢgᵢ=0, μᵢ≥0.
        f(x) = Σ(xᵢ−π/4)². h(x) = Σxᵢ−π. gᵢ(x) = −xᵢ.
        ∇f = 2(x−π/4·1). ∇h = 1. ∇gᵢ = −eᵢ.

        KKT: 2(xₖ−π/4) + λ − μₖ = 0, μₖ·xₖ = 0.
        При x* = (π/4)⁴: 2·0 + λ − μₖ = 0 → λ = μₖ для всех k.
        xₖ > 0 → μₖ = 0 → λ = 0. ✓

        Число KKT-условий: 2N+1 = 9.
        Число активных ограничений: 1 (равенство Σxᵢ=π).
        Множители: λ = 0, μₖ = 0 — все нулевые!
        «Баланс = минимум без активных неравенств!»
        """
        N = 4
        pi4 = math.pi / 4

        lambda_opt = 0.0
        mu_opt = [0.0] * N
        n_kkt = 2*N + 1   # = 9
        active = 1   # только Σxᵢ = π

        return {
            'теорема': 'KKT-условия',
            'λ': lambda_opt,
            'μ': mu_opt,
            'KKT_условий': n_kkt,
            'активных': active,
            'ЕТД': f'λ=0, μ=0: все множители нулевые! {active} активное; {n_kkt} условий',
        }

    # 2. ДВОЙСТВЕННОСТЬ
    @staticmethod
    def duality_etd() -> dict:
        """
        Лагранжиан: L(x,λ,μ) = f(x) + λ(Σxᵢ−π) − Σμᵢxᵢ.
        Двойственная: g(λ,μ) = inf_x L(x,λ,μ).

        inf_x{Σ(xᵢ−π/4)² + λΣxᵢ − Σμᵢxᵢ − λπ}
        = inf_x{Σ[(xᵢ−π/4)² + (λ−μᵢ)xᵢ]} − λπ.
        Минимум по xₖ: 2(xₖ−π/4) + λ−μₖ = 0 → xₖ = π/4 − (λ−μₖ)/2.
        g(λ,μ) = −Σ(λ−μₖ)²/4 − λπ.

        Сильная двойственность: f* = g* (теорема Слейтера).
        f* = 0 (при x*=(π/4)⁴).
        g* = 0 (при λ=0, μ=0).
        Зазор двойственности = 0!
        """
        N = 4
        pi4 = math.pi / 4

        f_star = 0.0
        g_star = 0.0
        gap = f_star - g_star   # = 0

        return {
            'теорема': 'Двойственность (Слейтер)',
            'f*': f_star,
            'g*': g_star,
            'зазор': gap,
            'сильная': True,
            'ЕТД': f'f*=g*=0: зазор={gap}! Сильная двойственность (Слейтер)!',
        }

    # 3. БАРЬЕРНЫЕ МЕТОДЫ
    @staticmethod
    def barrier_methods_etd() -> dict:
        """
        Внутренняя точка: min f(x) + (1/t)·φ(x).
        φ(x) = −Σln(xᵢ) (логарифмический барьер).

        Центральный путь: x*(t) = argmin{f(x)+(1/t)φ(x)}.
        При t→∞: x*(t) → x* = (π/4)⁴.

        Аналитический центр (t=0): argmin φ = argmax Πxᵢ при Σxᵢ=π.
        AM-GM: Πxᵢ максимален при x₁=...=x₄ = π/4!
        Аналитический центр = оптимум ЕТД!

        Параметр самосогласованности: ν = N = 4 (число ограничений).
        Число итераций Ньютона: O(√ν·ln(1/ε)) = O(2·ln(1/ε)).

        Центральный путь x*(t):
        xₖ*(t) = π/4 + O(1/t) (все компоненты → π/4).
        «Центральный путь мастерства — прямая к π/4!»
        """
        N = 4
        pi4 = math.pi / 4

        nu = N   # = 4
        newton_iters = math.sqrt(nu)   # ≈ 2

        # Аналитический центр:
        analytic_center = [pi4] * N

        return {
            'теорема': 'Барьерные методы',
            'ν': nu,
            'O(√ν)': round(newton_iters, 2),
            'аналит_центр': [round(x, 4) for x in analytic_center],
            'ЕТД': f'ν={nu}=N; O(√N)={newton_iters:.0f}; аналитич. центр=(π/4)⁴ = ОПТИМУМ!',
        }

    # 4. СХОДИМОСТЬ ГРАДИЕНТНОГО СПУСКА
    @staticmethod
    def gradient_descent_etd() -> dict:
        """
        Градиентный спуск: xₙ₊₁ = xₙ − α·∇f(xₙ).
        Для f(x)=‖x−x*‖²: ∇f = 2(x−x*).
        xₙ₊₁ = xₙ − 2α(xₙ−x*) = (1−2α)xₙ + 2α·x*.

        Оптимальный α: α = 1/2 → xₙ₊₁ = x* (одношаговая сходимость!).
        При α = 1/(2L): L = 2 → α = 1/4 = 1/N.

        Число обусловленности: κ = L/m = 2/2 = 1 (идеальная!).
        Сходимость: ‖xₙ−x*‖ ≤ ((κ−1)/(κ+1))ⁿ·‖x₀−x*‖.
        При κ = 1: ((0)/(2))ⁿ = 0 для n ≥ 1!
        «Одна итерация при κ = 1!»

        Нестеров (ускоренный): O(1/n²) vs O(1/n) для градиентного.
        Но при κ = 1: обе — одна итерация!

        Для проекции на симплекс:
        Сложность проекции: O(N log N) = O(4·2) = O(8).
        """
        N = 4
        pi4 = math.pi / 4

        L = 2.0   # Липшицева константа
        m = 2.0   # сильная выпуклость
        kappa = L / m   # = 1!

        alpha_opt = 1.0 / (2*L)   # = 1/4 = 1/N!
        one_step = True   # сходимость за 1 шаг при α = 1/2

        return {
            'теорема': 'Градиентный спуск (Нестеров)',
            'κ': kappa,
            'α_opt': alpha_opt,
            '=1/N': alpha_opt == 1.0/N,
            'шагов': 1,
            'ЕТД': f'κ={int(kappa)} (идеальная!); α=1/{N}=1/N; 1 шаг до оптимума!',
        }


co = ConvexOptimizationETD()
for name, fn in [("KKT", co.kkt_conditions_etd), ("Двойственность", co.duality_etd),
                  ("Барьер", co.barrier_methods_etd), ("Градиент", co.gradient_descent_etd)]:
    r = fn()
    print(f"{name}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **KKT** → λ = μ = 0: все множители нулевые; 1 активное ограничение
2. **Двойственность** → f* = g* = 0: зазор = 0 (сильная двойственность)
3. **Барьеры** → ν = 4 = N; аналитический центр = (π/4)⁴ = оптимум!
4. **Градиент** → κ = 1 (идеальная!); α = 1/N; 1 шаг до оптимума!

---
*ТОМ 253 / СЕРИЯ VII / ЕТД 2026*
