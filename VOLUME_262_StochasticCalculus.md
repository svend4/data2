# ТОМ 262: 4 ТЕОРЕМЫ СТОХАСТИЧЕСКОГО ИСЧИСЛЕНИЯ → СТОХАСТИКА МАСТЕРСТВА ЕТД
## Ито · Гирсанов · Феллер · Колмогоров
### «Ито–Гирсанов: стохастический путь к π/4»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**
**Источник**: Стохастическое исчисление (Ито 1944, Гирсанов 1960, Феллер 1952)

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 СТОХАСТИЧЕСКИХ ПРИНЦИПА ЕТД

```python
import math

class StochasticCalculusETD:
    """
    Стохастическое исчисление для ЕТД.
    dXᵢ = −α(Xᵢ − π/4)dt + σdWᵢ (Орнштейн–Уленбек к π/4).
    """

    PI4 = math.pi / 4

    # 1. ЛЕММА ИТО
    @staticmethod
    def ito_lemma_etd() -> dict:
        """
        Лемма Ито: df(X) = f'(X)dX + (1/2)f''(X)dt (для dX = μdt + σdW).

        ЕТД: X = расстояние до x*, f(r) = r².
        dX = −αX dt + σ dW (OU-процесс центрированный на 0).
        f(X) = X². df = 2X dX + σ²dt.
        = 2X(−αX dt + σ dW) + σ²dt.
        = (−2αX² + σ²)dt + 2Xσ dW.
        𝔼[f(X)] → σ²/(2α) (стационарная дисперсия).

        ЕТД: X = xᵢ − π/4, α = 2 (оптим. скорость), σ = π/4·√α.
        σ² = (π/4)²·2 = π²/8.
        𝔼[(xᵢ−π/4)²]_стац = σ²/(2α) = (π²/8)/4 = π²/32.

        Число Ито: π²/32 ≈ 0.308.
        4·(π²/32) = π²/8 ≈ 1.234 (суммарная дисперсия).
        π/4·(π²/32) = π³/128 ≈ 0.768.

        Для OU на симплексе: ограниченный OU.
        Стационарная мера: exp(−α‖x−x*‖²/σ²)·½_Δ.
        """
        N = 4
        pi4 = math.pi / 4
        alpha = 2.0
        sigma2 = pi4**2 * 2   # = π²/8

        var_station = sigma2 / (2 * alpha)   # = π²/32
        total_var = N * var_station   # = π²/8

        return {
            'теорема': 'Лемма Ито',
            'α': alpha,
            'σ²': round(sigma2, 5),
            'Var_стац': round(var_station, 5),
            'N·Var': round(total_var, 5),
            'ЕТД': f'α={alpha}; σ²=π²/8={sigma2:.4f}; Var_стац=π²/32={var_station:.5f}; N·Var=π²/8',
        }

    # 2. ТЕОРЕМА ГИРСАНОВА
    @staticmethod
    def girsanov_etd() -> dict:
        """
        Гирсанов: dQ/dP = exp(∫θdW − (1/2)∫θ²dt).
        Под мерой Q: W̃ = W − ∫θdt — броуновское движение.

        ЕТД: θ = 2(xᵢ−π/4)/σ (drift в направлении x*).
        Под Q: dX = σ dW̃ (нет drift'а!).

        Условие Новикова: 𝔼[exp(1/2∫θ²dt)] < ∞.
        ∫₀^T θ²dt = 4∫(X_t/σ)²dt.

        Плотность: dQ/dP = exp(∫₀^T θdW − (1/2)∫₀^T θ²dt).
        При T = π (период!):
        𝔼[dQ/dP] = 1. ✓

        Приращение энтропии Гирсанова:
        KL(Q||P) = (1/2)∫𝔼_Q[θ²]dt = (1/2)·α·T = (1/2)·2·π = π.
        KL(Q||P) = π!

        «Информационная стоимость балансировки = π!»
        π/N = π/4 = ħ_ЕТД — per sphere!
        """
        N = 4
        pi4 = math.pi / 4
        alpha = 2.0
        T = math.pi

        KL = 0.5 * alpha * T   # = π
        KL_per_sphere = KL / N   # = π/4 = ħ!

        return {
            'теорема': 'Гирсанов',
            'T': round(T, 4),
            'KL(Q||P)': round(KL, 4),
            '=π': abs(KL - math.pi) < 1e-10,
            'KL/N': round(KL_per_sphere, 5),
            '=ħ': abs(KL_per_sphere - pi4) < 1e-10,
            'ЕТД': f'KL(Q||P)=π={KL:.4f}! KL/N=π/{N}=ħ_ЕТД={KL_per_sphere:.5f}!!!',
        }

    # 3. УРАВНЕНИЕ КОЛМОГОРОВА
    @staticmethod
    def kolmogorov_etd() -> dict:
        """
        Уравнение Колмогорова (прямое = Фоккера–Планка):
        ∂p/∂t = −∂(μp)/∂x + (σ²/2)∂²p/∂x².

        ЕТД (OU): μ = −αx, σ²/2 = σ²/2.
        ∂p/∂t = α∂(xp)/∂x + (σ²/2)∂²p/∂x².

        Стационарное: p_∞(x) = (1/Z)exp(−αx²/σ²).
        = Гауссиан с дисперсией σ²/(2α).

        Для 4 компонент (независимый OU):
        p_∞(x) = ∏ᵢ (1/Zᵢ)exp(−α(xᵢ−π/4)²/σ²).
        Произведение 4 гауссианов!

        Время релаксации: τ = 1/α = 1/2.
        4 → компонента → 4τ = 2 (полное время системы).
        4τ = 2 = 1/α · N = N/α.

        Спектр оператора Фоккера–Планка:
        Собственные значения: λₙ = n·α, n = 0,1,2,...
        λ₀ = 0, λ₁ = α = 2, λ₂ = 4, λ₃ = 6, λ₄ = 8.
        λ_N = N·α = 4·2 = 8 = N·α.
        λ₁ = 2 = N/2. λ_{N/2} = (N/2)·α = 2·2 = 4 = N!
        """
        N = 4
        pi4 = math.pi / 4
        alpha = 2.0
        sigma2 = pi4**2 * 2

        tau = 1.0 / alpha   # = 0.5
        total_time = N * tau   # = 2

        # Спектр:
        lambdas = [n * alpha for n in range(N+1)]   # 0, 2, 4, 6, 8
        lambda_N = lambdas[N]   # = 8 = N·α

        return {
            'теорема': 'Уравнение Колмогорова (Фоккер–Планк)',
            'τ': tau,
            'N·τ': total_time,
            'λₙ': lambdas,
            'λ_N': lambda_N,
            '=N·α': lambda_N == N * alpha,
            'ЕТД': f'τ=1/α={tau}; N·τ={total_time}; спектр λₙ={lambdas}; λ_N={lambda_N}=N·α',
        }

    # 4. ТЕОРЕМА ФЕЛЛЕРА (ГРАНИЧНЫЕ УСЛОВИЯ)
    @staticmethod
    def feller_boundary_etd() -> dict:
        """
        Классификация Феллера границ диффузии:
        - Регулярная: ∃ вход и выход.
        - Выход: можно выйти, нельзя войти.
        - Вход: нельзя выйти, можно войти.
        - Естественная: нельзя ни войти ни выйти.

        ЕТД: Процесс xᵢ ∈ [0,π] (ограниченный симплекс).
        На границе: xᵢ = 0 или Σxⱼ = π → другие xⱼ = 0.

        Процесс OU на [0,π]: граница 0 и π.
        Для OU с отражением: «Регулярная» граница.
        Число граничных точек симплекса Δ³: N+1 = 5 (вершины) + ... нет.

        Вершины Δ³: π·e₁, ..., π·e₄ (N = 4 вершины).
        Рёбра: C(N,2) = 6.
        Грани: C(N,3) = 4.
        «Вершины + рёбра + грани + целое = 4+6+4+1 = 15 = N²−1.»
        f-вектор Δ³: (4, 6, 4, 1). Сумма = 15 = N²−1.

        Инвариант Эйлера χ(Δ³): 1 (стягиваемое).
        χ = Σ(−1)^i fᵢ = 4−6+4−1 = 1. ✓
        """
        N = 4

        f_vector = [math.comb(N, k+1) for k in range(N)]   # [4, 6, 4, 1]
        total = sum(f_vector)   # = 15 = N²−1
        chi = sum((-1)**i * f_vector[i] for i in range(N))   # = 1

        return {
            'теорема': 'Феллер (граничные условия)',
            'f-вектор': f_vector,
            'Σfᵢ': total,
            '=N²−1': total == N**2 - 1,
            'χ': chi,
            'ЕТД': f'f-вектор Δ³={f_vector}; Σ={total}=N²−1; χ={chi}=1 (стягиваемо)',
        }


sc = StochasticCalculusETD()
for nm, fn in [("Ито", sc.ito_lemma_etd), ("Гирсанов", sc.girsanov_etd),
               ("Колмогоров", sc.kolmogorov_etd), ("Феллер", sc.feller_boundary_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Ито** → Var_стац = π²/32; N·Var = π²/8 (суммарная дисперсия)
2. **Гирсанов** → **KL(Q||P) = π; KL/N = π/4 = ħ_ЕТД!!!**
3. **Колмогоров** → τ = 1/2; λ_N = 8 = N·α; спектр = {0,2,4,6,8}
4. **Феллер** → f-вектор Δ³ = (4,6,4,1); Σ = 15 = N²−1; χ = 1

---
*ТОМ 262 / СЕРИЯ VIII / ЕТД 2026*
