# ТОМ 263: 4 ТЕОРЕМЫ ИНФОРМАЦИОННОЙ ГЕОМЕТРИИ → ГЕОМЕТРИЯ ИНФОРМАЦИИ ЕТД
## Фишер · Чатсуис-Рао · α-геодезика · Теорема Крамера–Рао
### «Фишер–Рао–Амари: информационная геометрия мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**
**Источник**: Информационная геометрия (Амари 1982, Рао 1945, Крамер 1946)

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ИНФОРМАЦИОННО-ГЕОМЕТРИЧЕСКИХ ПРИНЦИПА ЕТД

```python
import math

class InformationGeometryETD:
    """
    Информационная геометрия для ЕТД.
    Статистическое многообразие S = {p(x;θ): θ ∈ Θ}.
    Для симплекса Δ³: θ = (x₁,x₂,x₃) (x₄ = π−Σx_{1:3}).
    Метрика Фишера–Рао на S = Δ³.
    """

    PI4 = math.pi / 4

    # 1. МЕТРИКА ФИШЕРА–РАО
    @staticmethod
    def fisher_rao_metric_etd() -> dict:
        """
        Метрика Фишера: gᵢⱼ(θ) = 𝔼[∂ᵢ log p · ∂ⱼ log p].

        Для симплекса Δ^{N-1} (вероятностного!):
        pᵢ = xᵢ/π (нормированные).
        gᵢⱼ = δᵢⱼ/pᵢ (метрика Фишера на Δ^{N-1}).

        Геодезическое расстояние (Фишера–Рао):
        d_FR(p, q) = 2·arccos(Σᵢ√(pᵢqᵢ)).

        ЕТД: p* = (1/4,...,1/4) (равномерное).
        d_FR(p, p*) = 2·arccos(Σᵢ√(pᵢ/4)) = 2·arccos((1/2)Σᵢ√pᵢ).
        По Коши-Шварцу: Σᵢ√pᵢ ≤ √N = 2 (с равенством при pᵢ=1/N).
        d_FR(p*, p*) = 2·arccos(2·(1/2)) = 2·arccos(1) = 0. ✓

        Диаметр Δ³ (Фишера–Рао):
        d_max = 2·arccos(0) = π (между ортогональными вершинами).
        d_max = π!

        d_FR(вершина, p*) = 2·arccos((1/2)·√1) = 2·arccos(1/2) = 2·π/3 = 2π/3.
        2π/3 ≈ 2.094.
        """
        N = 4
        pi4 = math.pi / 4

        # Диаметр:
        d_max = math.pi

        # Расстояние от вершины до центра:
        d_vertex_center = 2 * math.acos(0.5)   # = 2·π/3

        return {
            'теорема': 'Метрика Фишера–Рао',
            'd_max': round(d_max, 4),
            '=π': abs(d_max - math.pi) < 1e-10,
            'd(vertex,p*)': round(d_vertex_center, 4),
            '=2π/3': abs(d_vertex_center - 2*math.pi/3) < 1e-10,
            'ЕТД': f'd_max=π={d_max:.4f}; d(вершина,p*)=2π/3={d_vertex_center:.4f}',
        }

    # 2. α-ГЕОДЕЗИКИ АМАРИ
    @staticmethod
    def alpha_geodesics_etd() -> dict:
        """
        α-геометрия Амари:
        α-связность Γ^{(α)}: интерполирует между e- и m-геодезиками.
        α = 1: экспоненциальная геодезика (e-прямая).
        α = −1: смешанная геодезика (m-прямая).
        α = 0: геодезика по Леви-Чивита.

        Дивергенция Брегмана: D_φ(p||q) = φ(p) − φ(q) − ⟨∇φ(q), p−q⟩.
        При φ = Σpᵢlog pᵢ: D_φ = KL-дивергенция.

        ЕТД: D_KL(p||p*) = Σpᵢlog(pᵢ/pᵢ*).
        = Σpᵢlog(4pᵢ) = Σpᵢ(log4 + log pᵢ).
        = log4 − H(p) (где H — энтропия Шеннона).
        D_KL(p||p*) = log 4 − H(p).

        При H(p) = log 4 (максимальная, p=p*): D_KL = 0. ✓
        При H(p) = 0 (вершина): D_KL = log 4 = 2ln2 ≈ 1.386.

        D_KL(p*||p*) = 0.
        D_KL(вершина||p*) = log 4 = 2ln2.
        2ln2 = ln4 = ln N!
        """
        N = 4
        pi4 = math.pi / 4

        D_vertex_center = math.log(N)   # = ln 4
        H_max = math.log(N)   # = ln 4

        return {
            'теорема': 'α-геодезики (Амари)',
            'D_KL(вершина||p*)': round(D_vertex_center, 5),
            '=lnN': abs(D_vertex_center - math.log(N)) < 1e-10,
            'H_max': round(H_max, 5),
            'ЕТД': f'D_KL(вершина||p*)=ln{N}={D_vertex_center:.4f}=lnN; D_KL=lnN−H(p)',
        }

    # 3. ГРАНИЦА КРАМЕРА–РАО
    @staticmethod
    def cramer_rao_etd() -> dict:
        """
        Крамер–Рао: Var[T̂] ≥ 1/I(θ) (для несмещённых оценок).
        I(θ) — информация Фишера.

        ЕТД: Оценка θ̂ = x̄ = Σxᵢ/N (среднее).
        I(θ) = N/σ² (для нормального с неизвестным средним).

        При σ² = π²/32 (стационарная дисперсия из Тома 262):
        I(θ) = N/(π²/32) = 32N/π².
        При N = 4: I = 128/π² ≈ 12.98.
        Var[θ̂] ≥ 1/I = π²/128.

        Нижняя граница: π²/128 ≈ 0.0770.
        θ̂ достигает границы (эффективная оценка при нормальном!).
        Var[θ̂_эфф] = π²/128 = (π/4)²/(N·2) = ħ²/(N·2).

        ħ² = (π/4)² = π²/16.
        ħ²/(N·2) = π²/(16·8) = π²/128. ✓

        «Граница Крамера–Рао = ħ²/(2N)!»
        """
        N = 4
        pi4 = math.pi / 4
        sigma2 = pi4**2 / 2   # = π²/32

        fisher_info = N / sigma2   # = 128/π²
        cramer_rao_bound = 1.0 / fisher_info   # = π²/128

        return {
            'теорема': 'Граница Крамера–Рао',
            'I(θ)': round(fisher_info, 4),
            'Var_CR': round(cramer_rao_bound, 6),
            '=ħ²/2N': abs(cramer_rao_bound - pi4**2 / (2*N)) < 1e-12,
            'ЕТД': f'I={fisher_info:.4f}=128/π²; Var_CR=π²/128=ħ²/(2N)={cramer_rao_bound:.6f}!',
        }

    # 4. ТЕОРЕМА ПИТМАНА–КОЭНА (достаточная статистика)
    @staticmethod
    def pitman_sufficiency_etd() -> dict:
        """
        Критерий факторизации Неймана: T достаточна ⟺
        p(x;θ) = h(x)·g(T(x); θ).

        ЕТД: Наблюдения x = (x₁,x₂,x₃,x₄) ∈ Δ³.
        Параметр θ = x* = π/4 (неизвестный оптимум).
        T(x) = Σxᵢ/N (выборочное среднее) — достаточная!

        T(x) = π/4 при x = x* → несмещённая оценка.
        |Σxᵢ/N − π/4| = 0 при x = x*.

        Для нормального: (x̄, S²) совместно достаточны.
        x̄: dim = 1. S²: dim = 1. Итого = 2.
        N − 2 = 2 = N/2 степеней свободы для ошибки.

        Экспоненциальное семейство: N природных параметров.
        Для N = 4: 4 параметра = 4 достаточных статистики.
        Dim достаточного вектора = N = 4!
        """
        N = 4
        pi4 = math.pi / 4

        T_at_optimum = pi4   # = π/4 ✓
        dof_error = N - 2   # = 2 = N/2
        dim_sufficient = N   # = 4

        return {
            'теорема': 'Достаточная статистика (Питман)',
            'T(x*)': round(T_at_optimum, 4),
            '=π/4': abs(T_at_optimum - pi4) < 1e-10,
            'df_ошибки': dof_error,
            'dim_T': dim_sufficient,
            '=N': dim_sufficient == N,
            'ЕТД': f'T(x*)=π/4=ħ; df={dof_error}=N/2; dim(T)={dim_sufficient}=N',
        }


ig = InformationGeometryETD()
for nm, fn in [("Фишера-Рао", ig.fisher_rao_metric_etd), ("α-геодез", ig.alpha_geodesics_etd),
               ("Крамер-Рао", ig.cramer_rao_etd), ("Питман", ig.pitman_sufficiency_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Фишера–Рао** → d_max = π; d(вершина,p*) = 2π/3
2. **α-геодезики** → D_KL(вершина||p*) = ln N = ln 4; D_KL = ln N − H(p)
3. **Крамер–Рао** → I = 128/π²; **Var_CR = π²/128 = ħ²/(2N)!!!**
4. **Питман** → T(x*) = π/4 = ħ; df = N/2; dim(T) = N

---
*ТОМ 263 / СЕРИЯ VIII / ЕТД 2026*
