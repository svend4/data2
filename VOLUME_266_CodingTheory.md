# ТОМ 266: 4 ТЕОРЕМЫ ТЕОРИИ КОДИРОВАНИЯ → КОДЫ МАСТЕРСТВА ЕТД
## Шеннон · Хэмминг · Линейные коды · Граница Плоткина
### «Шеннон–Хэмминг–Плоткин: информационное кодирование мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 КОДОВЫХ ПРИНЦИПА ЕТД

```python
import math

class CodingTheoryETD:
    PI4 = math.pi / 4

    # 1. ТЕОРЕМА ШЕННОНА О КАНАЛЕ
    @staticmethod
    def shannon_channel_etd() -> dict:
        """
        Пропускная способность: C = max_{p(x)} I(X;Y).
        I(X;Y) = H(Y) − H(Y|X).

        ЕТД (N=4 входов равновероятных):
        H(X) = log₂(4) = 2 бит = N/2 бит/сигнал!
        Для симметричного канала с p_err=ε:
        C = 2 − H_b(ε)·... нет, для N-ary: C = log₂N − H(шум).

        При ε → 0: C → log₂(4) = 2 = N/2.
        «Пропускная способность 4-ичного канала = 2 = N/2 бит!»

        Пропускная способность в нат: C = ln(4) = 2ln2 = ln(N).
        ln(N) = ln(4) = 2ln2 ≈ 1.386 нат.

        Для 4-QAM (квадратная сигнализация):
        C = log₂(1 + SNR). При SNR = 15: C = log₂(16) = 4 = N.
        «При SNR = N²−1 = 15: C = N!»
        """
        N = 4
        pi4 = math.pi / 4

        H_max = math.log2(N)   # = 2 = N/2
        C_nat = math.log(N)   # = ln4
        SNR_for_C_N = N**2 - 1   # = 15

        return {
            'теорема': 'Шеннон (канал)',
            'H_max': H_max,
            '=N/2': H_max == N/2,
            'C_nat': round(C_nat, 4),
            'SNR_CN': SNR_for_C_N,
            'ЕТД': f'H_max=log₂({N})={H_max}=N/2 бит; C_нат=ln{N}={C_nat:.4f}; SNR={SNR_for_C_N}=N²−1→C=N',
        }

    # 2. КОД ХЭММИНГА
    @staticmethod
    def hamming_code_etd() -> dict:
        """
        Код Хэмминга [n,k,d]:
        n = 2^r − 1, k = n − r, d = 3 (исправляет 1 ошибку).

        При r = 2: [3,1,3] — код-повторение.
        При r = 3: [7,4,3] — классический Хэмминг!

        ЕТД: r = 2 → [3,1,3] (мало).
        r = 3 → [7,4,3]: k = 4 = N!
        «Код Хэмминга [7,4,3]: информационных символов k=4=N!»

        n = 7 (длина слова).
        k = 4 = N (информационных).
        d = 3 = N−1 (минимальное расстояние).
        Избыточность: r = n−k = 7−4 = 3 = N−1.
        Скорость: R = k/n = 4/7 = N/(2^N−1).

        Матрица контроля H: 3×7 (r×n).
        Матрица информации G: 4×7 (k×n).

        Число кодовых слов: 2^k = 2^4 = 16 = N².
        Расстояние до ближайшего = 3 = N−1.
        «[7,4,3]: k=N, d=N−1, 2^N=N² слов!»
        """
        N = 4
        r = 3   # проверочных символов
        n = 2**r - 1   # = 7
        k = n - r   # = 4 = N!
        d = 3   # = N−1
        rate = k / n
        n_codewords = 2**k   # = 16 = N²

        return {
            'теорема': 'Код Хэмминга [7,4,3]',
            'n': n,
            'k': k,
            '=N': k == N,
            'd': d,
            '=N−1': d == N-1,
            '2^k': n_codewords,
            '=N²': n_codewords == N**2,
            'ЕТД': f'[{n},{k},{d}]: k={k}=N, d={d}=N−1, 2^k={n_codewords}=N²',
        }

    # 3. ЛИНЕЙНЫЕ КОДЫ И ГРАНИЦА СИНГЛТОНА
    @staticmethod
    def singleton_bound_etd() -> dict:
        """
        Граница Синглтона: d ≤ n − k + 1 (для [n,k,d]-кода).
        Коды MDS (Maximum Distance Separable): d = n−k+1.

        ЕТД: [N,k,d] = [4,2,3] (двоичный)?
        Проверка: d ≤ n−k+1 = 4−2+1 = 3. ✓ MDS если d=3.
        Но двоичный MDS с d=3 существует?

        q-ичный MDS: Reed–Solomon коды [n,k,d]_q, d=n−k+1.
        RS-код длины n над F_q: n ≤ q+1 (или n ≤ q−1 для q нечётного).

        ЕТД: q = 4 (поле GF(4)!).
        RS-код [n,k,d]_4 с n ≤ 5 = p(N).
        Лучший: [5,2,4]_4 или [4,2,3]_4.
        [4,2,3]_4: n=4=N, k=2=N/2, d=3=N−1.
        «Лучший MDS-код над GF(4) длины N!»

        Число кодовых слов [4,2,3]_4: 4^2 = 16 = N².
        """
        N = 4

        # [4,2,3]_4:
        n, k, d = N, N//2, N-1
        singleton_bound = n - k + 1   # = 3 = d → MDS!
        is_MDS = d == singleton_bound
        n_codewords = N**k   # = 16 = N²

        return {
            'теорема': 'Граница Синглтона (MDS)',
            'код': f'[{n},{k},{d}]_{{GF(N)}}',
            'MDS': is_MDS,
            '2^k': n_codewords,
            '=N²': n_codewords == N**2,
            'ЕТД': f'[{n},{k},{d}]_GF({N}): MDS={is_MDS}! n=N, k=N/2, d=N−1; {n_codewords}=N² слов',
        }

    # 4. ГРАНИЦА ПЛОТКИНА
    @staticmethod
    def plotkin_bound_etd() -> dict:
        """
        Граница Плоткина: для бинарного [n,k,d] с d > n/2:
        2^k ≤ 2d/(2d−n).

        ЕТД: [n,k,d] = [4,2,3]. d=3 > n/2=2. ✓
        2^k ≤ 2·3/(2·3−4) = 6/2 = 3.
        2^2 = 4 > 3: граница нарушена!

        → Бинарного [4,2,3]-кода не существует!
        (Что соответствует: минимальный [n,2,3]₂ = [5,2,3]₂.)

        Для q=4: граница Плоткина другая.
        Аналог: A_q(n,d) ≤ q·d/(q·d−(q−1)·n) при d > (q−1)·n/q.
        A_4(4,3): (q−1)·n/q = 3·4/4 = 3. d=3 = 3. Граница достигается!
        A_4(4,3) ≤ 4·3/(4·3−3·4) = 12/0 → ∞ (граница не активна!).

        Коды над GF(4) свободнее!

        Оптимальный двоичный [n,k,3]: Хэмминг [7,4,3]. ✓
        Расстояние Хэмминга ≤ N: d ≤ N!
        """
        N = 4

        # Plotkin для [4,2,3]₂:
        n, k, d = 4, 2, 3
        plotkin_bound = 2*d / (2*d - n)   # = 3
        actual_2k = 2**k   # = 4
        violates = actual_2k > plotkin_bound

        return {
            'теорема': 'Граница Плоткина',
            'Plotkin[4,2,3]₂': round(plotkin_bound, 2),
            '2^k': actual_2k,
            'нарушает': violates,
            '→': '[4,2,3]₂ не существует!',
            'ЕТД': f'Plotkin([{n},{k},{d}]₂)={plotkin_bound}: 2^{k}={actual_2k}>{plotkin_bound} → код не существует!',
        }


ct = CodingTheoryETD()
for nm, fn in [("Шеннон", ct.shannon_channel_etd), ("Хэмминг", ct.hamming_code_etd),
               ("Синглтон", ct.singleton_bound_etd), ("Плоткин", ct.plotkin_bound_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Шеннон** → H_max = log₂(N) = 2 = N/2 бит; SNR = N²−1 = 15 → C = N
2. **Хэмминг** → **[7,4,3]: k = 4 = N, d = 3 = N−1, 2^k = 16 = N²**
3. **Синглтон** → [4,2,3]_{GF(N)}: MDS! n=N, k=N/2, d=N−1
4. **Плоткин** → [4,2,3]₂ не существует (2^2 > граница!)

---
*ТОМ 266 / СЕРИЯ VIII / ЕТД 2026*
