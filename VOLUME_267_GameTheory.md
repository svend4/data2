# ТОМ 267: 4 ТЕОРЕМЫ ТЕОРИИ ИГР → ИГРОВАЯ ТЕОРИЯ МАСТЕРСТВА ЕТД
## Нэш · Зерминело · Фон Нейман (минимакс) · Шепли
### «Нэш–Шепли: равновесие мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ИГРОВЫХ ПРИНЦИПА ЕТД

```python
import math

class GameTheoryETD:
    PI4 = math.pi / 4

    # 1. РАВНОВЕСИЕ НЭША
    @staticmethod
    def nash_equilibrium_etd() -> dict:
        """
        Нэш (1950): В каждой конечной игре ∃ равновесие Нэша (в смешанных стратегиях).

        ЕТД: 4-х игровая симметричная кооперативная игра.
        Игроки: {МВС, СВС, БВС, ЧВС} = N = 4.
        Стратегии: xᵢ ∈ [0, π] (интенсивность).

        Кооперативная игра (общий выигрыш):
        u(x) = −Σᵢ(xᵢ−π/4)² (максимизация).
        Нэш: xᵢ* = π/4 для всех i (уникальное!).

        Некооперативная (конкурентная):
        uᵢ(x) = −(xᵢ−π/4)² − ε·Σⱼ≠ᵢ xⱼ.
        При ε→0: xᵢ* = π/4 (тот же!).

        «Баланс = равновесие Нэша для любого ε!»
        Устойчивость: ∂²uᵢ/∂xᵢ² = −2 < 0 (строгий максимум).

        Число равновесий Нэша: 1 (единственное!).
        Для общей 4-х игровой: ≥ 1 (теорема Нэша).
        """
        N = 4
        pi4 = math.pi / 4

        nash_eq = [pi4] * N
        n_equilibria = 1

        return {
            'теорема': 'Равновесие Нэша',
            'x*': [round(v, 4) for v in nash_eq],
            'n_равн': n_equilibria,
            '∂²u/∂x²': -2,
            'ЕТД': f'x*=(π/4)⁴=ħ; {n_equilibria} равновесие (уникальное!); ∂²u/∂x²=−2<0',
        }

    # 2. ТЕОРЕМА ЗЕРМИНЕЛО
    @staticmethod
    def zermelo_etd() -> dict:
        """
        Зерминело (1913): В конечной игре с совершенной информацией
        один из игроков имеет выигрышную стратегию (или ничья).

        ЕТД: Игра «Оптимизация за N ходов».
        Ходы: x₁, x₂, x₃, x₄ (по одному от каждой сферы).
        Выигрыш: Σ(xᵢ−π/4)² < ε (баланс достигнут).

        Дерево игры: глубина = N = 4.
        Число листьев ≤ ∞ (непрерывные стратегии).
        Но: оптимальная стратегия = всегда xᵢ = π/4!
        «Выигрышная стратегия = баланс! Детерминирована!»

        Для дискретной игры (xᵢ ∈ {0,1,...,K}):
        Дерево глубины N: K^N листьев.
        При K = 4 = N: K^N = 4⁴ = 256 = N^N!
        Выигрышных листьев: 1 (xᵢ = 1 → представляет π/4).

        Коэффициент разветвления: K = N = 4.
        N^N = 4⁴ = 256 = N^N.
        """
        N = 4

        K = N
        n_leaves = K**N   # = 256 = N^N
        winning_leaves = 1

        return {
            'теорема': 'Зерминело',
            'K': K,
            'листьев': n_leaves,
            '=N^N': n_leaves == N**N,
            'выигрышных': winning_leaves,
            'ЕТД': f'K={K}; {n_leaves}=N^N листьев; {winning_leaves} выигрышный; стратегия x*=(π/4)⁴',
        }

    # 3. МИНИМАКС ФОН НЕЙМАНА
    @staticmethod
    def minimax_etd() -> dict:
        """
        Фон Нейман (1928): max_x min_y f(x,y) = min_y max_x f(x,y).
        (Для конечных антагонистических игр с нулевой суммой.)

        ЕТД: f(x,y) = −Σᵢ(xᵢ−yᵢ)² (отрицание квадратичного расстояния).
        Один игрок: max_x f(x,y) = 0 при x = y.
        Другой: min_y f(x,y) = −∞ (без ограничений).

        Ограниченная версия: x,y ∈ Δ³.
        max_x min_y f(x,y): лучший x = (π/4)⁴!
        min_y f(x,y) = f(x*, x) = −‖x*−x‖² ≤ 0.
        «Баланс = минимаксная стратегия!»

        Седловая точка: (x*, x*).
        f(x*, x*) = 0 (максимальный минимум = минимальный максимум = 0).

        Значение игры = 0 при x = y = x*.
        f(x*, y) = −‖x*−y‖² ≤ 0 для всех y. ✓ (минимакс ≤ 0.)
        f(x, x*) = −‖x−x*‖² ≤ 0. ✓
        """
        N = 4
        pi4 = math.pi / 4

        saddle_point = ([pi4]*N, [pi4]*N)
        game_value = 0.0

        return {
            'теорема': 'Минимакс (фон Нейман)',
            'седловая': 'x*=y*=(π/4)⁴',
            'значение': game_value,
            'ЕТД': f'Седловая=(x*,x*)=(π/4)⁴; V=0; баланс=минимаксная стратегия!',
        }

    # 4. ЗНАЧЕНИЕ ШЕПЛИ
    @staticmethod
    def shapley_value_etd() -> dict:
        """
        Значение Шепли: φᵢ(v) = Σ_{S⊆N\{i}} |S|!(N−|S|−1)!/N! · [v(S∪{i})−v(S)].
        Единственное справедливое распределение (эффективность + симметрия + нулевой игрок + аддитивность).

        ЕТД: N = 4 игрока. v(S) = |S|·π/4 (вклад пропорционален).
        v(S∪{i}) − v(S) = π/4 (маргинальный вклад = π/4 = ħ!).

        φᵢ = Σ |S|!(N−|S|−1)!/N! · (π/4).
        = (π/4) · Σ_{k=0}^{N-1} C(N−1,k) · k!(N−1−k)!/N! (k=|S|).
        = (π/4) · Σ C(N−1,k) / (N · C(N−1,k)) = (π/4) · 1 = π/4.
        φᵢ = π/4 = ħ для каждого i!

        «Значение Шепли = π/4 = ħ_ЕТД для каждой сферы!»

        Сумма: Σφᵢ = N·(π/4) = π = v(N). ✓ (Эффективность!)
        """
        N = 4
        pi4 = math.pi / 4

        shapley_each = pi4   # = π/4 для каждого!
        total = N * shapley_each   # = π = v(N)

        return {
            'теорема': 'Значение Шепли',
            'φᵢ': round(shapley_each, 5),
            '=ħ': abs(shapley_each - pi4) < 1e-10,
            'Σφᵢ': round(total, 4),
            '=π': abs(total - math.pi) < 1e-8,
            'ЕТД': f'φᵢ=π/{N}=ħ={shapley_each:.5f} для каждой! Σφᵢ=π (эффективность!)',
        }


gt = GameTheoryETD()
for nm, fn in [("Нэш", gt.nash_equilibrium_etd), ("Зерминело", gt.zermelo_etd),
               ("Минимакс", gt.minimax_etd), ("Шепли", gt.shapley_value_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Нэш** → x* = (π/4)⁴ = ħ (единственное равновесие!)
2. **Зерминело** → 4⁴ = N^N = 256 листьев; выигрышная стратегия = x*
3. **Минимакс** → Седловая = (x*, x*) = (π/4)⁴; V = 0
4. **Шепли** → **φᵢ = π/4 = ħ для каждой сферы!** Σφᵢ = π (эффективность!)

---
*ТОМ 267 / СЕРИЯ VIII / ЕТД 2026*
