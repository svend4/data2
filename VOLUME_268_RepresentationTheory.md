# ТОМ 268: 4 ТЕОРЕМЫ ТЕОРИИ ПРЕДСТАВЛЕНИЙ → ПРЕДСТАВЛЕНИЯ МАСТЕРСТВА ЕТД
## Машке · Шур · Характеры · Теорема Питера–Вейля
### «Машке–Шур–Вейль: представления группы мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ПРЕДСТАВЛЕНЧЕСКИХ ПРИНЦИПА ЕТД

```python
import math

class RepresentationTheoryETD:
    """
    Теория представлений для ЕТД.
    Группа G = S₄ (перестановки 4 элементов).
    |S₄| = 24 = N! = 4!.
    """

    PI4 = math.pi / 4

    # 1. ТЕОРЕМА МАШКЕ
    @staticmethod
    def maschke_etd() -> dict:
        """
        Машке (1898): Если char(k) не делит |G|, то kG полупроста.
        Каждое представление = прямая сумма неприводимых.

        ЕТД: G = S₄, |G| = 24 = N!
        Над ℝ или ℂ: char = 0, не делит |G| → полупростое.

        Неприводимые представления S₄:
        1: тривиальное (dim 1).
        sgn: знаковое (dim 1).
        std: стандартное (dim 3 = N−1).
        std⊗sgn: (dim 3 = N−1).
        ρ: (dim 2 = N/2).

        5 = p(N) неприводимых! ✓

        Σ dim² = 1+1+9+9+4 = 24 = N!. ✓ (Формула Бернсайда–Маклейна.)

        dim(std) = 3 = N−1 (стандартное = нестандартное-1).
        dim(ρ) = 2 = N/2.
        """
        N = 4
        dims = [1, 1, 3, 3, 2]   # dim неприводимых S₄
        n_irreps = len(dims)   # = 5 = p(N)!
        sum_sq = sum(d**2 for d in dims)   # = 24 = N!

        return {
            'теорема': 'Машке (полупростота)',
            'n_irreps': n_irreps,
            '=p(N)': n_irreps == 5,
            'dims': dims,
            'Σdim²': sum_sq,
            '=N!': sum_sq == math.factorial(N),
            'ЕТД': f'{n_irreps}=p({N}) неприводимых; Σdim²={sum_sq}={N}!=N!; dims={dims}',
        }

    # 2. ЛЕММА ШУРА
    @staticmethod
    def schur_lemma_etd() -> dict:
        """
        Лемма Шура: Гомоморфизм между неизоморфными неприводимыми = 0.
        Эндоморфизм неприводимого = скалярный (над ℂ).

        ЕТД: End(std) ≅ ℂ (скалярная! dim=1).
        Hom(std, std) = ℂ.
        Hom(std, ρ) = 0 (разные неприводимые).

        Матрица Граммиана: G_ij = ⟨χᵢ, χⱼ⟩ = δᵢⱼ (ортогональность характеров!).

        Для регулярного представления reg:
        reg ≅ ⊕ dᵢ · ρᵢ (каждое вх. dᵢ = dim ρᵢ раз).
        dim(reg) = |G| = 24 = N!.
        Сумма dᵢ² = 24 = N! (уже известно).

        Число неприводимых = число классов сопряжённости = 5 = p(N).
        Классы S₄: {e}, {(12)}, {(123)}, {(1234)}, {(12)(34)}.
        5 классов = 5 = p(N)! ✓
        """
        N = 4

        n_conj_classes = 5   # = p(N)
        dim_reg = math.factorial(N)   # = 24 = N!

        return {
            'теорема': 'Лемма Шура',
            'классов': n_conj_classes,
            '=p(N)': n_conj_classes == 5,
            'dim_reg': dim_reg,
            '=N!': dim_reg == math.factorial(N),
            'ЕТД': f'{n_conj_classes}=p({N}) классов = {n_conj_classes} неприводимых; dim(reg)={dim_reg}=N!',
        }

    # 3. ХАРАКТЕРЫ
    @staticmethod
    def characters_etd() -> dict:
        """
        Характер: χ_ρ(g) = tr(ρ(g)).
        Ортогональность: (1/|G|)Σ χᵢ(g)·χⱼ(g)̄ = δᵢⱼ.

        Таблица характеров S₄:
              e   (12)  (123) (12)(34) (1234)
        triv: 1    1     1      1       1
        sgn:  1   −1     1      1      −1
        std:  3    1     0     −1      −1
        s⊗sg: 3   −1     0     −1       1
        ρ:    2    0    −1      2       0

        χ_std(e) = 3 = N−1.
        χ_ρ(e) = 2 = N/2.
        χ_triv(e) = 1.

        χ_std((π/4-перестановка)): ...
        «Следы = размерности при g = e: 1,1,3,3,2.»

        Сумма χ²(e): 1+1+9+9+4 = 24 = N!. ✓
        χ_std((12)) = 1 = χ_triv((12)) (стандартное частично совпадает!).

        Для среднего по G:
        (1/|G|)·Σ χ_std(g) = (1/24)·(3·1+6·1+8·0+3·(−1)+6·(−1)) = (1/24)·(3+6+0−3−6) = 0.
        «Нетривиальные характеры дают 0 при усреднении!»
        """
        N = 4
        chars = {
            'triv': [1, 1, 1, 1, 1],
            'sgn': [1, -1, 1, 1, -1],
            'std': [3, 1, 0, -1, -1],
            's⊗sg': [3, -1, 0, -1, 1],
            'ρ': [2, 0, -1, 2, 0],
        }
        # Размеры классов в S₄: 1, 6, 8, 3, 6
        class_sizes = [1, 6, 8, 3, 6]

        # Сумма std по G:
        std_sum = sum(chars['std'][i]*class_sizes[i] for i in range(5))   # = 0
        G_size = math.factorial(N)

        return {
            'теорема': 'Характеры',
            'χ_std(e)': chars['std'][0],
            '=N−1': chars['std'][0] == N-1,
            'χ_ρ(e)': chars['ρ'][0],
            '=N/2': chars['ρ'][0] == N//2,
            'Σ_G χ_std': std_sum,
            '=0': std_sum == 0,
            'ЕТД': f'χ_std(e)={N-1}=N−1; χ_ρ(e)={N//2}=N/2; Σ_G χ_std={std_sum}=0 (нетривиальный→0)',
        }

    # 4. ТЕОРЕМА ПИТЕРА–ВЕЙЛЯ
    @staticmethod
    def peter_weyl_etd() -> dict:
        """
        Питер–Вейль (1927): Для компактной группы G:
        L²(G) ≅ ⊕_π dim(π) · Vπ (ортогональное разложение).
        Матричные коэффициенты образуют полный ортонормированный базис.

        ЕТД: G = SO(4) (компактная, связная).
        Неприводимые SO(4) параметризуются (m,n) ∈ ℤ×ℤ, m≥n≥0.
        Высший вес (m,n). dim = (m+1)(n+1)[(m+n+2)/2]... сложная формула.

        Простейший нетривиальный: (1,0) → векторное представление.
        dim(1,0) = 4 = N (стандартное!).
        «Стандартное представление SO(4) имеет dim = N = 4!»

        Следующий: (1,1) → бивекторное.
        dim(1,1) = ... для SO(4): разбивается как 3+3 (самодуальные + антисамодуальные).
        so(4) = su(2) ⊕ su(2): dim = 3+3 = 6 = C(N,2)!

        Алгебра Ли so(N) = so(4):
        dim so(4) = C(N,2) = 6.
        «dim so(N) = C(N,2) = 6!»
        """
        N = 4

        dim_vector_rep = N   # = 4 (стандартное SO(4))
        dim_so4 = math.comb(N, 2)   # = 6 = C(4,2)
        su2_su2 = (3, 3)   # so(4) = su(2)⊕su(2)

        return {
            'теорема': 'Питер–Вейль',
            'dim(вект_SO(4))': dim_vector_rep,
            '=N': dim_vector_rep == N,
            'dim(so(N))': dim_so4,
            '=C(N,2)': dim_so4 == math.comb(N, 2),
            'so(4)': f'su(2)⊕su(2): {su2_su2}',
            'ЕТД': f'dim(вект SO({N}))={dim_vector_rep}=N; dim so({N})={dim_so4}=C({N},2); so(4)=su(2)⊕su(2)',
        }


rt = RepresentationTheoryETD()
for nm, fn in [("Машке", rt.maschke_etd), ("Шур", rt.schur_lemma_etd),
               ("Харак", rt.characters_etd), ("Питер-Вейль", rt.peter_weyl_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Машке** → 5 = p(N) неприводимых; Σdim² = 24 = N!; dims = [1,1,3,3,2]
2. **Шур** → 5 = p(N) классов = p(N) неприводимых; dim(reg) = N!
3. **Характеры** → χ_std(e) = N−1; χ_ρ(e) = N/2; Σ_G χ_нетрив = 0
4. **Питер–Вейль** → dim(вект SO(N)) = N; **dim so(N) = C(N,2) = 6; so(4) = su(2)⊕su(2)**

---
*ТОМ 268 / СЕРИЯ VIII / ЕТД 2026*
