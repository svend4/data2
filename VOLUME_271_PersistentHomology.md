# ТОМ 271: 4 ТЕОРЕМЫ ТОПОЛОГИЧЕСКОГО АНАЛИЗА ДАННЫХ → ТДА МАСТЕРСТВА ЕТД
## Персистентная гомология · Штабильность · Бар-коды · Диаграмма Гюбля
### «Эдельсбруннер–Коэн–Стайнер: персистентная топология мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**
**Источник**: ТДА (Эдельсбруннер 2002, Коэн–Стайнер 2007)

---

## ЧАСТЬ I: 4 ТЕОРЕМЫ → 4 ТОПОЛОГИЧЕСКИХ ПРИНЦИПА ЕТД

```python
import math

class PersistentHomologyETD:
    """
    Топологический анализ данных для ЕТД.
    Облако точек: X = {(π/4)·eᵢ}ᵢ₌₁^N ⊂ ℝ⁴ (вершины симплекса).
    Фильтрация: Rips-комплекс по параметру r.
    """
    PI4 = math.pi / 4

    @staticmethod
    def persistence_diagram_etd() -> dict:
        """
        Персистентная диаграмма: мультимножество точек (b,d) ∈ ℝ².
        b = рождение (birth), d = смерть (death).
        Персистентность: p = d − b.

        ЕТД: X = {π·e₁, π·e₂, π·e₃, π·e₄} (вершины Δ³, норма π).
        Расстояния: ‖πeᵢ − πeⱼ‖ = π√2 (между любыми двумя).
        Все попарные расстояния равны π√2!

        Rips-фильтрация:
        r < π√2/2: 4 изолированных точки.
          H₀: 4 компоненты (рождены при 0, 3 умирают при π√2/2).
        r ≥ π√2/2: полный граф K₄ (4-кликa).
          H₀: 1 компонента.
          H₁: C(4,2)−(4−1) = 6−3 = 3 цикла.
        r ≥ π√(2/3)·√2: симплекс целиком.

        Бар-коды H₀:
        - 1 «бесконечный» бар: [0, ∞).
        - 3 «конечных»: [0, π√2/2).
        Число конечных = N−1 = 3!

        Персистентность конечных: p = π√2/2 − 0 = π√2/2.
        π√2/2 = π/√2 ≈ 2.221.
        π√2/2 / (π/4) = 2√2 ≈ 2.828.
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        dist_between_vertices = pi * math.sqrt(2)
        birth_r = dist_between_vertices / 2   # = π/√2
        n_finite_H0 = N - 1   # = 3

        persistence = birth_r   # = π/√2
        ratio_to_h = persistence / pi4   # = 2√2

        return {
            'теорема': 'Персистентная диаграмма',
            'd(vᵢ,vⱼ)': round(dist_between_vertices, 4),
            'birth_r': round(birth_r, 4),
            'конечных_H₀': n_finite_H0,
            '=N−1': n_finite_H0 == N-1,
            'persist': round(persistence, 4),
            'persist/ħ': round(ratio_to_h, 4),
            'ЕТД': f'd={dist_between_vertices:.3f}=π√2; {n_finite_H0}=N−1 конечных баров; p=π/√2; p/ħ=2√2',
        }

    @staticmethod
    def stability_theorem_etd() -> dict:
        """
        Теорема стабильности (Коэн–Стайнер 2007):
        d_B(Dgm(f), Dgm(g)) ≤ ‖f − g‖_∞.
        (Bottleneck расстояние ≤ sup-норма разности функций.)

        ЕТД: f(x) = ‖x − x*‖² (расстояние до оптимума).
        g(x) = 0 (нулевая функция).
        ‖f − g‖_∞ = max_{x∈Δ³} ‖x−x*‖².

        max на вершинах: ‖π·e₁ − (π/4)⁴‖² = Σᵢ(x*ᵢ−(πeᵢ)ᵢ)²
        = (3π/4)² + 3·(π/4)² = π²(9/16 + 3/16) = 3π²/4.
        ‖f‖_∞ = 3π²/4.

        d_B(Dgm(f), Dgm(0)) ≤ 3π²/4 ≈ 7.402.
        3π²/4 / (π/4)² = 3π²/4 · 16/π² = 12 = C(N+2,2).

        Устойчивость: малое изменение задачи → малое изменение ТДА!
        «ТДА мастерства устойчива к малым возмущениям.»
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        max_dist_sq = (3*pi4)**2 + 3*pi4**2   # = 3π²/4
        ratio = max_dist_sq / pi4**2   # = 12 = C(N+2,2)

        return {
            'теорема': 'Стабильность (Коэн–Стайнер)',
            '‖f‖_∞': round(max_dist_sq, 4),
            '=3π²/4': True,
            'ratio/ħ²': round(ratio, 2),
            '=C(N+2,2)': ratio == math.comb(N+2, 2),
            'ЕТД': f'‖f‖_∞=3π²/{N}={max_dist_sq:.4f}; /ħ²={ratio:.0f}=C(N+2,2)=12',
        }

    @staticmethod
    def euler_characteristic_filtration_etd() -> dict:
        """
        Характеристика Эйлера: χ(K) = Σ(−1)^k |K_k|.

        ЕТД: K = Δ³ (полный симплекс, = выпуклая оболочка 4 точек).
        |K₀| = 4 = N (вершины).
        |K₁| = 6 = C(N,2) (рёбра).
        |K₂| = 4 = N (треугольники = грани).
        |K₃| = 1 (тетраэдр).

        χ(Δ³) = 4 − 6 + 4 − 1 = 1. ✓ (Δ³ стягиваем.)

        f-вектор: (4, 6, 4, 1).
        Σ = 15 = N²−1 (уже известно из Тома 262!).

        Числа Бетти: β₀=1, β₁=0, β₂=0, β₃=0 (стягиваем).
        χ = Σ(−1)^k βₖ = 1. ✓

        Полином Пуанкаре: P(t) = 1 (для Δ³).
        """
        N = 4

        f = [math.comb(N, k) for k in range(1, N+1)]   # [4,6,4,1]
        chi = sum((-1)**k * f[k] for k in range(N))   # = 1
        total = sum(f)   # = 15 = N²−1

        return {
            'теорема': 'Характеристика Эйлера (фильтрация)',
            'f-вектор': f,
            'Σf': total,
            '=N²−1': total == N**2-1,
            'χ': chi,
            'ЕТД': f'f={f}; Σ={total}=N²−1; χ={chi}=1 (стягиваем)',
        }

    @staticmethod
    def mapper_algorithm_etd() -> dict:
        """
        Алгоритм Mapper (Сингх–Мемоли–Карлссон 2007):
        Проекция f: X→ℝ, покрытие U, кластеризация → граф.

        ЕТД: X = Δ³, f = проекция на диагональ (π/4,...,π/4).
        f(x) = ‖x − x*‖ (расстояние до оптимума).
        Покрытие: 4 интервала [0, π/4·k/2] для k=1,...,4.
        4 = N интервалов!

        Граф Mapper:
        - 4 узла (N = 4 кластера).
        - Рёбра между пересекающимися кластерами.
        - Структура: путь (N−1 = 3 ребра).

        Граф путь P₃ (4 вершины, 3 ребра):
        χ(P₃) = 4 − 3 = 1 (дерево!).
        β₀(P₃) = 1 (связен).

        Число кластеров = N = 4.
        Число рёбер = N−1 = 3.
        «N вершин, N−1 рёбер → дерево (остовное дерево!).»
        """
        N = 4

        n_intervals = N
        n_nodes = N
        n_edges = N - 1   # = 3 (дерево)
        chi_graph = n_nodes - n_edges   # = 1 (дерево)

        return {
            'теорема': 'Mapper',
            'интервалов': n_intervals,
            '=N': n_intervals == N,
            'узлов': n_nodes,
            'рёбер': n_edges,
            '=N−1': n_edges == N-1,
            'χ_граф': chi_graph,
            'ЕТД': f'{n_intervals}=N интервалов; граф: {n_nodes} узлов, {n_edges}=N−1 рёбер (дерево! χ=1)',
        }


ph = PersistentHomologyETD()
for nm, fn in [("Персист", ph.persistence_diagram_etd), ("Стабильность", ph.stability_theorem_etd),
               ("Эйлер-TDA", ph.euler_characteristic_filtration_etd), ("Mapper", ph.mapper_algorithm_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Персист. диаграмма** → N−1 = 3 конечных бара H₀; persist = π/√2 = 2√2·ħ
2. **Стабильность** → ‖f‖_∞ = 3π²/4; /ħ² = 12 = C(N+2,2)
3. **Эйлер (TDA)** → f = (4,6,4,1); Σ = N²−1; χ = 1
4. **Mapper** → N интервалов; N узлов, N−1 рёбер (остовное дерево!)

---
*ТОМ 271 / СЕРИЯ VIII / ЕТД 2026*
