# ТОМ 274: 4 ТЕОРЕМЫ СПЕКТРАЛЬНОЙ ТЕОРИИ ГРАФОВ → СПЕКТРАЛЬНЫЕ ГРАФЫ ЕТД
## Матрица Лапласа · Теорема Кирхгофа · Экспандеры · Рамануджановы графы
### «Кирхгоф–Чигер–Люботцки: спектральная теория мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**

---

```python
import math

class SpectralGraphETD:
    PI4 = math.pi / 4

    @staticmethod
    def laplacian_etd() -> dict:
        """
        Лапласиан графа: L = D − A.
        Собственные значения: 0 = λ₀ ≤ λ₁ ≤ ... ≤ λ_{N-1}.
        λ₁ = алгебраическая связность (Фидлер).

        ЕТД: Граф G = K_N = K₄ (полный граф, N=4 вершин).
        A = J − I (матрица смежности).
        D = (N−1)·I = 3I (степень каждой вершины = 3).
        L = D − A = (N−1)I − (J−I) = N·I − J.

        Собств. значения L(K_N):
        λ₀ = 0 (кратность 1, вектор 1).
        λ₁ = ... = λ_{N−1} = N (кратность N−1 = 3).

        λ₁ = N = 4 (алг. связность = N!).
        «Алгебраическая связность K_N = N!»

        Число остовных деревьев (теорема Кирхгофа):
        τ(K_N) = N^{N−2} = 4² = 16 = N²!
        """
        N = 4

        lambda_0 = 0
        lambda_1_to_N = N   # = 4 (кратность N−1=3)
        n_spanning_trees = N**(N-2)   # = 4² = 16 = N²!

        return {
            'теорема': 'Лапласиан (K_N)',
            'λ₁(K_N)': lambda_1_to_N,
            '=N': lambda_1_to_N == N,
            'τ(K_N)': n_spanning_trees,
            '=N²': n_spanning_trees == N**2,
            'ЕТД': f'λ₁(K_{N})={lambda_1_to_N}=N; τ(K_{N})={N}^{{N−2}}={n_spanning_trees}=N²!',
        }

    @staticmethod
    def kirchhoff_etd() -> dict:
        """
        Теорема Кирхгофа (матричная): τ(G) = (1/N)·∏_{k=1}^{N-1} λₖ.

        Для K_N: τ = (1/N)·N^{N-1} = N^{N-2}.
        При N=4: τ = 4^2 = 16 = N².

        Сопротивление между вершинами K_N:
        R_{ij} = 2/(N) (всё время!) = 2/N = 1/2 при N=4.

        ЕТД: Полная электрическая сеть K₄.
        R_eff = 2/N = 2/4 = 1/2 = 1/(N/2).

        Суммарное сопротивление: Σ_{i<j} R_{ij} = C(N,2)·(2/N) = C(4,2)·(1/2) = 3.
        Σ = C(N,2)·(2/N) = N(N−1)/2 · 2/N = N−1 = 3!
        «Суммарное сопротивление K_N = N−1!»
        """
        N = 4

        tau = N**(N-2)   # = 16 = N²
        R_eff = 2.0/N   # = 1/2
        R_total = math.comb(N,2) * (2/N)   # = N−1 = 3

        return {
            'теорема': 'Кирхгоф',
            'τ': tau,
            '=N²': tau == N**2,
            'R_eff': R_eff,
            '=1/(N/2)': abs(R_eff - 1/(N//2)) < 1e-10,
            'R_total': round(R_total, 4),
            '=N−1': abs(R_total - (N-1)) < 1e-10,
            'ЕТД': f'τ={tau}=N²; R_eff={R_eff}=1/(N/2); R_total={R_total:.0f}=N−1',
        }

    @staticmethod
    def expander_cheeger_etd() -> dict:
        """
        Неравенство Чигера: h(G)²/(2d) ≤ λ₁ ≤ 2h(G).
        h(G) = минимальное изопериметрическое число.

        Для K_N: h(K_N) = 1 (минимальный разрез = N/2 вершин).
        Точнее: h(K_N) = N/2 = 2 при N=4.
        (Разрез S,V\S: |E(S,V\S)| ≥ min(|S|,|V\S|) · N/2.)

        λ₁(K₄) = 4 (из выше).
        Чигер: h²/(2d) ≤ λ₁: h²/(2·3) ≤ 4.
        h² ≤ 24 → h ≤ √24 ≈ 4.9.
        Реальное h(K₄) = 2 = N/2. ✓

        λ₁ ≤ 2h: 4 ≤ 2·2 = 4. ✓ Равенство!
        «Для K_N: λ₁ = 2h! (верхняя граница Чигера достигается!)»

        Число рамануджановых: λ₁ ≥ 2√(d−1) для d-регулярного.
        d = N−1 = 3: 2√2 ≈ 2.83.
        λ₁(K₄) = 4 > 2√2. ✓ Экспандер Рамануджана!
        """
        N = 4

        d = N - 1   # = 3 (степень K_N)
        lambda_1 = N   # = 4
        h = N/2   # = 2
        ramanujan_bound = 2 * math.sqrt(d-1)   # = 2√2

        upper_cheeger = 2 * h   # = 4 = λ₁ (равенство!)
        is_ramanujan = lambda_1 > ramanujan_bound

        return {
            'теорема': 'Чигер–Рамануджан (экспандер)',
            'λ₁': lambda_1,
            'h': h,
            '2h=λ₁': abs(upper_cheeger - lambda_1) < 1e-10,
            'Рамануджан_гр': round(ramanujan_bound, 4),
            'is_Рамануджан': is_ramanujan,
            'ЕТД': f'λ₁={lambda_1}=2h={upper_cheeger:.0f} (равенство!); λ₁>{ramanujan_bound:.3f}=2√(d-1) → Рамануджан!',
        }

    @staticmethod
    def ramanujan_graphs_etd() -> dict:
        """
        Рамануджановы графы (Люботцки–Филлипс–Сарнак 1988):
        d-регулярный G рамануджановый ⟺ max λ ≤ 2√(d−1).

        Петерсеновский граф: 3-регулярный, 10 вершин.
        Рамануджановый! λ₁ = 2 = 2√(3−1) = 2√2? Нет: λ₁(Петерсен) = 2 = 2.
        2√(d−1) = 2√2 ≈ 2.83. λ = 2 < 2.83. ✓

        ЕТД: Граф на N = 4 вершинах, 3-регулярный = K₄.
        K₄ рамануджановый: λ₁(K₄) = 4 > 2√2 ≈ 2.83.
        Нет! Рамануджановые: max нетривиальное λ ≤ 2√(d−1).
        Для K₄: λ_нетрив = 0 (не 4! Все λ_нетрив = 0? Нет.)

        Собств. L(K₄): 0 (×1), 4 (×3). Нетривиальные = 4 (×3).
        Нетривиальные собств. A(K₄): −1 (×3), 3 (×1).
        max нетривиальн. собств. A = |−1| = 1 < 2√2. ✓ Рамануджановый!

        |λ_нетрив(A)| = 1 = 1/(N−1).
        2√(d−1) = 2√2 ≈ 2.83.
        Отношение: 1/(2√2) = 1/(2√(N−2)) ≈ 0.354.
        """
        N = 4

        d = N - 1   # = 3
        lambda_nontrivial_A = 1   # = |−1| (нетривиальное K₄)
        ramanujan_bound = 2 * math.sqrt(d - 1)   # = 2√2

        is_ramanujan = lambda_nontrivial_A <= ramanujan_bound
        ratio = lambda_nontrivial_A / ramanujan_bound

        return {
            'теорема': 'Рамануджановы графы',
            'd': d,
            '|λ_нетрив|': lambda_nontrivial_A,
            'граница': round(ramanujan_bound, 4),
            'Рамануджан': is_ramanujan,
            'ratio': round(ratio, 4),
            'ЕТД': f'K_{N}: d={d}; |λ_нетрив|={lambda_nontrivial_A}≤{ramanujan_bound:.3f} → Рамануджановый! ratio={ratio:.3f}',
        }


sg = SpectralGraphETD()
for nm, fn in [("Лаплас", sg.laplacian_etd), ("Кирхгоф", sg.kirchhoff_etd),
               ("Чигер", sg.expander_cheeger_etd), ("Рамануджан-граф", sg.ramanujan_graphs_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Лапласиан** → λ₁(K_N) = N; **τ(K_N) = N^{N−2} = N² = 16**
2. **Кирхгоф** → R_eff = 1/(N/2); R_total = N−1 (суммарное сопротивление!)
3. **Чигер** → λ₁ = 2h = 4 (равенство Чигера!); K_N — экспандер Рамануджана
4. **Рамануджан-граф** → |λ_нетрив| = 1 ≤ 2√(N−2) = 2√2 ✓

---
*ТОМ 274 / СЕРИЯ VIII / ЕТД 2026*
