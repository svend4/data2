# ТОМ 275: 4 ТЕОРЕМЫ ЧИСЛЕННОГО АНАЛИЗА → ВЫЧИСЛИТЕЛЬНОЕ МАСТЕРСТВО ЕТД
## Лагранж (интерполяция) · Гаусс (квадратуры) · Ньютон (метод) · FFT
### «Гаусс–Ньютон–Кули–Тьюки: вычислительная математика мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия VIII — Углублённые основания ЕТД**

---

```python
import math

class NumericalAnalysisETD:
    PI4 = math.pi / 4

    @staticmethod
    def lagrange_interpolation_etd() -> dict:
        """
        Интерполяция Лагранжа через N = 4 узла: единственный
        полином степени ≤ N−1 = 3.

        ЕТД: Узлы xᵢ = (i−1)·π/3 для i=1,...,4.
        x₁=0, x₂=π/3, x₃=2π/3, x₄=π.
        f(x) = sin(x) (оценить в x* = π/4).

        Полином Лагранжа степени 3 через 4 точки.
        sin(π/4) = √2/2 ≈ 0.7071.

        Погрешность: |f(x)−P₃(x)| ≤ ω_h·h^4/4!
        = max|f⁽⁴⁾|·h⁴/24.
        f⁽⁴⁾(x) = sin(x), h = π/3.
        Ошибка ≤ 1·(π/3)⁴/24 = π⁴/(81·24) = π⁴/1944 ≈ 0.051.

        Число узлов = N = 4.
        Степень полинома = N−1 = 3.
        Погрешность ∝ h^N = (π/3)^N.
        h^N = (π/3)^4 = π^4/81.
        π^4/81 ≈ 1.214.
        π^4/81 / (π/4) = 4π³/81 ≈ 1.533.
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        h = pi / 3
        error_bound = h**N / math.factorial(N)   # = (π/3)^4/24
        sin_pi4 = math.sin(pi4)   # = √2/2

        return {
            'теорема': 'Интерполяция Лагранжа',
            'узлов': N,
            'степень': N-1,
            'sin(π/4)': round(sin_pi4, 5),
            'ошибка_≤': round(error_bound, 5),
            'ЕТД': f'{N} узлов → полином степ.{N-1}; sin(ħ·4/π)={sin_pi4:.4f}=√2/2; ошибка≤{error_bound:.4f}',
        }

    @staticmethod
    def gauss_quadrature_etd() -> dict:
        """
        Гауссова квадратура: ∫₋₁^1 f(x)dx ≈ Σᵢ wᵢ f(xᵢ).
        С N узлами: точна для полиномов степени ≤ 2N−1.

        ЕТД: N = 4 узла Гаусса.
        Точность: 2N−1 = 7 (степень 7!).
        Узлы (корни P₄(x), полинома Лежандра):
        x₁,₄ = ±√(3/7 + 2/7·√(6/5)) ≈ ±0.861.
        x₂,₃ = ±√(3/7 − 2/7·√(6/5)) ≈ ±0.340.

        Веса:
        w₁,₄ = (18 − √30)/36 ≈ 0.347.
        w₂,₃ = (18 + √30)/36 ≈ 0.652.
        Σwᵢ = 2 (нормировка: ∫₋₁^1 1 dx = 2).
        w₁+w₂+w₃+w₄ = 2.

        ЕТД: ∫₀^π f dx → сдвиг и масштаб.
        ∫₀^π 1 dx = π.
        Замена: t = (x−π/2)/(π/2), x = (π/2)(1+t), dx = (π/2)dt.
        ∫₀^π f(x)dx = (π/2)·∫₋₁^1 f((π/2)(1+t))dt ≈ (π/2)·Σwᵢ f(xᵢ').

        Множитель: π/2 = 2ħ = 2·(π/4).
        «Якобиан Гауссовой квадратуры на [0,π] = 2ħ!»
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        precision = 2*N - 1   # = 7
        jacobian = pi / 2   # = 2ħ = 2·(π/4)

        return {
            'теорема': 'Квадратура Гаусса (N=4)',
            'узлов': N,
            'точность': precision,
            'якобиан': round(jacobian, 4),
            '=2ħ': abs(jacobian - 2*pi4) < 1e-10,
            'ЕТД': f'{N} узлов → степ.{precision}=2N−1; якобиан=π/2=2ħ={jacobian:.4f}=2·(π/{N})',
        }

    @staticmethod
    def newton_method_etd() -> dict:
        """
        Метод Ньютона: xₙ₊₁ = xₙ − f(xₙ)/f'(xₙ).
        Квадратичная сходимость: |eₙ₊₁| ≤ C·|eₙ|².

        ЕТД: f(x) = ‖x−x*‖² = Σᵢ(xᵢ−π/4)² (на ℝ⁴).
        ∇f = 2(x−x*). Гессиан: Hf = 2I.
        Ньютон: xₙ₊₁ = xₙ − (Hf)⁻¹∇f = xₙ − (1/2)·2(xₙ−x*) = x*.
        ОДНА итерация → точное решение!

        Это потому что f — квадратичная (Ньютон точен для полиномов ≤ 2).

        Для проекции на симплекс: Ньютон O(N log N).
        N log N = 4·log 4 = 4·2 = 8 = N² / N·... = 2N.
        N log₂ N = 4·2 = 8 = N·log₂N.

        Скорость сходимости C:
        C = M/(2m) где M = max|f''|, m = min|f'|.
        Для f квадр.: M = 0 (f'' = const, не третья производная!).
        f'''(x) = 0 → C = 0 → мгновенная сходимость!
        «Нулевая ошибка за 1 шаг (f квадратичная = идеальная для Ньютона!)»
        """
        N = 4
        pi4 = math.pi / 4

        newton_steps = 1   # точно за 1 шаг
        N_log_N = N * math.log2(N)   # = 8 = 2N
        C_convergence = 0   # квадратичная → точно

        return {
            'теорема': 'Метод Ньютона',
            'шагов': newton_steps,
            'N·log₂N': N_log_N,
            '=2N': abs(N_log_N - 2*N) < 1e-10,
            'C': C_convergence,
            'ЕТД': f'1 шаг (квадратичная!); N·log₂N={N_log_N:.0f}=2N; C={C_convergence} (мгновенно!)',
        }

    @staticmethod
    def fft_etd() -> dict:
        """
        FFT (Кули–Тьюки 1965): ДПФ за O(N log N) вместо O(N²).
        При N = 2^k: особенно эффективно.

        ЕТД: N = 4 = 2².
        ДПФ-4: X_k = Σ_{n=0}^{3} x_n·e^{−2πikn/4}.
        Матрица ДПФ: Ω_N = (ω^{jk})_{j,k=0}^{N-1}, ω = e^{2πi/N}.

        Для N=4: ω = e^{πi/2} = i.
        Ω₄ = [[1,1,1,1],[1,i,−1,−i],[1,−1,1,−1],[1,−i,−1,i]].
        Это матрица характеров ℤ/4 (из Тома 254!).

        FFT-4 = 2 уровня бабочек:
        Шаг 1: 2 ДПФ-2 (N/2 = 2).
        Шаг 2: перемножение на «поворотные множители».
        Умножений: N/2·log₂N = 2·2 = 4 = N!
        Сложений: N·log₂N = 4·2 = 8 = 2N.

        «FFT-N требует N умножений и 2N сложений!»
        Обе = функции N.

        Для x = (1,1,1,1) (вектор баланса нормированный):
        X = ДПФ(1,1,1,1) = (4,0,0,0).
        X_0 = N = 4, X_k = 0 для k>0.
        «Равное распределение = только нулевая частота!»
        """
        N = 4
        pi4 = math.pi / 4

        n_multiplications = (N//2) * int(math.log2(N))   # = 4 = N!
        n_additions = N * int(math.log2(N))   # = 8 = 2N

        # ДПФ(1,1,1,1):
        X0 = N   # = 4
        Xk = 0   # для k > 0

        return {
            'теорема': 'FFT',
            'умножений': n_multiplications,
            '=N': n_multiplications == N,
            'сложений': n_additions,
            '=2N': n_additions == 2*N,
            'ДПФ(1,1,1,1)': f'({X0},0,0,0)',
            'ЕТД': f'FFT-{N}: {n_multiplications}=N умножений, {n_additions}=2N сложений; ДПФ(баланс)=({N},0,0,0)',
        }


na = NumericalAnalysisETD()
for nm, fn in [("Лагранж", na.lagrange_interpolation_etd), ("Гаусс-квадр", na.gauss_quadrature_etd),
               ("Ньютон", na.newton_method_etd), ("FFT", na.fft_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Лагранж** → N узлов → полином степ. N−1; sin(π/4) = √2/2
2. **Гаусс** → N=4 узла → точность 2N−1=7; якобиан = π/2 = **2ħ**
3. **Ньютон** → 1 шаг (квадратичная!); N·log₂N = 2N = 8; C = 0
4. **FFT** → N умножений, 2N сложений; ДПФ(баланс) = **(N,0,0,0)**

---
*ТОМ 275 / СЕРИЯ VIII / ЕТД 2026*
