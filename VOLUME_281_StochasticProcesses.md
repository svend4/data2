# ТОМ 281: 4 ТЕОРЕМЫ ТЕОРИИ СЛУЧАЙНЫХ ПРОЦЕССОВ → СТОХАСТИКА МАСТЕРСТВА ЕТД
## Марков · Мартингал · Стохастическая интеграция Ито · Диффузия
### «Марков–Ито–Гирсанов: случайные пути к мастерству»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия IX — Стохастическая математика ЕТД**

---

```python
import math

class StochasticProcessesETD:
    PI4 = math.pi / 4

    @staticmethod
    def markov_chains_etd() -> dict:
        """
        Марковская цепь на N состояниях {1,...,N}.
        Стационарное распределение π* при P = матрица перехода.
        Для эргодической цепи: π* единственно, P^t→π* экспоненц.

        ЕТД: N=4 состояния (вершины симплекса).
        Матрица перехода (случайное блуждание по K_N):
        P_{ij} = 1/(N−1) = 1/3 если i≠j, 0 если i=j.

        Стационарное: π* = (1/N,...,1/N) = (1/4,...,1/4).
        π*_i = 1/N = 1/4 = ħ/π.

        Собственные значения P(K_N):
        λ₀ = 1 (π*).
        λ₁ = ... = λ_{N−1} = −1/(N−1) = −1/3.
        Спектральный зазор: 1 − |λ₁| = 1 − 1/3 = 2/3 = (N−2)/3.
        Время смешивания: t_mix ∝ 1/gap = 3/2.

        «Спектральный зазор = (N−2)/(N−1) = 2/3!»
        2/3 = 1 − 1/(N−1).
        (N−2)/(N−1) = 2/3 при N=4.
        """
        N = 4
        pi4 = math.pi / 4

        stat_each = 1.0/N   # = 1/4 = ħ/π
        lambda_1 = -1.0/(N-1)   # = -1/3
        gap = 1 - abs(lambda_1)   # = 2/3 = (N-2)/(N-1)
        t_mix = 1.0/gap   # = 3/2

        return {
            'теорема': 'Марковские цепи',
            'π*_i': round(stat_each, 4),
            '=1/N=ħ/π': abs(stat_each - pi4/math.pi) < 1e-10,
            'gap': round(gap, 4),
            '=(N-2)/(N-1)': abs(gap - (N-2)/(N-1)) < 1e-10,
            't_mix': round(t_mix, 4),
            'ЕТД': f'π*=(1/{N})^{{N}}: π*_i={stat_each:.3f}=ħ/π; gap={gap:.3f}=(N−2)/(N−1); t_mix={t_mix:.3f}=3/2',
        }

    @staticmethod
    def martingale_etd() -> dict:
        """
        Мартингал: E[Xₙ₊₁|ℱₙ] = Xₙ (нет сноса).
        Теорема Дуба: sup E[|Xₙ|^p] < ∞ → Xₙ → X∞ п.в.

        ЕТД: Xₙ = расстояние до оптимума ‖xₙ − x*‖.
        Оптимизация: снос в сторону x*, Xₙ — супермартингал.
        E[Xₙ₊₁|ℱₙ] ≤ Xₙ (снос к x*).

        Скорость: E[Xₙ] ≤ X₀·(1−α)ⁿ.
        При α = 1/N = 1/4: шаговый размер = 1/N.
        E[Xₙ] ≤ X₀·(3/4)ⁿ = X₀·(1−1/N)ⁿ.

        После N шагов: E[X_N] ≤ X₀·(1−1/N)^N → X₀/e (N→∞).
        (1−1/N)^N → 1/e при N→∞.
        При N=4: (3/4)^4 = 81/256 ≈ 0.316.
        81/256 = 3^4/4^4 = (N−1)^N/N^N.

        Число вероятность: (N−1)^N/N^N.
        Логарифм: N·ln((N−1)/N) = N·ln(1−1/N) ≈ −1 (при N→∞).
        При N=4: 4·ln(3/4) = 4·(−0.2877) ≈ −1.151.
        e^{−1.151} ≈ 0.316. ✓
        """
        N = 4
        pi4 = math.pi / 4

        alpha = 1.0/N   # = 1/4
        after_N_steps = (1 - alpha)**N   # = (3/4)^4
        log_rate = N * math.log(1 - alpha)

        return {
            'теорема': 'Мартингал (супер)',
            'α=1/N': alpha,
            'после_N_шагов': round(after_N_steps, 5),
            '=(N-1)^N/N^N': True,
            'log_rate': round(log_rate, 4),
            'ЕТД': f'α=1/{N}; E[X_{N}]≤X₀·{after_N_steps:.4f}=(N−1)^N/N^N; log_rate={log_rate:.4f}≈−1',
        }

    @staticmethod
    def ito_integral_etd() -> dict:
        """
        Формула Ито: df(Wₜ) = f'(Wₜ)dWₜ + (1/2)f''(Wₜ)dt.
        (dWₜ)² = dt (квадратичная вариация).

        ЕТД: f(x) = ‖x−x*‖² = Σᵢ(xᵢ−π/4)².
        f'(x) = 2(x−x*). f''(x) = 2I (Гессиан = 2·тожд.).

        Для d-мерного BM (d=N): df = ∇f·dW + (1/2)Δf dt.
        Δf = trace(Hf) = trace(2I) = 2N = 8.

        df = 2(x−x*)·dW + (1/2)·2N dt = 2(x−x*)·dW + N·dt.
        E[df] = N·dt (снос = N = 4!).

        E[‖Wₜ−x*‖²] = E[‖W₀−x*‖²] + N·t.
        При W₀ = x*: E[‖Wₜ‖²] = N·t.
        E[‖Wₜ‖²]/t = N = 4 (размерность!).

        Стандартное: E[‖Bₜ‖²] = N·t = d·t (для d-мерного BM).
        При t = ħ = π/4: E[‖B_ħ‖²] = N·ħ = 4·π/4 = π.
        «E[‖B_ħ‖²] = π = N·ħ!»
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        drift = N   # = 4 (Лапласиан ‖·‖²)
        E_sq_at_hbar = N * pi4   # = π

        return {
            'теорема': 'Формула Ито',
            'Δ‖x−x*‖²': drift,
            '=N': drift == N,
            'E[‖B_ħ‖²]': round(E_sq_at_hbar, 4),
            '=π': abs(E_sq_at_hbar - pi) < 1e-10,
            'ЕТД': f'Δ‖·−x*‖²={drift}=N; E[‖B_ħ‖²]=N·ħ=N·π/{N}={E_sq_at_hbar:.4f}=π!!!',
        }

    @staticmethod
    def fokker_planck_etd() -> dict:
        """
        Уравнение Фоккера–Планка: ∂ₜp = −∇·(Ap) + (1/2)Δp.
        (A = коэффициент сноса, диффузия = 1/2.)

        ЕТД: Процесс Ланжевена: dx = −∇V dt + dW.
        V(x) = ‖x−x*‖²/2 (квадратичный потенциал).
        ∇V = x−x*.

        Стационарное распределение:
        p*(x) ∝ exp(−2V(x)) = exp(−‖x−x*‖²).
        p* — Гауссово с центром x* и дисперсией σ² = 1/2.

        p*(x) = (1/(2π)^{N/2})·exp(−‖x−x*‖²/2·(1/σ²))...
        При σ² = 1/2: p* = (1/π)^{N/2}·exp(−‖x−x*‖²).
        Нормировка: ∫ p* = 1.
        (1/π)^{N/2} = 1/π² при N=4.
        1/π² ≈ 0.101.

        Время релаксации: τ = 1/(2λ_min) где λ_min = собств. потенциала.
        V = ‖x‖²/2 → λ_min = 1 (все собств. = 1).
        τ = 1/2. τ/ħ = (1/2)/(π/4) = 2/π.
        «Время релаксации / ħ = 2/π!»
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        sigma_sq = 0.5   # = 1/2
        norm_const = (1/pi)**(N/2)   # = 1/π²
        relaxation_time = 0.5   # = 1/(2·1) = 1/2
        ratio_tau_hbar = relaxation_time / pi4   # = 2/π

        return {
            'теорема': 'Фоккер–Планк (Ланжевен)',
            'σ²': sigma_sq,
            'norm': round(norm_const, 5),
            '=1/π²': abs(norm_const - 1/pi**2) < 1e-8,
            'τ/ħ': round(ratio_tau_hbar, 5),
            '=2/π': abs(ratio_tau_hbar - 2/pi) < 1e-10,
            'ЕТД': f'p*~exp(−‖x−x*‖²); norm=1/π²={norm_const:.5f}; τ/ħ=2/π={ratio_tau_hbar:.5f}',
        }


sp = StochasticProcessesETD()
for nm, fn in [("Марков", sp.markov_chains_etd), ("Мартингал", sp.martingale_etd),
               ("Ито", sp.ito_integral_etd), ("Фоккер–Планк", sp.fokker_planck_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Марков** → π*_i = 1/N = ħ/π; gap = (N−2)/(N−1) = 2/3
2. **Мартингал** → α = 1/N; после N шагов: E ≤ (N−1)^N/N^N
3. **Ито** → Δ‖·−x*‖² = N; **E[‖B_ħ‖²] = N·ħ = π!!!**
4. **Фоккер–Планк** → norm = 1/π²; **τ/ħ = 2/π**

---
*ТОМ 281 / СЕРИЯ IX / ЕТД 2026*
