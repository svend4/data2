# ТОМ 282: 4 ТЕОРЕМЫ КОМБИНАТОРНЫХ ПОЛИНОМОВ → КОМБИНАТОРИКА ЕТД
## Полином Тутта · Хроматический полином · Полином Джонса · Полином Александера
### «Тутт–Джонс–Александер: топологические инварианты мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия IX — Стохастическая математика ЕТД**

---

```python
import math

class CombinatoricsPolynomialsETD:
    PI4 = math.pi / 4

    @staticmethod
    def tutte_polynomial_etd() -> dict:
        """
        Полином Тутта T(G;x,y): универсальный инвариант графа.
        T(G;1,1) = τ(G) (число остовных деревьев!).
        T(G;2,1) = 2^{N−1} (число рёберных подграфов, содержащих остов).
        T(G;1,2) = 2^{|E|−N+1} (число ацикличных ориентаций).

        ЕТД: G = K_N = K₄.
        τ(K₄) = 4² = 16 = N² (из Тома 274!).
        T(K₄;1,1) = 16 = N².

        T(K₄;2,1): 2^{N−1} = 2^3 = 8 = N! / N = 6? Нет.
        2^{N-1} = 2³ = 8.
        T(K₄;0,−2): связан с полиномом Джонса.

        |E|(K₄) = C(N,2) = 6.
        N, |E|, τ: 4, 6, 16 = N, C(N,2), N².
        C(N,2) − N + 1 = 6 − 4 + 1 = 3 = N − 1 (бета-число!).
        β₁(K₄) = |E| − N + τ_0 = 6 − 4 + 1 = 3 = N − 1.
        «Первое число Бетти K₄ = N − 1 = 3!»
        """
        N = 4

        tau_K4 = N**2   # = 16 (остовных деревьев)
        E_K4 = math.comb(N, 2)   # = 6
        beta_1 = E_K4 - N + 1   # = 3 = N-1

        return {
            'теорема': 'Полином Тутта',
            'T(K_N;1,1)': tau_K4,
            '=N²': tau_K4 == N**2,
            '|E|': E_K4,
            'β₁': beta_1,
            '=N−1': beta_1 == N-1,
            'ЕТД': f'T(K_{N};1,1)={tau_K4}=N²; |E|={E_K4}=C(N,2); β₁={beta_1}=N−1',
        }

    @staticmethod
    def chromatic_polynomial_etd() -> dict:
        """
        Хроматический полином P(G,k): число правильных k-раскрасок.

        Для K_N: P(K_N,k) = k·(k−1)·...·(k−N+1) = k^{↓N} (падающий факториал).
        Хроматическое число χ(K_N) = N.

        ЕТД: P(K₄,k) = k(k−1)(k−2)(k−3).
        P(K₄,N) = P(K₄,4) = 4·3·2·1 = N! = 24.
        P(K₄,4) = N! = 24.
        «Число правильных N-раскрасок K_N = N!»

        P(K₄,k) при k = N+1 = 5:
        P(K₄,5) = 5·4·3·2 = 120 = 5! = (N+1)!.
        P(K₄,k) при k = N+1 = (N+1)!.
        «P(K_N, N+1) = (N+1)! (факториальное расширение!)»

        Дробный хроматический: χ_f(K_N) = N = 4.
        Кликовое число ω(K_N) = N = 4.
        χ = ω = N (для полного графа!).
        """
        N = 4

        P_at_N = math.factorial(N)   # = 24 = N!
        P_at_N_plus_1 = math.factorial(N+1)   # = 120 = (N+1)!

        return {
            'теорема': 'Хроматический полином',
            'P(K_N,N)': P_at_N,
            '=N!': P_at_N == math.factorial(N),
            'P(K_N,N+1)': P_at_N_plus_1,
            '=(N+1)!': P_at_N_plus_1 == math.factorial(N+1),
            'χ=ω': N,
            'ЕТД': f'P(K_{N},{N})={P_at_N}=N!; P(K_{N},{N+1})={P_at_N_plus_1}=(N+1)!; χ=ω={N}',
        }

    @staticmethod
    def jones_polynomial_etd() -> dict:
        """
        Полином Джонса V(L;t) — инвариант зацеплений.
        Для тривиального узла: V = 1.
        Для трёхлистника: V(t) = −t⁻⁴ + t⁻³ + t⁻¹.
        V(3-листник при t=−1): −1+1+1 = 1? Нет: (−1)⁻⁴=1, (−1)⁻³=−1, (−1)⁻¹=−1. V=1−1−1=−1.

        ЕТД: t = e^{2πi/N} = e^{iπ/2} = i (для N=4).
        V(тривиал; i) = 1.
        V(трёхлистник; i) = −i⁻⁴ + i⁻³ + i⁻¹.
        i⁻⁴ = (i^4)⁻¹ = 1. i⁻³ = (i³)⁻¹ = (−i)⁻¹ = i. i⁻¹ = −i.
        V = −1 + i + (−i) = −1.

        |V(трёхлистник; i)| = 1 = |V(тривиал; i)|.
        «При t = e^{2πi/N}: полином Джонса |V| = 1 для обоих!»

        Угол оценки: t = e^{2πi/N} = корень N-й степени из 1.
        t^N = 1 (t — N-й корень единицы!).
        Количество различных корней: N = 4.

        Скейн-соотношение (Джонс):
        t⁻¹·V(L₊) − t·V(L₋) = (t^{1/2} − t^{−1/2})·V(L₀).
        При t = e^{iπ/2} = i:
        i⁻¹·V₊ − i·V₋ = (e^{iπ/4} − e^{−iπ/4})·V₀.
        e^{iπ/4} = cos(π/4)+i·sin(π/4) = (1+i)/√2.
        e^{±iπ/4} − e^{∓iπ/4} = 2i·sin(π/4) = 2i·(√2/2) = i√2.
        «Разность показателей в скейн-соотн. = i√2 = i·√2 при t=e^{iπ/2}!»
        """
        N = 4
        pi4 = math.pi / 4

        # t = e^{2πi/N} = e^{πi/2} = i
        skein_diff = 2 * math.sin(math.pi/N)   # = 2sin(π/4) = √2
        n_roots = N

        return {
            'теорема': 'Полином Джонса',
            't=e^{2πi/N}': f'e^{{iπ/2}}=i',
            'скейн_|diff|': round(skein_diff, 5),
            '=√2': abs(skein_diff - math.sqrt(2)) < 1e-10,
            'N_корней': n_roots,
            'ЕТД': f't=i=e^{{2πi/{N}}}; |diff_скейн|={skein_diff:.4f}=√2; {n_roots} корней ед.',
        }

    @staticmethod
    def alexander_polynomial_etd() -> dict:
        """
        Полином Александера Δ(L;t): инвариант зацеплений.
        Δ(трёхлистник;t) = t − 1 + t⁻¹.
        Δ(trefoil;t) при t=−1: −1−1+(−1) = −3. |Δ| = 3 = N−1.

        Δ(трёхлистник;1) = 1−1+1 = 1.
        Δ(трёхлистник;−1) = −1−1−1 = −3. |Δ(−1)| = 3 = N−1.

        ЕТД: t = −1 (классическая точка).
        Сигнатура: σ(3-листник) = −2 = −(N/2).
        |σ| = 2 = N/2.
        «Сигнатура / N/2 = −1!»

        ЕТД: t = e^{2πi/N} = i.
        Δ(trefoil; i) = i − 1 + (−i) = −1.
        |Δ(trefoil;i)| = 1.

        Полином Александера удовлетворяет симметрии:
        Δ(t) = Δ(t⁻¹) (палиндромный!).
        Коэффициент Александера: a_0 = Δ(1) = 1.
        a_0 = 1 = (−1)^{N/2}·N/N = 1. ✓

        «Палиндромный полином с a_0 = 1 = N/N!»
        """
        N = 4

        # Δ(trefoil;t) = t − 1 + 1/t
        # При t = -1: -1 - 1 + (-1) = -3
        delta_at_minus1 = -1 - 1 + (-1)   # = -3
        abs_delta = abs(delta_at_minus1)   # = 3 = N-1
        signature = -2   # = -(N/2)

        return {
            'теорема': 'Полином Александера',
            'Δ(3-листник;−1)': delta_at_minus1,
            '|Δ|=N−1': abs_delta == N-1,
            'сигнатура': signature,
            '=−N/2': signature == -(N//2),
            'ЕТД': f'Δ(trefoil;−1)={delta_at_minus1}: |Δ|={abs_delta}=N−1; σ={signature}=−N/2',
        }


cp = CombinatoricsPolynomialsETD()
for nm, fn in [("Тутт", cp.tutte_polynomial_etd), ("Хроматич.", cp.chromatic_polynomial_etd),
               ("Джонс", cp.jones_polynomial_etd), ("Александер", cp.alexander_polynomial_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Тутт** → T(K_N;1,1) = N²; β₁ = N−1 = 3
2. **Хроматический** → P(K_N,N) = N!; P(K_N,N+1) = (N+1)!
3. **Джонс** → |diff_скейн| = √2 при t = e^{2πi/N}
4. **Александер** → |Δ(−1)| = N−1 = 3; σ = −N/2 = −2

---
*ТОМ 282 / СЕРИЯ IX / ЕТД 2026*
