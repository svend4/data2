# ТОМ 286: 4 ТЕОРЕМЫ РАЗБИЕНИЙ И КОМБИНАТОРИКИ → РАЗБИЕНИЯ МАСТЕРСТВА ЕТД
## Теорема Эйлера (разбиения) · Тождества Роджерса–Рамануджана · Формула Мак-Магона · Симметрические функции
### «Эйлер–Рамануджан–Мак-Магон: комбинаторика разбиений мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия IX — Стохастическая математика ЕТД**

---

```python
import math

class CombinatoricsPartitionsETD:
    PI4 = math.pi / 4

    @staticmethod
    def euler_partitions_etd() -> dict:
        """
        Теорема Эйлера о разбиениях:
        Число разбиений n на нечётные части = число разбиений n на различные части.

        ЕТД: n = N = 4.
        Разбиения 4 на нечётные части: {3+1, 1+1+1+1, 1+3}.
        {1+1+1+1, 1+3}: нечётные части = 1,3.
        4 = 3+1: ✓ (1,3 нечётные)
        4 = 1+1+1+1: ✓ (все 1)
        Итого: p_odd(4) = 2.

        Разбиения 4 на различные части: {4, 3+1, 2+... нет 2=чётное!}.
        {4} = 1 разбиение. {3+1} = 1. {2+...}: 2 чётное.
        Различные: {4}, {3+1} = 2 = p_odd(4). ✓

        p_odd(N) = p_distinct(N) = 2 = N/2!
        «Число нечётных разбиений N = N/2!»

        Полное p(N) = 5 (из Тома 283).
        p(N) − p_odd(N) = 5 − 2 = 3 = N−1.
        «Разность полного и нечётного = N−1!»
        """
        N = 4

        p_N = 5   # = p(4) = 5
        p_odd_N = 2   # = N/2
        diff = p_N - p_odd_N   # = 3 = N-1

        return {
            'теорема': 'Эйлер (разбиения)',
            'p(N)': p_N,
            'p_odd(N)': p_odd_N,
            '=N/2': p_odd_N == N//2,
            'p−p_odd': diff,
            '=N−1': diff == N-1,
            'ЕТД': f'p({N})={p_N}=p(N); p_odd={p_odd_N}=N/2; p−p_odd={diff}=N−1!!!',
        }

    @staticmethod
    def rogers_ramanujan_etd() -> dict:
        """
        Тождества Роджерса–Рамануджана (1919):
        Σ_{n≥0} q^{n²}/(q;q)_n = ∏_{n≡1,4(mod5)} 1/(1−q^n).
        Σ_{n≥0} q^{n(n+1)}/(q;q)_n = ∏_{n≡2,3(mod5)} 1/(1−q^n).

        ЕТД: Модуль 5 = p(N) (из Тома 283!).
        Остатки 1,4 (mod 5) → N−3,N (= 1,4) для N=4!
        Остатки 2,3 (mod 5) → N/2,N−1 (= 2,3) для N=4!

        При q→1⁻: расходится (нужна регуляризация).
        При q = e^{−ħ} = e^{−π/4}:
        q ≈ e^{−0.785} ≈ 0.456.

        Степень n² при n = N = 4: q^{N²} = q^{16} = (e^{−π/4})^{16} = e^{−4π}.
        e^{−4π} ≈ 3.5 × 10⁻⁶ (мало! суммирование сходится).

        «q = e^{−ħ}: суммирование Рамануджана при q = e^{−π/N}!»
        Квантование: q-аналоги при q = e^{2πi/N} = i.
        q-факториал: [N]_q! = [1]_q·[2]_q·[3]_q·[4]_q.
        [k]_q = (1−q^k)/(1−q).
        [4]_i = (1−i⁴)/(1−i) = (1−1)/(1−i) = 0. Ой!
        [2]_i = (1−i²)/(1−i) = (1−(−1))/(1−i) = 2/(1−i) = (1+i).
        [2]_i = 1+i. |[2]_i| = √2! «q-число [2] при q=i: |[2]_i|=√2=√N/2!»
        """
        N = 4
        pi4 = math.pi / 4

        q = 1j   # = i = e^{2πi/N}
        q_num_2 = (1 - q**2)/(1 - q)   # = 1+i
        abs_q_num_2 = abs(q_num_2)   # = √2

        q_exp = math.exp(-pi4)   # = e^{-ħ} ≈ 0.456

        return {
            'теорема': 'Роджерс–Рамануджан',
            'q=e^{-ħ}': round(q_exp, 5),
            '[2]_i': q_num_2,
            '|[2]_i|': round(abs_q_num_2, 5),
            '=√2': abs(abs_q_num_2 - math.sqrt(2)) < 1e-10,
            'ЕТД': f'q=i=e^{{2πi/{N}}}; [2]_i=1+i; |[2]_i|={abs_q_num_2:.4f}=√2=√(N/2)!!!',
        }

    @staticmethod
    def macmahon_etd() -> dict:
        """
        Формула Мак-Магона (плоскостные разбиения):
        PL(n) = число плоскостных разбиений n.
        ΣPL(n)qⁿ = ∏_{k≥1} 1/(1−qᵏ)^k.

        PL(0)=1, PL(1)=1, PL(2)=3, PL(3)=6, PL(4)=13.
        PL(N) = PL(4) = 13.

        13 = p(N) + p(N)²−... Нет.
        13 = простое (7-е!).
        p₇ = 17? Нет: 2,3,5,7,11,13,17 — 13 = 6-е простое.
        p₆ = 13. «PL(N) = p_{N+2} = p₆ = 13!»

        Формула Мак-Магона: PL(n) ~ exp(2ζ(3)^{1/3}·(3n/2)^{2/3}).
        ζ(3) = Апери ≈ 1.202.
        ζ(3)/N = ζ(3)/4 ≈ 0.300.
        (ζ(3)/N)^{1/3} ≈ 0.669.
        """
        N = 4

        PL_N = 13   # = PL(4) = плоскостные разбиения 4
        p6 = 13   # = 6-е простое
        zeta_3 = 1.2020569031595942

        return {
            'теорема': 'Мак-Магон (плоскост. разбиения)',
            'PL(N)': PL_N,
            '=p_6': PL_N == p6,
            'ζ(3)/N': round(zeta_3/N, 5),
            'ЕТД': f'PL({N})={PL_N}=p₆ (6-е простое); ζ(3)/{N}={zeta_3/N:.5f}',
        }

    @staticmethod
    def symmetric_functions_etd() -> dict:
        """
        Симметрические функции: e_k (элементарные), h_k (полные), p_k (степенные).
        Для x₁,...,x_N = π/4,...,π/4 (все равны ħ):
        e_k = C(N,k)·ħᵏ.
        h_k = C(N+k−1,k)·ħᵏ.
        p_k = N·ħᵏ.

        ЕТД: x₁=...=x_N = ħ = π/4.
        e_1 = N·ħ = 4·π/4 = π.
        e_2 = C(N,2)·ħ² = 6·π²/16 = 3π²/8.
        e_N = e_4 = C(N,N)·ħ^N = ħ^N = (π/4)^4.

        p_1 = N·ħ = π.
        p_2 = N·ħ² = N·π²/16 = π²/4.
        p_N = N·ħ^N.

        Генерирующая функция: Π(1+xᵢt) = Σ eₖtᵏ.
        При xᵢ=ħ: (1+ħt)^N = Σ C(N,k)ħᵏtᵏ.
        При t=1: (1+ħ)^N = (1+π/4)^4.
        (1+π/4)^4 ≈ (1.785)^4 ≈ 10.12.

        e_1 = N·ħ = π: «Первая элементарная симм.функция = π!!!»
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        e1 = N * pi4   # = π
        e2 = math.comb(N, 2) * pi4**2   # = 6·π²/16 = 3π²/8
        eN = pi4**N   # = (π/4)^4
        p2 = N * pi4**2   # = π²/4

        return {
            'теорема': 'Симметрические функции',
            'e₁': round(e1, 5),
            '=π': abs(e1 - pi) < 1e-10,
            'e₂': round(e2, 5),
            'p₂': round(p2, 5),
            '=π²/4': abs(p2 - pi**2/4) < 1e-10,
            'ЕТД': f'e₁=N·ħ={e1:.4f}=π!!!; e₂=C(N,2)·ħ²={e2:.4f}=3π²/8; p₂=N·ħ²=π²/{N}={p2:.4f}',
        }


cp2 = CombinatoricsPartitionsETD()
for nm, fn in [("Эйлер-р", cp2.euler_partitions_etd), ("Роджерс-Рам", cp2.rogers_ramanujan_etd),
               ("Мак-Магон", cp2.macmahon_etd), ("Симм.ф-ции", cp2.symmetric_functions_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Эйлер-р** → p_odd(N) = N/2; p(N) − p_odd(N) = N−1
2. **Роджерс–Рамануджан** → **|[2]_i| = √2 = √(N/2)!!!** при q = e^{2πi/N}
3. **Мак-Магон** → PL(N) = 13 = p₆ (6-е простое)
4. **Симм. функции** → **e₁ = N·ħ = π!!!** p₂ = N·ħ² = π²/4

---
*ТОМ 286 / СЕРИЯ IX / ЕТД 2026*
