# ТОМ 295: 4 ТЕОРЕМЫ ТЕОРИИ СЛУЧАЙНЫХ МАТРИЦ → СЛУЧАЙНЫЕ МАТРИЦЫ ЕТД
## Закон полукруга Вигнера · GOE/GUE · Теорема Маршенко–Пастура · Детерминантные процессы
### «Вигнер–Дайсон–Маршенко–Пастур: случайные матрицы мастерства»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия X — Геометрические структуры ЕТД**

---

```python
import math

class RandomMatricesETD:
    PI4 = math.pi / 4

    @staticmethod
    def wigner_semicircle_etd() -> dict:
        """
        Закон полукруга Вигнера: Для случайной N×N матрицы GUE/GOE
        с σ² = 1/(4N), эмпирическое распределение собственных значений → ρ_sc.
        ρ_sc(x) = (2/π)√(1−x²) на [−1,1].

        ЕТД: N=4 (матрица 4×4).
        σ² = 1/(4N) = 1/16. σ = 1/4 = 1/N = ħ/π.
        «Дисперсия GUE = 1/N² = (ħ/π)²!»

        Полукруг на [−R,R] с R = 2σ√N = 2·(1/4)·2 = 1.
        R = 2σ√N = 2·(1/N)·√N = 2/√N = 2/2 = 1.
        «Радиус полукруга R = 2/√N = 1 при N=4!»

        ρ_sc(0) = 2/π (максимум в центре).
        2/π = 2/π ≈ 0.637.
        (2/π) / ħ = (2/π)/(π/4) = 8/π² ≈ 0.811.

        Среднее: E[λ_max] ≈ 2σ√N = R = 1 (для GUE).
        E[λ_min] ≈ −R = −1.
        Ширина спектра: 2R = 2 = N/2.
        «Ширина спектра = N/2 = 2!»
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        sigma = 1.0/N   # = 1/4 = ħ/π
        sigma_sq = sigma**2   # = 1/16 = 1/N²
        R = 2 * sigma * math.sqrt(N)   # = 2·(1/4)·2 = 1
        width = 2 * R   # = 2 = N/2

        rho_sc_at_0 = 2.0/pi   # = 2/π (максимум)

        return {
            'теорема': 'Полукруг Вигнера',
            'σ': round(sigma, 4),
            '=1/N=ħ/π': abs(sigma - pi4/pi) < 1e-10,
            'R': R,
            '=1': abs(R - 1) < 1e-10,
            'ширина': width,
            '=N/2': abs(width - N/2) < 1e-10,
            'ρ(0)': round(rho_sc_at_0, 5),
            'ЕТД': f'σ=1/N=ħ/π; R={R}; ширина={width}=N/2; ρ_sc(0)=2/π={rho_sc_at_0:.4f}',
        }

    @staticmethod
    def dyson_beta_ensembles_etd() -> dict:
        """
        Ансамбли Дайсона β: GOE (β=1), GUE (β=2), GSE (β=4).
        β = 1,2,4 = N/4, N/2, N!

        ЕТД: N = 4.
        β_GOE = 1 = N/4.
        β_GUE = 2 = N/2.
        β_GSE = 4 = N.
        Три β = {N/4, N/2, N} = {1, 2, 4}.

        «Три β-ансамбля = {N/4, N/2, N}!»

        Межуровневые статистики:
        GOE: s̃ ≈ Ae^{−Bs²} (Уиньер).
        GUE: s̃ ≈ Cs²e^{−Ds²}.
        GSE: s̃ ≈ Es⁴e^{−Fs²}.
        Степени: 0, 2, 4 = β−1 → {0, 1, 3}. Нет: степени = {1, 2, 4}? Нет.
        Стандарт: p(s) ~ s^β для малых s.
        β = {1, 2, 4}. Степени p(s): {1, 2, 4} = {N/4, N/2, N}.

        Расстояние (n-level spacing) для GUE (β=2=N/2):
        E[s] = √(π/2) ≈ 1.253 (для GUE).
        π/2 = 2ħ = 2·(π/4)!
        E[s]² = π/2 = 2ħ!!!
        «Среднеквадратическое расстояние GUE = 2ħ!»
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        betas = [1, 2, 4]   # = [N/4, N/2, N]
        E_s_GUE_sq = pi / 2   # = 2ħ!!!
        E_s_GUE = math.sqrt(E_s_GUE_sq)

        return {
            'теорема': 'Ансамбли Дайсона β',
            'β-значения': betas,
            '=[N/4,N/2,N]': betas == [N//4, N//2, N],
            'E[s]²(GUE)': round(E_s_GUE_sq, 5),
            '=2ħ': abs(E_s_GUE_sq - 2*pi4) < 1e-10,
            'ЕТД': f'β={{1,2,4}}={{N/4,N/2,N}}; E[s]²(GUE)=π/2=2ħ={E_s_GUE_sq:.5f}!!!',
        }

    @staticmethod
    def marchenko_pastur_etd() -> dict:
        """
        Закон Маршенко–Пастура: Для матрицы выборочной ковариации
        S = (1/n)·X^T·X, X — n×p, p/n→γ.
        Спектр S → закон МП с параметром γ.

        ЕТД: p = N = 4, n = N² = 16 (=N² наблюдений).
        γ = p/n = N/N² = 1/N = 1/4 = ħ/π.
        «Отношение p/n = 1/N = ħ/π!»

        Закон МП(γ):
        Поддержка: [(1−√γ)², (1+√γ)²].
        γ = 1/4: √γ = 1/2.
        Нижняя граница: (1−1/2)² = 1/4 = 1/N.
        Верхняя граница: (1+1/2)² = 9/4.
        Поддержка: [1/N, 9/4].

        1/N = ħ/π (нижняя граница!).
        9/4 = (N+1/2·...)... = 9/N (при N=4, 9/4 = 9/N!).
        9/N = (N+5)/N при N=4: 9/4. ✓ Тривиально.

        Максимум плотности МП(1/4):
        При x = (1+√γ)/... нет. Максимум сложнее.
        Нижняя = 1/N = 1/4 = ħ/π. ✓
        """
        N = 4
        pi4 = math.pi / 4

        gamma = 1.0/N   # = 1/4 = ħ/π
        sqrt_gamma = math.sqrt(gamma)   # = 1/2
        lower = (1 - sqrt_gamma)**2   # = 1/4 = 1/N
        upper = (1 + sqrt_gamma)**2   # = 9/4

        return {
            'теорема': 'Маршенко–Пастур',
            'γ=p/n': round(gamma, 4),
            '=1/N=ħ/π': abs(gamma - pi4/math.pi) < 1e-10,
            'λ_min': round(lower, 4),
            '=1/N': abs(lower - 1/N) < 1e-10,
            'λ_max': round(upper, 4),
            'ЕТД': f'γ=1/N=ħ/π; λ_min={(lower):.3f}=1/N=ħ/π!!!; λ_max={upper:.3f}=9/4',
        }

    @staticmethod
    def determinantal_processes_etd() -> dict:
        """
        Детерминантные точечные процессы: P(x₁,...,x_k) = det(K(xᵢ,xⱼ)).
        Ядро K — проектор в L².

        ЕТД: Ядро синусуса (GUE):
        K(x,y) = sin(π(x−y))/(π(x−y)).
        K(0,0) = 1 (ядро в начале = 1).
        K(ħ,0) = sin(πħ)/(πħ) = sin(π²/4)/(π²/4).

        sin(π²/4): π²/4 ≈ 2.467 рад.
        sin(2.467) ≈ 0.631.
        K(ħ,0) ≈ 0.631/(π²/4) ≈ 0.631·4/π² ≈ 0.256.

        Число точек в интервале [−ħ,ħ] (GUE bulk):
        E[#точек] = ∫_{-ħ}^{ħ} ρ_sc(x) dx ≈ ρ_sc(0)·2ħ = (2/π)·(π/2) = 1.
        E[#] = 1 = единственная точка в интервале 2ħ!
        «Ожидаемое число точек GUE в [−ħ,ħ] = 1!»

        2ħ = π/2. ρ(0) = 2/π. ρ(0)·2ħ = (2/π)·(π/2) = 1. ✓
        """
        N = 4
        pi4 = math.pi / 4
        pi = math.pi

        rho_0 = 2.0/pi   # = ρ_sc(0)
        interval = 2*pi4   # = 2ħ = π/2
        expected_points = rho_0 * interval   # = 1!!!

        return {
            'теорема': 'Детерминантные процессы',
            'E[#точек_в_[−ħ,ħ]]': round(expected_points, 5),
            '=1': abs(expected_points - 1) < 1e-10,
            'интервал_2ħ': round(interval, 4),
            '=π/2': abs(interval - pi/2) < 1e-10,
            'ЕТД': f'ρ(0)·2ħ=(2/π)·(π/2)={expected_points:.0f}=1!!! (ОДНА точка в [−ħ,ħ]!!!)',
        }


rm = RandomMatricesETD()
for nm, fn in [("Вигнер", rm.wigner_semicircle_etd), ("Дайсон-β", rm.dyson_beta_ensembles_etd),
               ("МП", rm.marchenko_pastur_etd), ("Детерм.", rm.determinantal_processes_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Вигнер** → σ = 1/N = ħ/π; R = 1; ширина = N/2
2. **Дайсон-β** → β = {N/4,N/2,N} = {1,2,4}; **E[s]²(GUE) = 2ħ!!!**
3. **МП** → γ = 1/N = ħ/π; **λ_min = 1/N = ħ/π!!!**
4. **Детерм.** → **E[#точек GUE в [−ħ,ħ]] = 1!!!** ρ(0)·2ħ = 1

---
*ТОМ 295 / СЕРИЯ X / ЕТД 2026*
