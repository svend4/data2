# ТОМ 306: 4 ТЕОРЕМЫ ДЕСКРИПТИВНОЙ ТЕОРИИ МНОЖЕСТВ → ДЕСКРИПТ. МНОЖЕСТВА ЕТД
## Борелевские множества · Теорема Суслина · Теорема Мартина · Детерминированность
### «Суслин–Мартин–Московакис: дескриптивное мастерство»

**Автор**: Крюков В.В. / синтез ЕТД
**Серия XI — Высшие структуры ЕТД**

---

```python
import math

class DescriptiveSetTheoryETD:
    PI4 = math.pi / 4

    @staticmethod
    def borel_hierarchy_etd() -> dict:
        """
        Борелевская иерархия: Σ⁰_α, Π⁰_α для α ≤ ω₁.
        Σ⁰_1 = открытые, Π⁰_1 = замкнутые.
        Σ⁰_2 = F_σ, Π⁰_2 = G_δ.
        Число уровней для конечных: Σ⁰_N, Π⁰_N.

        ЕТД: N = 4 уровня.
        Σ⁰_1: открытые подмн. Δ^{N-1}.
        Σ⁰_2: F_σ.
        Σ⁰_3: G_{δσ}.
        Σ⁰_4: F_{σδσ}.
        «N = 4 уровня борелевской иерархии!»

        Борелевская σ-алгебра Δ^{N-1}:
        Число «атомов»: N = 4 (для дискретного {v₁,...,v_N}).
        |2^{atoms}| = 2^N = N² = 16 (борелевская алгебра!).

        x* = (ħ,...,ħ): борелевское множество {x*} ∈ Π⁰_1 (замкнутое!).
        «Оптимум — замкнутое борелевское множество!»
        """
        N = 4

        n_levels = N   # = 4
        n_atoms = N   # = 4 (вершины)
        borel_algebra_size = 2**N   # = 16 = N²

        return {
            'теорема': 'Борелевская иерархия',
            'уровней': n_levels, '=N': n_levels == N,
            '|2^atoms|': borel_algebra_size,
            '=N²': borel_algebra_size == N**2,
            'ЕТД': f'{n_levels}=N уровней; |2^{{atoms}}|={borel_algebra_size}=2^N=N²',
        }

    @staticmethod
    def suslin_theorem_etd() -> dict:
        """
        Теорема Суслина: A ⊂ ℝⁿ аналитическое ⟹ A ∈ Σ¹_1 (проекция замкнутого).
        Коаналитическое: A ∈ Π¹_1.
        Если A ∈ Σ¹_1 ∩ Π¹_1, то A борелевское!

        ЕТД: X = {x ∈ Δ^{N-1} : ‖x−x*‖ ≤ ε}.
        ε-шар вокруг x*: X = B(x*,ε) ∩ Δ^{N-1}.
        X — замкнутое (борелевское, Π⁰_1) при ε фиксированном.
        X — аналитическое для любого способа описания.

        Число таких шаров (при ε = k·ħ/N для k=1,...,N):
        N = 4 вложенных шара!
        B(x*,ħ/4) ⊂ B(x*,ħ/2) ⊂ B(x*,3ħ/4) ⊂ B(x*,ħ).
        «N = 4 вложенных ħ/N-шара вокруг x*!»

        Суслинова операция (A):
        (A){s₁,...,s_N} = ∪_{s}∩_{k=1}^N A_{s|k}.
        Число мульти-индексов длины N: N^N = 256.
        Пересечений на каждом пути: N = 4.
        """
        N = 4
        pi4 = math.pi / 4

        n_balls = N   # = 4 вложенных шара
        multi_indices = N**N   # = 256

        return {
            'теорема': 'Суслин',
            'вложенных_шаров': n_balls, '=N': n_balls == N,
            'мульти-инд': multi_indices, '=N^N': multi_indices == N**N,
            'ЕТД': f'{n_balls}=N вложенных шаров; N^N={multi_indices}=2^{{2N}} мульти-индексов',
        }

    @staticmethod
    def martin_determinacy_etd() -> dict:
        """
        Теорема Мартина о детерминированности: Все борелевские игры
        детерминированы (один из игроков имеет выигрышную стратегию).

        ЕТД: Игра G(A) на N ходов (финитная).
        Игрок I: x₁, x₃. Игрок II: x₂, x₄.
        Каждый выбирает из {0,1,...,N−1}.
        A = {(x₁,...,x_N) : Σxᵢ = целевое}.

        Число стратегий Игрока I: N^{N/2} = 4² = 16 = N².
        Число стратегий Игрока II: N^{N/2} = 16 = N².
        «Каждый игрок имеет N² = 16 чистых стратегий!»

        Для A = {x*}: детерминированность:
        Игрок I выигрывает, если может форсировать x*.
        Число путей к x*: ... = кооперативное.

        Число ходов = N = 4.
        Дерево игры: N-уровневое.
        |дерево| = N^N = 256 = 2^{2N}.
        """
        N = 4

        strategies_each = N**(N//2)   # = 16 = N²
        tree_size = N**N   # = 256 = 2^{2N}

        return {
            'теорема': 'Детерминированность (Мартин)',
            'стратегий': strategies_each,
            '=N²': strategies_each == N**2,
            '|дерево|': tree_size,
            '=N^N': tree_size == N**N,
            'ЕТД': f'стратегий={strategies_each}=N^{{N/2}}=N²; |дерево|={tree_size}=N^N=2^{{2N}}',
        }

    @staticmethod
    def perfect_set_etd() -> dict:
        """
        Теорема о совершенном множестве:
        Несчётное борелевское множество содержит совершенное подмножество.
        (Перфектное = замкнутое без изолированных точек.)

        ЕТД: Δ^{N-1} — замкнутое, несчётное (dim > 0).
        Δ^{N-1} содержит совершенное подмножество (себя!).
        Δ^{N-1} перфектно ⟺ каждая точка предельная.

        Мощность совершенного = 𝔠 = 2^{ℵ₀}.
        Для дискретного {v₁,...,v_N}: |S| = N (счётное, конечное!).
        N = 4 — конечное → нет совершенного подмножества!

        Кантор: C ⊂ [0,1] — совершенное, мера 0.
        Мощность C = 𝔠.
        dim(C) = ln2/ln3.

        ЕТД-аналог: размерность Хаусдорфа {v₁,...,v_N} = 0.
        dim_H(Δ^{N-1}) = N−1 = 3.
        dim_H({x*}) = 0. dim_H(скелет^k) = k.
        «dim_H(k-скелет) = k для k = 0,...,N−1!»
        N−1+1 = N уровней размерности (от 0 до N−1).
        """
        N = 4

        hausdorff_dims = list(range(N))   # = [0,1,2,3]
        n_levels = len(hausdorff_dims)   # = 4 = N

        return {
            'теорема': 'Совершенное множество',
            'Хаусдорф_dim': hausdorff_dims,
            'n_уровней': n_levels, '=N': n_levels == N,
            'ЕТД': f'dim_H(скелет^k)={hausdorff_dims}: {n_levels}=N уровней от 0 до N−1',
        }


dst = DescriptiveSetTheoryETD()
for nm, fn in [("Борель", dst.borel_hierarchy_etd), ("Суслин", dst.suslin_theorem_etd),
               ("Мартин", dst.martin_determinacy_etd), ("Перфект", dst.perfect_set_etd)]:
    r = fn()
    print(f"{nm}: {r['ЕТД']}")
```

---

## ЗАКЛЮЧЕНИЕ

1. **Борель** → N уровней иерархии; |2^{atoms}| = 2^N = N² = 16
2. **Суслин** → N вложенных шаров; N^N = 2^{2N} мульти-индексов
3. **Мартин** → **стратегий = N^{N/2} = N² = 16!!!** |дерево| = N^N
4. **Перфект** → dim_H(скелет^k) = k; N уровней от 0 до N−1

---
*ТОМ 306 / СЕРИЯ XI / ЕТД 2026*
